# 面试材料 · 追问预案（面试官视角）

> 2026-09-18 整理。题目按「真实面试官会怎么问」组织，每题给：**考察点 / 参考回答要点 / 出处**（出处指向 DECISIONS.md 的 D 编号或方案.md 章节，被追问时能落到具体文件）。
> 使用方法：先背第三节数字卡和第一节电梯稿 → 按层过题 → 最后自查第四节红旗清单。

---

## 一、电梯稿（必须背熟，三个时长版本）

**10 秒版（一句话定位）**
> 一个智能旅游规划系统：用户一句话或粘贴一份现成行程，系统生成行程后不是直接给用户，而是**先用代码校验、发现问题、自动修正、再校验**——核心是一个「验证→定位→修正→再验证」的 Agent 内核。

**30 秒版（加架构与量化）**
> 技术栈是 FastAPI + LangGraph + DeepSeek + MySQL + SSE。全 LangGraph 手写 StateGraph，不依赖框架的 agent 封装；带三个高德真实数据工具和一个搜索子 agent；校验分硬判据 5 类软判据 4 类，结果三态呈现（通过/不通过/无法判定）。前后端用 Pydantic 契约驱动，契约自动生成前端 TS 类型；621 个测试全绿，MySQL 真库验收通过。

**60 秒版（加差异化）**
> 在 30 秒版基础上加：
> 差异化在三处：① **校验三态是核心视觉语言**——「无法判定」和「通过」严格区分，因为高德数据大面积缺失，把缺数据伪装成通过是造假；② **成本预算内建**——工具循环、校验回环、单次行程总调用数都有硬上限，超限降级输出而不是死循环或抛异常；③ **评测集与线上库单向隔离**——攻略社区可以发布内容，但评测永远只读人工筛过的固定语料，防止「用线上数据考自己」。

---

## 二、面试官提问清单（六层，追问链式）

### 第一层：开场必问（几乎 100% 出现）

**Q1.1 介绍一下这个项目，你负责哪部分？**
- 考察点：定位清晰度、诚实度。
- 要点：用 30 秒版。分工口径（⚠️ 提前想好，别被问懵）：**后端、Agent 引擎、接口/数据契约、测试与验收我全程主导；前端页面用 AI 辅助实现，但我负责契约冻结和验收清单**——「契约唯一事实源是后端 schemas.py，前端 TS 类型是从它自动生成的，所以我改内部实现前端零改动」。这个分工方式本身是加分项（契约驱动开发），但**必然追问**「那你前端代码能看懂/能改吗」，准备一个真实例子：验收时用 tsc --strict 抓出「Pydantic 有默认值字段生成的 TS 类型可选、判别联合失效」的问题（见 Q5.5）。

**Q1.2 这个项目最难的地方是什么？**
- 考察点：是不是真做过（没做过的人答不出具体的）。
- 要点：推荐答「**假修复**」——高德 geocode("成都") 返回的坐标是市政府驻地，和「不加中心点」的搜索结果逐条相同，代码看起来做了地理约束，实际什么都没变。最后用 medoid（从已搜到的真实 POI 里选到其他所有点距离和最小的点）替代，因为质心会被远郊锚点拉偏（宽窄巷子+都江堰的质心落在郫都区，到两边各 28km，比不加还差），而 medoid 必然落在真实锚点上、天然抗离群（D75）。「看起来修了其实没修」比崩溃更危险，这个故事有完整的发生→发现→对比实验→解决链条。

**Q1.3 为什么做这个项目？（不是 TodoList/不是 RAG 问答套壳？）**
- 要点：校验回环是真 Agent 语义——**信息不在一次上下文里（要搜 POI）、需要外部工具验证（判据查库）、有环境反馈（校验结果打回重排）**，三条全占；而 TodoList 一条都不占。核心视觉「三态」和核心机制「成本上限」都是 RAG 项目做不出来的东西。

---

### 第二层：架构与选型（追问「为什么不用 X」系列）

**Q2.1 为什么全手写 StateGraph，不用 LangChain 的 create_agent？**（D1）
- 追问链：create_agent 缺什么 → 你的图长什么样 → 节点怎么划分。
- 要点：create_agent 是「一个模型挂一堆 tools 转圈」，而本项目的内核是**两种不同性质的循环叠加**：会话层循环（对话推进）+ 工具层循环（信息收集）+ 校验回环（打回重排），且「跑工具」和「出结构化 JSON」必须是不同节点（工具节点要挂 tools、JSON 节点不挂）。create_agent 表达不了「校验不通过就路由回重排节点」这种带状态的回环。手写图每个节点职责单一、可测。

**Q2.2 为什么 DeepSeek？换模型怎么办？**（D8/D9/D55/D58/D60）
- 要点：① 成本（评测要反复跑，百炼免费额度但 qwen 慢 14 倍，默认切回 deepseek-flash 有实测依据）；② 做了三个粒度的模型切换（会话级 B 档），模型是可插拔的；③ **必讲的坑**：带 tools 的节点思考模式必须显式关（`thinking.type=disabled`），否则历史 assistant 消息缺 `reasoning_content` 字段稳定 400——这是按节点分设思考模式的根因，不是全局开关。

**Q2.3 为什么 SSE 不用 WebSocket？**（D10/D27/D74）
- 追问：断线了怎么办？重连后内容重复怎么办？
- 要点：单向推送场景，WebSocket 的双向能力用不上还要自己管心跳；SSE 每帧带 `id:`，断线重连带 `Last-Event-ID`，后端从 LangGraph checkpoint **按节点粒度恢复**（不是重发请求）。**主动交代表价**（面试加分动作）：恢复粒度是节点不是 token，所以重连后当前节点那段会重打一遍、前端纯追加导致文字重复——评估过做节点级回滚，复杂度不值，**拍板接受为已知代价**（D74）。「知道代价并明说是设计决策，藏着才是坑」。

**Q2.4 为什么 MySQL 不用 PostgreSQL？限流为什么不用 Redis？**（D11/D28）
- 要点：单机部署、无高并发场景，MySQL 用 Docker 起即用；第三方 `langgraph-checkpoint-mysql` 包实测 16/16 通过才敢用，且**依赖无上界所以三个 langgraph 包版本全部写死**（有真实踩坑：松约束会被带崩）。限流是进程内滑动窗口按接口成本分层（chat 最严：10 次/时·30 次/天），引 Redis 是为了一个单进程计数器背一个新组件，不值——**诚实补一句**：如果多实例部署，进程内限流失效，那才需要 Redis，这是明确的扩展边界。

**Q2.5 契约怎么管理的？前端是谁写的？**（D12/D18/D23/D34）
- 要点：唯一事实源 = `schemas.py`（Pydantic），前端 62 个 TS 类型**从它自动生成**、生成后必须跑 `tsc --strict` 编译器验证；改契约的顺序是「先扩 schemas 再改文档」。mock 与 real 同签名、响应由同一个 Pydantic 模型序列化产出——前端拿 mock 并行开发，接真数据零改动。这套流程让我（或任何后端）换模型、改检索、优化循环时前端不动一行。

---

### 第三层：Agent 核心（最看深度的部分）

**Q3.1 校验体系具体怎么做的？**
- 追问链：判据有哪些 → 缺数据怎么办 → 打回之后发生什么。
- 要点：硬判据 5 类（POI 存在、营业时间、地理顺序、跨天距离下界、总里程口径等）**纯代码实现，不过 LLM**；软判据 4 类（节奏、同类扎堆等）**只产出「提醒」不产出「事实断言」**——因为软判据没有 `poi_exists` 那种硬守门，模型编一个「这家店排队太久」你没法证伪，所以它的输出定位是提醒不是断言（D45）。结果三态：`pass / fail / unknown`——高德数据大面积缺失（门票几乎没有），**「无法判定」绝不能伪装成「通过」**，这是本项目的诚信底线也是界面核心视觉（D6）。

**Q3.2 工具循环怎么防失控？**（D25/D32/D67/D71）
- 要点：两道闸——L2 工具层单轮 8 次（同参数重复调用直接拒）、L3 校验层打回 2 次、单次行程工具调用总数 ≤20；**超限降级输出 + 缺的数据标 `unknown`，绝不抛异常**（用户拿到一份标明哪些没验证的行程，好过一个 500）。跨 task 去重把「已搜过的关键词」写进子任务描述，防止子 agent 重复劳动。

**Q3.3 搜索结果怎么给模型？直接全给吗？**（D39/D40/D41）
- 要点：两段式窗口——取回 20 条、给模型看 8 条；候选噪音**只重排、不过滤**（过滤会把「名字不完全匹配但正确」的真结果扔掉，重排让模型在池子里自己选）；`types` 类别码定向搜是**实测否证**后才不做的（不是没时间），说清「实证否决」和「没做」的区别是加分点。

**Q3.4 子 agent 用在哪、为什么只要一个？**（D5/D65）
- 要点：官方四种多 agent 形态（Supervisor/Network/Subagents…）里选 Subagents，只切 1 个搜索子 agent——多 agent 的协调成本在这里没有对应收益，**形态跟着问题走，不是跟着时髦走**。跨 task 去重靠把已有候选池写进任务描述（上下文注入），不靠额外状态存储。

**Q3.5 粘贴现成行程这条线怎么实现的？**（D16/D50）
- 要点：两步法——LLM 只抽地名清单（不负责落地），POI 定位交给 search_poi（保证粘贴行程和生成行程**落同一套真实数据库**，不会出现 LLM 编的坐标）；粘贴行程不建会话（`trips.session_id` 可空），归属校验走 `owned_trip()` 按 `trips.user_id`，不能靠 session 反查。

---

### 第四层：工程与可靠性

**Q4.1 重试策略怎么定的？**（D26/D35）
- 要点：**只重试天然幂等的操作**（GET/搜索/天气/LLM），写状态的操作靠唯一索引兜底绝不重试；400/401/403 不重试（重试只会再挨一枪）。高德限速取 2.2 QPS 不是文档写的 3——**实测连打第 3~4 个就触发 CUQPS 限流**，贴着边界跑必挂；且限流响应 `status=0` 但 HTTP 200、不抛异常，**必须当重试信号处理，绝不能当「搜不到」**——否则模型以为景点不存在，触发假硬错白打回整轮。这是「错误语义」题的标准答案。

**Q4.2 私有数据隔离怎么保证的？**（D15/D31）
- 要点：**不靠「记得写 WHERE user_id」**——那是约定不是结构，违反不报错。三重防线：① 签名级：repo 函数第一个位置参数必须是 user_id；② 三个扫描断言写进测试，漏写就测试红；③ 承认 MySQL 无 RLS 这层拿不到。拿不到资源返回 **404 不是 403**（403 等于告诉攻击者「这个 id 存在，换个账号」）。

**Q4.3 头像上传怎么防攻击？**（D29）
- 要点：四道闸——真实解码（扩展名伪造拦掉）、解压炸弹检测、**强制重编码为 512×512 WebP**（重编码天然洗掉 payload）、文件名换 UUID（路径穿越+重名）；不收 SVG（内嵌 JS=XSS）和 GIF。

**Q4.4 评测怎么做的？**（D53/A38）
- 要点：判分**一行 LLM 都不碰**（LLM 判分等于用被测系统测自己，且贵且不稳）；评测集只读人工筛过的固定语料，**永不回读线上库**——攻略社区虽然开放发布，但那和评测集是单向隔离的。hotstart（粘贴）成功判据里刻意不含「硬错 0」，因为粘贴行程的硬错可能是用户原文自带的，修不了不算引擎的锅。

---

### 第五层：踩坑复盘题（拉开差距的地方，挑 2~3 个讲透）

| # | 坑 | 一句话讲法（体现什么能力） |
|---|---|---|
| 5.1 | **DeepSeek 400** | 带 tools 的请求 + 历史缺 `reasoning_content` 稳定 400，langchain 封装会剥该字段——**吃透 provider 的坑要读它的序列化行为，不能只看文档** |
| 5.2 | **高德限流假阴性** | HTTP 200 + `status=0`，不抛异常；限流当「搜不到」会引发假硬错打回整轮——**错误处理要先分类语义再写分支** |
| 5.3 | **`biz_ext` 双类型** | 同一字段餐饮返回字符串、酒店返回空数组 `[]`——空数组收敛为 None **绝不能当 0**（门票 0 元和没数据是两回事）——**脏数据防御在最底层做一次，上层全部受益** |
| 5.4 | **MySQL 1267 排序规则** | 驱动 `SET NAMES utf8mb4` 把连接排序规则设成字符集默认 `utf8mb4_0900_ai_ci`，跟 `collation_server` 无关——**修法必须与驱动强制的连接默认对齐，不是随便挑一个**；这个坑耗了一下午，根因是凭直觉猜而不是读文档 |
| 5.5 | **Pydantic→TS 判别联合失效** | 有默认值的字段生成成可选（`type?: "check"`），SSE 事件按 `type` 收窄直接失效；根因是生成规则错了——后端是全字段 dump，TS 里就该全必填。**靠 `tsc --strict` 编译器抓到，不是靠肉眼** |
| 5.6 | **geocode 假修复** | 见 Q1.2。对照组思维：没有「不加中心点」的基线对比，根本发现不了加了等于没加 |

---

### 第六层：压力题 / 边界题（考察自省与边界感）

**Q6.1 需求变了怎么办？比如要支持多城市/跨境？**
- 要点：引擎分层是城市无关的（判据、循环、契约都不绑定成都）；要动的是数据源适配和搜索中心点策略（medoid 从锚点算，天然适配新城市）；真正的边界是高德数据覆盖（跨境没有）——**池空分因机制**已经把「数据源覆盖不了的目的地」识别出来不进工具循环（D67），直接告诉用户而不是硬编。

**Q6.2 为什么不做门票筛选/热度排序？**
- 要点：不是偷懒，是**数据不存在**——高德 `biz_ext.cost` 是人均消费不是门票、没有热度字段（参考竞品里「热度 10.0 / 收录 21006 个景点」全是种子数据假象）。用不存在的数据做筛选 = 造假。**把「不做什么」讲清楚比堆功能更有说服力**。

**Q6.3 这个项目上线了吗？并发能扛多少？**
- 要点：诚实——**拍板不部署不上线**（实习作品，演示=本机跑），但工程按「能上」的标准做：限流分层、断线恢复、幂等重试、双实例重启持久化验收都做了。多实例扩展的两个明确短板是进程内限流和内存会话，知道边界在哪比虚报数字强。

**Q6.4 你一个人做的？AI 帮你写了多少？**
- 要点（⚠️ 这是 2026 年面试新常态题，诚实+展示驾驭力）：AI 深度参与了开发，但**架构决策、契约设计、验收判据是我定的**——每条技术决策都有「替代方案 + 否决理由 + 代价 + 重审触发」的记录（可举例：SSE 恢复粒度选节点级的代价）；AI 产出必须过编译器、过 621 个测试、过浏览器实测验收。**重点是展示「我能验收 AI」而不是回避 AI**。

---

## 三、数字卡（背下来，面试时随口引用 = 真做过的信号）

| 数字 | 含义 |
|---|---|
| **621** | 测试全绿（pytest，50s 量级） |
| **5+4 / 3** | 硬判据 5 类 + 软判据 4 类；结果三态 pass/fail/unknown |
| **8 / 2 / 20** | L2 工具层上限 8 次 / L3 校验打回 2 次 / 单行程总调用 ≤20 |
| **3 + 1** | 3 个高德工具（search_poi / get_weather / calc_distance）+ 1 个搜索子 agent |
| **20 / 8** | 搜索取回 20 条、给模型看 8 条（两段式窗口） |
| **2.2 QPS** | 高德出站限速（实测 3 QPS 边界就挂，文档不可信） |
| **10/h·30/d** | chat 接口限流（按接口成本分层，chat 最严） |
| **404** | 拿不到私有资源返回 404 不是 403 |
| **4 天** | 高德天气只给 4 天（从当天算），不是 3 |
| **14 倍** | qwen 免费但比 deepseek 慢 14 倍（默认模型选型的实测依据） |
| **37 / 9 / 62** | 接口契约 37 端点 / 9 种 SSE 事件 / 自动生成 62 个 TS 类型 |

---

## 四、红旗清单（说了就减分）

1. ❌ **「高德数据里有热度/门票」** ——没有，说这个等于没亲手接过数据。
2. ❌ **「系统已上线/支持高并发」** ——拍板不部署，虚报必被追问穿。
3. ❌ **「前端不是我写的所以我不太清楚」** ——要换成「前端是契约驱动 + 我出验收清单跑浏览器实测」，并备好 tsc 抓类型问题的实例。
4. ❌ **「没做 X 是因为没时间」** ——项目里所有「不做」都有实证或代价分析（types 定向搜是实测否证、门票筛选是数据不存在）。
5. ❌ **把 mock 数据当真实数据讲** ——mock 与 real 同签名是为了并行开发，演示时要声明数据源档位（缺 Key 启动期**响亮降级**非静默，D66）。
6. ❌ **被追问代价时答「没什么代价」** ——每个决策都有代价表（SSE 节点级恢复→文字重复=D74、medoid→多一轮距离计算）。说「没什么代价」等于说没做过取舍。

---

## 五、待拍板（不影响本材料使用）

- 简历项目描述 3~5 行正式版（上一轮列的候选项①），需要的话下一轮单独出。
- 面试讲解稿 / demo 动线（候选项③④）。
