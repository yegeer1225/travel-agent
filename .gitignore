# ---- 密钥与本地配置（绝对不能进仓库）----
.env
.env.local
.env.*.local
# 2026-09-17：手动备份的 .env.deepseek-402-bak 原来漏网（下面只写了 .env.bak，
# 而实际文件名不带点）。它含真密钥，一次 `git add .` 就会写进 git 历史 ——
# 而密钥进了历史，删文件是补不回来的，只能轮换密钥。
# 所以用 `.env.*` 通配兜住所有手动备份，再显式放行模板。
.env.*
!.env.example

# ---- Python ----
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
env/
build/
dist/

# ---- 测试与检查工具缓存 ----
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# ---- 运行产物（数据库、日志、导出的报告）----
data/*.db
data/*.db-journal
data/*.sqlite
data/*.sqlite3
*.log
logs/
# 评测报告 = 一次运行的快照：LLM 有随机性，重跑数字就会变。
# 提交进仓库等于把一个会过期的数字钉死（"仓库说 100%，重跑 80%"），不如按需生成。
eval/reports/

# ---- 前端（将来加页面时用得上，先备着）----
node_modules/
dist/
.next/
*.tsbuildinfo

# ---- IDE ----
.idea/
.vscode/
*.swp

# ---- 系统 ----
.DS_Store
Thumbs.db
desktop.ini

# ---- AI 助手的工作目录（不属于项目源码）----
.workbuddy/
backend/uploads/

# ---- 内部工作文档（2026-09-20 决定不随代码公开）----
# 开发过程记录 / 决策 / 交接 / 求职材料 —— 已移到仓库外的 agent-notes/
# 这里再挡一道：将来若有人把同名文件放回来，也不会被误提交
DECISIONS.md
方案.md
技术方案.md
界面设计.md
前端交接.md
后端交接.md
面试材料-*.md
docs/LLM修复交接-*.md
