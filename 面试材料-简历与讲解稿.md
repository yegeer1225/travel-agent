# 面试材料 · 简历描述 + 讲解稿 + Demo 动线

> 2026-09-18。与《面试材料-追问预案.md》配套：预案管「被问」，这份管「主动讲」。

---

## 一、简历项目描述（正式版）

### 1.1 主推版本（简历项目栏，5 行）

> **智能旅游规划系统**（个人项目 · 全栈 AI 应用）　`FastAPI` `LangGraph` `DeepSeek` `MySQL` `SSE` `React`
>
> - 设计「验证 → 定位 → 修正 → 再验证」Agent 内核：LLM 生成行程后由**纯代码硬判据（5 类）+ 软判据（4 类）**校验，结果以**通过 / 不通过 / 无法判定三态**呈现，不通过自动打回重排，杜绝「缺数据伪装成通过」
> - 全 LangGraph 手写 StateGraph（会话层 + 工具层双循环 + 校验回环），接入高德真实数据 3 工具 + 1 搜索子 agent；工具循环 / 校验回环 / 单次总调用数三重上限，超限降级输出、不死循环
> - SSE 流式 + 断线按 checkpoint 节点级恢复；限流按接口成本分层、重试只针对幂等操作；私有数据隔离靠签名级参数约束 + 扫描断言测试固化，不靠「记得写 WHERE」
> - 契约驱动开发：Pydantic schema 为唯一事实源，前端 62 个 TS 类型自动生成 + 编译器校验，前端实现与后端内部演进完全解耦
> - **621 个测试全绿**，MySQL 真库验收通过（含双实例重启持久化）；关键数据边界经实测确认（高德限流假阴性、2.2 QPS 出站限速、字段双类型防御）

### 1.2 精简版（一页简历空间紧张时，2 行）

> **智能旅游规划系统**：LangGraph 手写 StateGraph 实现「生成→代码校验→自动修正」Agent 内核（硬5软4判据·三态呈现·成本硬上限），高德真实数据工具 + SSE 断线 checkpoint 恢复，Pydantic 契约自动生成前端类型；621 测试全绿、真库验收通过。

### 1.3 使用注意

- 「621 个测试」是最大差异化数字——面试官追问测试写了什么，往 Q3/Q4 层引（隔离扫描断言、mock 同签名、评测判分不碰 LLM）。
- 不要写「支持高并发」「已上线」——见预案红旗清单。
- 若 JD 强调前端，可加一行：「前端 React 19 + Vite + Tailwind 4，契约驱动下与后端并行开发」。

---

## 二、3 分钟白板讲解稿（叙事线）

> 场景：面试官说「花几分钟讲讲这个项目」。目标是 3 分钟内讲完主干，把追问主动引到你最熟的三个区（三态 / 成本控制 / 数据边界）。

**【0:00–0:30 定位与动机】**
「我做的是一个智能旅游规划系统。市面上的行程生成本质是『LLM 一次输出』，但 LLM 会编景点、会把数据缺失当成事实。所以我的核心设计是：**生成完不直接给用户，先用纯代码校验**——查 POI 是否真实存在、营业时间是否对得上、地理顺序是否合理，发现问题自动打回重排，再验证。」

**【0:30–1:15 内核架构】**
「架构上是全 LangGraph 手写 StateGraph，三种循环叠加：会话层对话循环、工具层信息收集循环（挂 3 个高德真实数据工具 + 1 个搜索子 agent）、以及校验回环——校验不通过路由回重排节点。这里有个关键取舍：**硬判据全部纯代码实现、不过 LLM**，因为判据要可证伪；软判据只产出『提醒』不产出『事实断言』，因为它没有硬守门。输出是三态：通过、不通过、**无法判定**——高德门票数据大面积缺失，把『没数据』显示成『通过』是造假。」

**【1:15–2:00 成本与可靠性】**
「第二块我特别在意的是失控防护：工具层单轮上限 8 次、校验打回 2 次、单次行程总调用 ≤20，超限降级输出并标注哪些没验证，绝不死循环也不抛 500。流式用 SSE，断线从 LangGraph checkpoint 按节点恢复——我主动选了节点粒度，代价是重连后当前节点文字会重复一遍，评估后接受，因为做 token 级回滚的复杂度不值得。」

**【2:00–2:40 数据边界的实测】**
「第三块是外部数据 never trust：高德限流返回 HTTP 200 + status=0、不抛异常，如果当『搜不到』处理，模型会以为景点不存在、触发假硬错白打回整轮——所以限流必须走重试退避；出站限速实测 2.2 QPS，不是文档写的 3。搜索结果取回 20 条给模型看 8 条，候选只重排不过滤。」

**【2:40–3:00 收尾量化】**
「工程侧契约用 Pydantic 做唯一事实源，前端类型自动生成；621 个测试全绿，MySQL 真库验收含双实例重启持久化。项目的所有技术决策都有『替代方案 + 否决理由 + 代价』的记录文档。」（← 这句是钩子，引面试官问决策细节，正好进你背熟的预案。）

---

## 三、Demo 动线（现场/录屏脚本，本机演示 · 已拍板不部署）

### 3.1 演示前置检查（面试前一天跑一遍）

1. Docker MySQL 起来（`docker ps` 见 travel-mysql）；高德 Key 有效
2. 后端 `python -m uvicorn app.api.main:app --port 8000`；前端 `npm run dev`
3. 全量回归一遍 pytest（确认 621 绿）；演示期限流靠**重启清零**（D68），别改阈值
4. ⚠️ LLM 有延迟波动：**全程录屏一份做兜底**，现场卡了切录屏，讲「这是之前录的，现场网络波动」

### 3.2 演示动线（8 个站，约 6 分钟）

| # | 操作 | 边操作边说 | 风险与预案 |
|---|---|---|---|
| 1 | 首页 | 「轮播图是高德真实景点照片，不是素材库图——接的是真实数据源」 | 无 |
| 2 | 景点页搜「宽窄巷子」 | 「搜索直接查**本站收录库**，库里没有就返回空、**不回落高德**——不造假数据」 | 库里只收录了 5 个成都地标，搜别的词是空态，**就演示宽窄巷子** |
| 3 | 进 AI 助手，输入一句话需求（例：「帮我规划成都 3 天行程，2 个人，第一天下午才到」） | 「注意看左侧 agent 过程可视化：现在在工具循环，搜 POI、查天气、算距离」 | 生成 1~3 分钟，**这段的空档是讲架构的最佳时间**（白板讲解稿 1:15–2:00 段） |
| 4 | 指着三态标记 | 「绿色=代码验证过，红色=不通过会打回重排，灰色=**无法判定**——数据缺了就说缺了」 | **全片核心画面，停留最久** |
| 5 | 路线总览页，拖拽一个站点 | 「界面二能确定性修改，不经过 LLM——PATCH 是锚点顺推算法，改结果不重新生成」 | 无 |
| 6 | 粘贴一份现成行程（热启动） | 「另一个入口：粘贴不生成、直接校验修正。LLM 只抽地名，POI 落地走同一套真实数据库」 | 预先在记事本备好一段粘贴文本 |
| 7 | **断网演示**（拔网/关代理）：流中途断 → 恢复 | 「断线不重发请求，从 checkpoint 按节点恢复续跑——代价是当前节点文字重复，这是我自己拍板接受的取舍」 | ⚠️ 用**第二轮长流**断（第一轮追问轮几秒就完，断不到）；本机可用关 Clash 模拟 |
| 8 | 攻略社区 → 官方号攻略 | 「社区内容从真实行程发布而来，评测集与线上库单向隔离——不用线上数据考自己」 | 官方号已有 2 篇 seed 攻略（成都/杭州） |

### 3.3 演示红线

- ❌ 不演示注册上传等边角功能（时间花在 4/7 两站）
- ❌ 不说「生产环境」，说「工程按能上线的标准做，拍板不部署」
- ❌ mock 档位若被触发（缺 Key 启动期降级）要**主动声明**「现在是 mock 数据」——响亮降级是设计好的行为（D66），诚实声明反而加分

---

## 四、待拍板

- 简历版本是否要针对特定 JD 出定制变体（投递前给我 JD 即可）
- 讲解稿要不要录一遍你自己的口播版做复述练习（带读 A+B 模式的话术版）
