# 智能旅游规划系统 · 界面设计稿

> 本文件只回答「**界面长什么样、每页放什么、占位做到哪一步**」。
> 项目做什么见 `方案.md`，用什么技术见 `技术方案.md`。
>
> 状态：**v1 设计稿（占位阶段）**，2026-09-15
> 参考：用户提供的 B 站旅游平台前端截图 4 张（首页 / 景点库 / 个人中心 / 攻略社区）

---

## 一、前端分工与技术选型 ✅ 已定（2026-09-15 拍板）

### 结论：**页面实现交给「豆包」，本 AI 只做后端 + 契约 + 交接文档**

| 谁 | 干什么 |
|---|---|
| **豆包** | 全部页面实现（React + Vite + TS + Tailwind） |
| **本 AI** | 后端 + Agent 引擎 + **数据契约 / 接口契约 / SSE 事件表** + 评测 + **给豆包的交接文档** |
| **你** | 拿交接文档喂豆包、验收页面对不对、**前后端集成调试** |

### 为什么这样切不会"来回换工具很麻烦"

**切换成本 = 契约变更次数，跟换不换工具无关。**

| 后端改这些 | 前端要改吗 |
|---|---|
| qwen 换 deepseek | ❌ |
| 工具从 3 个加到 5 个 | ❌ |
| 校验从 2 轮改到 3 轮 | ❌ |
| 换 checkpoint 存法、加缓存、改 prompt | ❌ |
| **给 Trip JSON 加字段并要显示出来** | ✅ 必须改 —— **这时任何工具都得改** |

所以真实的成本是「**契约变更了几次**」，不是「换了几个工具」。契约变更时找豆包还是找本 AI，工作量一样。

**推论（决定了 M0 的分量）**：**契约一旦冻结，后端内部实现随便换，前端零改动；契约一旦变更 = 前后端双倍成本。** 所以 M0 必须一次定扎实，别边写边加字段。

### 前端技术栈（写给豆包，精确版本）

| 项 | 版本 / 说明 |
|---|---|
| 框架 | **React 19.3.0** |
| 构建 | **Vite 8.3.0**（要求 Node `^20.19 \|\| >=22.12`，本机 22.22.2 ✅） |
| 语言 | **TypeScript 7.0.2** |
| 样式 | **Tailwind CSS 4.3** —— ⚠️ **CSS-first，没有 `tailwind.config.js`**，主题走 `@theme` |
| 路由 | react-router-dom 7.18.3 |
| 拖拽 | `@dnd-kit/core` 6.3.1（**不用 SortableJS**，它要手动跟 React 的 DOM 对齐，更绕） |
| 地图 | `@amap/amap-jsapi-loader` 1.0.1（异步加载 + `useEffect`） |
| SSE 消费 | **`fetch` + `ReadableStream` 手写解析**（`EventSource` 只支持 GET，而聊天要 POST） |

**四个必须写进交接文档的坑**：

1. 🔴 **React 19 严格模式下 `useEffect` 跑两次** → SSE 若建在 effect 里，agent 会跑两遍、工具调两遍、**DeepSeek 扣两次钱**。effect 里必须 return abort 清理
2. 🔴 **Tailwind 4.3 没有 `tailwind.config.js`** → 默认按 v3 写法生成会直接跑不起来
3. **高德 JS API 的 key ≠ 后端那个 key** → 「Web 服务」给后端、「Web端(JS API)」给前端；2021-12 后申请的 JS key 还要配 `securityJsCode`
4. **Vite proxy 转发 SSE** → dev 阶段别压缩/缓冲，否则事件憋到最后一起吐

### 交接包的边界：什么给豆包，什么不给

| 给 | 不给 |
|---|---|
| 设计 token（色板 / 字体栈 / 三态色 / 形状规则） | ❌ 后端代码 |
| 六页结构 + 组件清单 | ❌ 数据库表结构 |
| **API 契约**（路径 / 方法 / 请求体 / 响应样例 / 错误码） | ❌ LangGraph 图结构、prompt |
| **SSE 事件表**（事件名 + payload + 该怎么渲染） | ❌ 高德 Key（前端只需 JS API 那个，且走 `.env`） |
| 异步加载 / 卸载清理 / 严格模式的代码约束 | ❌ 评测集原文 |

### 豆包并行开工的前提：mock 模式

`providers/` 里 **mock 与 real 同签名**（已定架构）。开 mock 模式 = **真结构 + 假数据**：

- 豆包不用等后端做完，拿到契约就能开工
- 前后端用同一份 `schemas.py` 对应的 TS interface，字段对不上**编译期就报错**

⚠️ **给我自己的硬要求**：mock 的响应结构**必须和 real 完全一致**（同一个 Pydantic 模型序列化出来）。mock 少一个字段，豆包写完的页面在接真数据时会当场崩 —— 而那时你会以为是豆包的锅。

---

## 二、视觉规范：**几何拼贴风** ✅ 已拍板（2026-09-15）

**风格内核**：白底 + 高饱和几何色块 + 直角 + 粗下划线高亮。孟菲斯波普 / 现代平面海报语言。
**它为什么被选中**：三套候选里唯一「**浅色底 + 高饱和**」的 —— 不用改高德地图样式，也不吃掉校验三态。

### 2.1 色板

| 用途 | 色值 | 说明 |
|---|---|---|
| 页面底色 | `#FFFFFF` | 纯白，拼贴风的必要留白 |
| 卡片底色 | `#FFFFFF` + `1px #1A1A1A` 细边 | 靠边线分区，不靠底色 |
| **主文字 / 描边** | `#1A1A1A` | 近黑 |
| **装饰色 1 珊瑚红** | `#FF6B6B` | — |
| **装饰色 2 青** | `#4ECDC4` | — |
| **装饰色 3 黄** | `#FFD93D` | — |
| **装饰色 4 绿** | `#6BCB77` | ⚠️ **禁止用于校验态**，见 2.3 |
| **装饰色 5 蓝** | `#4D96FF` | — |
| 次要文字 | `#6B7280` | — |
| **校验三态** | 通过 `#16A34A`／不通过 `#DC2626`／**无法判定 `#D97706`** | ⚠️ **独立定义，不复用装饰色板**，见 `方案.md` 6.2 |

### 2.2 形状与版式规则（风格的全部就在这里）

| 元素 | 规则 |
|---|---|
| 圆角 | **直角**（`0`）到 `4px` 为止 —— 这是跟"卡通风大圆角"最大的区别 |
| 描边 | `1px solid #1A1A1A`（细边）／强调块用 `2px` |
| 阴影 | **基本不用阴影**；需要凸起时用「实心色块错位」而不是投影 |
| **强调手段** | **4px 粗下划线**（关键词高亮）、**实心色块**（分类入口）、**轻微旋转 -1°**（黑色实心按钮） |
| 几何装饰 | 正圆（`border-radius: 50%`）、三角（`clip-path: polygon()`）、色块 —— 漂浮错落摆放 |
| 字体 | 标题：**Space Grotesk** 粗体 + 大写 + 宽字距；正文：Noto Sans SC |
| ⚠️ 字体栈必须写成三层 | `['Space Grotesk','Noto Sans SC','system-ui']` —— **Space Grotesk 不含中文字形**，不写 fallback 中文标题会掉档次 |

### 2.3 两条硬约束（不然会出问题）

**① 装饰色绝对不能拿去当语义色。**
装饰绿 `#6BCB77` 跟校验通过的绿是同一色感区间。校验三态必须独立定义：

```css
--color-status-pass:    #16A34A;
--color-status-fail:    #DC2626;
--color-status-unknown: #D97706;
```

否则将来一改品牌色，校验的区分度跟着一起毁 —— 而**校验三态是这个项目最值钱的可视化**（面试官会盯着问"你怎么区分'确认不对'和'不知道对不对'"）。

**② 装饰分主次：工具页必须收敛。**

| 页面 | 任务 | 装饰用量 |
|---|---|---|
| 首页 / 旅游攻略社区 | **讲故事** | ✅ 满铺：漂浮圆 + 三角 + 色块卡 + 旋转徽章 |
| AI 助手页 / 路线总览 | **读数据** | ⚠️ **只保留两样：色块 + 粗下划线**。不叠漂浮几何 —— 那里要放 12 站表格 + 地图浮层 + 校验角标，再加装饰会读不下去 |

### 2.4 Tailwind 4 写法（CSS-first，**没有 `tailwind.config.js`**）

```css
/* src/index.css —— Tailwind 4 的 @theme，不是 v3 的 config 文件 */
@import "tailwindcss";

@theme {
  --color-ink: #1A1A1A;
  --color-pop-red: #FF6B6B;
  --color-pop-cyan: #4ECDC4;
  --color-pop-yellow: #FFD93D;
  --color-pop-green: #6BCB77;
  --color-pop-blue: #4D96FF;
  --color-status-pass: #16A34A;
  --color-status-fail: #DC2626;
  --color-status-unknown: #D97706;
  --font-display: 'Space Grotesk', 'Noto Sans SC', system-ui, sans-serif;
}
```

### 2.5 不做什么（视觉层面）

| 不做 | 理由 |
|---|---|
| 暗色模式 / 主题切换 | 用户明确不要（毕设那次也是这个偏好） |
| 渐变、玻璃拟态、柔和阴影、发光 | 跟"平涂色块"的拼贴语言冲突 |
| 圆角超过 4px / 胶囊按钮 | 这是"卡通风"的特征，跟本风格相反 |
| 投影式阴影 | 需要层次时用**错位实心色块** |
| 动画超过 200ms | 拖拽排序要跟手 |

### 2.6 ⚠️ 这套风格的最大成本：那张几何插画

参考图的分数 **70% 在那张插画本身**（几何拼贴的风景/建筑），不在配色。配色表谁都会抄，**插画抄不来**。

| 选项 | 成本 | 风险 |
|---|---|---|
| **自己画 SVG**（多边形拼） | 1 页约 2~3 小时 | 需要一点审美，但可控、无版权风险 |
| 买/找素材 | 免费图库质量不稳，付费约 ¥20~80 张 | 版权与风格统一问题 |
| 🔴 **让 AI 生成后描出来** | — | **AI 出的是位图、文字乱码、风格每张都不一样**，不适合当插画素材 |

**建议**：每个需要的页面各画 1 张 SVG（首页 Hero 1 张 + 攻略社区头图 2~3 张），存在 `frontend/src/assets/` 里，**交接文档里直接给豆包**，别让它重画（它大概率会糊成色块加渐变，那就废了）。

---

## 三、全局骨架

### 3.1 导航栏（全站唯一，`<Layout>` 组件里写一次，六个路由共用）

```
┌──────────────────────────────────────────────────────────────────────────┐
│ [TR] 旅游规划平台  首页 旅游景点 AI路线规划助手 路线总览 旅游攻略 个人中心  │
│                        ↑黄色高亮当前页                                    │
│                              [搜索景点/攻略/城市____] [搜索] [👤admin][退出]│
└──────────────────────────────────────────────────────────────────────────┘
```

| 项 | 说明 |
|---|---|
| logo + 站名 | 左侧，青绿色方块 `TR` + 「旅游规划平台」 |
| **6 个导航项** | 首页 / 旅游景点 / **AI 路线规划助手** / **路线总览** / 旅游攻略 / 个人中心 |
| 当前页高亮 | **实心色块底**（`#FFD93D`）+ 黑字；不用描边，不用圆角 |
| 搜索框 | 占位：输入后回车跳到「旅游景点」页并带上关键词（不实现真搜索） |
| 用户区 | 头像 + 昵称 + 「退出登录」；M9 之前写死显示 `admin` |

> ⚠️ **`路线总览` 是我们比参考设计多出来的一项，也是核心** —— 参考设计的导航里没有它，它聊完就结束了。我们多了"结构化行程 + 地图 + 能手改"这整个闭环，**这一项就是差异化本身**，不能省。

### 3.2 页面通用结构

```
┌─ 导航栏 ─────────────────────────────────────────────┐
├─ 页头区（白卡）：英文小标 + 中文大标题 + 一句话说明 + 右侧动作按钮 ─┤
├─ 内容区 ────────────────────────────────────────────┤
└─ 页脚（一行版权 + 一行"数据来源：高德开放平台"）──────────┘
```

---

## 四、逐页设计

标注：**🟢 真做**（M6/M7）｜**🟡 占位**（结构做出来，数据后用）｜**🔵 半真**（结构 + 真数据，功能后置）

---

### 4.1 首页 🟡 占位

**用途**：第一眼说清"这是什么"，并把用户推进 agent。

```
┌─ Hero（纯白底 + 右侧几何拼贴插画 SVG，无遮罩、无渐变）──────────────┐
│                                       ┌────────────────────┐        │
│   规划一次不用返工的旅行               │   ◆ 几何拼贴插画 ◆  │        │
│   ← 主标题（大、粗、大写、宽字距）     │   山 / 路 / 太阳    │        │
│                                       │   高饱和多边形拼贴   │        │
│   说出目的地和日期，AI 帮你查天气、     └────────────────────┘        │
│   排路线、验可行性                                                   │
│   ← ★ 这句就是"一句项目介绍"（位置定了）   ○ △ □ ← 漂浮几何装饰      │
│                                                                      │
│   [ 开始规划 → ]  ← 黑色实心块，旋转 -1°，直跳「AI 路线规划助手」    │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
┌─ Recommended / 猜你喜欢 ────────────────────────────[ 查看全部景点 ]─┐
│  ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐                     │
│  │ 图块   │  │ 图块   │  │ 图块   │  │ 图块   │                     │
│  ├────────┤  ├────────┤  ├────────┤  ├────────┤                     │
│  │ 武侯祠 │  │宽窄巷子│  │杜甫草堂│  │  锦里  │                     │
│  │ 成都   │  │ 成都   │  │ 成都   │  │ 成都   │                     │
│  └────────┘  └────────┘  └────────┘  └────────┘                     │
└──────────────────────────────────────────────────────────────────────┘
```

**★ "一句项目介绍"的位置已定：Hero 大图上的副标题**（主标题正下方，大按钮正上方）。理由：这是全站最显眼的位置，且和"开始规划"按钮连成一句完整的话——"这是什么 → 点这里开始"。

| 区块 | 内容 | 数据来源 | 占位怎么做 |
|---|---|---|---|
| Hero 主标题 | 「规划一次不用返工的旅行」 | 写死 | — |
| Hero 副标题 | **一句项目介绍**（见上） | 写死 | — |
| Hero 插画 | 几何拼贴 SVG（山/路/太阳） | **自绘**（见 2.6） | 先放几何色块占位，插画后补 |
| 大按钮 | 「开始规划 →」 | — | 直接跳 `/assistant` |
| 猜你喜欢 | 4 张卡：图 + 景点名 + 城市 | 高德 `search_poi`（M2 后） | **用真实景点名 + 纯色/灰色图块，不写任何数字** |

⚠️ 猜你喜欢**不做轮播图**（参考设计的 Hero 是可轮播的景点大图）—— 我们的 Hero 位置留给"项目介绍 + 行动按钮"，那才是首页该干的事。轮播是平台型网站的做法。

---

### 4.2 旅游景点 🟡 占位

**用途**：让用户按关键词找景点。**这一页跟参考设计的"景点库"根本不同 —— 详见下方警告。**

```
┌─ Scenic Spots / 旅游景点库 ────────────────────────────────────────┐
│ 输入关键词搜索景点，数据来自高德开放平台（实时）                      │
├────────────────────────────────────────────────────────────────────┤
│ 关键词[____________]  城市[全部城市 ▾]  [筛选] [重置]                │
├────────────────────────────────────────────────────────────────────┤
│  ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐                    │
│  │ 图块   │  │ 图块   │  │ 图块   │  │ 图块   │                    │
│  │ 名称   │  │ 名称   │  │ 名称   │  │ 名称   │                    │
│  │ 城市·区│  │ 城市·区│  │ 城市·区│  │ 城市·区│                    │
│  │ 人均消费│ │ 评分   │  │        │  │        │                    │
│  │[查看详情]│ │[查看详情]│ │[查看详情]│ │[查看详情]│                  │
│  └────────┘  └────────┘  └────────┘  └────────┘                    │
└────────────────────────────────────────────────────────────────────┘
```

🔴 **这一页最容易做错，三条必须遵守：**

| 参考设计有 | 我们**不能**抄 | 原因 |
|---|---|---|
| 「共收录 21006 个景点」 | ❌ 删掉 | **我们没有本地 POI 数据**（不爬虫、不建 POI 表）。写个 21006 就是编的 |
| 「门票」筛选 + 价格「免费/35.0/405.0」 | ❌ 改成「人均消费」，且**不做门票筛选** | 高德 `biz_ext.cost` 是**人均消费不是门票**，且门票数据大面积缺失。它那个卡片能整齐显示价格 = 它自己造的 |
| 「热度 10.0 / 405.0」 | ❌ 删掉 | 高德**没有这个字段**，跟已删的"趣味度"是同一类自造指标 |

**结构性差异（必须理解）**：参考设计的景点页是**列表页**（进来就摆出全部），我们是**搜索页** ——

```
参考设计： 进来 → 数据库查 21006 条 → 展示（有本地数据）
我们：     进来 → 调高德 search_poi(keywords) → 展示（无本地数据）
```

**所以我们的默认状态是"空"，必须显示引导文案**（💡 建议："输入景点名称或关键词开始搜索，例如「成都 博物馆」"），而不是硬凑几张卡。

---

### 4.3 AI 路线规划助手 🟢 真做（M6）

**用途**：项目的核心页一。左历史栏 + 右聊天框，**SSE 实时显示 agent 在干什么**。

```
┌─ 左：历史会话栏（260px） ─┬─ 右：聊天区 ──────────────────────────┐
│ [+ 新建规划]              │  ┌────────────────────────────────┐  │
│ ─────────────────────     │  │ 🤖 你准备去哪儿？想玩几天？     │  │
│ ▸ 成都3日游·带爸妈        │  │    预算和同行人是？（必问项）   │  │
│   2026-09-15              │  └────────────────────────────────┘  │
│ ▸ 杭州2日周末             │  ┌────────────────────────────────┐  │
│   2026-09-14              │  │ 成都，3天，带爸妈              │  │
│ ▸ 粘贴的成都行程          │  └────────────────────────────────┘  │
│                           │  ┌────────────────────────────────┐  │
│                           │  │ 🤖 正在搜索「成都 宽窄巷子」... │  │
│                           │  │    ⟳ 工具轨迹卡（实时刷新）     │  │
│                           │  ├────────────────────────────────┤  │
│                           │  │ 🗺 行程卡（3天·12站·无硬错）    │  │
│                           │  │    [ 生成路线总览 → ]           │  │
│                           │  └────────────────────────────────┘  │
│                           │  ┌────────────────────────────────┐  │
│                           │  │ 输入消息...              [发送]│  │
│                           │  └────────────────────────────────┘  │
└───────────────────────────┴────────────────────────────────────┘
```

| 区块 | 内容 | 说明 |
|---|---|---|
| 历史栏 | 会话列表（标题 + 日期），点击继续改 | **活会话**：点开能接着聊（A9） |
| 首条消息 | LLM 生成，但**必问项一个不漏** | 代码校验字段集合（A8） |
| **工具轨迹卡** | 「正在查天气」「正在搜武侯祠」逐条出现 | **SSE 节点级事件**，不是打字机效果 |
| 行程卡 | 天数 / 站数 / 硬错数 + 「生成路线总览」按钮 | 点按钮跳界面二（A10） |
| 校验卡 | 通过 / 不通过（已修）/ 无法判定 三类分开列 | 三态（A18） |

> 视觉重点：**让"agent 在干活"看得见**。轨迹卡是这一页跟"普通聊天机器人"的唯一区别，颜色和动效要给它最多注意力。

---

### 4.4 路线总览 🟢 真做（M7）

**用途**：核心页二。左表格 + 右地图 + 下方按天卡片，**能改**。

```
┌─ 路线总览 ──────────────────────────[重新深度检查] [导入高德 APP]─┐
├───────────────────────────┬───────────────────────────────────────┤
│ 左：站点表格（可拖拽排序） │  右：高德地图                          │
│ ┌──┬────────┬─────┬────┐  │  ┌─────────────────────────────┐      │
│ │序│ 名称   │到达 │停留│  │  │[Day1][Day2][Day3] ← 左上角页签│      │
│ ├──┼────────┼─────┼────┤  │  │                              │      │
│ │ 1│武侯祠  │09:00│ 90'│  │  │   ①──②──③  只画当天路线       │      │
│ │ 2│杜甫草堂│10:45│ 60'│  │  │   (12 站编号 + 连线 + 距离)   │      │
│ │ 3│锦里    │12:15│ 90'│  │  │                              │      │
│ └──┴────────┴─────┴────┘  │  └─────────────────────────────┘      │
│  ⠿ 拖动手柄  ✕ 删站  ✎ 改时间│  ⚠️ 切页签只显示当天（A17）          │
├───────────────────────────┴───────────────────────────────────────┤
│ Day 1  10月1日  晴 26°/18°   ·  12.4km · 车程 46min · 步行 4.2km    │
│ ┌────────┐ ┌────────┐ ┌────────┐                                  │
│ │①武侯祠 │ │②杜甫草堂│ │③ 锦里  │   ← 按天分组卡片                 │
│ │09:00   │ │10:45   │ │12:15   │                                  │
│ │✅✅⚪  │ │✅⚪    │ │✅✅    │   ← 校验三态角标                 │
│ └────────┘ └────────┘ └────────┘                                  │
└───────────────────────────────────────────────────────────────────┘
```

| 交互 | 触发什么 |
|---|---|
| 拖拽改顺序 | **纯代码重算**（时间 + 距离 + 校验），**不调 LLM**（A13） |
| 删站 / 改时间 | 同上 |
| 「重新深度检查」 | 才调 LLM 跑软校验 |

> 这一页的 Day 页签只显示当天路线 —— 参考设计里完全没有这一页，这是我们的原创部分。

---

### 4.5 旅游攻略 🟡 占位（M10 真做）

**用途**：攻略社区。列表 + 搜索 + 发布。

```
┌─ Travel Notes / 旅游攻略社区 ──────────────────────[ 发布攻略 ]─┐
│ 搜索目的地、标题和正文，查看其他用户的旅行经验                    │
├─────────────────────────────────────────────────────────────────┤
│ 关键词[___________]  城市[全部城市 ▾]  [搜索] [重置]            │
├─────────────────────────────────────────────────────────────────┤
│ ┌──────────┐ ┌──────────┐ ┌──────────┐                         │
│ │  图块    │ │  图块    │ │  图块    │                         │
│ │[成都][12]│ │[杭州][8] │ │[哈尔滨][5]│  ← 城市标签 + 浏览数     │
│ │成都3日游 │ │杭州2日游 │ │哈尔滨3日 │                         │
│ │摘要一行…  │ │摘要一行…  │ │摘要一行…  │                         │
│ │作者·日期 │ │作者·日期 │ │作者·日期 │                         │
│ │[查看攻略]│ │[查看攻略]│ │[查看攻略]│                         │
│ └──────────┘ └──────────┘ └──────────┘                         │
└─────────────────────────────────────────────────────────────────┘
```

**占位阶段怎么填**：直接用**评测集那 10~20 篇真实攻略**（它们和 `guide_posts.content_md` 同源）。

**这样做的两个好处**：① 不用编数据 ② 顺便验证了"攻略与评测集同源"这条设计。

🔴 **不抄的地方**：

| 参考设计有 | 我们 |
|---|---|
| 「共收录 51 篇攻略」 | ❌ 不写总数（初期列表本来就是空的） |
| 作者 `guide_seed_user` | ❌ 不要这种种子账号名，用真实昵称；**系统攻略显示"官方"**（`author_type = system`，`user_id` 为 NULL） |
| 「122 浏览」 | 💡 可留（我们自己能算），但**占位阶段先不显示** |

**M10 补齐**：详情页 + 发布页 + **评论 + 点赞**（A29）。

---

### 4.6 个人中心 🟡 占位（M9 真做）

**用途**：资料 + 我的资产入口。

```
┌─ User Center / 个人中心 ────────────────[我的收藏] [我的攻略]─┐
│ 管理个人资料、收藏景点、发布攻略和 AI 路线规划记录。           │
├──────────────────────┬─────────────────────────────────────────┤
│ ┌───── 左卡 ──────┐  │ ┌──────── Profile / 资料设置 ────────┐  │
│ │    ┌──────┐     │  │ │ 用户名 [admin__________]          │  │
│ │    │ 头像 │     │  │ │ 邮箱   [________________________]  │  │
│ │    └──────┘     │  │ │ 头像   [选择文件 未选择任何文件]    │  │
│ │  2026-09-15 加入│  │ │                                   │  │
│ │     admin       │  │ │        [ 保存资料 ]               │  │
│ │  暂未填写邮箱   │  │ └───────────────────────────────────┘  │
│ └─────────────────┘  │ ┌─ 我的 AI 路线规划记录 ─────────────┐  │
│                      │ │ ▸ 成都3日游·带爸妈   2026-09-15   │  │
│                      │ │ ▸ 杭州2日周末        2026-09-14   │  │
│                      │ └───────────────────────────────────┘  │
└──────────────────────┴─────────────────────────────────────────┘
```

✅ **采纳参考设计的一点**：它的个人中心说明里写着"管理个人资料、收藏景点、发布攻略和 **AI 路线规划记录**" —— 这跟我们已经定的「个人中心 = `trips` 表展示」**撞上了**，说明这个设计方向是对的。所以加一块「我的 AI 路线规划记录」区块，点进去就是那次的路线总览。

> M9 之前：导航右侧固定显示 `admin`，这一页能打开、能看、**点了保存不报错就行**（不落库）。

---

## 五、数据来源标注（画界面时必须知道每块数据哪来的）

| 界面元素 | 数据来源 | 什么时候有 |
|---|---|---|
| 景点名 / 坐标 / 地址 | 高德 `search_poi` | M2 |
| 评分 `rating` | 高德 `biz_ext.rating`（**仅部分 POI 类型有**） | M2 |
| 人均消费 | 高德 `biz_ext.cost`（**是人均，不是门票**） | M2 |
| 天气（白天/夜间/温度/风力） | 高德天气 `extensions=all`（**只有 3 天**） | M2 |
| 距离 / 车程 / 步行 | **代码算**（高德测距） | M2 |
| 校验三态 | 代码（硬判据）／LLM（软判据） | M3 |
| 行程结构 | agent 产出 | M1 |
| 攻略正文 | 评测集原文（手存 10~20 篇） | M0 后 |
| 用户名 / 头像 | 本地 | M9 |
| 热度 / 趣味度 / 门票 | ❌ **高德没有，不做** | — |

---

## 六、占位阶段的完成判据

占位做到什么算过：

1. **6 个导航项都能点开**，页面之间能互相跳转，不报错
2. **风格统一**（同一套装饰色板 / 直角 / 细边 / 粗下划线；工具页已收敛掉漂浮几何装饰）
3. **零假数据** —— 没有编造的数字（2 万个景点、热度 10.0、门票价格）；缺数据的格子显示引导文案或留白
4. **两个核心页有明确入口**（首页大按钮 + 导航项 + 行程卡上的「生成路线总览」）
5. **响应式不要求**（只保证 1440px 宽度下正常，不做手机端）
6. **三态色用的是独立变量**（`--color-status-*`），没有复用装饰色板

---

## 七、已定 / 待定

### 7.1 已定

| 项 | 结论 |
|---|---|
| 视觉风格 | **几何拼贴风**（见第二节），白底 + 高饱和色块 + 直角 + 粗下划线 |
| 前端分工 | **页面实现交豆包**，本 AI 只做后端 + 契约（见第一节） |
| Hero 用什么图 | ❌ **不用照片**，用**自绘几何拼贴 SVG**（不下载网图，避免版权；也不用 AI 生成的位图） |
| 图标 | 内联 SVG 手写几个（搜、用户、退出、拖拽、删除、编辑） |
| 首页「猜你喜欢」4 个景点 | 成都四选：武侯祠 / 宽窄巷子 / 杜甫草堂 / 锦里（跟首个评测集城市一致） |
| 导航栏搜索框行为 | 占位：回车跳「旅游景点」页并带上关键词 |

### 7.2 待定（不阻塞，做到那步再定）

| # | 问题 | 什么时候必须定 |
|---|---|---|
| 1 | 几何插画具体画几张、画什么 | M6 做首页前（见 2.6 成本分析） |
| 2 | 首页轮播/动效要不要 | 不做轮播（已定）；动效只做 200ms 内 |
| 3 | 攻略社区头图 | M10 前 |

---

## 七、还没定的

| # | 问题 | 建议 |
|---|---|---|
| 1 | Hero 大图用什么图 | 先用纯色块 + 渐变压暗占位，等有真实图片再换（**不下载网图，避免版权**） |
| 2 | 图标怎么来 | 内联 SVG，手写几个（搜、用户、退出、拖拽、删除、编辑） |
| 3 | 首页「猜你喜欢」的 4 个景点定哪几个 | 先用成都的（武侯祠 / 宽窄巷子 / 杜甫草堂 / 锦里）—— 跟首个评测集城市一致 |
| 4 | 搜索框（导航栏）行为 | 占位：回车跳「旅游景点」页并带上关键词，不实现真搜索 |
