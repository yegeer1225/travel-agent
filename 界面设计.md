# 智能旅游规划系统 · 界面设计稿

> 本文件只回答「**界面长什么样、每页放什么、占位做到哪一步**」。
> 项目做什么见 `方案.md`，用什么技术见 `技术方案.md`。
>
> 状态：**v1 设计稿（占位阶段）**，2026-09-15
> 参考：用户提供的 B 站旅游平台前端截图 4 张（首页 / 景点库 / 个人中心 / 攻略社区）

---

## 一、前端技术选型 ✅ 已定（2026-09-15）

### 结论：**FastAPI + Jinja2 模板 + 原生 JS**，不引入构建工具

| 层 | 选型 | 说明 |
|---|---|---|
| 后端 | **FastAPI**（不变） | 已定，不动 |
| 页面渲染 | **Jinja2 模板** | FastAPI 官方原生支持，`Jinja2Templates` 一行接入 |
| 复用 | `{% extends "base.html" %}` + `{% include %}` | 导航栏、页脚、卡片写一次全站复用 |
| 交互 | **原生 JS（ES 模块）** | 聊天 SSE 用 `fetch` + `ReadableStream`；拖拽用 SortableJS |
| 样式 | **手写 CSS**（一套变量 + 一个 components.css） | 不引 Tailwind/UnoCSS，避免构建步骤 |
| 不引入 | ❌ React / Vite / npm / node_modules | 不需要构建，改完刷新即可 |

### 为什么否掉 Django

| # | 问题 | 说明 |
|---|---|---|
| 1 | **后端已定 FastAPI，换 Django = 推翻既有决策** | LangGraph 的 SSE 流式（`astream_events`）、checkpoint（SQLAlchemy 系）、Pydantic 数据契约、路由全都要重写 |
| 2 | **两套 ORM 会打架** | LangGraph checkpoint 底层是 SQLAlchemy，Django 自带 ORM。同库共存要维护两套数据访问层 |
| 3 | **Django 的优势我们一条都用不上** | Django 强在「自带 admin 后台 + auth 全家桶 + 快速搭完整网站」—— 那是**毕设场景**的最优解。我们只有一个薄后端 |
| 4 | **模板解决的是占位页，而占位页不是难点** | 真正的难点是聊天页的 SSE 流式渲染、路线总览的拖拽 + 地图联动 —— **这两块 Django 模板一点忙都帮不上**，最后照样写 JS |

**关键纠正**：**"卡通风格"跟 Django 没有任何关系。** 风格是 CSS 决定的；模板引擎只负责"把数据填进 HTML"，不负责"长什么样"。这两件事被绑在一起了。

### 但你要的"模板渲染"白拿到了

Jinja2 的语法跟 Django 模板**几乎一模一样**（`{% for %}` / `{% if %}` / `{{ var }}`），会 Django 模板就会 Jinja2。所以：

- ✅ 页面复用（导航栏、卡片）→ Jinja2 的 `extends` / `include` 全都有
- ✅ 不用学 React
- ✅ 不用装 node / npm / 构建工具
- ✅ 后端一行不用改

### 那 React 呢？—— 我上一轮推荐过，这里改口，说明理由

上一轮我推荐 React，理由是"表格 + 地图 + 卡片三处状态同步"。**重新算账后，这个理由站不住**：

| 判断 | 说明 |
|---|---|
| 我们的状态有多复杂？ | **一份行程数据，三处展示，用户偶尔改一下**（拖一下、删一站） |
| 这种规模需要 React 吗？ | **不需要**。写一个 `render()` 函数，数据一变全量重绘，一天 12 个站的量级**性能完全不是问题** |
| React 真正值钱的场景 | 复杂状态树 + 高频局部更新 + 大量组件复用 —— 我们都不沾 |
| 引入 React 的代价 | 学 hooks/状态管理、装 node + npm + Vite、配开发服务器 —— **为了省一个 `render()` 函数，付 2~3 天学习成本 + 一套构建链** |

**结论：原生 JS 够用，且省掉整条构建链。** 前端框架留到第二个项目或者专门学，不要在这个项目里付学费。

⚠️ **唯一要提前说清的代价**：原生 JS 手写复杂交互，代码会"散"（DOM 操作散在各处）。对策是**约定一条**：所有页面都遵守「**数据 → `render(state)` → DOM**」单向流，禁止在事件回调里直接改 DOM。守住了这条，原生 JS 一样干净。

---

## 二、视觉规范（卡通 / 新粗野主义）

参考截图的风格特征：**粗黑描边 + 高饱和色块 + 硬阴影 + 大圆角**。这套风格好复刻、辨识度高、不需要美术。

### 2.1 色板

| 用途 | 色值 | 说明 |
|---|---|---|
| 页面底色 | `#E9EEF6` | 淡蓝灰，撑出"纸"的感觉 |
| 卡片底色 | `#FFFFFF` | — |
| **描边 / 主文字** | `#151515` | **近黑不用纯黑**，避免生硬 |
| **主高亮**（当前导航、强调标签） | `#FFD84D` | 黄，截图里当前导航就是这个 |
| **主按钮** | `#FF9A2E` | 橙 |
| **次按钮 / 辅助** | `#7FD7C4` | 薄荷绿，配"重置"这类动作 |
| 品牌色（logo / 小图标） | `#14B8A6` | 青 |
| 危险（删站 / 出错） | `#FF6B6B` | 红 |
| 次要文字 | `#6B7280` | 灰 |
| 校验三态 | 通过 `#2E9E6B`／不通过 `#E5484D`／**无法判定 `#B0870B`** | 见 `方案.md` 6.2 |

### 2.2 形状规则（风格的全部就在这里）

| 元素 | 规则 |
|---|---|
| 描边 | 卡片 `3px solid #151515`，按钮 `2px solid #151515` |
| 阴影 | **硬阴影**：`4px 4px 0 #151515`（不是模糊阴影，这是新粗野主义的标志） |
| 圆角 | 卡片 `16px`／按钮 `12px`／标签（胶囊）`999px` |
| 悬浮态 | 阴影变大到 `6px 6px 0`，元素 `translate(-2px,-2px)` —— 像"抬起来" |
| 按下态 | 阴影缩到 `0`，元素 `translate(4px,4px)` —— 像"按下去" |
| 字体 | 系统无衬线栈（`PingFang SC / Microsoft YaHei`）；标题 `font-weight: 900` |
| 不引入外部字体 | 卡通感靠**描边 + 色块**造，不靠字体，避免字体版权和加载问题 |

### 2.3 不做什么（视觉层面）

| 不做 | 理由 |
|---|---|
| 暗色模式 / 主题切换 | 用户明确不要（毕设那次也是这个偏好） |
| 任何渐变、玻璃拟态、柔和阴影 | 跟这套硬边风格冲突 |
| 动画超过 200ms | 拖拽排序要跟手，动画长了反而"粘" |
| 外部字体 / 图标字体库 | 图标直接用内联 SVG，几个就够 |

---

## 三、全局骨架

### 3.1 导航栏（全站唯一，`base.html` 里写一次）

```
┌──────────────────────────────────────────────────────────────────────────┐
│ [TR] 旅游规划平台  首页 旅游景点 AI路线规划助手 路线总览 旅游攻略 个人中心  │
│                        ↑黄色高亮当前页                                    │
│                              [搜索景点/攻略/城市____] [搜索] [👤admin][退出]│
└──────────────────────────────────────────────────────────────────────────┘
```

| 项 | 说明 |
|---|---|
| logo + 站名 | 左侧，青绿色方块 `TR` + 「旅游规划平台」 |
| **6 个导航项** | 首页 / 旅游景点 / **AI 路线规划助手** / **路线总览** / 旅游攻略 / 个人中心 |
| 当前页高亮 | 黄底 `#FFD84D` + 描边（照截图） |
| 搜索框 | 占位：输入后回车跳到「旅游景点」页并带上关键词（不实现真搜索） |
| 用户区 | 头像 + 昵称 + 「退出登录」；M9 之前写死显示 `admin` |

> ⚠️ **`路线总览` 是我们比参考设计多出来的一项，也是核心** —— 参考设计的导航里没有它，它聊完就结束了。我们多了"结构化行程 + 地图 + 能手改"这整个闭环，**这一项就是差异化本身**，不能省。

### 3.2 页面通用结构

```
┌─ 导航栏 ─────────────────────────────────────────────┐
├─ 页头区（白卡）：英文小标 + 中文大标题 + 一句话说明 + 右侧动作按钮 ─┤
├─ 内容区 ────────────────────────────────────────────┤
└─ 页脚（一行版权 + 一行"数据来源：高德开放平台"）──────────┘
```

---

## 四、逐页设计

标注：**🟢 真做**（M6/M7）｜**🟡 占位**（结构做出来，数据后用）｜**🔵 半真**（结构 + 真数据，功能后置）

---

### 4.1 首页 🟡 占位

**用途**：第一眼说清"这是什么"，并把用户推进 agent。

```
┌─ Hero 大图（一张长城/山水的图，上面压一条半透明深色遮罩）────────────┐
│                                                                      │
│   规划一次不用返工的旅行          ← 主标题（大、白、900字重）          │
│   说出目的地和日期，AI 帮你查天气、排路线、验可行性                  │
│                                   ← ★ 这句就是"一句项目介绍"（位置定了）│
│                                                                      │
│   [ 开始规划 → ]                  ← 大按钮，直跳「AI 路线规划助手」   │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
┌─ Recommended / 猜你喜欢 ────────────────────────────[ 查看全部景点 ]─┐
│  ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐                     │
│  │ 图块   │  │ 图块   │  │ 图块   │  │ 图块   │                     │
│  ├────────┤  ├────────┤  ├────────┤  ├────────┤                     │
│  │ 武侯祠 │  │宽窄巷子│  │杜甫草堂│  │  锦里  │                     │
│  │ 成都   │  │ 成都   │  │ 成都   │  │ 成都   │                     │
│  └────────┘  └────────┘  └────────┘  └────────┘                     │
└──────────────────────────────────────────────────────────────────────┘
```

**★ "一句项目介绍"的位置已定：Hero 大图上的副标题**（主标题正下方，大按钮正上方）。理由：这是全站最显眼的位置，且和"开始规划"按钮连成一句完整的话——"这是什么 → 点这里开始"。

| 区块 | 内容 | 数据来源 | 占位怎么做 |
|---|---|---|---|
| Hero 主标题 | 「规划一次不用返工的旅行」 | 写死 | — |
| Hero 副标题 | **一句项目介绍**（见上） | 写死 | — |
| 大按钮 | 「开始规划 →」 | — | 直接跳 `/assistant` |
| 猜你喜欢 | 4 张卡：图 + 景点名 + 城市 | 高德 `search_poi`（M2 后） | **用真实景点名 + 纯色/灰色图块，不写任何数字** |

⚠️ 猜你喜欢**不做轮播图**（参考设计的 Hero 是可轮播的景点大图）—— 我们的 Hero 位置留给"项目介绍 + 行动按钮"，那才是首页该干的事。轮播是平台型网站的做法。

---

### 4.2 旅游景点 🟡 占位

**用途**：让用户按关键词找景点。**这一页跟参考设计的"景点库"根本不同 —— 详见下方警告。**

```
┌─ Scenic Spots / 旅游景点库 ────────────────────────────────────────┐
│ 输入关键词搜索景点，数据来自高德开放平台（实时）                      │
├────────────────────────────────────────────────────────────────────┤
│ 关键词[____________]  城市[全部城市 ▾]  [筛选] [重置]                │
├────────────────────────────────────────────────────────────────────┤
│  ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐                    │
│  │ 图块   │  │ 图块   │  │ 图块   │  │ 图块   │                    │
│  │ 名称   │  │ 名称   │  │ 名称   │  │ 名称   │                    │
│  │ 城市·区│  │ 城市·区│  │ 城市·区│  │ 城市·区│                    │
│  │ 人均消费│ │ 评分   │  │        │  │        │                    │
│  │[查看详情]│ │[查看详情]│ │[查看详情]│ │[查看详情]│                  │
│  └────────┘  └────────┘  └────────┘  └────────┘                    │
└────────────────────────────────────────────────────────────────────┘
```

🔴 **这一页最容易做错，三条必须遵守：**

| 参考设计有 | 我们**不能**抄 | 原因 |
|---|---|---|
| 「共收录 21006 个景点」 | ❌ 删掉 | **我们没有本地 POI 数据**（不爬虫、不建 POI 表）。写个 21006 就是编的 |
| 「门票」筛选 + 价格「免费/35.0/405.0」 | ❌ 改成「人均消费」，且**不做门票筛选** | 高德 `biz_ext.cost` 是**人均消费不是门票**，且门票数据大面积缺失。它那个卡片能整齐显示价格 = 它自己造的 |
| 「热度 10.0 / 405.0」 | ❌ 删掉 | 高德**没有这个字段**，跟已删的"趣味度"是同一类自造指标 |

**结构性差异（必须理解）**：参考设计的景点页是**列表页**（进来就摆出全部），我们是**搜索页** ——

```
参考设计： 进来 → 数据库查 21006 条 → 展示（有本地数据）
我们：     进来 → 调高德 search_poi(keywords) → 展示（无本地数据）
```

**所以我们的默认状态是"空"，必须显示引导文案**（💡 建议："输入景点名称或关键词开始搜索，例如「成都 博物馆」"），而不是硬凑几张卡。

---

### 4.3 AI 路线规划助手 🟢 真做（M6）

**用途**：项目的核心页一。左历史栏 + 右聊天框，**SSE 实时显示 agent 在干什么**。

```
┌─ 左：历史会话栏（260px） ─┬─ 右：聊天区 ──────────────────────────┐
│ [+ 新建规划]              │  ┌────────────────────────────────┐  │
│ ─────────────────────     │  │ 🤖 你准备去哪儿？想玩几天？     │  │
│ ▸ 成都3日游·带爸妈        │  │    预算和同行人是？（必问项）   │  │
│   2026-09-15              │  └────────────────────────────────┘  │
│ ▸ 杭州2日周末             │  ┌────────────────────────────────┐  │
│   2026-09-14              │  │ 成都，3天，带爸妈              │  │
│ ▸ 粘贴的成都行程          │  └────────────────────────────────┘  │
│                           │  ┌────────────────────────────────┐  │
│                           │  │ 🤖 正在搜索「成都 宽窄巷子」... │  │
│                           │  │    ⟳ 工具轨迹卡（实时刷新）     │  │
│                           │  ├────────────────────────────────┤  │
│                           │  │ 🗺 行程卡（3天·12站·无硬错）    │  │
│                           │  │    [ 生成路线总览 → ]           │  │
│                           │  └────────────────────────────────┘  │
│                           │  ┌────────────────────────────────┐  │
│                           │  │ 输入消息...              [发送]│  │
│                           │  └────────────────────────────────┘  │
└───────────────────────────┴────────────────────────────────────┘
```

| 区块 | 内容 | 说明 |
|---|---|---|
| 历史栏 | 会话列表（标题 + 日期），点击继续改 | **活会话**：点开能接着聊（A9） |
| 首条消息 | LLM 生成，但**必问项一个不漏** | 代码校验字段集合（A8） |
| **工具轨迹卡** | 「正在查天气」「正在搜武侯祠」逐条出现 | **SSE 节点级事件**，不是打字机效果 |
| 行程卡 | 天数 / 站数 / 硬错数 + 「生成路线总览」按钮 | 点按钮跳界面二（A10） |
| 校验卡 | 通过 / 不通过（已修）/ 无法判定 三类分开列 | 三态（A18） |

> 视觉重点：**让"agent 在干活"看得见**。轨迹卡是这一页跟"普通聊天机器人"的唯一区别，颜色和动效要给它最多注意力。

---

### 4.4 路线总览 🟢 真做（M7）

**用途**：核心页二。左表格 + 右地图 + 下方按天卡片，**能改**。

```
┌─ 路线总览 ──────────────────────────[重新深度检查] [导入高德 APP]─┐
├───────────────────────────┬───────────────────────────────────────┤
│ 左：站点表格（可拖拽排序） │  右：高德地图                          │
│ ┌──┬────────┬─────┬────┐  │  ┌─────────────────────────────┐      │
│ │序│ 名称   │到达 │停留│  │  │[Day1][Day2][Day3] ← 左上角页签│      │
│ ├──┼────────┼─────┼────┤  │  │                              │      │
│ │ 1│武侯祠  │09:00│ 90'│  │  │   ①──②──③  只画当天路线       │      │
│ │ 2│杜甫草堂│10:45│ 60'│  │  │   (12 站编号 + 连线 + 距离)   │      │
│ │ 3│锦里    │12:15│ 90'│  │  │                              │      │
│ └──┴────────┴─────┴────┘  │  └─────────────────────────────┘      │
│  ⠿ 拖动手柄  ✕ 删站  ✎ 改时间│  ⚠️ 切页签只显示当天（A17）          │
├───────────────────────────┴───────────────────────────────────────┤
│ Day 1  10月1日  晴 26°/18°   ·  12.4km · 车程 46min · 步行 4.2km    │
│ ┌────────┐ ┌────────┐ ┌────────┐                                  │
│ │①武侯祠 │ │②杜甫草堂│ │③ 锦里  │   ← 按天分组卡片                 │
│ │09:00   │ │10:45   │ │12:15   │                                  │
│ │✅✅⚪  │ │✅⚪    │ │✅✅    │   ← 校验三态角标                 │
│ └────────┘ └────────┘ └────────┘                                  │
└───────────────────────────────────────────────────────────────────┘
```

| 交互 | 触发什么 |
|---|---|
| 拖拽改顺序 | **纯代码重算**（时间 + 距离 + 校验），**不调 LLM**（A13） |
| 删站 / 改时间 | 同上 |
| 「重新深度检查」 | 才调 LLM 跑软校验 |

> 这一页的 Day 页签只显示当天路线 —— 参考设计里完全没有这一页，这是我们的原创部分。

---

### 4.5 旅游攻略 🟡 占位（M10 真做）

**用途**：攻略社区。列表 + 搜索 + 发布。

```
┌─ Travel Notes / 旅游攻略社区 ──────────────────────[ 发布攻略 ]─┐
│ 搜索目的地、标题和正文，查看其他用户的旅行经验                    │
├─────────────────────────────────────────────────────────────────┤
│ 关键词[___________]  城市[全部城市 ▾]  [搜索] [重置]            │
├─────────────────────────────────────────────────────────────────┤
│ ┌──────────┐ ┌──────────┐ ┌──────────┐                         │
│ │  图块    │ │  图块    │ │  图块    │                         │
│ │[成都][12]│ │[杭州][8] │ │[哈尔滨][5]│  ← 城市标签 + 浏览数     │
│ │成都3日游 │ │杭州2日游 │ │哈尔滨3日 │                         │
│ │摘要一行…  │ │摘要一行…  │ │摘要一行…  │                         │
│ │作者·日期 │ │作者·日期 │ │作者·日期 │                         │
│ │[查看攻略]│ │[查看攻略]│ │[查看攻略]│                         │
│ └──────────┘ └──────────┘ └──────────┘                         │
└─────────────────────────────────────────────────────────────────┘
```

**占位阶段怎么填**：直接用**评测集那 10~20 篇真实攻略**（它们和 `guide_posts.content_md` 同源）。

**这样做的两个好处**：① 不用编数据 ② 顺便验证了"攻略与评测集同源"这条设计。

🔴 **不抄的地方**：

| 参考设计有 | 我们 |
|---|---|
| 「共收录 51 篇攻略」 | ❌ 不写总数（初期列表本来就是空的） |
| 作者 `guide_seed_user` | ❌ 不要这种种子账号名，用真实昵称；**系统攻略显示"官方"**（`author_type = system`，`user_id` 为 NULL） |
| 「122 浏览」 | 💡 可留（我们自己能算），但**占位阶段先不显示** |

**M10 补齐**：详情页 + 发布页 + **评论 + 点赞**（A29）。

---

### 4.6 个人中心 🟡 占位（M9 真做）

**用途**：资料 + 我的资产入口。

```
┌─ User Center / 个人中心 ────────────────[我的收藏] [我的攻略]─┐
│ 管理个人资料、收藏景点、发布攻略和 AI 路线规划记录。           │
├──────────────────────┬─────────────────────────────────────────┤
│ ┌───── 左卡 ──────┐  │ ┌──────── Profile / 资料设置 ────────┐  │
│ │    ┌──────┐     │  │ │ 用户名 [admin__________]          │  │
│ │    │ 头像 │     │  │ │ 邮箱   [________________________]  │  │
│ │    └──────┘     │  │ │ 头像   [选择文件 未选择任何文件]    │  │
│ │  2026-09-15 加入│  │ │                                   │  │
│ │     admin       │  │ │        [ 保存资料 ]               │  │
│ │  暂未填写邮箱   │  │ └───────────────────────────────────┘  │
│ └─────────────────┘  │ ┌─ 我的 AI 路线规划记录 ─────────────┐  │
│                      │ │ ▸ 成都3日游·带爸妈   2026-09-15   │  │
│                      │ │ ▸ 杭州2日周末        2026-09-14   │  │
│                      │ └───────────────────────────────────┘  │
└──────────────────────┴─────────────────────────────────────────┘
```

✅ **采纳参考设计的一点**：它的个人中心说明里写着"管理个人资料、收藏景点、发布攻略和 **AI 路线规划记录**" —— 这跟我们已经定的「个人中心 = `trips` 表展示」**撞上了**，说明这个设计方向是对的。所以加一块「我的 AI 路线规划记录」区块，点进去就是那次的路线总览。

> M9 之前：导航右侧固定显示 `admin`，这一页能打开、能看、**点了保存不报错就行**（不落库）。

---

## 五、数据来源标注（画界面时必须知道每块数据哪来的）

| 界面元素 | 数据来源 | 什么时候有 |
|---|---|---|
| 景点名 / 坐标 / 地址 | 高德 `search_poi` | M2 |
| 评分 `rating` | 高德 `biz_ext.rating`（**仅部分 POI 类型有**） | M2 |
| 人均消费 | 高德 `biz_ext.cost`（**是人均，不是门票**） | M2 |
| 天气（白天/夜间/温度/风力） | 高德天气 `extensions=all`（**只有 3 天**） | M2 |
| 距离 / 车程 / 步行 | **代码算**（高德测距） | M2 |
| 校验三态 | 代码（硬判据）／LLM（软判据） | M3 |
| 行程结构 | agent 产出 | M1 |
| 攻略正文 | 评测集原文（手存 10~20 篇） | M0 后 |
| 用户名 / 头像 | 本地 | M9 |
| 热度 / 趣味度 / 门票 | ❌ **高德没有，不做** | — |

---

## 六、占位阶段的完成判据

占位做到什么算过：

1. **6 个导航项都能点开**，页面之间能互相跳转，不报错
2. **风格统一**（同一套色板 / 描边 / 阴影 / 圆角）
3. **零假数据** —— 没有编造的数字（2 万个景点、热度 10.0、门票价格）；缺数据的格子显示引导文案或留白
4. **两个核心页有明确入口**（首页大按钮 + 导航项 + 行程卡上的「生成路线总览」）
5. **响应式不要求**（只保证 1440px 宽度下正常，不做手机端）

---

## 七、还没定的

| # | 问题 | 建议 |
|---|---|---|
| 1 | Hero 大图用什么图 | 先用纯色块 + 渐变压暗占位，等有真实图片再换（**不下载网图，避免版权**） |
| 2 | 图标怎么来 | 内联 SVG，手写几个（搜、用户、退出、拖拽、删除、编辑） |
| 3 | 首页「猜你喜欢」的 4 个景点定哪几个 | 先用成都的（武侯祠 / 宽窄巷子 / 杜甫草堂 / 锦里）—— 跟首个评测集城市一致 |
| 4 | 搜索框（导航栏）行为 | 占位：回车跳「旅游景点」页并带上关键词，不实现真搜索 |
