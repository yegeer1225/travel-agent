# 决策记录 · DECISIONS

> **这份文件回答「为什么」，不回答「是什么」。**
>
> | 想知道 | 去哪看 |
> |---|---|
> | 项目做什么 / 需求层面的决定（A1~A39 原话） | `方案.md` 第十章 |
> | 技术上怎么实现 | `技术方案.md` |
> | **为什么这么定、没选什么、代价是什么** | **本文件** |
>
> ⚠️ **决定本身不在本文件重复。** 这里每条的标题都带 `A` 编号（对应 `方案.md` 第十章）
> 或 `M0-x`（M0 阶段的契约决定）。**要改一条决定，先改 `方案.md`，再来这里补代价与替代方案**
> —— 反过来做就是两份文件开始漂移，而漂移是查不出来的。

**收 / 不收的判据**（决定这份文件会不会烂掉）：

| 收 | 不收 |
|---|---|
| 有**替代方案**、有**真实代价** | 纯需求选择（导航放几项、界面要不要历史栏）→ 只进 `方案.md` |
| 面试会被追问"为什么不用 X" | 一行的常识（用 UTF-8、密码要哈希） |
| 当时**差点选错**、或**实测推翻了直觉** | 还没定的事 → 进第六节「悬而未决」，别在正文假装定了 |

---

## 一、Agent 引擎与循环

### D1 · 手写 `StateGraph`，不用 `create_agent`　`A5 / A20`　2026-09-14

> **决定**：整个图手写。不用 LangGraph 的 `create_agent` 高层封装。

| 替代方案 | 否决理由 |
|---|---|
| `create_agent` 高层封装 | 它把"循环怎么编排"藏进黑盒 —— 而这**正是本项目要证明的能力**。用了它，面试被问"循环边界在哪、什么时候打回"只能答"框架自己决定的" |
| 混合：封装为主 + 手写关键节点 | 两套范式混用，出问题时不知道该看框架层还是自己那层。调试成本远大于省下的样板代码 |

**代价**：样板代码自负（state 定义、条件边、循环计数全靠自己）；LangGraph 升级要跟着调；封装自带的 tool 路由、消息裁剪要自己实现一遍。
**重审触发**：无。这是**项目定位**决定的（要证明会编排 loop），不是技术优劣比较。

---

### D2 · 双层循环：会话层 + 工具层　`A15`　2026-09-14

> **决定**：外层 = 会话轮次（用户插话/steering 能进来）；内层 = 工具调用循环（模型自己决定调几次、够了没）。

| 替代方案 | 否决理由 |
|---|---|
| 单层 `while` 循环 | 用户中途插话只能等整轮跑完 —— 而"边聊边改行程"是核心体验 |
| 图里直接写递归（无上限） | 每次工具调用都进 checkpoint，**状态体积与费用无界**，一个坏 prompt 就能烧掉一整天的配额 |

**代价**：状态里要维护两个计数器（见 D6 与 `方案.md` 7.1），循环退出条件比单层多一倍；出问题时要先判断"卡在哪一层"。
**重审触发**：无。

---

### D3 · 循环只收事实，出 JSON 另起固定节点　`A6`　2026-09-14

> **决定**：工具循环阶段**只允许收集事实**（POI、天气、距离）；循环结束后另起一个**不挂 tools 的固定节点**生成结构化 JSON。

| 替代方案 | 否决理由 |
|---|---|
| 让模型在循环里随手输出最终 JSON | 循环中途的上下文里混着工具原始返回，模型容易把接口字段和自己记忆混着编；而且**循环每次打回都要重新生成一遍结构**，贵且不稳 |
| 循环里既收事实又输出 JSON | 输出格式错时无法区分"是数据没查够"还是"格式没写好"，两种错要两种修法，混在一起就都没法定位 |

**代价**：多一次 LLM 调用（几乎必然）；事实与生成之间要过一次显式的 state 传递。
**白捡的好处**：这个切法**天然等于"关思考/开思考"的分界**（见 D8）—— 架构本来就要分开，不用额外设计。

---

### D4 · 工具只要 3 个，细节在工具内部消化　`A7`　2026-09-14

> **决定**：`search_poi` / `get_weather` / `calc_distance` 三个。**天气要的 adcode、开放时间解析，都在工具内部做，不加新工具。**

| 替代方案 | 否决理由 |
|---|---|
| 加 `geocode`（地址→adcode）作为第 4 个工具 | 模型得先调 geocode 再调 weather，**两次调用都能出错**，还得在 prompt 里教它顺序。而这步是确定性的，代码做 100% 对 |
| 按数据源拆工具（POI 搜索 / POI 详情 / POI 周边） | 工具越多，模型选错的概率越高（同参不同名最容易被误选），而这三个是同一份数据的三种问法 |

**代价**：工具内部逻辑变厚（缓存、重试、adcode 转译都在里面）；单测要覆盖到内部那几层。
**重审触发**：出现"代码写死的步骤，其实每次都该由模型决定"的场景（目前没有）。

---

### D5 · 多 agent 只切 1 个子 agent，形态定为 Subagents　`A19`　2026-09-14　`搜索子 agent 结构被 D79 推翻（2026-09-18）`

> **决定**：主 agent 把子 agent 当工具调。**先只切 1 个搜索子 agent。**

| 替代方案 | 否决理由 |
|---|---|
| 一次切 3~4 个子 agent（搜索/校验/渲染各一个） | 单次请求多 4 次模型调用，成本翻倍；**而且校验和渲染本来就是确定性代码**（D3），包成 agent 反而把确定性变成不确定 |
| 做 Handoffs（agent 之间移交控制权） | 移交后 context 要整体重传，且"谁在负责"变得难追踪 —— 调试一个连"当前是谁"都说不清的系统，成本极高 |

**代价**：单个搜索子 agent 无状态，**每次都要重走全流程**（不能复用上一轮查到的 POI）；这条是被明确接受的成本。
**面试口径**：多 agent 在这里**不是为了并行，是为了收窄上下文** —— 子 agent 的搜索过程不污染主 agent 的上下文。这个理由比"我用了多 agent"值钱。

**实现注记（2026-09-16，M4 落地）**：

- **内部预算**：子 agent 自限规划轮 ≤4、搜索 ≤6。主图会计口径 = task 计 **1 次**工具调用，内部搜索不占主图预算（结构决定：工具函数写不进主 state）。最坏账 = 3 task × (4 轮 + 6 搜索)，常量有测试钉住（`test_subagent.py`）。
- **规划 LLM 不挂 tools**：模型每轮出 JSON 意图（search / final），**搜索由代码执行**。比"子 agent 自己带工具循环"便宜且稳：一轮 = 1 次小调用 + N 次确定性搜索，格式坏了直接降级不连环空转。
- **隔离实现**：trace 走 `contextvars`（与 poi_pool 同款机制），`tool_step` 开 scope、结束 drain 进 `state.subagent_trace`（`operator.add`）。**上下文隔离 ≠ 数据隔离** —— 隔离的是"模型看到的话"，池子（事实底座）本来就该共享。
- **终选过池子**：模型 final 的 `poi_id` 逐个过池子，编的直接丢弃 —— 封闭世界在子 agent 出口再守一道。
- **降级语义**：预算用尽 / 规划失败 → 把池里站点型候选（`is_visit_type` 过滤住宿与交通）重排后"未经精选"端出，**绝不抛异常**（D25 精神）。
- **实测观察（首个真跑）**：模型倾向**烧满 6 次搜索预算**后才被强制收尾（3 次 task 全部降级）——`sub_search` 在预算耗尽时直接强制降级、跳过模型终选，是省 1 次 LLM 调用的取舍（终选价值本来就部分被 `generate_plan` 的全池挑选覆盖）。

**重审触发**：若真跑中"烧满预算 → 降级"成为**常态**（>50% 的 task 降级），考虑改为"预算耗尽后仍给模型一轮终选"（代价 = 每 task 多 1 次 LLM 调用）；或收紧 prompt 引导模型 2~3 轮内主动收尾。

---

### D6 · 校验三态 `pass` / `fail` / `unknown`，废除 `warn`　`A18`　2026-09-15

> **决定**：校验结果只有三态。**软判据的"有风险"用 `fail` + `level=soft` 表达**，不引入第 4 个状态。

| 替代方案 | 否决理由 |
|---|---|
| 四态（加 `warn`） | 契约初稿里确实同时出现过 `unknown` 和 `warn` —— 被查出**自相矛盾**。`warn` 和"软 fail"语义重叠，两个值表达一件事，前端配色就要多定义一套 |
| 两态（只有通过/不通过） | **数据拿不到**和**数据说不行**是两件事。天气查不到时标成 `fail`，等于用"没数据"给用户扣分；标成 `pass` 则是撒谎 |

**关键分工**（这条最容易搞错）：**`level` 管要不要打回，`status` 管显示什么颜色。** `soft + fail` = 显示红色但不阻塞流程。

**代价**：前端可能要解释"为什么红色却没拦住"（`方案.md` 6.2 有交互说明）；评测报告必须**单列 `unknown` 占比**，否则拿 `unknown` 当"通过"会虚高分数。
**重审触发**：若某天判据需要表达"信息缺失但已知风险"（介于 unknown 与 fail 之间）—— 目前用 `unknown` + msg 说明即可。

---

### D7 · MCP 走「门面降级」，对模型只暴露 1 个工具名　`A23`　2026-09-15

> **决定**：`search_poi` 内部 `provider=mcp` 先试，失败自动降级 httpx 直连。**对模型永远只有一个 `search_poi`。**

| 替代方案 | 否决理由 |
|---|---|
| 门面**不**降级（MCP 挂了就报错） | 实测：高德 MCP 层返回的字段**比直连少**（`location` 经纬度、`biz_ext` 都丢）—— 挂了就是整个产品挂了 |
| 直连写一套、MCP 写一套，两套工具都给模型 | 模型会看到两个功能重叠的工具，选错率高；而且两套实现要各自维护解析层 |
| 默认走 MCP，直连当"高级选项" | MCP 多一跳、多一个进程，**且实测字段更少** —— 没有理由把它设成默认 |

**代价**：多一层抽象（provider 协议 + 降级判断），且**降级发生时必须让前端可见**（否则用户永远不知道自己在用降级数据）。MCP 默认关。
**重审触发**：高德 MCP 层补齐字段后（可回归默认开）；或降级本身不稳定时。

---

## 二、模型与数据层

### D8 · 思考模式「按节点分设」，不是全局开关　`A24`　2026-09-15（当晚复测修正）

> **决定**：挂 tools 的节点**关**思考（`extra_body={"thinking":{"type":"disabled"}}`）；不挂 tools 的规划/生成节点**开**思考。

🔴 **本条被复测修正过一次 —— 决定不变，但理由换掉了。原理由不成立。**

**原理由（作废）**：旧版记的是"带 `tools` + 历史缺 `reasoning_content` → **400**（2/2 稳定复现）"。

**复测结果：未能复现。** flash / v4-pro 各测，五种组合**全部通过**：

| 组合 | 结果 |
|---|---|
| 带 tools + 思考 + **原样回传** `reasoning_content` | ✅ 通过 |
| 带 tools + 思考 + **剥掉** `reasoning_content` 再回传 | ✅ 通过 |
| 带 tools + **全程关思考** | ✅ 通过 |
| 不带 tools + 思考 + 剥掉 reasoning | ✅ 通过 |
| **连续 4 轮**，每轮都剥掉 reasoning 再回传 | ✅ 通过 |

→ 可复现脚本：`backend/scripts/probe_llm_structured.py`（随时重跑）

**现在的理由（两条，都比原来更硬）**：

| 理由 | 实测依据 |
|---|---|
| 🔴 **成本**：`reasoning_tokens` 计入 `completion_tokens` | 一个最简请求实测 = `prompt 42 + completion 163`，**其中推理占 160**。工具循环 ≤8 轮，每轮开思考 = 多烧数倍 token —— 而"搜武侯祠还是搜熊猫基地"根本不需要 CoT |
| 🔴 **确定性**：思考模式下 `temperature` 生效不了 | 官方原话 "will also have no effect"。**关掉之后 `temperature=0` 才真正生效** → 同样的输入得到同样的工具选择，可复现、可调试 |

⚠️ **真正的教训（比结论本身重要）**：旧版把"2/2 复现"写成了稳定结论，却**没留下可重复的复现脚本** ——
所以过了半天就没人能验证它到底是真是假。**"我实测过"而不给出可重跑的路径，等于没实测。**
现已固化成脚本，结论与复现方式绑在一起。

| 替代方案 | 否决理由 |
|---|---|
| 全程关思考 | 规划节点的 CoT 没了，行程质量下降；思考是这个模型的主要卖点 |
| 全程开思考 + 自己把 `reasoning_content` 塞回历史 | 要绕过 `langchain-openai` 的剥离行为（自定义消息序列化）—— **框架一升级就再崩一次**，且这是给一个未文档化字段做兼容层 |
| 换掉 `langchain-openai` 直接手写 OpenAI SDK | 为了一个字段放弃整个生态，代价与收益不成比例 |

**代价**：节点要分两类写；**新增节点时必须显式选择思考开关**
（`config.py` 已有守卫：`LLM_THINKING_TOOL=enabled` 直接 `raise`）。

**同一轮复测还钉下三条会踩到的真事实**：

1. 🔴 **漏回应 `tool_calls` 会 400**（实测复现，这条**真的会踩**）：
   `An assistant message with 'tool_calls' must be followed by tool messages responding to each 'tool_call_id'`
   → 模型一次返回**多个** `tool_calls` 时，必须**逐个**用 `tool` 消息回应。
   **手写 `tool_step` 不能只取 `tool_calls[0]`。**
2. 🔴 **`with_structured_output()` 默认不可用**：
   `response_format={"type":"json_schema"}` → `400 This response_format type is unavailable now`
   （去掉 `additionalProperties` 也一样 —— **是整个类型不支持，跟 schema 内容无关**）。
   只有 `{"type":"json_object"}` 可用 → `generate_plan` 走 **`json_object` + prompt 写清 schema + Pydantic 校验**。
3. **思考内容在 SDK 层看不到**：原始 HTTP 响应里 `message.reasoning_content` 有 94~252 字、
   `usage` 里有 `reasoning_tokens`，但 `langchain-openai` 会剥掉它（`additional_kwargs` 里没有）。
   想要思考内容只能自己发原始请求。

**附带事实**：模型名只有 `deepseek-flash` / `deepseek-v4-pro`，**没有** `deepseek-v4.1-flash`（填错直接 404）。中文需显式约束，无 system prompt 时回英文。

---

### D9 · 「切换模型」做三个粒度，不做 UI 开关　`A24 延伸`　2026-09-15

> **决定**：① `.env` 全局默认 ② 节点级分配（工具循环用 `flash`、规划节点可挂 `v4-pro`）③ **离线对比评测**。**不做运行时的前端模型切换按钮。**

| 替代方案 | 否决理由 |
|---|---|
| 前端放一个模型下拉框 | 用户凭什么知道该选哪个？**这不是用户能做的决策**，只是把复杂度推给用户 |
| 只做全局 `.env` 切换 | 学不到东西，"按成本与质量分配模型"是能讲的设计点，全局切换讲不出来 |

**代价**：`MODEL_REGISTRY` 要维护；节点级差异会让"同一次请求用了两个模型"，日志要能区分。
**真正的价值在第③个粒度**：评测模块本来就要做，**顺手就能产出"两个模型在同批 case 上的分数对比"** —— 这才叫"支持切换模型"。

---

### D10 · SSE，不用 WebSocket　`A21`　2026-09-15

> **决定**：`astream_events` → FastAPI `StreamingResponse`，单向推流。

| 替代方案 | 否决理由 |
|---|---|
| WebSocket | **本场景不需要双向** —— 用户消息走 `POST` 就行，只有 agent 的输出需要推。选 WS 等于为了"看起来高级"多背一套心跳/重连/状态机 |
| 前端轮询 | 打字机效果做不到；且每次轮询都要打数据库，agent 跑一次几十秒，轮询次数不可控 |

**代价（都是前端要吃的）**：

1. 浏览器 `EventSource` **只支持 GET** → 聊天要 POST，必须 `fetch` + `ReadableStream` 手写解析
2. **HTTP 状态码在流开始后就固定了** → 流内错误只能靠 `error` 事件报，不能靠 4xx/5xx
3. Vite 的 dev proxy **会缓冲 SSE**，要单独配
4. React 19 严格模式 `useEffect` **跑两次** → SSE 连在 effect 里会**双倍扣费**，必须 return abort 清理

**重审触发**：出现真正的双向需求（如多人协同编辑同一行程）—— 目前没有。

---

### D11 · MySQL 8（Docker）+ 第三方 checkpoint 包，版本全部钉死　`A22`　2026-09-15

> **决定**：业务表与 checkpoint 都放 MySQL，用 `langgraph-checkpoint-mysql`。**三个 langgraph 包写死 `==` 版本。**

**先诚实说包的性质**（面试会问）：`langgraph-checkpoint-mysql` 是**单人维护的第三方包，不是 LangChain 官方**。官方只有 sqlite 和 postgres 两个。

| 替代方案 | 否决理由 |
|---|---|
| `langgraph-checkpoint-postgres`（官方） | 要再起一个 Postgres 容器 —— **多一个数据库只为了存 checkpoint**，运维与心智成本不成正比 |
| `langgraph-checkpoint-sqlite`（官方） | 单文件、无并发，服务化后多 worker 写同一文件会锁；**且业务数据在 MySQL，checkpoint 在 SQLite 等于每次请求跨两个数据源** |
| 不持久化（纯内存） | 重启丢会话 = 核心功能（"接着改"）没了 |

**已实测关闭（16/16 通过）**：`langgraph-checkpoint-mysql 3.0.0` + `langgraph 1.2.11` + `langgraph-checkpoint 4.2.0` 同步/异步两条链路全通，含"**换新 saver 实例仍能读出同一 thread**"（这才是真落库）。
**降级预案**（若日后包坏掉）：业务表仍 MySQL，checkpoint 退回 SQLite —— 两者可并存。

| 实测踩到 | 机理（记下来，别再猜错） |
|---|---|
| 🔴 `1267 Illegal mix of collations` | 驱动 `charset='utf8mb4'` 发 `SET NAMES utf8mb4`，服务端把 `collation_connection` 设为**该字符集的默认**（`utf8mb4_0900_ai_ci`），**与 `collation_server` 无关**。所以 `collation_server` 必须**和驱动强制的连接默认对齐**，不是随便挑一个。附带：`collation_server` **只在初始化空数据目录时生效**，改完必须 `down -v` 重建，重启容器无效 |
| `asetup()` 不存在 | 异步版建表方法**还叫 `setup()`**，只是它是协程。这包里 `aput`/`aget_tuple`/`adelete_thread` 带 `a`，`setup` 不带 |
| `langgraph` 没有 `__version__` | 只能 `importlib.metadata` |

**为什么必须钉死版本**（这条是**唯一不能"约束松一点更灵活"**的地方）：

| 包 | 依赖声明 | 上界 |
|---|---|---|
| `langgraph-checkpoint-sqlite`（官方） | `<5.0.0,>=4.1.0` | ✅ |
| `langgraph-checkpoint-postgres`（官方） | `<5.0.0,>=4.1.0` | ✅ |
| **`langgraph-checkpoint-mysql`（第三方）** | **`>=2.1.2`** | 🔴 **无** |

第三方包最新版停在 2026-01，而核心已 4.2.0。**无上界 = pip 在任何升级场景都认为"满足"，不拦任何不兼容。**

**代价**：依赖升级要靠人工判断；该包若停更，将来要自己 fork 或迁移。
**重审触发**：LangChain 官方出 MySQL checkpoint 包时立刻换。

---

## 三、数据归属与契约

### D12 · 契约唯一事实源 = `schemas.py`，前端类型自动生成　`M0-1`　2026-09-15

> **决定**：所有结构定义只写在 `backend/app/schemas.py`。前端 TS 类型**由脚本生成**（`backend/scripts/gen_ts_types.py` → `docs/types.ts`），**禁止手写**。

| 替代方案 | 否决理由 |
|---|---|
| 手写 TS 类型（或让豆包照 `api.md` 抄） | **抄一遍两天后必然不一致，而不一致不报错** —— 只是某字段是 `undefined`，运行到那一步才发现 |
| 用 OpenAPI 自动生成（FastAPI 自带 `/openapi.json`） | 方向对，但要额外引入 `openapi-typescript` 一类工具链；而我们的契约是 **Pydantic 模型直接产出 JSON Schema**，写 30 行生成器就够，少一个依赖 |

**代价**：多一个生成步骤要记得跑（已写进契约变更流程）；生成器认不出的类型**直接抛错**（不猜），扩展契约时可能要改生成器。
**当场抓到过的真错**：`tsc --strict` 发现 SSE 事件的 `type` 被生成成**可选**（因为 pydantic 有默认值），导致判别联合无法收窄 → 根因是规则错了：**后端是全字段 dump，TS 里所有字段都该必填**。
**这条决定的正向价值**：改契约后重跑一次生成器，**TS 编译期会把所有受影响的地方全指出来**。

---

### D13 · 行程存整块 `trip_json`，不拆表　`M0-2`　2026-09-15

> **决定**：`trips` 表里一列 JSON 存整份行程，不拆成 `stops` / `days` 子表。

| 替代方案 | 否决理由 |
|---|---|
| 拆成 `trip_days` / `trip_stops` 关系表 | 行程是**整体读写**的（读出来就渲染整页，写回去就整份替换），拆表后每次都要 join 回来拼装，**收益只有"能用 SQL 查某个景点"这种本项目没有的需求** |
| 存两份（JSON + 关系表） | 两份数据必然漂移，且要处理"改了 JSON 忘了改表" |

**代价**：**不能用 SQL 查询行程内部字段**（如"哪些行程含熊猫基地"）—— 现在没有这个需求，未来有的话要做一次迁移。
**重审触发**：出现跨行程的结构化查询需求时。

---

### D14 · `visibility` 只给 `guides`，不要每张表都加　`A27`　2026-09-15

> **决定**：9 张业务表里**只有 `guides` 有 `visibility`**；其余一律默认全私有，靠 `user_id` 就够。

| 替代方案 | 否决理由 |
|---|---|
| 每张表都加 `visibility` 字段 | 会让人**误以为存在公开读路径**，从而在查询里漏写 `WHERE user_id` —— **一个字段的代价是系统性的安全错觉** |
| 用一张 `permissions` 表统一管 | 本项目只有"私有 / 公开"两态且规则固定，权限表是过度设计 |

**代价**：将来若某类资源也要公开（如公开行程），要改表结构。
**配套**：`visibility` 默认 `private`，用户点「发布」才置 `public` 并写 `published_at`。
🔴 **但"靠 `user_id`"是约定不是结构** —— 约定会被违反且不报错。**如何让违反当场暴露，见 [D31](#d31)（三重防线 + 扫描断言）**。

---

### D15 · 隔离写在统一依赖里，拿不到返回 404 不是 403　`A27 / A28 / A30`　2026-09-15

> **决定**：`owned_session()` / `owned_trip()` / `visible_guide()` 三个依赖统一校验归属；**无权限与不存在都返回 404**。

| 替代方案 | 否决理由 |
|---|---|
| 每个路由里自己写 `WHERE user_id` | 漏一处就是一个越权漏洞，**而漏写不会报错** —— 只有双账号互测改 id 才能发现 |
| 返回 403（明确告知"这个存在但你无权") | **泄露资源存在性**：改 id 递增就能探出"哪些 id 真实存在"，进而探测系统规模 |

**必须同时说清的一件事**：**JWT 不带来隔离。** JWT 只证明"你是谁"，隔离靠 `WHERE user_id`。把"用了 JWT"当成"做了隔离"是最常见的误解。

| 附带的字段决定 | 理由 |
|---|---|
| `comments` 必须**多态**（`target_type` + `target_id`） | 攻略和行程都要能评论，拆两张表会让"取某目标的评论"变成两个分支 |
| **点赞数先 `COUNT(*)`，不加冗余计数字段** | 加了就要维护一致性（事务/触发器/定时校准），现在没有性能问题 |
| 点赞**一人一次靠唯一索引**，不靠代码判重 | 代码判重有并发窗口（先查后写），唯一索引是数据库层保证 |
| `guides.user_id` **必须可空** | 系统发布的内容作者为 `NULL`（`A29` 的双来源）|

---

### D16 · 粘贴不建会话 → `trips.session_id` 可空 + 新增 `owned_trip()`　`A37`　2026-09-15

> **决定**：界面二粘贴**不建会话**，行程直接落库（`source=pasted`、`session_id=NULL`）。

| 替代方案 | 否决理由 |
|---|---|
| 粘贴时自动建一个会话 | 会污染界面一的历史栏 —— 那里每一条都该能"接着聊"，而粘贴来的行程**本来就没什么可聊的**（是已经讨论完的攻略） |
| 建会话但标记为隐藏 | 多一个状态字段，且"隐藏"的判据要贯穿所有查询（又是 D14 那类隐患） |

**连锁影响（这才是这条决定的真实成本）**：

1. `trips.session_id` 由**必填改可空** —— 设成 `NOT NULL` 会在**第一次粘贴时**直接报错
2. **访问控制载体必须换**：`/trips/{id}` 不能靠 `trip.session_id` 反查会话（为 NULL 时反查不到）→ 必须直接用 `owned_trip()` 按 `trips.user_id` 校验
3. `DoneEvent.session_id` 跟着改可空（粘贴时没有会话）
4. 界面一的历史栏**不显示**粘贴来的行程，但界面二的"确定性改"（调顺序/删站/改时间）**两者完全一样**

**代价**：多一个依赖函数；"行程有没有会话"变成前端也要判断的分支（能否回界面一接着聊）。

---

### D17 · 公开攻略不做审核，评测集改单向同步　`A38`　2026-09-15

> **决定**：**砍掉审核开关**。评测集只读 `eval/fixtures/`，**永不回读线上库**。

| 替代方案 | 否决理由 |
|---|---|
| 做审核（人工/自动） | 功能膨胀，且审核队列本身要一整套状态机 —— **为作品项目引入一个与主线无关的子系统** |
| 审核砍掉、但评测集直接读线上 `guides` | 用户能发布 ⇒ **任何人都能污染评测集** ⇒ 分数不可信。这是"不做审核"后**必须**付的代价 |

**代价**：线上内容与评测集是两份数据，**要手动同步**（人工把好攻略存进 `fixtures/`）；「去校验」入口的数据与评测集只是同源、不是同一份。
**重审触发**：若评测集要覆盖大量真实内容，再考虑加"人工挑一部分同步"的半自动流程。

---

## 四、前端与视觉

### D18 · 前端页面实现交「豆包」，本 AI 只出契约　`A36`　2026-09-15

> **决定**：页面实现交外部工具；本 AI 负责后端 + Agent 引擎 + **契约** + 交接文档。

| 替代方案 | 否决理由 |
|---|---|
| 本 AI 也写前端 | 已试过一轮：视觉产出未达要求（用户判定），**继续试是浪费双方时间** |
| 前后端都给同一个工具，避免切换 | 用户的顾虑是"前端写完再优化后端要换工具、来回切很麻烦" —— 但**解法不是同一个工具，是先冻结契约** |

**关键论证**：**切换成本 = 契约变更次数，与工具无关。** 契约冻结后：改后端实现（换模型/改检索/优化循环）**前端零改动**；反向则契约一变等于前后端双倍返工。

**代价**：**M0 必须认真过字段**（这就是现在这一步）；豆包若要改契约，流程要走 `api.md` 那三步（改 `schemas.py` → 重跑生成器 → 改 `api.md` + 本文件追加）。
**配套交付物**：`前端交接.md`（技术栈版本 / 设计 token / 页面路由与接口映射 / SSE 完整参考实现 / **双份验收清单**），目的是**用户不用当传话筒**。

---

### D19 · 视觉风格 = 几何拼贴风（迭代 5 轮后定）　`A35`　2026-09-15

> **决定**：白底 + 高饱和几何色块 + 直角（≤4px）+ 4px 粗下划线高亮。**三态色独立定义，不复用装饰色板。工具页收敛装饰。**

迭代过程本身是有信息量的（说明为什么不能靠"感觉"选）：

| 轮次 | 给的东西 | 被否的原因 |
|---|---|---|
| 1 | 同族变体（5 个近似方案） | 差别太小，选不出 |
| 2 | 跨流派（宇宙 3D / 赛博朋克 / 蒸汽朋克） | **与"旅游"这个主题完全不符** |
| 3 | 旅游流派 | 仍不契合 |
| 4 | 参考图 | 未采纳 |
| 5 | **几何拼贴风**（并做了实例） | ✅ 采纳 |

**关键设计约束**：**装饰色 ≠ 语义色**。三态（通过/不通过/未知）的颜色**独立定义**（`--color-status-*`），**不从装饰色板取** —— 否则"红色"既是装饰又是"校验不通过"，用户分不清哪个红是警告。

| 替代方案 | 否决理由 |
|---|---|
| 卡通 / 新粗野主义（`A32`，已作废） | 见上表第 2 轮 |
| 奶油拼贴风（同批候选） | 对比度不足，且三态色在奶油底上不醒目 |

---

### D20 · 占位零假数据 + 景点页做搜索页　`A33`　2026-09-15

> **决定**：**禁抄**参考设计的门票筛选 / 热度值 / 收录总数；景点页定为**搜索页**而非列表页。

| 替代方案 | 否决理由 |
|---|---|
| 照抄参考设计的"热度 10.0 / 收录 21006 个景点" | **高德没有"热度"字段** —— 那些数字全是参考项目自己的种子数据。抄了就是**假数据**，被问"这数字哪来的"直接答不上 |
| 门票筛选 | `biz_ext.cost` 是**人均消费不是门票**，且实测大面积缺失（景点全为 `[]`）→ 筛选器点了没结果 |
| 景点页做列表页 | 无热度/无收录数 ⇒ **列表无从排序**，只能按名称；而搜索才是高德真正支持的能力 |

**代价**：首屏比"列表页"看起来空（要靠搜索框撑）；本来能抄的视觉丰富度主动放弃。
**这条的价值在面试**：**能说清"哪些数据我拿不到，所以我没做"** —— 比做了满屏假数字强得多。

---

### D21 · 首页轮播素材 = 高德 `photos` 字段　`A39`　2026-09-15

> **决定**：Hero 用轮播图 + **真实景点照片**；素材**首选高德 `search_poi` 返回的 `photos`**，无版权图库兜底。

| 替代方案 | 否决理由 |
|---|---|
| AI 生图充当景点照片 | **会画出不存在的景点** —— 旅游产品的图文不符是硬伤 |
| 网上随手找图 | 版权风险，被问"这图哪来的"答不上 |
| 自绘几何插画 | 用户已明确否决（"不用插画"） |

**实测支撑**（风险 15 当场关闭）：`photos` 字段**每个 POI 都返回 3 张实景图**，URL 实测 HTTP 200 / `image/jpeg` / 653KB。
**代价 / 坑**：图片有两个域名 —— `aos-comment.amap.com`（HTTPS ✅）和 `store.is.autonavi.com`（**HTTP，非 HTTPS**），前端混用要处理混合内容告警。
**附带约束**：轮播**只在 Hero 出现一次** —— 下面的「猜你喜欢」是静态 4 张卡，一页两个轮播会互相打架。

---

## 五、工程流程与运行约束（跨会话必须遵守）

### D22 · 每完成一步就 commit　`C1`　2026-09-14

> **决定**：不等功能完整，一个可描述的进展就提交一次。

| 替代方案 | 否决理由 |
|---|---|
| 攒一个大 commit | **已有实证**：`界面设计.md` 被编辑器静默截断（523 → 158 行，三、四、五、六、七节全丢），**能零损失恢复的唯一原因就是每段都 commit 过** |

**代价**：commit 数多；偶尔要 squeeze。
**配套判据**：**批量改文档前先跑一次"当前行数 vs HEAD 行数"对比**（2 秒成本），能挡住静默损坏。

---

### D23 · mock 与 real 同签名，结构由同一个 Pydantic 模型产出　`A36 配套`　2026-09-15

> **决定**：`providers/` 的 mock 与 real **接口同签名、响应同结构**；mock 数据必须是**真结构假数据**。

| 替代方案 | 否决理由 |
|---|---|
| 豆包等后端接完再开工 | 前端全部工时堆到最后，且真数据才暴露的问题会集中爆发 |
| mock 随便返回简化结构 | 少一个字段，豆包写的页面**接真数据时必崩** —— 而 mock 存在的意义正是让两边能并行 |

**代价**：mock 要跟着契约改（但生成器与同一个模型能把成本压到最低）。

---

### D24 · 第三方 API / 库：先探针实测，再写业务代码　`流程约定`　2026-09-15

> **决定**：接入高德、DeepSeek、任何第三方库之前，**先写一次性探针脚本把结论从推测变成实测**。

**这条是被反复打脸打出来的**（每一条都真实发生过）：

| 实测推翻了什么 | 如果不测会怎样 |
|---|---|
| 高德天气预报是 **4 天**，不是文档写的 3 天 | 三份文档全写错，前端按 3 天做 UI |
| `biz_ext.cost/open_time/rating` 是 **`str \| list` 双类型**，空数组须收敛为 `None` **不能当 0** | `0` 是"免费"、`None` 是"没数据"，界面显示成"免费"= 撒谎 |
| 高德**有 QPS 限流**（连续第 4 个请求即触发，且**只把 `status` 改 0 不抛异常**） | 被当成"这个景点搜不到" → 触发**假硬错**、白白打回重排 |
| 「JS API Key」与「安全密钥」在服务端**表现完全一样**（都是 `INVALID_USER_KEY`），**无法自动区分** | 猜错的代价是双端返工 |
| `langgraph-checkpoint-mysql` 依赖**无上界** | 某次 `pip install` 静默装上不兼容版本 |
| 第三方库的异步类名是 `AIOMySQLSaver`、建表法叫 `setup()` 不叫 `asetup()` | 照文档写就 import 失败 |

**代价**：每接一个外部依赖要先花时间写探针（通常半小时）。
**判据**：**"测不出来"本身就是要交付的事实** —— 不要把它包装成结论。已固化为 skill：`third-party-api-probe` / `third-party-lib-compat-spike`。

---

### D25 · 循环上限与单次成本预算　`M1 开工前定`　2026-09-15

> **决定**：L2 工具层 **8 次**（同一参数重复调直接拒、不计数）｜L3 校验层 **2 次**｜单次行程**工具调用总数 ≤ 20**。

**超限行为 = 降级输出，不抛异常**：把没跑完的校验项标成 `unknown` 并写进 `validation.issues`。

| 替代方案 | 否决理由 |
|---|---|
| 不设上限 | 一次坏 prompt（比如模型执着于搜同一个 POI）就能烧掉当天配额，且请求永不返回 |
| 上限压到 3 次 | 一个 3 天行程十几站，**每站一次 POI 搜索就爆** —— 上限必须大于"站数"这个自然规模 |
| 超限抛异常 `timeout` | 用户已经等了 2 分钟，抛出去等于**全部白等**；降级输出至少给一半结果 + 明确说哪一半没验 |

**代价**：极端行程（15 天 60 站）会撞上限，输出不完整 → 靠 `unknown` 如实标注，**不假装完整**。
**重审触发**：M1 跑通后统计实际调用分布 —— 正常行程的 P95 若超过 6 次，说明 8 偏紧。

---

### D26 · 重试：只重试天然幂等的操作　`M1（LLM/高德）/ M5（HTTP）`　2026-09-15

> **决定**：**不改状态的重试才允许自动重试。** 判据先于参数。

| 类别 | 操作 | 策略 |
|---|---|---|
| ✅ 允许重试 | POI 搜索 / 天气 / 距离、`GET`、LLM 调用 | 最多 **3 次**，退避 **1s → 2s → 4s** |
| ❌ 禁止重试 | 建会话、发消息、写库、发布攻略 | 失败即报错，**靠唯一索引兜底重复** |

**只重试这些错误**：`429` / `5xx` / 超时 / 连接错误。
**`400` / `401` / `403` 不重试** —— 同样的请求重试一次还是同样的结果，只是多烧一次配额。

🔴 **高德出站限 3 QPS**（令牌桶）。实测连续第 4 个请求即 `CUQPS_HAS_EXCEEDED_THE_LIMIT`。
**并且它不抛异常，只把 `status` 改成 `0`** —— 必须显式识别 `infocode` 并重试，**不能当成"这个景点搜不到"**（否则触发假硬错、白白打回重排，见风险 16）。

| 替代方案 | 否决理由 |
|---|---|
| 通用幂等键中间件（`Idempotency-Key`） | 要额外一张 key 表 + 清理策略 + 过期语义；而前端**不会重复提交同一个业务动作**，收益接近 0 |
| 完全不重试，失败即报错 | 高德 QPS 限制会让正常用户频繁看到"数据源不可用" —— 把上游的抖动直接暴露给用户 |
| 引 `tenacity` 做全套退避策略 | 现阶段只有两种退避（线性 1/2/4 与令牌桶等待）。**M1 先手写 3 行循环**，等出现第三种策略再引库 |

**代价**：LLM 重试会**重复计费**；带 tools 的调用重试必须**整轮重发**（含历史 `reasoning_content`），**不能只补半截**。
**重审触发**：出现任何一次"重试导致重复写入"的案例 → 立刻收紧为"只重试只读操作"。

---

### D27 · SSE 断线走 checkpoint 恢复，不重发请求　`契约 M1 / 实现 M6`　2026-09-15

> **决定**：每个 SSE 帧带 `id: <序号>`；前端重连带 `Last-Event-ID: N`；后端用 `thread_id = session_id` 从 LangGraph checkpoint 恢复，**从 N+1 继续推**。

**恢复粒度 = 节点，不是 token。**

| 替代方案 | 否决理由 |
|---|---|
| 前端重新 POST 一遍 | agent 从头再跑 = **重复扣费** + 可能生成不同结果 → 用户看到**两份行程**，不知道该信哪个 |
| 前端把已收到的 token 拼起来本地续 | 前端不做业务状态。那会造出**两个事实源**，刷新页面就分叉 |
| 不做恢复，断了就让用户重来 | 一次行程 30s~2min，**在弱网下这是必然踩到的**，不是边缘情况 |

**代价**：断在一个 LLM 节点中途时，**那个节点会重跑**（多花一次 token），用户会看到这一段重新打字。
⚠️ **由此产生的 UI 表现已在 2026-09-18 实测确认**（重连后出现「半句话+整句话」的**重复**，轨迹也重复）→ 处置见 **D74**（**接受为已知代价，不修**）。
🔴 **诚实标注：M6 才实现。** M1 只把 `id:` 帧格式**定进契约** —— 现在不定，M6 补 = 改契约 = 双倍成本。
**重审触发**：M6 实测发现节点粒度太粗（一个节点 40 秒，"重新打字"体验很差）→ 改为后端缓存最近 N 帧补发。

---

### D28 · 限流：按成本分层，进程内实现不引 Redis　`M5`　2026-09-15

> **决定**：限流的**第一目的不是防攻击，是防钱**（LLM token 成本），第二目的是保护高德 QPS。
> 因此**必须按接口成本分层** —— 一刀切会同时误伤高频合法操作、放过最贵的那个。

| 接口 | 维度 | 阈值 | 依据 |
|---|---|---|---|
| `POST /sessions/{id}/chat` | **用户** | **10 / 小时 · 30 / 天** | 一次 30s~2min；正常用户一天规划 2~3 次，10/小时余量充足 |
| **同一 session 并发流** | **会话** | **同时 1 个** | 并发 2 个 = 费用翻倍 + 互抢高德 QPS。**这条比次数限流重要** |
| `POST /trips/paste` | 用户 | **20 / 小时** | 粘贴比 chat 频繁，但不含多轮工具循环 |
| `POST /trips/{id}/recheck` | 用户 | **60 / 小时** | 便宜（硬校验为主），用户会反复点 |
| `GET /spots/search` | 用户 | **120 / 分钟** | 高频交互，但要挡住脚本 |
| `POST /auth/register`·`login` | **IP** | **10 / 分钟** | 防撞库（这里 IP 比用户维度更合适 —— 攻击者还没有账号） |
| `POST /users/me/avatar` | 用户 | **5 / 小时** | 换头像不该频繁，且每次要重编码 |
| 全局兜底 | IP | **300 / 分钟** | 防单机脚本 |
| 高德**出站** | 全局 | **3 QPS** | 实测限流（见 D26） |

**返回**：`429` + `Retry-After: <秒>` 头 + body 里也带 `detail.retry_after`（前端要倒计时，读 body 比读头方便）。

| 替代方案 | 否决理由 |
|---|---|
| 一刀切"每用户 60 次/分钟" | 同时**误伤**搜索（合法高频）和**放过** chat（1 次就贵，60 次更是灾难） |
| Redis + 令牌桶 | 单实例部署，多一个组件 = 多一个部署故障点 + 多一份运维。**这条的重审触发就是"多实例部署"** |
| 只在 Nginx 层限 | 后端跑在 127.0.0.1，Nginx 那条链路上没有"用户"概念，做不到按用户分层 |

**代价**：进程重启计数清零（软限流可接受）；多实例部署时各实例独立计数会超发。
**重审触发**：多实例部署 / 上线公网 → 换 Redis。

---

### D29 · 头像上传 10MB，落盘前强制重编码　`M9`　2026-09-15

> **决定**：上传上限 **10MB**（用户定）｜类型白名单 **`jpeg` / `png` / `webp`**｜**落盘前用 Pillow 重编码为 512×512 的 WebP**（10MB → 约 30~60KB）。

**四道闸门，缺一不可**：

| # | 闸门 | 挡什么 |
|---|---|---|
| 1 | 大小 ≤ **10MB** | 撑爆磁盘 / 拖死请求 |
| 2 | **不信 `Content-Type`**，用 Pillow 真实解码失败即拒 | 改扩展名 / 伪造 MIME |
| 3 | 宽高 > **8000px** 直接拒 + `Image.MAX_IMAGE_PIXELS = 50_000_000` | 解压炸弹（10KB 的 PNG 解出 10GB） |
| 4 | **强制重编码** + 文件名换 **UUID** | 图片内嵌的 polyglot 负载 / 路径穿越 |

🔴 **不收 SVG**（能带 `<script>` = 存储型 XSS），**不收 GIF**（重编码后动图没了，且 GIF 可作 polyglot 载体）。

| 替代方案 | 否决理由 |
|---|---|
| 原图直接存 | 10MB × N 个用户；且图片里能嵌恶意负载，等于把风险存进自己服务器 |
| 只校验扩展名 / MIME | 两个都可伪造，等于没校验 |
| 只做 magic bytes 校验、不重编码 | 能验出真实类型，但**杀不掉图片内嵌的 polyglot 负载**。重编码是顺带最彻底的一步 —— **一举三得：缩体积 + 清负载 + 剥 EXIF（含 GPS）** |

**代价**：动图变静图（已决定不收 GIF）；**重编码后无法还原原图**。
**重审触发**：需要保留原图（画质投诉）→ 改为"展示图重编码 + 原图隔离存储"双份。

---

### D30 · 只做桌面网页端　`A40`　2026-09-15

> **决定**：最佳 **1440px**，最小可用 **1024px**。**不做手机适配、不做 App / 小程序 / 响应式。**

| 替代方案 | 否决理由 |
|---|---|
| 移动优先 | 主场景是"**坐在电脑前规划**旅行"；手机上看行程属于**查看**不是规划，产品形态对不上 |
| 做响应式（两套布局） | 每页布局成本翻倍，收益只有面试时那 10 秒 —— 而这个项目的展示场景本就是桌面 |
| 干脆不管窄屏 | 面试官用手机点开链接 = **负分**。所以加一层 `<768px` 的"请在电脑上打开"提示（10 行成本） |

**代价**：手机上打开会明显难看（已用提示层缓解）。
**重审触发**：面试要求展示移动端 / 出现真实手机端需求。

---

### D31 · 私有数据隔离：三重防线，不靠"记得写 `WHERE`"　`D14 / D15 强化`　2026-09-15
<a id="d31"></a>

> **决定**：隔离的正确性**不能建立在"每个查询都记得写 `user_id`"这个约定上** —— 约定会被违反，而且**违反不报错**。

**触发**：用户 2026-09-15 对 D14 提出质疑 —— "其余表靠 `user_id`"这句读起来像是**做法**，其实它只是**约定**。
一旦某处漏写，结果是**悄悄返回别人的数据**（或悄悄返回空），**没有任何报错**。
这和 D24 那句"不一致是查不出来的"是同一个病。

**三道防线**：

| 层 | 做什么 | 挡住什么 |
|---|---|---|
| **1. 签名级** | 私有表的读操作**只允许**走 `store/queries.py`；函数**第一个位置参数必须是 `user_id`**，返回 `T \| None`。公开读走**另一组**函数（名字带 `public_`） | 让"读私有"和"读公开"在**函数名上就分开**，写法混不起来 |
| **2. 测试门禁** 🆕 | 三个扫描断言（见下），把"记得写"变成"**写错就测试红**" | 约定违反**当场暴露**，不再靠人记得 |
| **3. 数据库层** | ❌ **MySQL 没有 Row Level Security**（那是 Postgres 的） | 诚实说明：**这层拿不到**，所以才必须靠 1+2 补 |

**三个扫描断言（M9 落地，约 60 行）**：

- **A**｜`api/**` 里**不出现私有表名**（`sessions` / `messages` / `trips` / `favorites`）—— 出现 = 绕过 `queries.py` 直接写 SQL 了
- **B**｜`queries.py` 里每条 `SELECT` 私有表**必须含 `user_id`** —— 漏一个就红
- **C**｜建表 DDL 里**只有 `guides` 带 `visibility`** —— 防后人"顺手"给别的表也加上

**权限判据只能有一处**：`visible_guide(conn, guide_id, uid)` —— 全项目**只有这里**出现 `or visibility = 'public'`。
判据分散 = 漏一处就是一个洞，而**漏掉的那处不报错**。

| 替代方案 | 否决理由 |
|---|---|
| 给每张表都加 `visibility` | 会让人**误以为存在公开读路径**，从而在查询里漏写 `WHERE user_id` —— 这正是 D14 说的"系统性安全错觉" |
| 靠 code review 把关 | 漏一次就是一个洞，且洞**不报错**。人眼是概率，扫描是确定性 |
| 数据库层 RLS | **MySQL 不支持**。为了这个功能换 Postgres，代价远大于收益 |
| 给每张表建"每用户视图" | N 个用户 × M 张表 = 视图爆炸，且用户是**运行期新增**的，建视图赶不上 |

**代价**：多一个测试文件；`queries.py` 成为所有数据访问的必经之路（但这正是它的价值）。
**重审触发**：扫描测试若频繁误报（例如出现合法的跨用户查询，如"运营看全部"）→ **加白名单机制，不是撤掉测试**。

---

### D32 · 工具调用上限要**两道闸**，而不是一道　`D25 补漏`　2026-09-16
<a id="d32"></a>

> **决定**：在 `agent_step`（防"继续问模型"）之外，`tool_step` 里**再加一道执行预算**（防"单轮调用爆量"）。

**触发**：第一次跑真实模型，输出 `工具调用 23 次` —— **超过 D25 定的 20**。
不是守卫写错了，是**守卫的判据有结构性缺口**：

```
agent_step 的守卫只能在「下一轮开始前」判断
而模型一轮能发 8 个 tool_calls
→ 已有 19 次时依然放行（19 < 20），这一轮又发 4 个 → 总数 23
```

**两道闸各守什么**（它们守的是**不同的失控方式**，所以不算重复）：

| 闸 | 位置 | 守什么 |
|---|---|---|
| 一 | `agent_step` | 反复空转：模型每轮只调 1 个、但要调 20 轮 |
| 二 | `tool_step` | 单轮爆量：一轮发 8 个调用，而上限只剩 1 次余额 |

第二道闸的实现有个**必须遵守的约束**：超出的调用**不执行，但仍要回一条 `ToolMessage`**。
理由见 D8 相关的实测结论 —— 漏回应任何一条 `tool_calls`，下一轮请求直接 400。
所以"拒绝执行"和"不回应"是两件事，只有前者是安全的。

| 替代方案 | 否决理由 |
|---|---|
| 只留 `agent_step` 那道闸 | 实测会超到 23。上限变成"约 20"，而**任何"约"的上限在账单上都是"没有上限"** |
| 把 `MAX_TOOL_CALLS` 从 20 调到 30 来"盖住"超发 | 掩盖症状。真正的病是"单轮可以发很多"，调大只是把 23 变成 33 |
| 在 `tool_step` 里只处理前 N 个、其余**直接丢弃不回应** | 🔴 漏回应 = 下一轮 400。这是本项目的实测硬约束（见 D8 那条 400） |
| 靠 prompt 说"不要超过 10 次" | 已经在 prompt 里说了（并实测把 23 次降到 10 次），但**prompt 是建议、守卫是保证**，两者不能互替 |

**代价**：`tool_step` 多约 15 行；`tool_call_count`（模型请求数）与"实际执行数"不再相等 ——
前者会超上限、后者不会。**这是故意的**：一个记录"模型想干什么"，一个记录"系统做了什么"，
混成一个数会让"上限到底守住没有"变得看不出来。

**重审触发**：若日志显示"被拒的调用"占比长期 > 20%，说明 prompt 没教会模型预算感 → 改 prompt，**不是放宽上限**。

---

### D33 · `check_rounds` 记的是**重排轮数**，不是"被打回次数"　`D25 澄清`　2026-09-16
<a id="d33"></a>

> **决定**：计数器由 **`repair`** 累加（不是 `check_plan`），语义 = "已经重排过几轮"。

两种读法差一轮，而这一轮让"上限 2"实际变成"上限 1"：

| 计数器语义 | 谁累加 | 上限 2 时的实际行为 |
|---|---|---|
| 被打回次数 | `check_plan` | 第 2 次打回时直接放弃 → **plan 只跑了 2 次（重排 1 次）** |
| ✅ 重排轮数 | `repair` | 重排 2 次 → **plan 跑 3 次** |

**为什么后者才对**：D25 写的是"L3 = 2 轮"，"轮"指的是**重排**这个动作的次数。
按前一种读法，用户实际只得到 1 次改正机会 —— 而那与"上限 2"承诺的东西不符。

⚠️ 判定这个只靠读代码看不出来：两种写法都能跑、都不报错，
差别只在"模型被多问了一次还是少问了一次"。**是测试（`llm_plan.call_count == 3`）钉住的。**

**代价**：最坏情况下多一次生成调用（上限 2 轮的代价，本来就算在预算里）。
**重审触发**：若评测显示"第 2 次重排的成功率接近 0"→ 把上限降到 1，省下那次调用。

---

### D34 · mock 的「数据保真」和「行为保真」是两件事　`D23 补强`　2026-09-16
<a id="d34"></a>

> **决定**：mock 的 **POI 数据字段**必须诚实（没有就 `None`），但**检索行为**要放宽到接近真实高德 —— 两者分开对待。

**触发**：M1 端到端测试第一次跑，13 条全红，全卡在"候选池是空的"。
排查后不是图接错了，是 **mock 的 `search_poi` 只比名称和别名**：

| | 真实高德 | 最初的 mock |
|---|---|---|
| 匹配方式 | **全文检索**（名称/分类/地址/标签） | 只比名称和别名 |
| 搜"古迹" | 返回一堆景点 | **返回空** |
| 搜"火锅" | 返回一堆火锅店 | 返回空 |

而 prompt 里恰恰引导模型"同一类换不同关键词各搜一遍"——
所以 mock 模式下**几乎必然**卡住，而 M1 的验收就是在 mock 模式下跑出行程。

**修法**：加一张 `_MOCK_SEARCH_TAGS`（poi_id → 类别词），只决定"搜什么词能命中它"。

| 类别 | 规则 | 理由 |
|---|---|---|
| **POI 数据字段**（`name`/`lng`/`rating`/`open_time`…） | 必须实测真值，没有就 `None`，**绝不编** | 它们会进产物、会被校验层读、会显示给用户 |
| **检索标签** | 可以是人为定义的 | 它**不出现在任何产物里**，只影响"能不能搜到" |

| 替代方案 | 否决理由 |
|---|---|
| 给 `AmapPoi.type` 填类别串 | 我只知道格式（`"风景名胜;风景名胜;国家级景点"`），**不知道那 9 个 POI 的真实值** —— 填了就是编造，正是这个项目要证明自己不会做的事 |
| 把类别词塞进 `alias` | `alias` 是实测字段（`'武侯祠\|武侯祠景区'`），改它就是改数据 |
| 让 prompt 要求模型只搜具体地名 | 真实高德下类别词搜得很好，改 prompt 等于**为了迁就 mock 而降低真实环境的表现** |
| 不管，测试里只用能命中的关键词 | 那 mock 模式下人工试用必炸，而"人工试用"是 M1 唯一的验收方式 |

⚠️ 这个教训比修法本身值钱：**一个"数据诚实、行为失真"的 mock，
会让所有端到端测试红得毫无线索** —— 因为红的位置（候选池空）离病根（检索不匹配）很远。

**代价**：mock 多一张表，且它与池子**必须同步维护**（加了 POI 忘了加标签 → 那个 POI 永远搜不到，且**不报错**）。
**重审触发**：M2 接真高德后整张表连同 mock 一起消失，无需维护。

### D35 · 出站限速取 2.2 QPS，不按文档写 3 QPS　`D26 细化`　2026-09-15

**决定**：`AmapHttpProvider` 两次出站请求之间强制 ≥ **0.45s**（≈2.2 QPS），进程内锁串行化。
探针脚本 `probe_amap.py` 用 **1.2s**（27 次请求零失败，最保守）。

**替代方案**

| 方案 | 否决理由 |
|---|---|
| 按文档取 3 QPS（间隔 0.33s） | 实测上限**就贴着 3 QPS 边界**：连打时第 3~4 个必炸（复现两次：失败区间 `#3~#6` 与 `#4~#7`）。取值等于上限，任何抖动都踩线 |
| 不限速，纯靠 `10021` 重试兜底 | 退避是 1s→2s→4s，而多等 0.1s 什么都不会发生。**用"被限流后补救"替代"不触发限流"是负收益** |
| 引 Redis 令牌桶 | 多一个进程外依赖。当前单进程（D28），`ContextVar` 那套已经够隔离 —— 收益为零 |
| 并发 + 信号量 | 高德这个是 **QPS 限制不是并发限制**，并发只会更容易撞 |

**代价**：一次行程约 8~10 次高德调用，多花 **4.5s 墙钟时间**。这是拿 4.5 秒换"不出现偶发限流"，值。

**重审触发**：
· 上多 worker（那时进程内计数**会翻倍**，2.2×N，必须换 Redis）→ **M5 部署时必看这条**
· 高德个人 Key 提额（提了就可以放宽）

---

### D36 · 封闭世界校验管「真实性」，**不管「质量」**　`D5 补边界`　2026-09-15

**发现方式**：M2 真跑一份 3 天成都行程，候选池 32 个 POI 里 **14 个不能当站点**（44%）：

| 噪音类型 | 实例 | 数量 |
|---|---|---|
| 住宿 | 成都香格里拉大酒店 / 桔子酒店(春熙路店) | 8 |
| 交通设施 | 武侯祠(地铁站) / 武侯祠(公交站) / 武侯祠(锦里)(公交站) | 3 |
| 售票处 / 出入口 | 成都武侯祠博物馆东侧门售票处（**暂停营业**） | 2 |
| 道路 | 武侯祠大街 | 1 |

🔴 **危险之处**：这些 POI 的 `poi_id` 全是真的、全在候选池里 → `poi_exists` **会打 ✅**。
"一个暂停营业的售票处"和"武侯祠博物馆"在校验层眼里**没有任何区别**。

**决定（这一步只澄清边界，不做过滤）**：

> `poi_exists` 回答的是「**这个地点是模型编的，还是它真的搜到过**」。
> 它**不回答**「**这个地点值不值得去**」。

**为什么只澄清不实现**：过滤是**业务规则**，而业务规则需要产品决策（要不要保留住宿？以后加不加"住宿推荐"模块？
按 `typecode` 剔除交通设施，还是按名字剔除"售票处/大门/停车场"？两者都会误伤）。
**这类决策不属于 AI 能替用户拍的**，所以只记录发现 + 给选项（见第六章）。

**替代方案**

| 方案 | 现状 |
|---|---|
| provider 层过滤 | ❌ 不对。`AmapProvider` 的契约是**忠实的数据源**，过滤属于"给模型看的视图" |
| 工具层过滤（`amap_tools.search_poi`） | ⏸ 位置对（工具有"用途"概念），但过滤规则要拍板 |
| 只在工具返回文本里标注类型，让模型自己筛 | ⏸ 成本最低。M2 实测**模型自己筛对了**（12 站全是正经景点/餐厅），但那是运气还是必然，**一次样本说明不了** |
| 不管 | ❌ 池子脏会让 prompt 变长（32 条 vs 15 条）、模型被误导的概率上升，且万一选错**校验拦不住** |

**代价**：现在"模型选到噪音"这条路径是**敞开的**，只能靠 `prompt` 提醒 + 事后人工看。
**重审触发**：M3 补完校验判据后，如果实测**再出现一次**模型选错噪音 → 立刻按第六章的选项处理。

**顺带记一条实测**：`/v3/place/text` 的 `types=<类别码>` 比 `keywords` **明显更精准**
（`types=140100` 首条就是成都武侯祠博物馆，`keywords=博物馆` 首条是小众馆）。
但 `types` 是"包含"匹配（`types=050000` 搜餐饮会混进度假酒店），且模型要记住类别码 → **M2 没用**，记在 `技术方案.md` 备用。

---

### D37 · `Day.checks` 是**天级判据**的落点（契约变更，发生在前端动手之前）　`M3`　2026-09-16

**决定**：`Day` 新增 `checks: list[Check] = Field(default_factory=list)`，
与 `Stop.checks` **并列**。不是把天级判据挂到某个 `Stop` 上。

**为什么非加不可**：`方案.md` 6.1 的硬判据里有两条的**主语是"这一天"而不是"某一站"** ——
`weather_conflict`（暴雨撞上户外景点）和 `walk_load`（当天累计走路量）。
没有 `Day.checks` 这个字段，它们**没有地方安放**。

| 替代方案 | 否决理由 |
|---|---|
| 挂到当天第一个 `Stop` 上 | 判据语义被扭曲（"这一天有暴雨风险"变成"这一站有暴雨风险"）；且一份错误在一
天里重复 N 次 —— 前端会显示 5 个一模一样的红标 |
| 塞进 `Trip.validation.remaining` | `remaining` 是 **`ValidationIssue`**，只有 `code/msg/level` 三个字段，**没有 `status`** |
| 前端自己从 `weather` + `stops` 推 | 把判据逻辑搬到前端 = "校验"这件事有两个实现，迟早不一致；而且"三态"就落不下来了 |
| ⏸ **改用 `Trip.checks` 扁平一级** | 位置丢了。前端要按天渲染卡片，扁平之后得自己按 `date` 反查归组 |

**代价**：M0 冻结后又动了一次契约（第 2 次，第 1 次是 `Stop.checks`）。
**重审触发**：无 —— 但**要守住一条**：以后再加判据，先问「主语是站还是天」，
别图省事一律往 `Stop` 上堆。

**时点为什么安全**：变更发生在**豆包动手之前**（`D18` 契约先冻结的意义就在这里）。
契约冻结之后改字段 = 前后端双倍成本；冻结之前改 = 零成本。**这条时间账是 D18 的全部理由。**

---

### D38 · 跨天段判据用「下界」，且**绝不进** `total_distance_km`　`M3`　2026-09-16

**背景**：`Stop.from_prev_km` 对**当天第 1 站恒为 0**（它没有"上一站"），
于是"从住处到当天第一站"这一段的距离**没有任何字段承载**（第六章那条悬而未决项）。

M3 实测后果（2026-09-16 真跑）：一份含都江堰 + 青城山的 3 天行程，
输出「总里程 **10.7 km**」—— 而**光「锦里 → 都江堰」一段实测就是 63.9 km**。
差额不是算错，是契约里那一段**根本没人算**。

**决定**：新增 `check_cross_day_hop`，算「**昨天最后一站 → 今天第一站**」，
仅用于 `reachable` 判据，**不写进任何汇总数字**。

**它是"从住处出发"的什么**：**下界**。用户没说住哪，那个距离**算不出来**；
而不管昨晚回没回住处，"昨天最后一站 → 今天第一站"这段路**他都得走**，
所以实际耗时 **≥** 这个值。

| 替代方案 | 否决理由 |
|---|---|
| 就当"从住处出发"直接算 | 谎报。用户住哪不知道，这个数字**可能比真实值大也可能小**，而判据需要的是"错也要错得可预期" |
| ✅ **取下界** | **只会漏报，不会误报**。误报（把好行程判成坏的）会让 agent 白烧一轮重排，代价高得多 |
| 问用户住哪（加必填项） | 必填项已经 4 个（`方案.md` A25）；且用户常常还不知道住哪 —— 加了会卡在追问态 |
| 把跨天段加进 `total_distance_km` | ❌ **这条特别危险**：会让"总里程"把"昨天的落脚点"算成"今天的起点"，语义错；而且它仍是下界，混进去后**没人分得清哪些是实算、哪些是估计** |
| 挂在 `DayStats` 上加字段显示 | ⏸ 有价值（见第六章），但那是**展示**决策，跟判据是两件事 |

**代价**：
· **`total_distance_km` 有已知低估**（不含跨天段、不含往返住处）—— 必须写进文档和界面文案，
  否则用户拿它判断行程强度会误判
· 跨天段要多打一次高德接口 → 所以 `validate_trip` 的 `distance_fn` **是可选的**：
  不传就不打（"拖拽后重算"这种场景不该被它拖慢）。⚠️ 代价是那个入口会漏掉远郊首站
· 面试会被追问「那真实总里程到底多少」→ 诚实答：**区间**，下界是这个数，上界未知（取决于住哪）

**重审触发**：加了"住宿"相关功能（比如问用户住哪个区域）→ 那时可以真的算"从住处出发"，把这个下界换成真值。

---

### D39 · 搜索取回 **20** 条、给模型看 **8** 条（两段式窗口）　`M3 收尾`　2026-09-15

**背景**：`MAX_CANDIDATES = 8` 原来同时是"取回量"和"展示量"。实测发现这两个量
**该分开**：搜「都江堰」，高德返回的前 8 条里只有 **2 条**是景点（其余是市政府 /
客运中心 / 快铁站 / 火车站 / 医院 / 高速出口 / 道路名）—— 取 8 条就**没得挑**。

实测（`scripts/probe_pool_quality.py`，13 个关键词 × 20 条）：

| 关键词形态 | 池子有用率 | top-8 有用率 |
|---|---|---|
| 泛词 / 场景词（"古迹""博物馆""寺庙"） | 84.4% | 84.4% |
| **具体地名（"都江堰""宽窄巷子""武侯祠"）** | **43.3%** | **41.7%** |

**决定**：`search_poi` 内部取回 `SEARCH_FETCH = 20` 条（高德 `offset` 上限 25，取 20 不贴边界），
进 `poi_pool` 记**全量**，重排后**只把前 `MAX_CANDIDATES = 8` 条**放进给模型看的文本。

| 替代方案 | 否决理由 |
|---|---|
| ✅ **两段式（取 20 / 展示 8）** | 多取的 12 条**不进 prompt** → token 成本**一点不变**；只多存 12 条 POI 在内存里 |
| 取回量也改成 20，全给模型看 | prompt 从 8 行变 20 行（约 +2.5 倍字符），且噪音一多模型更容易挑错 |
| 保持取 8 条 | 实测"没得挑"：好地点被服务设施挤出窗口，**无法靠排序补救**（池子里没有的东西排不出来） |
| 池子只记展示的 8 条 | ❌ 会把"模型引用了一条它其实搜到过、只是没被展示的 POI"判成**编造** —— 冤枉模型 |

**代价**：
· 每次搜索的内部数据量 ×2.5（响应约 40KB）→ 高德同一次请求，**不额外计费**
· `poi_pool` 会比"模型实际见过的"大 → 封闭世界校验的边界**宽了一格**（宽松方向，不会误报）
· 面试会被追问「为什么是 20 不是 25」→ 诚实答：25 是文档上限，**上限值会随上游变**，取 20 是留余量

**重审触发**：`prompt` 预算变紧（比如加了多轮对话历史）→ 那时该压的是 `MAX_CANDIDATES`（展示量），**不是取回量**。

---

### D40 · 候选噪音**只重排、不过滤**（含多值拆分 + 末级否决）　`M3 收尾`　2026-09-15

**背景**：D36 记了"候选池 44% 是噪音"。第六章当时给了三个候选解法
（①按 `typecode` 过滤 / ②只标注让模型自己筛 / ③用 `types` 类别码定向搜）。
**实测之后三个都不选** —— 而是第四个方案。

同一个脚本里的策略对比（口径：top-8 窗口里的有用条数）：

| 策略 | 窗口有用率 | 窗口内可站点数 | 误杀 |
|---|---|---|---|
| S0 现状（原序，不标注） | 76.9% | 64 | — |
| S1 + 标注类型（顺序不变） | 76.9% | 64 | — |
| S2 硬黑名单过滤（剔 9 类硬噪音） | 90.4% | 78 | 0（修完判定口径后） |
| ✅ **S3 按可游玩性重排（不删条目）** | **97.1%** | **85** | **不可能有** |

**决定**：新增 `app/tools/poi_rank.py`，按"可游玩性分层"**只改顺序、不删条目**：

| 档位 | 大类 |
|---|---|
| 0 | 风景名胜 / 公园广场 |
| 1 | 科教文化服务 |
| 2 | 体育休闲服务 |
| 3 | 餐饮服务 / 购物服务 |
| 4 | 住宿服务 |
| 5（兜底） | 其余一切 + 不认识的 |

同时**在给模型看的每一行加类型标签**（`[国家级景点]` `[火锅店]` `[地铁站]`）。

| 替代方案 | 否决理由 |
|---|---|
| ✅ **重排** | **判错的代价只能是"排后面"，不能是"消失"**。而且 `poi_pool` 保持全量 → 校验层能力一点没少 |
| ① 按 `typecode` 过滤 | 实测**会误杀**：`宽窄巷子景区` 的 `type` 是 `购物服务;...\|风景名胜;...` —— 只看第一段会把顶级景点判成购物场所 |
| ② 只标注让模型自己筛 | **治不了"好地点被挤出窗口"**：公告里说 8 条里 6 条是噪音时，模型没得选。标注只解决"选错"，不解决"没得选" |
| ③ `types` 类别码定向搜 | 实测质量**不可控**：`types=110200` 前 8 全是偏远小寺庙（彭州市磁峰镇莲花寺 / 河游寺 / 木兰寺）。而且它也是"包含"匹配（`types=050100` 搜餐饮混进 30% 非餐饮）。∴ 见 D41 |
| 全局黑名单（剔掉餐饮/住宿） | ❌ 对"想吃什么"的需求会**把目标全滤掉** —— 噪音是**按场景**定义的，工具层不知道场景 |

**代价**：
· 排序规则依赖高德的 `type` 字段 → 高德改分类体系时**排序会退步**（但有实测回归测试守着，见下）
· `科教文化服务` 这一档太粗（博物馆和驾校同档）→ 补了 `NON_VISIT_LEAVES` 末级否决表，
  **那是个补丁，不是分类体系**（维护原则写在该文件里：只在实测见到噪音时才加一条）
· S1（标注）的收益**量不出来** —— 它不改变条目，只改变模型手上的信息。所以它是"零成本保险"，
  不能拿它当主要收益来源去汇报

**做过的三件事让这个决定可复核**：
1. 探针脚本 `scripts/probe_pool_quality.py` **落盘**（含 13 个关键词 × 20 条原始 fixture）
2. 策略对比做成脚本里的固定一节 → 换个人跑一遍就能复现上面的表
3. `tests/test_poi_rank.py` 里有**实测回归**：样本变了会红 → 提醒"当初的结论过期了，要重新量"

**重审触发**：高德改了 `place/text` 的排序或 `type` 分类 → 跑 `probe_pool_quality.py --analyse`，看 S3 的窗口有用率是否还 > 90%。

---

### D41 · 不做 `types` 类别码定向搜（实测否证，不是"没时间做"）　`M3 收尾`　2026-09-15

**背景**：`types=<类别码>` 在单点测试里表现极好 —— `types=140100` 搜博物馆，
首条直接命中「成都武侯祠博物馆」，前 8 全是知名博物馆。看起来像"精准搜索"的答案。

**决定**：**不采用**。理由是同一个参数在别的类别上**翻车**：

| 类别码 | 最大类占比 | 前 8 实际内容 |
|---|---|---|
| `140100` 博物馆 | 100% | ✅ 武侯祠博物馆 / 杜甫草堂博物馆 / 成都博物馆 / 四川博物院 … |
| `110200` 风景名胜 | 100% | ❌ 彭州市磁峰镇莲花寺 / 河游寺 / 尹昌衡故里 / 白云寺 / 木兰寺（全是偏远小庙） |
| `050100` 中餐厅 | 70% | ❌ 混进体育休闲 2 / 住宿 3 / 商务住宅 1 / 汽车服务 1 |

**关键区别**：`140100` 好是因为**那个类别本身就窄**（成都的博物馆就那么几十个，
排序随便排都能排到好的）；`110200` 差是因为**类别太宽**（"风景名胜"覆盖全成都市域），
而 `types` 搜索**没有"靠近市中心"这个权重**，于是把郊区小景点全带出来了。

∴ **`types` 的精度取决于类别宽度，不是统一收益**。而且它要求模型记住类别码（`110200`、`050100`），
一个记错就搜出另一类东西 —— 这是**把风险从"数据层"挪到"模型层"**，更不可控。

| 替代方案 | 否决理由 |
|---|---|
| ✅ 不做（用 `keywords` + 重排，见 D40） | 重排对**两类**返回都有效（96~97% 窗口纯度），不需要区分类别宽度 |
| 让模型按需传类别码 | 模型要记码；且它**判断不了"这个类别宽不宽"** —— 那正是翻车的原因 |

**代价**：放弃了"定向搜"带来的**召回增强**（`types=140100` 带回了模型想不到的
"四川建川博物馆聚落"）。缓解方式：重排 + 标注让模型**看得见**候选里的每个名字，
它自己会判断要不要搜得更细。

**重审触发**：如果实测发现"模型总是漏掉某类地点"（比如从没推荐过小众博物馆）→ 那时再考虑
"用 `types` 做**补召回**的第二轮搜索"，而不是替换主检索。

---

### D42 · 第一版（引擎 v1）= 做到 M5；**M3 的定义修正回「硬 5 + 软 4」**　`M3 收尾`　2026-09-16

**用户拍板**：第一版做到 **M5**（引擎 v1 完整）—— 补 M3 软判据 → M4 搜索子 agent → M5 服务化。

🔴 **但开工前必须修正一件事：M3 只做了硬 5 条，软 4 条没做。**

`方案.md` 第九节对 M3 的定义是 **「校验体系：硬 5 类 + 软 4 类 + 三态 + 打回上限」**，
完成判据三条都达到了（故意喂冲突约束被打回 ✅ / 无数据显示"无法判定" ✅ / 循环有上限 ✅），
但 `draft.py` 里的 `soft_warnings` 至今仍是硬编码 `0`，注释写着"M1 恒为 0：软判据排在 M3"。

**越界的地方**：本 AI 在 `技术方案.md` 的里程碑表里，把 M3 那行改写成了
「5 硬判据 + 三态」，**等于单方面缩小了 M3 的范围且没有说明**。
范围变更属于用户决定的事，不该由 AI 顺手改掉。已恢复口径。

**软判据 4 类**（原定全部由 LLM 判、`level=soft`、`fail` 只提醒不打回）：

| code | 判什么 |
|---|---|
| `needs_booking` | 是否需要提前预约 |
| `queue_time` | 旺季排队时长 |
| `elder_friendly` | 是否适合带老人 |
| `overall_feasible` | 整体可行不可行 |

**补做时必须同时定"它怎么被评测"** —— 软判据没有 ground truth，且 LLM 输出不完全确定
（该节点不挂 tools，但**思考模式下 `temperature` 失效**）→ M8 的 `pass@k` 会被它污染。
→ 落地方案见 D45。

---

### D43 · 「总里程」的口径 = **当天第一站就是出发点**（不加字段、不改文案）　`M3 收尾`　2026-09-16

**用户原话**：「就叫总里程，第一站就是出发点」

**语义定死**：

```
total_distance_km = 从「当天第一站」起算的站间里程之和
                    ↓
       "住处 → 第一站"这一段**不存在** —— 因为第一站就是出发点
```

用户自己负责让行程的第一站是他实际出发的地方（界面二可以拖拽重排，见 D44）。

**因此三个候选解法全部不做**：

| 候选 | 结论 |
|---|---|
| `Day` 加 `from_base_km` / `from_base_min` | ❌ 不加 |
| 改叫「城内里程」+ 副标 | ❌ 不改文案，就叫「总里程」 |
| 问用户住哪（加必填项） | ❌ 不加 |

⚠️ **但口径必须写进契约文档**（`docs/api.md` 的字段说明 + `前端交接.md` 的"禁止出现的字段"节）——
否则将来接手的人会把它读成"全天里程"，然后去"修"一个本来就是对的东西。
**这类"看起来像 bug 的正确行为"必须留文字，不能只留在脑子里。**

⚠️ **与 D38 不冲突**：`check_cross_day_hop`（昨天最后一站 → 今天第一站）作为**判据**保留 ——
判据要保守（宁可漏报不误报），它只是**下界**，**仍然绝不进 `total_distance_km`**。
展示口径 = 站间；判据口径 = 允许拿跨天下界做粗筛。两件事。

---

### D44 · 界面二**允许跨天拖拽** —— 代价是必须实现「站点易日」重算　`M3 收尾`　2026-09-16

**用户拍板**：允许跨天拖拽。

⚠️ 这条**扩展了** `方案.md` 5.3 原本的范围（原文只写"调顺序 / 删站 / 改时间"，没说跨天），
所以 5.4 的链路要补一个分支。跨天把某一站拖到另一天，`date` 就变了 → 下面这些**全部要重算**：

| 重算什么 | 为什么 | 依赖 |
|---|---|---|
| `Day.weather` | 那天天气不同 | 高德天气（**只有 4 天窗口**） |
| `open_today` 判据 | 到达的是另一个日期 | POI 营业时间 |
| `check_cross_day_hop` | 前一天最后一站变了 | 距离接口 |
| `arrive` / `leave` 时间轴 | 整天顺序变了 | 纯代码 |
| `day_stats` / `total_distance_km` | 里程变了 | 距离接口 |

**三条实现边界（本 AI 定，不需要用户拍板）**：

1. **跨天拖拽后不重跑 LLM** —— 走确定性重算，遵 `方案.md` 5.4 已定的分工
   （拖一下等 LLM 3~8 秒体验会崩；想跑软校验点【重新深度检查】按钮）
2. **天气超出高德 4 天窗口 → `weather = None`，判据落 `unknown`** —— 三态已覆盖，不新增机制
3. **必须加距离缓存** —— 同一次拖拽会重算整条链，同一对坐标只该算一次；
   否则一次拖拽打出 N 次高德请求，会把 D35 的限速窗口直接顶满

**与 D43 的关系**：跨天拖拽正是 D43 "第一站就是出发点"的落地手段 ——
用户把想先去的站拖到某天第一位，那一站就成了他的出发点。

---

### D45 · 软判据**只产出「提醒」，不产出「事实断言」**　`A43 / M3 欠账`　2026-09-16

**问题**：软判据由 LLM 判，而 LLM 判"是否需要预约"时会**凭记忆编**：

> ❌ "武侯祠需要**提前 3 天**预约"

系统**根本没有这个数据源**，这句话是编的。而一旦它出现在界面上，
这个项目"**结果不是模型编的**"这条立身之本，就在软判据上破了。
硬判据有 `poi_exists` 守着（封闭世界校验），**软判据没有对应的守门人** ——
它必须靠另一种纪律。

**决定**：把软判据的输出限制成「提醒」，不许是「事实断言」。

| | 例子 | 能不能自动查越界 |
|---|---|---|
| ❌ **事实断言** | "需提前 **3 天**预约"、"排队约 **2 小时**"、"每天限流 **5000 人**" | ✅ 出现具体数字即越界 |
| ✅ **提醒** | "武侯祠是热门景点，节假日建议提前确认预约政策" | ✅ 不含任何具体数字 |

**三条硬规则**（写进 prompt，同时也是测试的断言点）：

1. **不许出现具体数字** —— 天数 / 小时 / 人数 / 价格 / 限额
2. **不许用断言语气** —— "需要 / 必须 / 会 / 是" → 换"建议 / 留意 / 可以考虑"
3. **不许引入输入里没给的实体** —— 只能说我们真给过的事实（站名 / 日期 / 同行人 / 自己算出的步行量与车程 / 天气）

**第 3 条是关键**：它把软判据从「模型的知识」拉回「模型对已给事实的推理」。
→ 于是软判据**变成可自动评测的**（检查 msg 里的实体是否都在输入里）。

**按这个原则重新设计 4 条软判据**（原设计见 D42 的表）：

| code | 判什么 | 信息价值来自 | 可验证性 |
|---|---|---|---|
| `elder_friendly` | 这天的安排对同行人是否吃力 | **我们算出的数**（步行量 / 车程 / 站数） | ✅ 高（可阈值断言） |
| `overall_feasible` | 整份行程的整体节奏 | 同上（三天共几站 / 每天几站） | ✅ 中 |
| `needs_booking` | 这个**组合**值不值得提醒去确认预约 | 模型对"热门 + 周末/假期"的常识 | ⚠️ 低（只能评措辞） |
| `queue_time` | 这天的人流压力 | 同上 | ⚠️ 低 |

前两条**完全基于已知事实**（不涉及模型外部知识）→ 可验证；
后两条必然涉及外部知识 → 只能做成"提醒确认"，措辞必须软化。

**连带的评测口径（M8）**：

| 类别 | 评法 |
|---|---|
| 可验证类 | **阈值断言** —— 步行量超阈值时必须给出提醒 |
| 不可验证类 | **措辞合规率** —— 越界率（出现具体数字）**必须为 0**，作为门槛 |

**为什么值得单列一条**：面试必问"**没有 ground truth 的判据你怎么评**"。
答案就是这个 —— **评不了对错，就评它有没有越界**。

**代价**：
- 软判据的"信息量"被削弱了 —— 它不能说"需要预约"，只能说"建议确认预约政策"。
  **这是诚实的代价**：系统确实不知道。硬答就是编。
- 多了一层需要维护的措辞检查（正则 + 实体白名单），且它**会误伤**
  （站名里本来就可能带数字，如"三星堆"没有数字但"3号门"有）→ 检查要用**白名单式**而非黑名单式。

**重审触发**：如果以后真的接上了"预约政策"数据源（景区官方接口），
那时 `needs_booking` 就该从软判据**升级成硬判据**，这条约束对它自然失效。

---

### D46 · checkpoint 生命周期 = 首次 chat 懒建，进程内持有　`M6`　2026-09-16

> **决定**：`AIOMySQLSaver` 在**第一次 chat 请求**时才连库建表（`await saver.setup()`），
> 之后随进程生命周期持有、所有会话复用 —— 不随请求开关。

| 替代方案 | 否决理由 |
|---|---|
| 应用启动时建（import/ lifespan 里连） | 把"起服务"和"有 MySQL"绑死：M5 的读路径接口、冒烟页在无库环境也应可用；测试起 app 还得先起库 |
| 每个请求新建连接 | 每次 chat 多一次 TCP + 建表检查（`setup()` 要查 22 条迁移），30s 的流首帧延迟变大 |
| 连接池按请求借还 | langgraph-checkpoint-mysql 的 saver 持有自己的连接，拆池等于绕开它的实现 |

**代价**：首次 chat 的首帧延迟多一次建库检查（实测毫秒级，可接受）；
saver 是进程级单例 —— **测试里不能用默认 factory**（会真连库），必须注入 fake（已如此）。
**重审触发**：多 worker 部署（uvicorn --workers >1）时，每个进程各自持有 saver 是否触达连接数上限。

---

### D47 · steer（插队）= `update_state` 写 `pending_messages`，不用 interrupt/HITL　`M6`　2026-09-16

> **决定**：插队实现为一次 LangGraph `update_state` —— 先读当前 `pending_messages` 再追加写回
> （该键**无 reducer**，覆盖语义，直接写 `[msg]` 会冲掉没被消费的旧插话）；
> 正在跑的节点**不会被打断**，插话在下一个 `agent_step` 被 drain。

| 替代方案 | 否决理由 |
|---|---|
| LangGraph `interrupt()` + `Command(resume=...)` | 人机交互范式是为"节点**开跑前**等人批准"设计的；插队发生在"节点跑着呢"——要中断得重新设计 SSE 事件流（前端收到 interrupt 该显示什么？契约已冻结，改 = 双倍成本） |
| 独立队列（Redis / 进程内）+ 节点轮询 | 多一个状态存储，且 checkpoint 里已有 `pending_messages` 键 —— 两处状态必然漂移 |

**代价**：插话**不即时生效**（当前节点跑完才被读到）——用户体感"我说了它没反应"最多持续一个节点的时长（最长的节点是 agent_step，约 10~30s）；steer 返回 `ok:true` 只代表"写进了"，**不代表"会被采纳"**。
**重审触发**：用户实测反馈"插话永远没反应"（说明 drain 逻辑漏消费）→ 再考虑 interrupt 方案。

---

### D48 · 终态取根 run 的 `on_chain_end`，路由层再兜一道 done　`M6`　2026-09-16

> **决定**：① 流终态从**根 run 的 `on_chain_end` output** 拿（`parent_ids` 为空），不用 `aget_state`；
> ② 节点计时起点存本地 `node_t0` 字典，不塞进事件 dict；
> ③ `_sse_body` 对句柄流的异常再兜一层：补发 `error + done` —— "error 后必发 done"是**双保险**。

| 替代方案 | 否决理由（全部**实测踩过**，不是推测） |
|---|---|
| `aget_state(config)` 拿终态 | 无 checkpointer 的图（CLI/测试路径）直接抛 `No checkpointer set` |
| 计时起点写进 `ev["data"]["_t0"]` | start/end 是**两个独立的事件 dict**，存进去 end 里取不到 → `elapsed_ms` 恒 None（测试抓到） |
| 只靠 `graph_chat_stream` 的 producer except 转错误帧 | 那层漏了（或测试替身抛异常）客户端就遇到**无声死流**：HTTP 200 + EOF 没 done = 前端按"断流"提示，与业务错误混为一谈 |

**代价**：终态依赖"根 run output = 最终 state"这个 LangGraph 实现细节（若升级后根 output 语义变化，终态会拿空 → 走"流程结束但没产出"的错误分支，**会报错而不是静默错**，可接受）；
路由兜底分支里的异常**跳过落库**（行程可能收到一半就断了，落一半不如不落）。
**重审触发**：langgraph 升级后根 run 的 output 不再等于最终 state（发现方式：happy path 测试红）。

---

### D49 · PATCH 时间轴 = pinned 锚点优先 + 顺推 + 23:59 截断　`M7`　2026-09-16

> **决定**：重算时间轴（`recompute_trip`）—— 起点取当天首站当前的 `arrive`
> （`update_time` 显式设置过则用设置值）；之后每站 `arrive[i] = leave[i-1] + from_prev_drive_min[i]`。
> 锚点按**站的 `poi_id`** 记录，不按 `(day, seq)` —— 同一批 op 里 `update_time` 之后再 move/delete
> 会重排 seq，按坐标寻址的锚点会指错站（实测抓到过这个隐患，回归测试
> `test_pinned_arrive_survives_same_batch_move` 钉死）。顺推超 23:59 **截断**，不造非法 HHMM、不挪站。

| 替代方案 | 否决理由 |
|---|---|
| 首站恒用 `DAY_START`（09:00） | 覆盖用户 `update_time` 设的值 —— pinned（"我设的时间你别动"）语义失效 |
| 锚点按 `(day, seq)` 寻址 | 键位是**应用时刻**的坐标，同批后续 op 重排后指错站：12:00 被顺推悄悄覆盖，且不报错（最危险的一类 bug） |
| 溢出时把站挪到次日 | "重算不改结构"是 D44 的边界，挪站超出 PATCH 职责；溢出是"这天排太满"的信号，交给硬判据提醒 |
| 溢出返回 4xx 拒绝落库 | PATCH 是编辑器：日程排太满是**合法编辑结果**不是非法请求 |

**代价**：① 截断出的 23:59 是"假到达时间"——溢出的站全叠在 23:59，数据层不反映"到底晚了多久"；② 同批"update_time + move 同一站"语义定为**锚点跟随**（用户显式设的时间只有显式再设才能改）—— 换一种理解（"拖走 = 放弃设定"）也说得通，这里钉死前者。
**重审触发**：前端要做溢出标记（红色角标）→ 需给 Stop 加 `overflow` 注记（契约变更，走三步纪律）。

---

### D50 · paste 解析 = 两步法：LLM 只出地名清单，POI 落地交给 search_poi　`M7`　2026-09-16

> **决定**：粘贴行程解析拆两步 —— ① LLM 只做「文本 → 地名清单」（`PastePlan`，**不产 POI id**）；
> ② 逐地名跑 `search_poi` 拿真 id + 真坐标，手动 `pool.record()` 入池 —— 粘贴行程的站与生成行程**同一条可信度链路**。
> 搜不到的站**跳过不中断**：进 `skipped` 列表 + token 总结提醒；解析失败回灌重试 1 次，仍失败 → `paste_parse_failed` 错误帧。

| 替代方案 | 否决理由 |
|---|---|
| LLM 直接产完整行程（含 POI id） | 模型给的 id 必是编的 —— 封闭世界校验的立身之本就是"id 只能来自工具返回"；整批被 poi_exists 打回，多绕一圈且成功率存疑 |
| 搜不到的站整单拒绝 | 手写行程常见错别字/小地名，一票否决 = 热启动基本不可用；"能救的先救回来"更符合粘贴场景 |
| 复用 chat 的 parse_intent | paste 的输入是"成形行程"不是"模糊需求"：prompt、校验路径、事件流（无 session 帧）都不同，硬复用把 intent 图搅浑 |

**代价**：① 每地名一次串行 search_poi（10 站 ≈ 4.5s @ D35 限速），比纯 LLM 慢；② 地名搜不到时该站**直接丢**，不猜近似名（猜 = 搜出别的城市同名点的风险）；③ 解析失败路径最多烧两次 LLM 调用。
**重审触发**：长行程（>20 站）超时，或用户反馈"写对了却没导入" → 考虑地名去重批搜 / 失败站名模糊重试（各加成本，先量化再动）。

---

### D51 · PATCH 三条细则：`to_seq` 以移出后的列表为参照、空天保留不删、池从行程反构　`M7`　2026-09-16

> **决定**：`apply_ops` / `recompute_trip` —— ① `move` 的 `to_seq` 参照系统一为**移出后的目标列表**（remove → renumber → insert(min(to_seq-1, len))），同日/跨天同一条代码路径；
> ② 被 delete/move 掏空的 day **保留**（空 stops 不删天、不重排 day 号）；
> ③ 硬判据候选池用 `pool_from_trip` **从行程自身反构** —— 已入库的站都是闸门放行过的，`poi_exists` 在 PATCH 后恒 pass 是**预期行为**。

| 替代方案 | 否决理由 |
|---|---|
| `to_seq` 以移入前列表为参照 | 同批多个 op 时参照物漂移（前一 op 已改列表）；remove-first 让每个 op 看到的都是"执行时刻的实际列表"，语义唯一 |
| 自动删空天、后续 day 号前移 | day 号是同批后续 op 的**寻址坐标**，动了等于地址变了，后面的 op 全部错位；UI 上空天还有"继续加站"的价值 |
| PATCH 不建池、跳过 poi_exists | 校验器出现"有时跑有时不跑"的分支，测试难写；反构池让 validate_trip 行为统一 |

**代价**：① 空天出现在响应里（`stop_count=0`、`day_stats=null`），前端渲染要容忍（契约本允许 `stops=[]`）；② "删一整天"要前端连发 N 个 delete；③ `poi_exists` 对 PATCH 恒 pass —— 它保证的是结构自洽，不再反映"实时库里还有没有这个 POI"。
**重审触发**：前端反馈空天碍事 → 加**显式** `delete_day` op（不是自动删，day 号寻址语义不动）。

---

### D52 · recheck 不收 body，用默认 Requirements　`M7`　2026-09-16

> **决定**：`POST /trips/{id}/recheck` **无 body**；软校验输入用 `Requirements().with_defaults()`。
> 只跑一次软校验 LLM + `flatten_checks` 全量返回，`rounds=0` 不重排（重排是 agent 的事）。

| 替代方案 | 否决理由 |
|---|---|
| body 传 Requirements | 前端没有 Requirements 输入面（只在 chat 首问收集），Trip 表也不存 —— 为一个接口加一列 + 一个 body 契约模型，收益为负 |
| 从会话 checkpoint 反查当时约束 | pasted 行程没有会话（A37）；checkpoint 是 LangGraph 的持久化机制不是业务参数库，跨层读层次反了 |

**代价**：**recheck 的上下文完整度低于生成时** —— 用户在 chat 里说过"带老人出行"，recheck 的 LLM 看不到，软提醒可能漏相关项（诚实接受；面试被追问"为什么不让前端传"就答"输入面都没有，传是空传"）。
**重审触发**：前端要做"按约束重查"（勾选人群再查）→ 届时扩 body（契约变更走三步纪律）；或 Trip 加 `requirements_snapshot` 列（存储成本换上下文完整度）。

---

### D53 · 评测判分**一行 LLM 都不碰**；hotstart 的成功判据不含"硬错 0"　`M8`　2026-09-16

> **决定**：M8 运行器（`eval/run_eval.py`）—— ① 判分全部确定性代码：硬判据读引擎算好的
> `checks`（`flatten_where` 保留位置信息以支持按 day 匹配）、措辞合规用 `find_overreach`
> 正则、结构断言纯比较；② coldstart 成功 = 跑通 + 出行程 + **硬错 0** + expect 全满足；
> hotstart 成功 = 跑通 + 出行程 + expect 全满足，**不要求硬错 0** —— 粘贴行程的错误被
> `should_flag` 抓到才是价值；③ pass@k（≥1 次成功）/ pass^k（全成功）按 case 聚合。

| 替代方案 | 否决理由 |
|---|---|
| 用 LLM 给行程打分（"你觉得这个行程合理吗"） | 裁判是被评的东西本身：换裁判模型数字全变，"评测说变好了"永远可被追问"是不是评测模型变了" |
| hotstart 也要求硬错 0 | 粘贴行程**本来就该带错**（这才需要校验）；按这个标准热启动永远 0 分，抓错率（产品核心价值）反而测不出来 |
| 复用 `flatten_checks` 做 should_flag 的 day 匹配 | 它为喂前端把**位置丢了**，"第 2 天车程爆"会匹配成"任意一天爆"；评测自收 `flatten_where` 保留 `d2` / `d2s3` 前缀 |

**代价**：① `should_flag` 的 code 集合受限于硬判据（4 个 code，其中单跳/当日车程共用 `reachable`，靠 msg 区分）——主观问题标不进评测（这正是风险 11 要的纪律，不是缺陷）；② k 次重复跑真 LLM 有成本（case 里 k 默认 1，真实评测 k≥3，单次全流程 0.3~0.5 元）。
**重审触发**：要评"行程质量好坏"而不是"对错"（如站点选择优劣）→ 那需要 pairwise 对比或人工评，另立口径，不塞进这套。

---

### D54 · 粘贴文本的同行人（travelers）要抽进判据上下文　`M7 欠账 / M8 抓到`　2026-09-16

> **决定**：`PastePlan` 加 `travelers` 字段（`PARSE_SYSTEM_PROMPT` 第 6 条抽取规则：
> "带爸妈/老人"→elders≥1，"带小孩"→children≥1，**没提到一律 null 不猜**）；
> `check_plan` 的 `Requirements` 从 `plan.travelers` 构造 —— 硬判据（`walk_load`
> 长辈 5km 上限）与软判据（`elder_friendly`）从此看到同行人。

| 替代方案 | 否决理由 |
|---|---|
| 不抽，粘贴路径统一按"无同行人"判 | "带爸妈的行程"按 8km 步行上限判 —— 长辈 5km 阈值在热启动**永远不生效**，`elder_friendly` 软判据也失去主语；产品缺口，不是评测专用的边角 |
| travelers 加进 API 契约（`PasteTripRequest`） | 前端没有同行人输入面（粘贴场景它就在**文本里**）；`PastePlan` 是内部模型不动契约，成本为零 |

**代价**：LLM 多抽一个字段有误抽风险（"陪爸妈吃饭"≠同行长辈）——靠"没提到一律 null"的规则 + Pydantic 宽容解析兜底；误抽的后果是判据阈值变严（保守方向，可接受）。
**重审触发**：无。这条是 M8 demo case 真跑抓出来的缺口（`walk_load day2` 未触发 → 排查发现 paste 路径 travelers 恒空），属于"评测第一天就抓到一个真 bug"，证明评测架子搭对了。

---

### D55 · 会话级模型切换（B 档）　`A45`　2026-09-16

> **决定**：`POST /sessions` 加可选 `model` 字段（`str | None`）；省略 = 跟随
> `.env` 默认（现状行为不变）；选定后**会话内固定**。两槽位（tool/plan）都换成
> 该模型，**思考开关仍按节点类型**（`supports_thinking=False` 的模型强制
> disabled，防止给不支持思考的模型发 thinking 参数）。校验在**建会话时**
> fail-fast：不在 `MODEL_REGISTRY` 或该 provider 凭据未配 → 400 `invalid_param`。

| 替代方案 | 否决理由 |
|---|---|
| 全局 `.env`（现状） | 不满足需求本体——用户要的是**对话窗口里选**，运维式重启换模型不是产品能力 |
| 每轮切换（`ChatRequest` 加 model，A 档） | 契约面更大；且同一会话两轮用不同模型 = **checkpoint 历史混两个模型的输出**，DeepSeek 对历史消息格式敏感（坑 1 那类 400 风险），还要额外验证混来源历史不炸——收益撑不起风险 |

**代价**：① 契约变更一次（SessionCreateRequest + Session 响应 → types.ts 重生成，豆包加选择器 UI）；② `sessions` 表加 `model` 列（幂等迁移）；③ `chat_factory` 从"单图复用"变"按模型缓存图"（多占一份编译图的内存，图对象本身轻，checkpointer 共享）。**粘贴 / recheck 不在范围**（不挂会话，永远默认模型）——模型切换是**对话体验**不是全局开关，边界收窄才值得做。
**重审触发**：前端要做"对话中途换模型" → 得走每轮级或扩 `PATCH /sessions`，届时必须先解决混模型历史问题（或换会话）；MODEL_REGISTRY 加新 provider 时凭据路由要跟着扩（见 D56）。

---

### D56 · qwen-plus 的凭据现实：名字注册 ≠ 能用　`A45` 附带　2026-09-16

> **决定**：`_build` 按 `MODEL_REGISTRY` 的 `provider` 字段路由凭据——
> `deepseek` 用 `LLM_API_KEY` + `LLM_BASE_URL`；`bailian`（qwen-plus）用新增的
> `LLM_BAILIAN_API_KEY` + OpenAI 兼容 base_url（`LLM_BAILIAN_BASE_URL`，默认
> dashscope compatible-mode）。key 未配时模型仍留在 registry（可选面展示用），
> 但**建会话选它 → 400**（"模型 qwen-plus 未配置凭据"）。

| 替代方案 | 否决理由 |
|---|---|
| 从 MODEL_REGISTRY 删掉 qwen-plus | registry 承担"可选面"的职责（前端选择器数据源），删了以后要加回来还得改代码；留着 + fail-fast 校验，配 key 即生效 |
| 全部模型共用一套 key/base_url | DeepSeek 的 key 打百炼的地址必 401——两个 provider 的凭据**本来就不通用**，路由是事实不是设计 |

**代价**：config 多一组可选环境变量（不配不报错，只有选了 qwen 才触发 400）；探针要多验一条路由路径。
**重审触发**：接第三个 provider（如 GLM）→ 照 bailian 模式加一组，代价线性。

---

### D57 · M9 鉴权核心：JWT 形态与 401 语义　`A30` 落地　2026-09-16

> **决定**：JWT = HS256，payload **只有 `uid` + `exp`（+ `iat`）**，TTL 默认
> **7 天**（不做 refresh，api.md 第一节定死）；`JWT_SECRET` 生产必填（泄露 =
> 全部 token 可伪造，改动 = 全员重新登录）。**401 三态（缺失/签名错/过期）
> 对外统一为一句话**；登录时"用户不存在"与"密码错"也是**同一个 401 同一句话**
> （防用户名枚举）。用户名重复是唯一例外 → 409 `conflict`（注册页本来就要提示，
> api.md 错误码表定死）。JWT 校验**不查库** —— 资源路由的隔离靠 `WHERE user_id`
> 自然产生 404；只有 `/auth/me` 自己确认用户存在。
>
> **接线层的三个实测教训**：
> 1. 🔴 **限流依赖与鉴权的死锁** —— 全局限流 `make_dependency` 原来用
>    `Depends(get_current_user_id)`，M5~M8 它是常量 1 无所谓，M9 换真校验后
>    register/login（ip 范围）**在登录之前**就 401，永远注册不进去。修法：
>    ip/session 范围不碰 token，user 范围才手动解析 —— 依赖图的"隐式全局
>    依赖"在依赖语义变化时会变成死锁，审依赖图要连着审。
> 2. **M5 埋的 `Depends(get_current_user_id)` 占位如期兑现**：所有业务路由
>    零改动通过真 JWT（当初的设计值回票价）；但限流器与测试夹具是两个漏网
>    的"直接消费者"。
> 3. **真库验收脚本抓到隔离真生效**：脚本硬编码 `user_id=1` 存行程，验收
>    用户 uid≠1 → 404。两次失败都是隔离在正确工作，不是 bug。

| 替代方案 | 否决理由 |
|---|---|
| 401 区分"token 过期"与"token 无效" | 帮攻击者区分"重新登录就行"与"你在伪造"——信息泄露零收益 |
| token 里带 nickname/avatar（前端不用调 /me） | token 签出后改不了，用户改昵称就脏读；api.md 明令禁止 |
| JWT 校验时查库（用户被删立即失效） | 每个请求多一次 DB 往返，防的只是"删用户不删 token"这个边角；TTL 7 天兜底 |
| refresh token 双 token 机制 | 第一版规模不需要；"过期就重登"是可接受的体验成本，换来零 refresh 存储 |

**代价**：全测试套件要注入 Authorization 头（6 处 helper，机械改动）；限流依赖结构小改；`.env` 多一个必填项。
**重审触发**：做多端（App/小程序）或 token 泄露事故 → 上 refresh token / 短 TTL；做管理员后台 → 要加角色声明（token 里就得多一个 claim，契约变更走三步纪律）。

---

### D58 · 模型选项换血：qwen-plus → qwen3.7 系，评测走百炼免费额度　`A45` 附带　2026-09-16

> **决定**：`MODEL_REGISTRY` 的百炼槽从 `qwen-plus` 换成 **`qwen3.7-flash` + `qwen3.7-plus`**
> （2026-09-16 models 列表 + 真实调用实测均通过）；`.env` 主链路（`LLM_MODEL_TOOL/PLAN`）
> 切到 **`qwen3.7-flash`** —— 日常 e2e、评测全部走百炼免费额度，DeepSeek key 降级为备用。
> 分槽吃额度的依据：**官方免费额度按模型独立，互不共享**（各 100 万 token / 90 天，
> 北京地域）→ flash 当工作马（开发+评测），plus 当对话窗口的演示质量档，两份额度不互相烧。

| 替代方案 | 否决理由 |
|---|---|
| 保留 qwen-plus 当第三个选项 | 名字旧（能力停在 2025 快照），且它若已被 kb_api 项目消耗过额度，剩余不透明；flash/plus 是 2026 新快照，额度全新 |
| 评测继续走 DeepSeek（原计划） | DeepSeek 按量计费，评测一轮 10+ case × pass@k 要烧几十万 token；用户明确"评测也走免费" |
| flash 和 plus 都进对话选项但主链路留 deepseek | 主链路（e2e/评测）才是烧 token 大头，不切等于没省 |

**代价**：① flash 规划质量弱于 plus/DeepSeek，评测数字会偏低一点 —— 报告 label 会如实写 `qwen3.7-flash`；② 免费额度 90 天到期，比赛/面试若在其后要用需按量（flash 也仅 0.2/0.8 元每百万，几乎无感）；③ 契约选项列表变 4 个，前端下拉框直接读交接文档新列表。
**重审触发**：额度耗尽或 90 天到期 → 换下一个 qwen 快照（新模型发布自带新 100 万额度，改 `MODEL_REGISTRY` 一行 + `.env` 两行）；qwen 规划质量实测明显拖后腿 → 评测切回 DeepSeek 只烧出报告那一轮。

---

### D59 · checkpointer 改请求级连接：进程级单连接会被一次断流打死　`M6` 修复　2026-09-16

> **决定**：`make_default_chat_factory` 从「进程生命周期持有**一根** MySQL 连接」改为
> **每请求新建 `AIOMySQLSaver`（独立连接）+ 流结束 `aclose()` 归还**。`ChatHandle`
> 增加 `closer`/`aclose()`；`_sse_body` 的 finally 与路由的 `except BaseException`
> 两个归还点，正常走完和客户端断开（CancelledError 也走 finally）都必须还。
>
> **实测复现的故障**（2026-09-16 冒烟）：`AIOMySQLSaver.from_conn_string` 内部是
> `aiomysql.connect()` —— 单连接不是池。SSE 流被客户端中断（curl 掐管道模拟
> 关页面）时，正跑在这条连接上的查询被 asyncio 取消，aiomysql 把连接标记为死
> （`InterfaceError: Cancelled during execution`），此后**所有** chat 复用死连接
> 全部 500，直到重启。单连接在"不同 session 并发 chat"下还会互相踩。

| 替代方案 | 否决理由 |
|---|---|
| aiomysql 连接池（create_pool） | `langgraph-checkpoint-mysql` 的 `BaseAsyncMySQLSaver` 泛型绑死单 `Connection`（`_cursor()` 直接 `conn.cursor()`），不支持池 —— 除非绕开包自写 saver，复杂度不成比例 |
| 死连接探活 + 重建（factory 里 ping） | 只救"请求开始时已死"；流中途打死仍要等下一次请求探活，且重建后还得同步重建所有缓存的图 —— 请求级连接从根上消除状态共享，代码反而更简单 |

**代价**：每次 chat 多一次 TCP 握手（本地 <1ms；chat 限流 10/小时，完全可忽略）+ 每请求重建图实例（纯内存毫秒级，原来的图缓存随之删除）。
**重审触发**：并发 chat 量大到连接建立成本可见时 → 自写池版 saver 或换 PostgreSQL（`langgraph-checkpoint-postgres` 官方支持池）。

---

### D60 · 默认模型切回 deepseek-flash：百炼免费但慢 14 倍　`M9 后半` 2026-09-16　`→ 部分被 D78 推翻（2026-09-18 额度耗尽）`

> **决定**：`.env` 的 `LLM_MODEL_TOOL/PLAN` 默认档从 `qwen3.7-flash`（D58）切回
> `deepseek-flash`。**部分推翻 D58**：qwen3.7-flash/plus 仍在 `MODEL_REGISTRY` 里
> （会话级下拉可选，走百炼免费额度），但不再当默认工作马。
>
> **实测依据**（2026-09-16，前端联调期间真跑）：同一个软校验 prompt，
> 百炼 qwen3.7-flash **51.8s**，deepseek-flash **3.7s** —— 慢 14 倍，且
> 输出质量无优势（越界丢弃为零）。软校验在 recheck 和 paste 里是同步等待，
> 用户对着转圈等 50 秒不可接受。

| 替代方案 | 否决理由 |
|---|---|
| 全走百炼省 DeepSeek 费用 | DeepSeek flash 定价本身就低，评测/演示的消耗折算成钱可忽略；换来的是每次交互多等 ~48s |
| qwen3.7-plus 当默认（质量档） | 更慢，且免费额度 90 天有期限，默认档依赖它不稳 |

**代价**：默认档烧 DeepSeek 按量费用（量级：演示一周 <1 元）；评测报告如仍要省额度，跑评测时显式把 `.env` 两行临时切回 qwen3.7-flash（评测代码零改动）。
**重审触发**：百炼侧 qwen 系列出与 deepseek-flash 同量级延时的模型；或 DeepSeek 费用实际可见时。

---

### D61 · 攻略内容来源 = 行程发布；允许多篇 + 幂等键　`A46`　2026-09-17

> **决定**：`POST /trips/{trip_id}/publish-as-guide` 把已有行程渲染成**公开**攻略，
> 正文由 `app/services/guide_render.py` 这个**零 LLM 纯函数**生成。`guides` 加两列：
> `source_trip_id`（**只溯源，不加唯一约束**）与 `idempotency_key`
> （唯一键 = `(user_id, idempotency_key)`）。**同一行程允许发多篇。**

| 替代方案 | 否决理由 |
|---|---|
| `(user_id, source_trip_id)` 唯一索引（同行程只能一篇） | 会把"想改标题再发一版"这种**合法操作**误判成错误。唯一索引防的是"同一**业务事实**出现两条"（`likes`/`favorites` 用它），不是"同一**请求**被处理两次" |
| upsert：重发 = 更新原篇 | 语义其实更干净（攻略是行程快照，行程改了就更新），但**用户手改过的正文会被自动渲染的正文覆盖**，且丢掉"同一行程两个版本"的能力 |
| 抓网上攻略内容入库 | 版权风险；更要命的是**标不出 ground truth** —— 评测集的立身之本正是"你知道原文哪里错"，用模型的判断去标判据答案等于自证 |
| 让模型生成攻略正文 | 同一行程发布两次会得到两篇不同的文章：不可复现、无法单测 |

**代价**：多两列 + 一个唯一键 + 端点读一个头 + **前端要管 key 的生成时机（写错会静默失效：能跑、不报错、只是偶尔出重复）**。攻略正文**不写 `checks`** —— 攻略给人看，不是校验报告。发布出的攻略**永不进评测集**（自家模型生成的 = 自评）。
**重审触发**：真出现"同一行程只能有一篇攻略"的需求（那时改 upsert 语义，并接受覆盖手改正文）。

---

### D62 · "是否已发布过"的判据：每次点击查后端，不做本地缓存　`A46 附带`　2026-09-17

> **决定**：前端点发布时**先打一次** `GET /guides?source_trip_id=<id>`
> （隐含"只看自己的"，匿名 401），有 N 篇则弹确认框；确认后才带**新** key 发 POST。

| 替代方案 | 否决理由 |
|---|---|
| 页面加载时查一次，点击时读本地状态 | 页面开久了状态必然过期（别的标签页发过、自己发过又忘了），弹窗会谎报"没发过"，兜底价值归零 |
| 不做这个判据查询 | 用户手滑重复发布时社区列表看着像刷屏，作者自己也懵 |

**代价**：每次点击多一次往返。**实测不构成压力**（2026-09-17 用临时表造 10 万行攻略实测）：按 `(user_id, source_trip_id)` 点查 p50 **0.816ms** / p95 1.247ms / max 2.914ms；对照组的社区列表全表 `visibility` 扫描 COUNT 是 13.1ms，而一次 chat 要调 LLM 数十秒。何况这是"用户点一次按钮"才发生一次的低频查询。
**重审触发**：这个判据查询的 QPS 涨到与列表/chat 同量级时（那时说明它被塞进了轮询或渲染路径，是设计问题而非性能问题）。

---

### D63 · 发布即公开，不做"草稿"分支；响应统一 201　`A46 附带`　2026-09-17

> **决定**：`publish-as-guide` 固定 `visibility=public` + `published_at=now`，
> 请求体**不带** `publish` / `visibility` 字段；幂等命中（没新建）也返回 **201**。

| 替代方案 | 否决理由 |
|---|---|
| 默认私有草稿，用户自己再点"公开" | 多一个分支、多一组测试、多一次操作；而这个端点的语义**就是**"发布" —— 点它 = 要分享。真想先存草稿走 `POST /guides`（那条默认 private） |
| 命中返 200、新建返 201 | 前端要为"命中"和"新建"写两条分支，而对用户来说它们是同一件事：发布成功 |

**代价**：没有"先发给自己看"这个中间态。想撤回用已有的 `POST /guides/{id}/unpublish`。
**重审触发**：出现"想私下整理攻略集"的需求时。

---

### D64 · 迁移判据的表名必须进元组（修一个会崩 init_db 的 bug）　`工程流程`　2026-09-17

> **决定**：`_DDL_MIGRATIONS` 从 `(列名, DDL)` 改成 **`(表名, 列名, DDL)`**，判据 SQL 的表名走参数。

**修的是什么**：判据 SQL 把表名**写死成 `'sessions'`**。第一条迁移（`sessions.model`）看不出问题，**从第二条起永远判错表** → 恒返回"列不存在" → 每次都执行 ALTER → 第二次 `Duplicate column name` 直接把 `init_db` 打崩。加 `guides` 两列时才暴露出来。

| 替代方案 | 否决理由 |
|---|---|
| 判据改查 `information_schema.STATISTICS`（能覆盖索引） | 需要另一套参数形状，而 `ADD COLUMN` 与 `ADD KEY` 可以写在**同一条 ALTER** 里 —— 用列判据就够（列在则键也在，它们是同一次加上去的） |
| 引入 Alembic 之类的迁移框架 | 为一个单机作品引入迁移框架的运维面（版本表、autogenerate、前后向脚本）远大于收益 |

**代价**：迁移仍只靠"列存在性"判据，**不认识的漂移**（列在、但索引被手工删了）不会补。已用"连续跑三次 `init_db`"验证幂等。
**重审触发**：需要**改列类型或删列**时（当前机制只会加，不会改）。

---

### D65 · 子代理跨 `task` 去重：把候选池已有条目写进任务描述　`M3 收尾遗留`　2026-09-17

> **决定**：`build_task_tool` 在 `subgraph.ainvoke` **之前**，从 `poi_pool.current_pool()` 取已收录 POI 的名字（截断，只喂名字不喂详情），拼进传给子代理的 `objective` 末尾（形如「⚠️ 这些地点前面已经搜到过，不要重复推荐：…」）。
> **子图结构 / 子代理 prompt / 主 agent prompt 一律不动。**

**根因（实读代码，不是推测）**：

| # | 事实 | 位置 |
|---|---|---|
| 1 | 每次 `task` 调用都 `ainvoke({... "keywords": [], "notes": [], ...})` → 子代理的"已搜过"清单**每次从空开始** | `subagent.py` `build_task_tool` 内 |
| 2 | `sub_plan` 的 prompt 只由 `objective / city / notes / keywords` 拼出，**从不读候选池** | `subagent.py` `sub_plan` |
| 3 | 池**只被读一次**，且在 `sub_finalize`（渲染结果 / 兜底清单），规划阶段读不到 | `subagent.py` `_fallback_candidates` |
| 4 | 主 agent 侧同样看不到池内容 → 派 `task` 时**没有去重依据** | 主 agent 上下文里只有 task 的文本回执 |

→ 两头都缺"已经有什么"的信息，去重全靠模型自觉 → 实测杭州那轮派 **7 个 `task`**，
其中第 5/6/7 个高度重叠（都在搜灵隐寺 + 杭帮菜餐厅）→ 成都 146s / 杭州 **371s**。

⚠️ **作者已经修过这个问题的"内层版本"**：`SubState.new_keywords` 单开字段，注释明写
「否则同一个词连烧 3 次搜索配额」—— 那是 **task 内部跨轮**的重复。
**task 之间（子代理实例之间）的外层版本漏了**，本条补的就是它。

| 替代方案 | 否决理由 |
|---|---|
| 加只读工具 `list_found()` 让子代理自己查池 | 要改工具集 + 子代理 prompt + 轮数预算（多一次调用）；而且子代理**仍可能不查** —— 用一次工具调用的确定成本换一个不确定的行为 |
| 在主 agent 的 system prompt 里要求"先看池再派 task" | 主 agent 的上下文里**根本没有池内容** → 这是在要求它遵守一个它看不见的约束。**无效约束比没有约束更糟**（会让人以为已经防住了） |
| 按 `objective` 相似度拦截重复 task | 阈值判据必然有假阳性/假阴性，且命中的是"模型措辞"不是"地点集合"；拦错的代价是**漏搜**（比多搜更贵） |
| 不改，只在文档记录 | 耗时方差已实测到 2.5 倍；重复 task 同时烧 LLM token 与高德配额（还有 QPS 限流） |

**代价**：
1. `objective` 变长（必须截断 —— 只喂名字）→ 子代理输入 token 略增
2. **它是"提示级"去重，不是结构级强制** —— 模型仍可能重复，只是有了依据、概率下降。**不保证消除**
3. 依赖「`task` 工具在 `tool_step` 的 `poi_pool_scope` 作用域内执行」。若以后有人把 `task`
   移出该作用域，去重会**静默失效**（读不到池 → 拼空 → 不报错）→ 这条依赖要写进代码注释

**重审触发**：① 实测重复率**没降** —— 说明根因不是"缺信息"而是"模型不遵守"，那时要改成结构级过滤；
② 子代理改成有状态 / 带 checkpointer —— 那时应**直接给它池的视图**，而不是拼字符串。

---

### D66 · 默认数据源档 = `real`；缺 Key 时**启动期**降级到 mock（响亮、非静默）　`上线就绪`　2026-09-17

> **决定**：
> ① `AMAP_PROVIDER` 的**代码默认值**从 `mock` → `real`；`.env.example` 同步写 `real`。
> ② `real` 但缺 `AMAP_WEBSERVICE_KEY` 时**不再抛错** —— 降级为 mock 继续启动，但在启动日志打**醒目警告**，且 `Settings` 同时保留「请求档」与「生效档」两个值。
> ③ `mock` 的定位正式改为「**评测 / 测试 / 无凭据兜底**」，不再承担「默认档」。

**先纠正一个此前被引用的论据**（我之前的说法不准确，2026-09-17 实读代码纠正）：

| 说法 | 实际 |
|---|---|
| 「默认 mock 是为了让 clone 的人零配置跑起来」 | ❌ **不成立**。`LLM_API_KEY` 是**无条件必填**（`config.py:236`），没有 `.env` 的人在 **import 阶段**就抛 `ConfigError` —— 跟 `AMAP_PROVIDER` 无关。"零 Key 可跑"的准确含义是"**零高德 Key** 可跑" |
| 「默认 mock 是产品形态」 | ❌ 它是"没有凭据时的降级形态"。项目**按上线形态开发**（用户 2026-09-17 定）→ 默认就该是真数据 |

| 替代方案 | 否决理由 |
|---|---|
| 保持默认 `mock` | 与"按上线形态开发"冲突。配好 Key 的人也会因为**忘改一个字母**而跑假数据，而且**跑假数据不报错** —— 这正是本次认知成本的来源 |
| 默认 `real` + 缺 Key **fail fast**（保留 `_require`） | clone 的人必须先配好高德 Key 才能启动 → "别人拿到源码能跑起来"这条彻底没了。而"别人能用"恰是用户要的 |
| 默认 `real` + 缺 Key **静默**落 mock | 静默降级是最难发现的错（用户以为看的是真数据）。降级必须**响亮**：启动日志 + `/health` 可查 |

**代价**：
1. `Settings` 出现「请求档 vs 生效档」两个概念 → 所有判定必须看**生效档**；看错一处就会"日志说 real、实际跑 mock"
2. `HealthResponse` 要加字段 → **契约变更**，必须走 `schemas.py → gen_ts_types.py → tsc --strict` 全套（纪律 ②）
3. 引入一处"降级"，需与 D34 划清边界：**D34 管的是「运行期请求失败时静默换源」（会混源、破封闭世界）；本条是「启动期凭据缺失时选生效档」，不混源、不换源**。这条边界必须写进代码注释，否则后来者会以为原则被破了
4. `tests/test_mock_provider.py` 那条断言要跟着改（**第二次**改它 —— 它已经改过一次，说明它对配置太敏感）

**重审触发**：① 真对外上线（多租户 / 安全审计场景）→ 那时应改成"缺 Key 直接拒绝启动"，不允许降级；② 高德换成多 Key / 多数据源 → "缺 Key"这个单一判据要重写。

---

### D67 · 池空要分因；数据源覆盖不了的目的地**不进工具循环**　`池空修复`　2026-09-17

> **决定**：
> **A（分因报错）** `generate_plan` 那句"模型没有成功调用过 `search_poi`"按成因拆成两种：① 模型真没调 ② 调了但数据源返回全空（覆盖不了该目的地）。②的文案要说人话并给出两条出路。
> **B（前置拦截）** 给 `AmapProvider` 协议加**覆盖范围自述**（`covers(city) -> bool`）；目的地不被覆盖时，在**进入工具循环之前**返回明确错误，不再白等。

**为什么要 B**：实测非成都目的地 → 子代理反复空搜 7 次 ≈ 100s → 撞轮数上限 → 才报错，**总计 159s**。这些时间全花在搜一个**已知搜不到**的地方。

| 替代方案 | 否决理由 |
|---|---|
| 只做 A（改文案） | 用户仍要白等 159s 才看到那句人话。**"能看懂"和"不用等"是两件事** |
| 扩 mock 池（C 方案） | 每加一个城市要手抄 POI + 天气 + photos；且行为变得**不可预测**（池里有就正常、没有就白等）→ 演示时无法解释 |
| 硬编码"目的地 == 成都" | 把**数据源的知识写进了业务层**。切 real 后判据仍生效 → **真数据也会被拦** |
| mock 搜不到时自动落 real | 混源、破封闭世界（D34 已否决） |

**为什么覆盖范围必须由 provider 自述**：只有数据源自己知道"我能覆盖到哪"。`MockAmapProvider.covers` 回答"是不是成都"，`AmapHttpProvider.covers` **恒 `True`** → 切 real 后 B 自动失效，调用点一行不改。这是 D23「工具层不关心数据源是谁」的延续。

**代价**：
1. `Protocol` 加方法 → 两个实现都必须补齐（结构化检查能抓到掉队）
2. `covers` 必须**保守**：判错的代价是「拦住一个本可排出的行程」，比白等更糟
3. 判定位置要能拿到"目的地"，且必须**早于**派子代理（等到 `generate_plan` 已经白等完了）
4. 3 个测试断言跟着改

**重审触发**：① 接入全量数据源后 `covers` 恒真 → 若测试里该分支再没被走过，**保留**（它仍是未来多数据源的接口）；② 覆盖粒度从"按城市"变成"按半径 / 按类型" → `covers` 签名要重设计。

---

### D68 · 演示期限流靠**重启清零**，不改阈值、不加白名单　`演示就绪`　2026-09-17

> **决定**：`chat` 的 10 次/小时 · 30 次/天保持不变。演示靠"启动前重启后端"把进程内滑动窗口清零。

| 替代方案 | 否决理由 |
|---|---|
| 调宽阈值 | 这组数值是写进文档的**防滥用设计**，为演示调宽等于自己开一个洞；演示后忘记调回 → 长期带一个宽阈值 |
| 加演示账号白名单 | 多一套特例分支；面试官问"为什么这个账号不限流"要解释为什么留后门 |

**代价**：演示当天**不能提前烧额度** —— 上午彩排 10 次，下午正式演示就撞 429。→ "演示前重启后端"必须进演示清单（重启同时解决两件事：`.env` 生效 + 限流清零）。

**重审触发**：真对外上线时这组阈值是按"单用户"定的 → 要按多用户重算，且 D28 的"进程内滑动窗口、不引 Redis"前提那时不再成立。

---

### D69 · 能跨请求共享的缓存只能是「外部事实」，不能是「本次运行的结果」　`工程修复`　2026-09-17

> **决定**：`AmapHttpProvider` 的 adcode 缓存（城市名 → 行政区划码）从**实例级**提到**模块级**（`_ADCODE_CACHE`）；并加 `clear_adcode_cache()` 给测试清理。

**问题**：provider 是**每请求新建**的（`api/chat_stream.py` 每请求建图 → 建 provider）→ 实例级缓存**命中率恒为 0**。实测：连续建 5 个 provider，每个的 `_adcode_cache` 都是空的 → **同一城市每次重新地理编码**，白花 1 次高德调用 + 0.45s 出站（一次 3 天行程至少 3 次 `get_weather`）。

**判据（这条比改动本身值钱）**：一个缓存能不能跨请求共享，看它装的是**谁的**事实：

| 内容 | 是谁的事实 | 能否共享 | 共享错了的后果 |
|---|---|---|---|
| POI 池 | **本次运行**搜到什么 | ❌ 必须按请求隔离 | **串台** → 产出错行程 |
| adcode | 城市名 → 行政区划码（**外部世界**的稳定映射） | ✅ 可以 | 最多多查一次（浪费一次配额） |
| 限速时间戳 | **进程级资源约束** | ✅ 必须共享（D35 修订时改的） | QPS 翻倍 → 撞高德限流 |

→ 三者都栽在同一个"每请求新建 provider"的副作用上，但只有后两个**该**提上来。

| 替代方案 | 否决理由 |
|---|---|
| 把 provider 提成进程级单例 | 用一个大改换一个小收益；而且会把 provider 上的其它状态一起变成共享 |
| 给缓存加 TTL | adcode 几乎不变（除非高德调整行政区划），TTL 只是多一个要调的数字 |
| 不改（每次重查） | 实测确认是纯浪费，且修法只有几行 |

**代价**：
1. **模块级状态会跨用例泄漏** → `test_amap_provider` 必须加 autouse fixture 清理。不清的后果是**假绿**：`test_adcode_is_cached_across_calls` 断言的 `count("/v3/geocode/geo") == 1` 会被别的用例填好的缓存变成 `0` —— **测试仍然绿，但测的已经不是它想测的东西**
2. 进程内永不失效 → 高德若改行政区划要重启后端才更新（可接受：年为单位的变化）
3. 多进程部署时每进程一份（与限速状态同一个边界，D28）

**重审触发**：① 再接入其它「外部事实」缓存（行政区划树、POI 详情的不可变字段等）→ 按本条判据判断，并归到同一个模块级容器而不是散落各处；② 上多 worker → 这些缓存与 D28 一起挪 Redis。

---

### D70 · 景点页数据源 = 本站**收录库**，收录库里没有就返回空（**不回落高德**）　`A47`　2026-09-17

> **决定**：`GET /spots/search` 从"代理高德实时搜索"改为"搜本站 `spots` 收录库"；收录库**只由 `scripts/seed_spots.py` 写入**，且该脚本**强制走 real provider**（不看 `AMAP_PROVIDER` 档位，避免把 mock 的演示数据当成"收录"）。

| 替代方案 | 否决理由 |
|---|---|
| 照旧代理高德实时搜索 | 用户要的语义就是"搜**本站收录的**景点"；另外高德 0.45s/次的出站限速（D35）在搜索页上表现为"每翻一页都有感的延迟"；更根本的是实时搜索让这一页永远是"搜索工具"，而不是**有内容的内容站** |
| 手写 5 条 `INSERT` 当收录 | = 编数据，与 A33 删掉参考设计那句"共收录 21006 个景点"**同一个错**（被问"这数据哪来的"答不上） |
| 收录库优先 + 空了回落高德 | 同一页混两种来源，前端没法解释"这条为什么来自高德"；且把"**确实没收录**"这条真实路径掩盖掉（与 D67「池空要分因」同理） |
| 复用 `guides.poi_ids` 当收录库 | 只存了 id、没有名称/坐标 → 搜索要按名称匹配就得逐个反查高德（限流 + 慢），而"按名字搜"正是搜索页的核心动作 |

**代价**：
1. 收录库是**静态快照**，会过期（POI 关停 / 营业时间变）→ 每行记 `collected_at`，重跑脚本即刷新（幂等 upsert）
2. **冷启动空库**：clone 下来没跑过 seed 的话景点页**永远空** → `README.md` 必须把收录列为**必做步骤**。这是"用真实数据"的必然代价，不是实现缺陷
3. 多一张表（第 9 张）+ 一个新 repo；`SpotRepo` 是**全项目唯一不带 `user_id` 的 repo**（共享只读内容）→ 破坏了"每个 repo 方法第一个参数都是 `user_id`"的整齐，所以判据必须写在代码里（见重审触发 ②）
4. 🔴 **搜索排序失去高德的相关性算法**，只能用"前缀命中 → 评分降序"近似。这是**真的降级**，只是当前 5 条数据下看不出来
5. `cached` 字段变成**恒 `false` 的死字段**，留着是为了不动前端契约

**重审触发**：
① 收录条数上千 → "前缀 + 评分"会明显不够用，那时要真的相关性（全文索引 / 向量），而不是继续堆 `LIKE`；
② **若出现第二个"共享只读内容"的 repo** → 说明"带不带 `user_id`"的判据该正式写进 D31，而不是散在各个类的 docstring 里；
③ 若某天允许**用户提交/收录景点** → 收录从"开发期动作"变成"用户写操作"，`SpotRepo` 就必须按用户隔离，**本条作废**。

---

### D71 · 跨 `task` 去重要补「已搜过的**关键词**」，而不是加 task 数上限　`D65 补强`　2026-09-17　`机制保留，落点改到 tool_step（D79）`

> **决定**：`tool_step` 里开一段「已搜关键词台账」作用域（台账**由 state 持有**，字段 `searched_keywords`），子 agent 每次**真正执行**搜索后追加；`task` 工具构建新任务描述时把它拼进去。**明确不做 task 数上限。**

**实测**（2026-09-17 成都那轮，4 个 task）：task1 点名举例「杜甫草堂 / 武侯祠 / 锦里」；task3「逐个确认」又列同一批；task4 第三次提到「锦里」—— **3 个地点被问了 2~3 次**。

🔴 **根因（比改动本身值钱）**：D65 传下去的是「**结果**」（池里已有哪些地点），而模型派 task 时用的是「**已选 / 已排除**」的口径 —— 所以 task3 写的是"排除宽窄巷子和人民公园（**已选**）"，但「**已搜过、还没被选**」的地点它看不见。**重复不是模型偷懒，是主图没有"谁搜过什么"这个信息。**

| 替代方案 | 否决理由 |
|---|---|
| **给 task 数加上限**（方案 B） | 用户判断："有重复景点证明这个景点高频率需要，那更应该纳入行程，不是坏事"。且上限会**砍掉补搜能力**（一次行程本就按主题分批搜）。**去重的目标是省重复动作，不是禁止搜索** —— 加硬闸门等于用"少搜"换"不重复搜"，代价错配 |
| 关键词做**硬拦截**（同词直接拒） | 同一个词在不同意图下是**合法的两次搜索**（"成都"先搜景点、后搜餐厅）→ 硬拦会误杀 |
| 给子 agent 加"查台账/查池"工具 | D65 已否过一次：多一次调用，且子 agent **仍可能不查**；"拼进描述"是**零额外调用** |
| 台账挂**进程级 contextvar**（不落 state） | 作用域随 `tool_step` 结束而清 → **第二个工具轮次就看不见第一轮的词**，去重退化成"只在同一轮内有效"。**必须由 state 持有**（与池子同款：对象归 state，contextvars 只解决"工具函数够不着 state"） |

**代价**：
1. **提示级去重，不保证消除**（与 D65 同一性质）：模型仍可能无视提示换词搜同一批地点。判据是"**减少**"不是"归零"，别拿它当保证
2. 新 state 字段**没有 reducer**（覆盖语义）→ 谁改成 `operator.add` 会得到重复项
3. 台账会进子 agent 的 prompt → 长行程下描述变长约 100~200 字符（截断到最近 `MAX_DEDUPE_KEYWORDS=16` 个）
4. **只记真正执行过的词**：预算撞顶时被跳过的词不记 —— 「没搜过」被记成「搜过了」是**不可逆**的信息错误（下个 task 会被禁止搜一个从没搜过的词）；反过来少记只是少省一点

**重审触发**：① 实测去重率仍低（如 7 个 task 里仍有 3 个高度重叠）→ 提示级手段到顶，该在主图侧**预分配搜索主题**；② 若主图开始**并行**发多个 `task`（同一轮多个 `tool_calls`）→ 同轮并行 task 互相看不见彼此刚写的词（append 有先后），要重估时序。

---

### D72 · mock 池**暂不扩**（挂起，不是"决定不做"）　`工程待办`　2026-09-17

> **决定**：`MOCK_POI_POOL` 保持 9 条成都数据不动；扩充挂起，**等收录库（D70）成熟后统一处理**。

**理由（按重要度）**：
1. **会和收录库重复劳动**：收录库建好后，mock 池最合理的做法是**从收录库导出**（一份真实快照两种用法：收录库 = 景点页内容，mock = 离线兜底）。现在另抓一份 JSON，等收录库建好还要再迁一次
2. **扩全量会杀掉一条有价值的失败路径**：`mock.py` 头部明写「搜不到就返回空列表，**这是故意保留的**」（校验层的"搜不到"提示、模型遇到空结果怎么办都靠它）。池子越大越测不到
3. **演示不需要**：mock 只影响"无 Key 时的**行程生成**"，而收录库落库后**景点页不依赖 Key**

**若将来要扩：成本与最省顺序**（先记下来，免得重新调研）
- 采集脚本已有（`probe_amap.py --collect-mock`），但是**半自动**：只 `print` 代码片段，要人工贴回 `mock.py`
- 9 条是硬编码 tuple；**100 条 ≈ 260KB 源码** → 必须改成 JSON fixture（这是"改造"不是"补数据"）
- 池里有 4 个字段没采（`address` / `adname` / `adcode` / `cost_per_person`）——**收录库的表已经把这些列都存了**，将来直接导出即可
- 多城市要改 `covers()`（现为单城市字符串判断）+ D67 的拦截文案

**代价**：mock 档下**只能生成成都**的行程（`covers` 会拦住其它目的地并给出分因文案）。用户已接受（不上线、本机演示、用成都）。
**重审触发**：① 需要在无 Key 环境演示非成都目的地；② 收录库覆盖到 2 个以上城市（那时"导出"的收益最大）。

---

### D73 · 攻略社区的**起始内容走官方号**，不为它打通 `author_type = system`　`A48`　2026-09-18

> **决定**：建一个**真实的官方账号**（`username = travel_platform` / 昵称「旅游规划平台」）承载社区起始内容；
> 内容 = `trips` 表里**真实生成的行程**经 `guide_render` 渲染后发布（`scripts/seed_official_guides.py`，幂等键 `seed:<trip_id>`）。
> **`guides` 表结构不动**：`user_id` 保持 `NOT NULL`、不加 `author_type` 列 → 契约里的 `author_type` **恒为 `"user"`**，`SYSTEM` 留作扩展位。

**说明**：本条**推翻 `方案.md` 8.2 的一处设计要求**（原写「系统发布的内容 `user_id` 为 NULL → 所以 `user_id` 必须可空」）。
推翻理由不是"做不到"，而是**收益不抵风险**：`guides` 已被 614 个测试覆盖，为"起始内容"动表结构 + 写迁移，
换来的只是作者显示成「官方」徽章而不是站名昵称 —— 对用户是同一件事。

⚠️ 与 `A46` 的关系：`A46` 定的是「攻略内容来源 = **行程发布**（不是抓来的、也不是播种的）」。
本条**不违背它** —— 内容仍走同一条 `guide_render` 渲染路径，只是发起者从"用户点按钮"扩到"官方号批量发"。
`A46` 里"不是播种的"针对的是**抓来的/编的内容**，而这里播的是**我们自己真实生成的行程**。

| 替代方案 | 否决理由 |
|---|---|
| **真打通 `author_type = system`**（`guides.user_id` 改可空 + 加列 + 迁移 + 改 `views.py`） | 收尾期动核心表结构；收益仅"官方"徽章 vs 官方号显示的站名昵称。`author_type` 留着恒为 `user` 成本为零 |
| **用 `eval/fixtures/` 那 2 篇抓来的攻略当起始内容** | **版权**：WebSearch 抓的**别人的文章**（来源 URL 还"待补"），文件头自己写着「只作本地评测素材，**不进攻略库公开内容**」。放社区 = 转载且没署名 |
| **不做起始内容**（社区页空着） | 数据源（真实生成的行程）现成、**零 LLM 成本、零版权风险**；空页面在演示里是纯损失 |

**代价**：
1. `author_type` 成为**永不产出 `system` 的字段** —— 契约里留着一个"类型上合法、运行时不存在"的值，属**刻意留下的不对称**（`schemas.py` 已注明）
2. `guides` 里多一个**不用于登录的账号**；密码只在脚本首跑时打印一次（丢了重设，不走产品流程）
3. 起始内容**读感像行程单**（渲染器产出），不如游记体攻略 —— 这是"零版权风险"换来的

**重审触发**：
① 真需要「官方」这个身份在 UI 上与用户区分（V 标 / 徽章）—— 那时 `author_type` 才值得打通
② 出现**真实的、可署名的原创攻略**（自己写的或拿到授权的）—— 那时 `fixtures` 的版权边界要重新评估

---

### D74 · SSE 重连的「最后一段文字重复」**接受为已知代价**，不做节点级回滚　`D27 配套`　2026-09-18

> **决定**：前端 15.13 只做「断流 → 退避重连」，**不做**「重连后回滚已重复的内容」。
> 断在**节点执行中途**时，界面会出现「半句话 + 整句话」的重复（轨迹同理），**接受，不修**。

**说明**：这不是前端写错，是 **D27 的必然表现**。D27 明文写的就是「**恢复粒度 = 节点不是 token**」——
`chat_stream.py` 里 `if resume: return None` → LangGraph 从 checkpoint 停下处继续，
而 **checkpoint 只在节点边界写** → 那个没执行完的节点会被**整个重跑**、它的 token 全部重发；
前端 `token` 分支是纯追加（`last.content + evt.text`），追加即重复。

⚠️ 找到这条时不能顺手"修"：**修它要么改前端状态机、要么重审 D27**，两条都不是"补个 if"。

| 替代方案 | 否决理由 |
|---|---|
| **前端按 `node` 边界回滚**（收到 `node(phase=start)` 时记消息快照，重连时回滚到快照再续写） | 要在 `handleEvent` 里加"消息 ref 镜像 + 快照"才能拿到进入节点时的内容长度；React 19 严格模式下 updater 跑两次，次序敏感。**为极小概率事件给已跑通的聊天页加状态机** —— 与"收尾期不碰已跑通代码"直接冲突 |
| **后端改成按 `seq` 真续传**（记已发到第几号 seq，重连只发其后的帧） | 最彻底（不重复也不重复打字），但要动 SSE 层实现，且 **D27 那条决策要推翻重写**。收益是"断线时界面更好看" —— 不为它付这个代价 |
| **不管，也不记录** | 下一个接手的人（包括三个月后的自己）会把"重连后文字重复"当 bug 去修，修法还必然是上面两条之一 → **必须留档**（本条就是留档） |

**代价**：
1. 断线场景下 UI 会**出现重复文字**；没有"重连中"的进度指示来掩盖它（前端目前也没有 token 级进度）
2. 同源的**轨迹重复**一并接受：`upsertTrajectory` 的 `node` 事件按"同 node 第 N 次"生成新 key（重发 = 新增条目）、`tool_call` 用 `call_id` 做 key 但**不查重**
3. 已知的**低危边界**一并留着：对**已经正常 `done` 过**的轮再发一次恢复请求，会**再落一条 assistant 消息**（正常断线场景共一条，只在这个交错窗口里多一条）

**重审触发**：
① 项目**真的上公网/弱网环境**（断线从"极小概率"变成常态）—— 那时 D27 要连本条约一起重审
② 前端要加**token 级进度指示**（比如"已生成 N 字"）—— 那一步必然要先把节点/seq 边界拿到前端

---

### D75 · 工具层搜索**带上中心点**（medoid，从已搜到的 POI 算）　`关闭悬而未决③`　2026-09-18

> **决定**：`search_poi` 调高德时带 `location` + `radius=20km`，
> 中心点 = **池子里已有 POI 的 medoid**（到其余点距离之和最小的那个）。
> 池子空（第一次搜索）→ **不加**，请求与改动前逐字一致。

**为什么值得做**：不加中心点时，泛关键词的落点由高德自己决定。实测成都搜「公园」
返回的全是高新区那批（中位距市中心 **10.7 km**）；搜「博物馆」时**成都博物馆排第 9**
（前 8 是立巢航空博物馆、交子金融博物馆这类小众馆）。

| 替代方案 | 否决理由 |
|---|---|
| **`geocode(city)` 当中心点** | 实测**无效**：`/v3/geocode/geo` 查"成都"给的是**市政府驻地**（104.066301,30.572961），而成都市政府 2010 年搬到了高新区 —— 那一带正好就是"不加中心点"时高德自己会返回的那批。实测搜「博物馆」两种做法返回**逐条相同**（中位距都是 8.3 km）。**加了等于没加，代码却看起来"做了地理约束"** —— 这种假修复比不做更危险 |
| **硬编码"天府广场"** | 效果最好（公园中位距 10.5→**2.2 km**、成都博物馆从第 9 跳到第 1），但**换个城市就废** —— 工具层没有"每个城市的中心地标"这份知识，也不该有 |
| **用质心**（各锚点坐标平均） | 实测**会被远郊景点拉进中间地带**：「宽窄巷子 + 都江堰」的质心落在**郫都区**（到两边各 28 km）→ 搜「公园」返回郫都区的公园，**比不加还差** |
| **加工具参数让模型传 `near`** | 多一个能设错的地方，且模型未必知道每个城市的中心地标。而"已搜到的 POI"是**它自己搜出来的** —— 不用它额外知道任何事 |

**medoid 为什么能抗住**：它**一定落在某个真实锚点上**，不会凭空产生坐标。
实测三个锚点（市区两个 + 都江堰一个）→ 选中宽窄巷子 → 返回市中心公园（0.8~2.6 km）。
两点平局时 `min` 取**先出现的** —— 先搜到的通常就是主景点，实测正好落在这个方向。

**代价**：
1. **结果依赖搜索顺序**：同一个关键词在池子不同的两次调用会返回不同结果。这是**有意的**
   （行程在收敛，搜索范围跟着收敛），但它意味着"同一关键词不保证两次结果一致"——
   排查问题时要知道这件事，别当成随机 bug
2. 行程**真的跨区域**时（市区 + 都江堰各半），medoid 会偏向先搜的那一侧。
   代价是"另一侧的泛搜索不够就近"，**不是搜不到**（`location` 是排序权重不是过滤）
3. 多了一个"看起来像魔法"的隐式输入：模型看不到 `center`，输出却受影响。
   所以工具签名**没变**但行为变了 —— docstring 里没写这事，只写在这里
4. `radius` 取 20 km **没有精确依据**，只有"同城游玩量级"这个理由。
   因为它是排序权重，取错只影响排序、不会造成硬故障 —— 这是"敢先定一个值"的前提

**重审触发**：
① 出现"行程跨区域 + medoid 偏向坏侧"的真实案例 → 改成按当天区域分段算中心点
② **住宿模块做出来之后**（那时有"用户的落点"这个**外部**信息）→ 它一定比 medoid 准，优先用它
③ 高德改 `location` 语义（排序权重 → 硬过滤）→ `radius` 要重新取值，且要防"搜不到远处目的地"

**同一次提交的配套改动**：
- `haversine_km` 从 `providers/amap.py` + `providers/mock.py` **两份重复实现提取到 `app/utils.py`**
  （工具层是第三个用户；两份算法逐行相同，只是措辞不同 —— 趁这次收敛掉）
- 三个 `search_poi`（base 抽象 / real / mock）同步加 `center: str | None = None`，
  有 `test_mock_and_real_have_same_public_surface` 守着参数名逐个一致
- **前端零改动、契约零改动**：`center` 是 provider 层参数，不进 HTTP 契约、不进工具 schema

---

### D76 · `tool_step` 同批工具调用**并行执行**　`2026-09-18`　实测收益有限，保留

> **决定**：`tool_step` 里预算内的 tool_calls 用 `asyncio.gather` **并发**执行
> （原为串行 for 循环）；`AGENT_SYSTEM_PROMPT` 明确要求"相互独立的任务
> 在同一次回复里一起派"。ToolMessage 按原 `tool_calls` 顺序回填（顺序变了模型对不上号）。

**背景（2026-09-18 实测）**：一次完整规划 9m48s，其中 60%（401s）花在「执行查询」，
且模型每轮只派 1 个 task —— 串行 await 就是纯等待。行业数据（LLMCompiler 等）
并行 fan-out 普遍 1.8~3.7x 提速。

| 替代方案 | 否决理由 |
|---|---|
| **保持串行** | 60% 耗时锁死在纯等待上；模型同轮派多任务时（D76 prompt 鼓励后常见）收益直接漏掉 |
| **LLMCompiler 全套 DAG 调度**（planner 输出依赖图 + 拓扑排序） | 收益来自"独立调用并发"，同批 gather 已拿到；依赖图对"task 内部子 agent 循环"（真正的耗时大头）无能为力，为不到的收益加一层调度器不值 |

**并发安全依据**：高德限速是"取号式"模块级排队（单 event loop 原子），并发自动串成 2.2 QPS 队列；
池子/关键词台账/trace 是同 context 内追加，无共享写冲突。测试：两个 0.3s 慢工具并行 <0.6s 秒表断言。

**代价 / 实测诚实记录**：
1. 总耗时 **9m48s → 9m14s**（-6%，远低于理论值）—— 真正的时间黑洞是**子 agent 内部的串行 LLM 循环**（每任务 5~8 次调用），并行只在"同轮多任务"时重叠它们，而模型实际分了 4 轮、每轮 1~2 个任务
2. 并发 LLM 调用可能触发模型服务商并发限速（本轮未观测到；DeepSeek 波动大，同需求另一跑 14m33s，方差 > 优化量）

**重审触发**：模型能稳定单轮派 3 个任务、或子 agent 内部循环能砍短时，重测并行收益。

---

### D77 · **骨架先行**：搜索前先给"未验证的先看版"　`首结果延迟`　2026-09-18

> **决定**：`parse_intent` 通过后（需求齐、不追问），先跑 `make_skeleton` ——
> **一次不挂工具的快速 LLM 调用**（关思考，tool 档，实测 **4.5s**）产出
> `{title, days:[{day,date,theme,stops[地名文本]}]}`，经新 SSE 事件
> `skeleton` 推给前端立刻渲染；后台精排继续，`trip` 事件到达后**整体替换**骨架。
> **best-effort**：LLM 挂 / JSON 解析不出（重试 1 次）/ 结构不对 → 返回空 dict
> 静默跳过，**绝不写 error、绝不断主流程**（与 soft_check 同哲学：装饰性调用不许有破坏性失败）。

**动机**：用户质疑"谁能等 9 分钟"。调研（2026-09-18）结论：行业正确指标是
**首结果延迟**不是完成时间 —— 纯生成产品 30~120s 但**零验证**；企业级架构
（agixtech）明说"stream value early + validate in background"。

| 替代方案 | 否决理由 |
|---|---|
| **砍验证做纯生成**（30~120s 出完整行程） | 验证是这个项目的存在理由（面试判据：套壳 ≠ Agent 项目）；砍掉后与"直接调 DeepSeek"无差异 |
| **骨架用 mock 数据/热门榜单** | 假数据进界面违背"候选池里没有的地点绝不出现"的纪律；骨架虽未验证但 title/stops 来自真实模型对用户需求的理解，仍优于硬编码 |
| **只做 D76 不做骨架** | D76 实测只省 34s —— 用户感知为 0；首结果必须靠骨架 |

**代价**：
1. **契约 +1 事件**（`SkeletonEvent`/`SkeletonDay`）→ 前后端双份成本：types.ts 已重生成（66 类型）、api.md 事件表 9→10 种、前端交接第十八节（骨架卡渲染规则 + `trip` 到达整体替换）
2. 骨架与正式行程**可能不一致**（骨架没验证过）→ 用强制显示的 `note` 声明缓解；若用户实测反馈困惑，加"骨架→成稿 diff 高亮"是下一步
3. 骨架里的地名**没有 POI id**，前端绝不能渲染成可点站点卡（交接 18.3 红线）

**实测**：首结果 **9m48s → 11.5s**（parse_intent 7s + 骨架 4.5s）。追问轮（缺目的地/日期）不发骨架。

**重审触发**：① 骨架与成稿差异被用户抱怨 → 做 diff 高亮或砍 date 字段只留 theme；② 模型档位换更快的 → 骨架可并入 parse_intent 一次调用。

---

### D78 · 默认模型切回百炼 `qwen3.7-flash`：deepseek-v4-flash-0731 额度耗尽　`部分再推翻 D60；重审触发已按 D81 走完`　2026-09-18

> **决定**：`.env` 的 `LLM_MODEL_TOOL/PLAN` 从百炼托管的 `deepseek-v4-flash-0731`
> 切回 `qwen3.7-flash`（D58 档）。触发 = 百炼侧 deepseek-v4-flash-0731 **免费额度耗尽 403**
> （2026-09-18 soft_check 0.2s 静默失败破案确认）。DeepSeek **官方开放平台** key
> （`LLM_API_KEY` + `api.deepseek.com`）保留，账户还剩 1 元 —— 只留作一次性测试/演示，
> 想用就把 `LLM_MODEL_*` 改回 `deepseek-flash`，registry 自动路由。

**⚠️ 漂移自曝**：D60 白纸黑字写"切回官方 `deepseek-flash`"，但线上 `.env` 实际跑的
是**百炼托管的 `deepseek-v4-flash-0731`**（D60 之后某次改动没回写记录）——
本条顺带把漂移修正。教训：`.env` 改默认档的每次切换都要在 D 编号里留痕。

| 替代方案 | 否决理由 |
|---|---|
| 充值百炼 / DeepSeek | 免费额度按模型独立还有 qwen3.7-flash 的 100 万 token；开发期不值得花钱 |
| 保持 deepseek 官方档（1 元余额） | 1 元只够一次性测试；日常评测/e2e 烧它等于主动花钱 |

**代价**：默认档回到"慢"侧 —— soft_check 单次 ~50s（D60 实测 51.8s），recheck/paste 同步等待变长；评测报告 label 如实写 `qwen3.7-flash`。
**重审触发**：qwen3.7-flash 额度也耗尽 → 换下一个 qwen 快照（新模型自带新额度，改 `MODEL_REGISTRY` + `.env` 两行）；或用户拍板充值。

---

### D79 · **搜索子 agent 退役**：主图直搜 + 首轮批量派发（计划式执行）　`推翻 D5 搜索结构，改写 D71 落点`　2026-09-18

> **决定**：删掉 `subagent.py` 的搜索子图与 `task` 工具，`search_poi` 回到主图
> 工具集（`AGENT_SYSTEM_PROMPT` 强制**第一条回复一次性派出全部搜索**，5~12 个
> 关键词覆盖景点/美食/住宿等类别，D76 并行 gather 执行）；关键词台账（D71）
> 从子 agent 提示级去重改为 `tool_step` **执行闸** —— 同一个 `city|keyword`
> 不重复执行（省高德配额，ToolMessage 照回，漏回应=400 的坑不重踩）。
> 预算（L2≤8 / 总调用≤20）与池子/封闭世界机制全部不变。

**实测依据（触发整条改造的账）**：2026-09-18 拆账发现一次规划的耗时 **85% 在
LLM 调用**，其中子 agent 的"出关键词→搜→看结果"决策轮占 **12~20 次 LLM 调用**
（每 task ≤4 轮 × 4~6 task）；而高德搜索本身只花 **~15s**（0.45s 限速 × ~15 词）。
慢的根本不是工具，是"决定搜什么"的 LLM 往返。上下文隔离（D5 的立论）经实测
价值有限：`search_poi` 展示层本来就截断 top-8，一次 ~300 token，15 次 ≈ 4-5k。

| 替代方案 | 否决理由 |
|---|---|
| 只并行不删子 agent（task 级 fan-out） | 决策轮次数不变，只省轮内等待；模型"每轮 1-2 个 task"的行为方差照旧（另一跑 14m33s 的主因） |
| 保留子 agent 只改"一次出全量关键词" | 与主图直搜等价但多留一层壳；上下文隔离收益已证明不值一层图 |
| 砍验证换速度 | 项目立身之本，不动（D77 已否决过） |

**代价**：
1. 模型失去"看结果换词"的**临场多轮**探索 —— 空结果兜底改为：prompt 允许补搜一轮 + 台账闸防逐字重复 + 降级池兜底。换词本就是低风险决策，规则够用
2. `subagent_trace` 过程记录没了（run_cli 改印台账）—— 前端"子 agent 在搜什么"可视化一并退役，SSE 契约零变动
3. 首轮批量派发**依赖模型听话**（prompt 纪律非硬约束）→ 真实模型若退化成逐个派，轮数上限（L2≤8）兜底刹车
4. 测试 -25（test_subagent.py 整删）+ 台账新测试；`build_sub_llm` 退役

**重审触发**：真实模型首轮不批量（e2e 轮数 ≥5）→ 把"计划"升级为结构化 JSON 输出（`plan_searches` 节点）而非 prompt 纪律；发现关键词质量明显掉档 → 恢复"看结果再定词"的第二轮补搜权重。

---

### D80 · generate_plan 两段式并行生成：**实测无收益，已回退**　`P2 尝试`　2026-09-18

> **决定**：尝试把草稿生成拆成"分配（1 次小输出）+ 各天填充（N 天并行）"，
> **e2e 实测后回退**，保持单次大调用。这是一条"做了、量了、否了"的记录，
> 留着防止未来再走一遍这条路。

**实测数据**（同需求北京 5 天，qwen3.7-flash，各跑一次端到端）：

| 指标 | P1 基线 | P2 两段式 |
|---|---|---|
| generate_plan | 67.9s | 89.9s |
| soft_check（对照，代码未动） | 49.9s | 71.4s（+43% = 服务端方差） |
| 全程 | 234.9s | 252.1s |

用 soft_check 做对照校准服务端方差后，P2 的 generate ≈ 方差内**持平**，没有任何收益。

**根因（比收益数字更有价值）**：qwen 免费档**每次调用的固定开销 ≈ 20-35s**
（parse_intent 26s、make_skeleton 32s 都是小输出照样慢 —— 网络 + prefill + 排队 + 首 token）。
拆成 6 次调用 = 固定开销 ×6，并行又被服务端排队/限流吃掉一部分。
→ **在慢档模型上，唯一的耗时杠杆是调用次数**（D79 已把它压到 6 次），
"减小单次输出"是伪杠杆。**这个结论同时预言：换 DeepSeek 官方快档（每次调用 3-7s）
后，两段式才可能真省时间 —— 但那时总量已进 1 分钟量级，不值得复杂度。**

| 替代方案 | 否决理由 |
|---|---|
| 保留两段式，用配置开关默认关 | 没有收益的死代码 + 双路径维护成本；git 历史里留着就够 |
| 再跑几轮取平均确认 | 每轮烧几十万 token；对照组（soft_check）已经给出方向性结论，够拍板 |

**代价**：无 —— 回退干净（608 测试全绿），唯一支出是两轮 e2e 的 token。
**遗留知识点**：测试基建里出现了 `plan_calls`（从草稿反推两段式脚本）的思路，
若未来真需要拆生成，fakes.py 历史 commit `925cd35` 之后的工作区里有现成实现可翻。

**重审触发**：默认档换成"单次调用固定开销 < 5s"的快模型，且 generate_plan 重新成为
占比最大的耗时块时，再考虑拆段 —— 但那时大概率直接砍 soft_check 或换流式更有性价比。

### D81 · qwen3.7-flash 额度耗尽：默认档切 plus，**D79 质量评测不重跑**　`D78 重审触发`　2026-09-18

> **决定**：qwen3.7-flash 免费 100 万 token 被 D79 质量评测重跑（13 case）烧完
> （403 `AllocationQuota.FreeTierOnly`，34 分钟无产物白跑）。默认档按 D78 预设的
> 重审触发切到 **qwen3.7-plus**（独立额度，探针 1.0s 存活）。**评测不重跑**：
> D79 的质量验收以"608 测试全绿 + 轻需求真实链路抽检全过"收口，
> 面试材料如实标注"评测硬数字是旧架构基线（deepseek 档）"。

| 替代方案 | 否决理由 |
|---|---|
| plus 重跑评测 | 与基线（deepseek 档）不同源，对比要标模型变量；且可能把 plus 也烧死 → 免费池清零 |
| DeepSeek 官方 0.78 元跑评测 | 同 family 最可比，但动用面试演示专款换一个"补充数字"，不值 |
| 充值跑评测 | 不为求职项目买额度（用户原则） |

**代价**：① D79 没有跨 13 case 的 pass@k/pass^k 硬数字，面试被追问"架构改了质量怎么保证"时只能答测试+抽检+封闭世界代码闸（判分是零 LLM 的代码，模型换档不判分失真——这句是防御）；② plus 额度余量未知，再耗尽只能换下一个 qwen 快照。
**重审触发**：用户主动想重跑评测（e.g. 面试前时间充裕）→ 用当时的最新 qwen 快照，报告标注模型档与基线的差异。

---

### D82 · 后端日志落盘：`app.*` logger 挂按天滚动文件 handler　`P2 可运维性`　2026-09-19

> **决定**：`create_app` 内给 `app.*` logger 挂 `TimedRotatingFileHandler`
> （`backend/logs/app.log`，午夜滚动、保留 14 天、UTF-8），幂等 guard 防重复挂。

| 替代方案 | 否决理由 |
|---|---|
| 维持 stderr-only（现状） | uvicorn 控制台一关，`errors.py` 500 兜底打的 exception 堆栈就没了 —— 事后无法排障 |
| loguru / structlog | 单进程小项目标准库够用，不为此加依赖 |
| 中间件记全量 access log | uvicorn 自带 access log；要的是"错误可追溯"，不是流量报表 |

**代价**：`backend/logs/` 持续增长（14 天自动清，可控）；pytest 造 app 也会建目录（无害，目录已在 .gitignore）。
**重审触发**：需要结构化日志 / 集中采集时。

---

## 六、悬而未决（明确没定，别在正文假装定了）

| 项 | 现状 | 什么时候定 | 不定的后果 |
|---|---|---|---|
| **项目目录名** | 现为 `agent`（太泛）。**决定不改** —— 见下方说明 | — | — |
| ✅ **`AMAP_JS_KEY` 真伪** | **已验（2026-09-18）：有效** —— headless Chrome 真实加载前端地图：`AMap` 加载成功、`map.on('complete')` 触发、**瓦片全渲染 + 地名标注正常**（天府广场 / 成都东站 / 双流国际机场 / 环球中心）、高德版权水印 `© 2026 AutoNavi · GS(2025)5996号` 正常显示。配套的 `VITE_AMAP_JS_SECURITY_CODE` 一并验过 | — | — |
| ✅ **当天第一站的"从住处出发"距离 —— 判据那一半** | **M3 已做**：`check_cross_day_hop` 用「昨天最后一站 → 今天第一站」作**下界**判 `reachable`（D38）。远郊首站不再是盲区 | — | — |
| ✅ **当天第一站的"从住处出发"距离 —— 展示那一半** | **已定（D43，2026-09-16）**：**不加字段、不改文案**，口径定为「当天第一站就是出发点」→ `total_distance_km` 从第一站起算；用户自己负责把第一站设成他的出发点（界面二可拖拽重排，D44）。⚠️ 口径必须写进 `api.md` / `前端交接.md` | — | — |
| ✅ **候选池质量过滤**（M2 新增） | **已定（D40/D41，2026-09-15）**：不是"过滤 + 三选一"，而是**重排 + 标注**；`types` 定向搜实测否证。实测数据见 D39/D40 | — | — |
| ✅ `types` 类别码要不要用 | **已定（D41）**：不用。理由是它在宽类别上翻车（`110200` 前 8 全是偏远小庙），且精度取决于类别宽度而非统一收益 | — | — |
| ✅ **搜索中心点从哪来**（2026-09-15 新增） | **已定（D75，2026-09-18）**：用**池子里已有 POI 的 medoid** —— 零额外请求、不硬编码地名、抗离群。实测搜「公园」从高新区那批（中位距 10.5 km）变成市中心（0.7~3 km）；「博物馆」里成都博物馆从第 9 跳到第 1。`geocode` 那条路已实测否证（给的是高新区市政府驻地，加了等于没加） | 已关 | — |

**`AMAP_JS_KEY` 怎么验的（2026-09-18）—— 顺带拿到一条新判据**：

D24 当年排除的 4 种试法**全是"服务端调高德 API"**，所以结论是"服务端验不了"—— 那是真的。2026-09-18 换了个层面：

```bash
curl -s "https://webapi.amap.com/maps?v=2.0&key=<JS_KEY>"
```

| 返回 | 含义 |
|---|---|
| **90 字节**，内容是 `window.console.log("<AMap JSAPI> Error key!")` | key 无效（**加载层**就拒了） |
| **~968 KB** 完整 JSAPI 库 | 通过加载层 |

⚠️ **"加载层通过"≠"运行时有效"** —— `securityJsCode`、key 的平台类型、域名白名单**都在运行时**校验。
所以最终确认仍必须走浏览器：headless Chrome 打开一个最小页即可
（⚠️ `_AMapSecurityConfig` 必须在 `<script src>` **之前**注入，写晚了不报错、静默失效），
**`map.on('complete')` 触发 + 瓦片渲染出来 = 真有效**。本次截图确认瓦片与地名标注全部正常。

**当天第一站的距离：判据与展示都已定**（2026-09-16 收尾）：

M2 时发现的是"**判据漏了**"（远郊首站没人判）→ M3 用 D38 的**下界**补上。
M3 真跑时又量出一个更刺眼的数：

```
实测输出    总里程 10.7 km
光一段      锦里 → 都江堰  实测 63.9 km（distance_calibration.json）
→ 显示值是"真实值的一小部分"，不是"略低"
```

差额不是 bug 算错，是**契约定义就这样**：

```
Stop.from_prev_km  = 距「当天上一站」
当天第 1 站           → 没有上一站 → 恒为 0
```

于是"从住处/市中心到当天第一站"这一段的距离**没有任何字段承载**。

**✅ 已拍板（D43）**：口径定为「**当天第一站就是出发点**」——
`total_distance_km` 从第一站起算，那段距离**不存在**；用户自己把第一站设成他的出发点。
**不加字段、不改文案**，三个候选解法全部不做。

⚠️ **必须写进契约文档**：这类"看起来像 bug 的正确行为"如果只留在脑子里，
下一个接手的人（包括三个月后的自己）会把它当 bug 去"修"。→ 已落到
`docs/api.md` 的字段说明 + `前端交接.md` 的禁止字段节。

**候选池质量过滤 → 已定，见 D40**（2026-09-15 实测后否掉了当初给的三个选项，
改成"重排 + 标注"；同时 D41 否掉了 `types` 定向搜）。下面这段保留作**过程留痕**：

| 当初的选项 | 为什么没选（已实测） |
|---|---|
| ① 工具层按 `typecode` 过滤 | 会**误杀宽窄巷子景区**（多值 `type` 的第一段是"购物服务"） |
| ② 只在文本里标注类型 | 治不了"好地点被挤出 top-8" —— 8 条里 6 条是噪音时模型没得选 |
| ③ 用 `types` 类别码定向搜 | 别的类别上翻车（`110200` 前 8 全是偏远小庙），见 D41 |

---

🔴 **搜索中心点：`location` + `radius` 实测有效，但"中心点从哪来"没解决**（2026-09-15 新增）

> **✅ 已解决（2026-09-18，D75）**：用**池子里已有 POI 的 medoid** 当中心 —— 零额外请求、
> 不硬编码地名、抗离群。下面的"质心"候选方案也被否了（质心会被远郊锚点拉进中间地带，
> 实测「宽窄巷子+都江堰」质心落郫都区，到两边各 28 km；medoid 恒落真实锚点）。
> 实测效果见上方表格行的 D75 摘要。**以下保留作过程留痕**：

这是本轮实测里**最值钱也最没解决**的一条。事实先摆清楚：

| 关键词 | 无 `location` 前 8 | 加 `location=天府广场` 后 |
|---|---|---|
| 公园 | 交子公园 / 桂溪生态公园 / 锦城公园（**全在高新区，中位距市中心 10.7 km**） | 文化公园 / 百花潭公园 / 人民公园 / 望江楼公园（**中位距 2.5 km**） |
| 博物馆 | 立巢航空博物馆 / 交子金融博物馆 … 成都博物馆排 **第 9** | 成都博物馆 / 四川博物院 / 杜甫草堂博物馆 / 永陵博物馆 |

**而且它是排序权重不是硬过滤**：搜「都江堰」时给它 `location=天府广场, radius=15km`
（都江堰在 56 km 外），返回与**不加完全一致**（重合 20/20，目标仍排第 1）。
→ 意思是**技术上可以无脑加，没有"搜不到远处目的地"的副作用**。

**卡在哪**：中心点从哪来。试过三条路，**都不行**：

| 来源 | 结果 |
|---|---|
| `/v3/geocode/geo` 查"成都" | `104.066301,30.572961` —— **市政府驻地**，比天府广场**偏南 9.6 km**，用它搜公园返回的**还是高新区那批** |
| `/v3/config/district` 的 `center` | **同一个值**（高德的"城市中心"就是这个点） |
| geocode 拼"成都市中心" | **还是同一个值** —— 高德把"市中心"当行政区理解 |
| geocode "春熙路" / "天府广场" | ✅ 有效，但**那是城市特异地名**，工具层不能硬编码 |

**三个候选解法**（等做 M4/M5 时定）：

| 方案 | 代价 |
|---|---|
| 让模型传一个"参考地点"（`near="春熙路"`） | 加工具参数；且模型未必知道每个城市的中心地标 |
| 用**已搜到的 POI 的质心**当中心（自适应） | 零额外请求，但要**引入隐式状态**：同一次搜索在不同调用顺序下返回不同结果 → 可复现性变差、调试更难 |
| 不加，靠 D40 的重排兜住 | 最保守。**当前状态**：重排把可站点都提到前排，但"哪个区的公园"仍然由高德决定 |

⚠️ **不做第三条的"偷偷加 geocode 坐标"** —— 实测那个坐标**没有效果**（返回的还是高新区），
加了只会让代码看起来"做了地理约束"而实际没起作用。那种"看起来生效了"的假修复最危险。

---

**目录名为什么不改**（2026-09-15 决定 · **2026-09-18 复核并修正了成本判断**）：

- 简历上写的是**项目名**（"智能旅游规划系统"），不是本地目录名 —— 改名收益 ≈ 0
- **代码与配置里零硬编码项目路径**（2026-09-18 全仓 grep 复核过，含 `.py`/`.ts`/`.json`/`.yml`/`.env`）
  → 改名对代码零影响
- 要推到 GitHub 时**仓库名单独起**即可，与本地目录名无关
- ⚠️ **旧记载被实测推翻**：原来写"`.venv` 要重建虚拟环境"。实测把 `.venv` **复制**到新路径后跑，
  `Scripts/python.exe` **完全可用** —— `sys.prefix` 自动指向新路径，
  `fastapi` / `pydantic` 等全部正常 import（它靠同级的 `pyvenv.cfg` 定位，路径跟着目录走）。
  **真正失效的只有 15 个 entry-point `.exe`**（`pip.exe` / `uvicorn.exe` / `pytest.exe` 这类，
  内部硬编码了旧绝对路径），而我们一直用 `python.exe -m xxx` 调用，**用不到它们**
- 🔴 **真正拦住改名的是另一件事**（2026-09-18 实测）：**IDE 打开着项目时目录被锁住** ——
  `mv` 报 `Device or resource busy`，PowerShell `Rename-Item` 也失败；
  而**在父目录里改别的目录名能成功**（对照组）→ 说明锁就在项目根目录本身。
  真改的话要先关掉 IDE 里的这个项目（代价是当期会话断掉）

---

## 七、怎么追加一条

**触发时机**（`api.md` 契约纪律第三步）：改了 `schemas.py` 的字段名 / 类型 / 删字段，或做了有任何代价的技术取舍。

**模板**（`决定` 已经写在 `方案.md` 里的，这里只写"替代方案 / 代价"）：

```markdown
### D{n} · <一句话决定>　`A{n} / M{x}`　YYYY-MM-DD

> **决定**：<一句话。若方案.md 已有 A 编号，这里只写摘要不复述>

| 替代方案 | 否决理由 |
|---|---|
| <考虑过但没选的> | <为什么> |

**代价**：<接受了什么坏处。诚实写，包括"面试会被追问 X">
**重审触发**：<什么条件下该重新考虑。没有就写"无">
```

**三条纪律**：

1. **决定本身不在本文件重复** —— 改了 `方案.md` 的 A 表，再来这里补代价；**反过来做就是漂移**
2. **代价必须是真的**。写"无代价"的条目应该删掉（说明它不值得记录）
3. **推翻旧决定要留痕**：不删旧条目，标注作废并指向新编号（如 `A31 → A36`、`A32 → A35`）
