# 2026-09-14

## 重大事件：项目被全删，重开方案讨论

### 现场（20:58 实测）
`D:\vscode Projiects\agent\` **被清空**，只剩一个 20:57 新建的空 `.git`（branch `main`，**零 commit**）。
此前 M1（跑通）+ M2（写了没验证）全部代码、`DECISIONS.md`、`.workbuddy/memory/` 一并删除。

### 用户原话
> **"项目没git过导致我只能删除，我把全部都删了，现在重新讨论项目方案，和技术方案"**

### 核心教训（必须写进开工流程）
**AI 上次建目录时写了 `.gitignore`，却没有 `git init`、没有 commit。**
→ 后果：用户无法 `git checkout` 回滚，只能整目录删除，一整轮工作作废。
→ **修正：任何项目第 0 步 = `git init` + 首次 commit，先于任何业务代码。** 每个里程碑结束再 commit 一次。

### 本机工作区（`D:\vscode Projiects\` 下已存在）
`_jarvis_upstream/` `agent/` `agents-sdk-learn/` `ai-jiaweisi/` `cet-reader/`
`interview-agent/` `kb_api/` `personal_website/` `react-basic/` `todo.API/`

### 状态
用户要求**重新讨论项目方案 + 技术方案**。已进入纯讨论模式：**零落盘、不写任何项目文件**，只出分析/选项/取舍/决策点。
已给出 3 个候选项目方案（A 从零生成行程 / B 航司客服改签退票 / C 行程可执行性校验与修正），等用户选。
