# 前端交接文档（给「豆包」）

> **这份文件是唯一交接入口。** 看完它 + 三个附件，就能把六个页面全部实现完，不需要问任何问题。
>
> | 附件                          | 作用                             |
> | --------------------------- | ------------------------------ |
> | **`docs/types.ts`**         | **前端所有 TS 类型的来源**（自动生成，不要手改）   |
> | **`docs/api.md`**           | 接口契约 + SSE 事件表 + 错误码           |
> | **`docs/sample_trip.json`** | 一份**真实**行程响应（高德真数据），可直接当开发期假数据 |
> | `界面设计.md`                   | 视觉规范与逐页线框（本文件第五节是它的浓缩版）        |
>
> 最后更新：2026-09-15



---

## 一、你要做什么（以及不做什么）

### ✅ 做

六个页面 + 一个布局，React + Vite + TS + Tailwind 4，**严格按 `docs/api.md` 的契约调接口**。

### ❌ 不做（也不许改）

| 不做                                  | 为什么                                                      |
| ----------------------------------- | -------------------------------------------------------- |
| 后端（FastAPI / LangGraph / 数据库）       | 已经由另一端实现，你只需要调它的接口                                       |
| 自己直连高德接口（`restapi.amap.com`）        | key 会暴露在浏览器里，而且绕过后端的限流和缓存。**所有高德数据都通过后端 `/api/spots/*`** |
| 自己造接口 / 自己加字段                       | 契约是唯一接口。需要新字段请提出来，由另一端改 `schemas.py` 后**重新生成类型**给你       |
| 用 `localStorage` 拼一份"本地假行程"让页面看起来能跑 | 会把联调期的问题掩盖到很晚（接口其实没通，但页面是好的）                             |
| 手机端响应式                              | 只保证 **1440px** 宽度下正常。不做移动端                               |
| 暗色模式 / 主题切换                         | 明确不要                                                     |

---

## 二、技术栈（版本写死，别用"最新版"）

| 项                       | 版本         | 备注                                                                         |
| ----------------------- | ---------- | -------------------------------------------------------------------------- |
| React                   | **19.3.0** | ⚠️ 严格模式下 `useEffect` 跑两次，见 6.3                                             |
| Vite                    | **8.3.0**  | 需要 Node `^20.19 \|\| >=22.12`                                              |
| TypeScript              | **7.0.2**  | `strict: true`                                                             |
| Tailwind CSS            | **4.3**    | ⚠️ **CSS-first，没有 `tailwind.config.js`**，主题写在 `src/index.css` 的 `@theme` 里 |
| react-router-dom        | **7.18.3** |                                                                            |
| @dnd-kit/core           | **6.3.1**  | 拖拽排序。**不要用 SortableJS**（它要手动跟 React 的 DOM 对齐，更绕）                           |
| @amap/amap-jsapi-loader | **1.0.1**  | 高德地图                                                                       |
| swiper                  | **14.2**   | 只用于首页 Hero 轮播（`swiper/react`）                                              |

### 2.1 初始化

```bash
npm create vite@latest frontend -- --template react-ts
cd frontend
npm i react-router-dom@7.18.3 @dnd-kit/core@6.3.1 @amap/amap-jsapi-loader@1.0.1 swiper@14.2
npm i -D tailwindcss@4.3 @tailwindcss/vite
```

`vite.config.ts`（**两处都要有**：Tailwind 插件 + 代理）：

```ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [react(), tailwindcss()],
  server: {
    proxy: {
      // 前端统一请求 /api/xxx，由 Vite 转发到后端
      // ⚠️ 前端不要写死后端端口，也不要写死 http://127.0.0.1:8000
      '/api': { target: 'http://127.0.0.1:8000', changeOrigin: true },
    },
  },
})
```

> ⚠️ **SSE 经代理的注意点**：dev 阶段如果发现事件不逐条到达（憋到最后一起吐），  
> 优先怀疑代理缓冲。**验证方法**：`curl -N http://127.0.0.1:8000/api/...` 直连后端对比 ——  
> 直连逐条、代理不逐条就是代理的问题。这一条在联调第一步验掉，别等页面写完。

### 2.2 环境变量（`frontend/.env.local`，**不入库**）

```bash
VITE_AMAP_JS_KEY=前端那个「Web端(JS API)」Key
VITE_AMAP_JS_SECURITY_CODE=配套的安全密钥
```

🔴 **前端用的高德 Key 跟后端那个不是同一个**：  
后端用「Web服务」Key，前端用「Web端(JS API)」Key，**互换了都用不了**。  
2021-12 之后申请的 JS Key 还必须配 `securityJsCode`，否则报 `INVALID_USER_SCODE`。

---


## 三、目录结构（按这个建，路径要和第六节的路由对上）

```
frontend/
├── .env.local                VITE_AMAP_* （不入库）
├── vite.config.ts            Tailwind 插件 + /api 代理
└── src/
    ├── index.css             ★ Tailwind 4 的 @theme（第四节全文照抄）
    ├── main.tsx              ★ 高德安全密钥必须在**模块顶层**注入，见 6.4
    ├── App.tsx               路由表
    ├── types/
    │   └── contract.ts       ★ 从 ../docs/types.ts 复制过来（第七节）
    ├── lib/
    │   ├── api.ts            ★ 请求层（`docs/api.md` 3.7 给了实现）
    │   └── sse.ts            ★ fetch + ReadableStream 手写 SSE（同上）
    ├── layouts/
    │   └── Layout.tsx        导航栏 + 页脚，六个路由共用
    ├── components/
    │   ├── ToolTrajectory.tsx   轨迹卡（SSE 的 node/tool_call/tool_result）
    │   ├── TripCard.tsx         行程卡（SSE 的 trip）
    │   ├── CheckCard.tsx        校验卡（SSE 的 check，三态）
    │   ├── CheckBadge.tsx       三态角标（✅ / ⛔ / ⚪）
    │   ├── StatusPill.tsx       三态通用胶囊
    │   ├── TripTable.tsx        站点表格（dnd-kit 拖拽 + 删站 + 改时间）
    │   ├── TripMap.tsx          高德地图（按天画线 + 12 站编号）
    │   ├── DayTabs.tsx          Day 页签（切页签只显示当天路线）
    │   ├── DayCards.tsx         按天分组卡片
    │   └── HeroCarousel.tsx     首页轮播（Swiper，全站唯一）
    └── pages/
        ├── Home.tsx
        ├── Spots.tsx
        ├── Assistant.tsx
        ├── Overview.tsx
        ├── Guides.tsx
        ├── GuideDetail.tsx
        ├── Profile.tsx
        └── Login.tsx
```

---

## 四、设计 token（几何拼贴风）—— `src/index.css` 全文照抄

**风格内核**：白底 + 高饱和几何色块 + 直角 + 粗下划线高亮。孟菲斯波普 / 现代平面海报语言。

```css
@import "tailwindcss";

@theme {
  /* 基础 */
  --color-ink: #1A1A1A;          /* 主文字 / 描边，近黑 */
  --color-muted: #6B7280;        /* 次要文字 */

  /* 装饰色（可以随便用在色块、标签、下划线） */
  --color-pop-red: #FF6B6B;
  --color-pop-cyan: #4ECDC4;
  --color-pop-yellow: #FFD93D;
  --color-pop-green: #6BCB77;
  --color-pop-blue: #4D96FF;

  /* 🔴 校验三态 —— 独立定义，禁止复用上面的装饰色 */
  --color-status-pass: #16A34A;
  --color-status-fail: #DC2626;
  --color-status-unknown: #D97706;

  /* 🔴 三层字体栈 —— Space Grotesk 不含中文字形，缺 fallback 中文标题会掉档次 */
  --font-display: 'Space Grotesk', 'Noto Sans SC', system-ui, sans-serif;
}

@font-face { /* Space Grotesk（Google Fonts 或本地），标题用 */ }
```

> ⚠️ **Tailwind 4 是 CSS-first**：上面这些变量写在 `index.css` 里，用的时候直接  
> `bg-pop-yellow` / `text-status-fail` / `font-display`。  
> **不要建 `tailwind.config.js`** —— v3 的写法在 v4 里不生效，页面会全部没样式。

### 4.1 形状与版式规则（风格的全部就在这里）

| 元素   | 规则                                                      |
| ---- | ------------------------------------------------------- |
| 圆角   | **直角（`0`）到 `4px` 为止** —— 这是跟"卡通风大圆角"最大的区别               |
| 描边   | `1px solid #1A1A1A`；强调块用 `2px`                          |
| 阴影   | **基本不用**。需要"凸起"时用**实心色块错位**，不用投影                        |
| 强调手段 | **4px 粗下划线**（关键词高亮）／**实心色块**（分类入口）／**轻微旋转 -1°**（黑色实心按钮） |
| 几何装饰 | 正圆、三角（`clip-path`）、色块，漂浮错落                              |
| 字体   | 标题：Space Grotesk 粗体 + 大写 + 宽字距；正文：Noto Sans SC          |

### 4.2 装饰分主次（⚠️ 这条最容易做错）

| 页面                | 任务      | 装饰用量                                                         |
| ----------------- | ------- | ------------------------------------------------------------ |
| 首页 / 旅游攻略社区       | **讲故事** | ✅ 满铺：漂浮圆 + 三角 + 色块卡 + 旋转徽章                                   |
| **AI 助手页 / 路线总览** | **读数据** | ⚠️ **只保留两样：色块 + 粗下划线**。那里要放 12 站表格 + 地图浮层 + 校验角标，再叠漂浮几何会读不下去 |

### 4.3 校验三态的视觉（**这个项目的核心视觉语言**）

| `status`      | 颜色变量                     | 角标 | 文案                                   |
| ------------- | ------------------------ | -- | ------------------------------------ |
| `pass`        | `--color-status-pass`    | ✅  | 通过                                   |
| `fail`        | `--color-status-fail`    | ⛔  | 不通过（`level=soft` 时是"有风险"，但**不打回重排**） |
| **`unknown`** | `--color-status-unknown` | ⚪  | **无法判定** + 必须显示 `msg` 里的原因           |

🔴 **`unknown` 绝不允许显示成"通过"的样子。**  
这是最值钱的可视化 —— 面试会专门问"你怎么区分『确认不对』和『不知道对不对』"。  
真实例子：青城山的高德数据里没有营业时间 → `open_today` 判据只能 `unknown`。

⚠️ **判据挂在两个层级上，别混**（D37）：`Stop.checks` 是**站级**（这一站编造了吗 / 到的时候开门吗 /  
从上一站开多久），`Day.checks` 是**天级**（这天暴雨撞户外景点吗 / 这天走太多路吗 / 这天累计开太久吗）。  
天级判据渲染在**当天卡片头部**（跟"当日 4.0 km / 车程 26 分钟"同一行），站级渲染在**站点行后面**。  
⚠️ 同一个 `code` 会在两处出现（`reachable` 两个字面相同、含义不同）——  
**靠挂载位置区分，千万不要按 `code` 去重**，那会吃掉一条真判据。详见 `docs/api.md` 4.1。

### 4.4 六个不做（视觉层面）

渐变／玻璃拟态／柔和阴影／发光｜圆角 > 4px｜胶囊按钮｜投影式阴影｜动画 > 200ms（拖拽要跟手）｜暗色模式

---

## 五、六个页面：路由 + 调哪些接口

| 路由                  | 页面         | 阶段            | 要调的接口                                                                                           |
| ------------------- | ---------- | ------------- | ----------------------------------------------------------------------------------------------- |
| `/`                 | 首页         | 🟡 占位         | `GET /home`                                                                                     |
| `/spots?q=`         | 旅游景点       | 🟡 占位         | `GET /spots/search`、`GET /spots/{poi_id}`                                                       |
| `/assistant`        | AI 路线规划助手  | 🟢 M6         | `GET /sessions`、`POST /sessions`、`GET /sessions/{id}`、**`POST /sessions/{id}/chat`（SSE）**       |
| `/overview/:tripId` | 路线总览       | 🟢 M7         | `GET /trips/{id}`、`PATCH /trips/{id}`、`POST /trips/{id}/recheck`、`POST /trips/{id}/amap-import` |
| `/overview`         | 路线总览（空手进来） | 🟢 M7         | **`POST /trips/paste`（SSE，不建会话）**                                                               |
| `/guides`           | 旅游攻略       | 🟡 占位（M10 真做） | `GET /guides`、`POST /guides`、`GET /guides/{id}`                                                 |
| `/guides/:id`       | 攻略详情       | 🟡 M10        | `GET /guides/{id}`、评论/点赞接口（M11）                                                                 |
| `/profile`          | 个人中心       | 🟡 占位（M9 真做）  | `GET /auth/me`、`PATCH /auth/me`、`GET /trips`、`GET /favorites`                                   |
| `/login`            | 登录         | M9            | `POST /auth/login`、`POST /auth/register`                                                        |


### 5.1 四条界面级约束（不是样式问题，是会做错的）

**① 导航栏 6 项，`路线总览` 不能省**

```
[TR] 旅游规划平台   首页 · 旅游景点 · AI路线规划助手 · 路线总览 · 旅游攻略 · 个人中心
                    当前页 = 实心黄色块 #FFD93D + 黑字（不用描边、不用圆角）
```

`路线总览` 是**比参考设计多出来的一项，也是核心差异化** —— 参考设计里聊完就结束了，  
我们有"结构化行程 + 地图 + 能手改"这整个闭环。

**② 路线总览的两个入口，行为不同**

| 入口               | 行程来源                                        | 能不能回助手页接着聊        |
| ---------------- | ------------------------------------------- | ----------------- |
| 助手页点「生成路线总览」     | `source = "generated"`，有 `session_id`       | ✅ 能               |
| 空手进来直接**粘贴**一段行程 | `source = "pasted"`，**`session_id` 为 null** | ❌ 不能（它本来就"已经定稿了"） |

⚠️ 粘贴**不建会话**：粘贴那条路径 **不会收到 `session` 事件**，`done.session_id` 是 `null`。  
前端**不能假设这条事件一定到**。

**③ 景点页是搜索页，不是列表页**

进来时**没有数据**（我们没有本地 POI 库），所以默认态显示引导文案：  
「输入景点名称或关键词开始搜索，例如「成都 博物馆」」。  
⚠️ **关键词为空时不要发请求**（后端会返回 400 `keyword_required`）。

**④ 拖拽**支持跨&#x5929;**，且 `to_seq` 是「插入」不是「交换」**

界面二的拖拽**允许把某一站从第 1 天拖到第 3 天**（D44）。跨天时 op 要带上 `to_day`：

```json
{ "op": "move", "day": 1, "seq": 3, "to_day": 3, "to_seq": 1 }
```

| 坑                     | 定死的做法                                                                             |
| --------------------- | --------------------------------------------------------------------------------- |
| `to_seq` 是插进去还是和那一站交换 | **插入**（1-based），目标天原有的站顺延。**不是交换** —— 交换会让两站的 `arrive`/`stay_min` 串位，用户看到的是"时间乱了" |
| 跨天要不要本地重算             | ❌ **不要**。前端**只提交 op、只渲染后端返回的整份 `Trip`**。跨天会让那天的天气 / 营业时间判据 / 时间轴全变，本地算必然和后端对不上    |

⚠️ 同日内重排和跨天移动**不是同一个分支**：前者 `Day.weather` 不变，后者会变。  
前端只管提交、整体替换，**别自己维护一份"哪些要重算"的判断**。  
细节见 `docs/api.md` 3.2。

### 5.2 禁止出现的字段（会直接毁掉项目可信度）

高德**没有**这三个字段，参考设计里那些数字全是它自己造的种子数据：

| ❌ 禁止            | 为什么                                                        |
| --------------- | ---------------------------------------------------------- |
| 「共收录 21006 个景点」 | 我们没有 POI 库，这个数是编的                                          |
| 「热度 10.0」/「趣味度」 | 高德没有这个字段                                                   |
| 「门票 ¥405」+ 门票筛选 | 高德的 `cost` 是**人均消费不是门票**，且门票数据大面积缺失。卡片上写「人均 ¥73」可以，写「门票」不行 |

**没有来源的字段一律隐藏，不显示"暂无"、更不填占位值。** 详见 `docs/api.md` 4.2。

**⚠️ 另有一个"看起来像 bug、但不是"的数字**：`summary.total_distance_km`。

```
从「当天第一站」起算的站间里程之和 —— "住处 → 第一站"这一段不存在
因为第一站就是出发点（用户自己会把第一站拖成他实际出发的地方）
```

含都江堰 + 青城山的 3 天行程，它是 **10.7 km**，而光「锦里 → 都江堰」**一段**实测 **63.9 km**。  
**这个数字是对的。** 标签就叫「总里程」，不要加副标、不要按"全天里程"去理解它、更不要"帮它补上"。  
完整口径见 `docs/api.md` 4.3。

---

## 六、代码约束（这几条不做对接时会当场炸）

### 6.1 类型从 `docs/types.ts` 来，不要手写

```bash
# 在项目根目录（任何一次契约变更后都要重跑）
cd backend && ../.venv/Scripts/python.exe scripts/gen_ts_types.py
```

生成到 `docs/types.ts` → **复制一份到 `frontend/src/types/contract.ts`**（或直接配 alias 引过去）。

**三条约定**：

1. 字段名是 **`snake_case`**，后端不下发 camelCase，**前端也不转**（省一层转换和一类 bug）
2. `| null` 是**业务值**（"接口没给"），**不要用 `?? ''` / `?? 0` 抹平**：  
   `0` 是"免费"、`''` 是"空字符串"，跟 `null`（没数据）是三件事
3. **字段一律必填**（没有 `?`）：后端响应全字段 dump。手写 mock 数据时要补全字段

### 6.2 错误按 `code` 分支，不要匹配 `msg`

统一错误体：

```json
{ "error": { "code": "not_found", "msg": "会话不存在", "detail": null } }
```

- `msg` 是给人看的，会改；`code` 是给机器判别的
- **404 = 不存在 或 无权**，前端不要区分（本来就分辨不了），统一显示"不存在"。**不要显示"无权限"**

**重试与限流**（完整规则见 `docs/api.md` 1.5，这里只说你该做的）：

| 你会见到的                              | 你该做什么                                                    | 你不该做什么                                                                                          |
| ---------------------------------- | -------------------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| `429 rate_limited`                 | 读 **`error.detail.retry_after`**（秒）→ 按钮置灰 + 倒计时 → 到点自动恢复 | 🔴 **不要静默重试**（用户会以为按钮坏了）。也不要只读 `Retry-After` 头（`fetch` 拿它要 `Access-Control-Expose-Headers`，不可靠） |
| `502 llm_error` / `503 amap_error` | 显示文案 + **可重试按钮**（这两种后端已重试 3 次，再点才有意义）                    | 不要自己写指数退避重试 —— 后端已经做过了，你再包一层 = 请求数翻数倍                                                           |
| `400 invalid_param`                | **改参数**，把 `msg` 显示在表单下方                                  | 🔴 **绝对不要重试** —— 同样请求重试结果一样，只多烧配额                                                               |

**一个具体场景**：用户快速点两次"发送" → 第二次拿到 `429`。  
正确做法是**第一次点下去就本地置灰按钮**（后端 429 只是兜底，不该让用户看到）。

### 6.3 🔴 React 19 严格模式：SSE 必须能取消

`useEffect` 在 dev 阶段**跑两次**。如果 SSE 建在 effect 里而不清理 →  
**agent 跑两遍、工具调两遍、模型扣两次钱**。

```tsx
useEffect(() => {
  const ac = new AbortController()
  streamSSE(`/api/sessions/${sessionId}/chat`, { message: text, steer: false }, onEvent, ac.signal)
    .catch((e) => { if (e.name !== 'AbortError') setError(String(e)) })
  return () => ac.abort()      // ★ 这一行必须有，否则双倍扣费
}, [sessionId, text])
```


### 6.4 🔴 高德 JS API：安全密钥必须在加载之前注入

```tsx
// main.tsx —— 必须在**模块顶层**，写在组件里或 useEffect 里就晚了
window._AMapSecurityConfig = {
  securityJsCode: import.meta.env.VITE_AMAP_JS_SECURITY_CODE,
}

// 需要给 window 补类型（TS strict 下不补会报错）
declare global {
  interface Window { _AMapSecurityConfig: { securityJsCode: string } }
}
```

```tsx
// TripMap.tsx
import AMapLoader from '@amap/amap-jsapi-loader'

useEffect(() => {
  let map: AMap.Map | null = null
  let cancelled = false

  AMapLoader.load({
    key: import.meta.env.VITE_AMAP_JS_KEY,
    version: '2.0',
    plugins: ['AMap.Driving'],       // 按需，别全引
  }).then((AMap) => {
    if (cancelled) return            // ★ 严格模式第二次进来时第一次可能还没 resolve
    map = new AMap.Map('map-container', { zoom: 12, center: [center.lng, center.lat] })
    // …画点、连线
  })

  return () => { cancelled = true; map?.destroy() }   // ★ 必须 destroy，否则地图实例泄漏
}, [day, stops])
```

**三个坑**：

1. `_AMapSecurityConfig` 写在 `<script>` 加载**之后** → **不报错、静默失效**（最难查）
2. 安全密钥缺 → `INVALID_USER_SCODE`；Key 用错平台 → `INVALID_USER_KEY`
3. 切 Day 页签要 `destroy()` 再重建，否则旧线还在

### 6.5 坐标直接用，不要转换

高德返回 **GCJ-02**，前端 JS API 也是 GCJ-02，**天然一致，不做任何坐标转换**。  
契约里 `coord_sys` 字段值就是 `"GCJ-02"`。

### 6.6 轮播只在首页出现一次

写成 `<HeroCarousel>` 组件。**首页下面的「猜你喜欢」不做轮播**（静态 4 张卡）——  
一页两个轮播会互相打架，也让"猜你喜欢"没法一眼扫完。

### 6.7 上传头像（M9 落地，但约束现在就定死了）

| 项   | 约定                      | 你要写的                                                                                   |
| --- | ----------------------- | -------------------------------------------------------------------------------------- |
| 上限  | **10MB**                | `<input accept="image/jpeg,image/png,image/webp">` + 选完先本地判大小，超了**直接中文提示**（省一次往返）      |
| 类型  | `jpeg` / `png` / `webp` | 🔴 **`accept` 里不许出现 `image/svg+xml` 和 `image/gif`** —— SVG 能带脚本（XSS），GIF 重编码后动图就没了     |
| 压缩  | **前端不用做**               | 后端统一重编码为 512×512 WebP（10MB → 约 30~60KB）。前端压缩是重复劳动，且压得不如后端稳                             |
| 返回值 | **重编码后的新 URL**          | 🔴 **必须用返回值刷新头像** —— 不能继续用 `URL.createObjectURL(file)`（那是本地 blob，刷新页面就没了，而且它指向的是未压缩原图） |

**只做桌面端**（`DECISIONS.md` D30）：最佳 **1440px**，最小 **1024px**，**不做手机适配**。  
窄屏加一层提示（`<768px` 显示"请在电脑上打开"）—— **10 行成本，能挡住"面试官用手机点开 = 负分"**。  
🔴 **不要做响应式**：每页两套布局成本翻倍，收益只有面试那 10 秒。

---

## 七、SSE 消费：完整参考实现

`EventSource` **只能 GET**，而聊天必须 POST → 用它根本发不出请求。  
必须 `fetch` + `ReadableStream` 手写解析。**完整代码在 `docs/api.md` 3.7 第 ④ 小节**，用途：

```ts
import { streamSSE } from './sse'

streamSSE('/api/sessions/xxx/chat', { message: '成都，3天', steer: false }, (evt) => {
  switch (evt.type) {
    case 'session':     setSessionId(evt.session_id); break
    case 'node':        upsertTrajectory(evt); break          // 轨迹卡
    case 'tool_call':   upsertTrajectory(evt); break
    case 'tool_result': upsertTrajectory(evt); break
    case 'token':       appendText(evt.text); break           // 打字机
    case 'check':       setChecks(evt); break                 // 校验卡
    case 'trip':        setTrip(evt.trip); break              // 行程卡
    case 'done':        setDone(evt); break                   // ★ 只有收到它才算成功
    case 'error':       setError(evt.msg); break
    default:            /* ★ 未知 type 必须忽略，不要抛错 */ break
  }
}, ac.signal)
```

### 7.1 六个必须知道的行为（不然会以为后端坏了）

| 行为                       | 说明                                                                                                                                                    |
| ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| **每帧带 `id:`，现在就要存下来**    | 这是 SSE 标准字段，但**我们用 `fetch` 手写解析，浏览器不会替你管**（只有 `EventSource` 才自动管）。把最后一个 `id` 存进 state —— M6 要加"断线恢复"（带 `Last-Event-ID` 从第 N+1 帧续推）。**现在不存 = M6 前端返工** |
| **`token` 只在"出文本给你看"时发** | 工具循环里的模型调用**不流式**。所以 `agent_step` 期间**看不到 `token`** —— 那段时间界面靠 `node` / `tool_call` / `tool_result` 动起来                                               |
| **轨迹卡 ≠ 打字机**            | 前者是 `node`/`tool_call` 级事件，后者是 `token`。别把两者混成一个组件                                                                                                     |
| **`check` 可能来多次**        | 每轮校验一次（`round` 从 1 开始）。硬错打回重排后会有第 2 次                                                                                                                 |
| **只有 `done` 代表成功**       | EOF 而没收到 `done` = 断流，提示"连接中断，请重试"。**不要靠 HTTP 状态判断**（流内出错 HTTP 也是 200，错误走 `error` 事件）                                                                  |
| **心跳 `: ping` 要跳过**      | 每 15s 一行的注释行，解析时跳过以 `:` 开头的行                                                                                                                          |

> **M6 之后**再加：断流 → 带 `Last-Event-ID` 重连 → 后端从 checkpoint 恢复（**从第 N+1 帧继续，不从头跑**）。  
> 🔴 **恢复粒度是"节点"不是"token"**：断在一个模型节点中途时，那一段会**重新打字** —— 这是预期行为，不是 bug。

---

## 八、联调顺序（按这个来，别跳）

| 步   | 做什么           | 怎么算过                                                                                                                                                    |
| --- | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | 后端起服务         | `curl http://127.0.0.1:8000/api/health` 返回 `{"ok":true,...}`                                                                                            |
| 1.5 | **浏览器开冒烟页**   | `http://127.0.0.1:8000/verify/` 点「开始自检」→ 12 项全绿（Docker 没起时 SSE 启动 1 项标黄跳过，属正常）。它把已上线端点的 HTTP 行为（错误形状/404 语义/分页/删除/**SSE 流启动**）真实打了一遍 —— **这页全绿 = 后端可接** |
| 2   | 看 `mock_mode` / `amap_degraded` | `mock_mode:true` = 后端在跑 mock（真结构假数据）。**照常开发，不用管**。⚠️ 若**同时** `amap_degraded:true` = 配了 `real` 但缺 `AMAP_WEBSERVICE_KEY`，后端自己降级了（D66）—— 这时"数据只有成都"是预期的，不是 bug |
| 3   | 前端 proxy 通    | 前端里 `fetch('/api/health')` 能拿到结果                                                                                                                        |
| 4   | **SSE 逐条到达**  | 用 `curl -N` 直连对比。**这一步不过，不要往下写聊天页**                                                                                                                     |
| 5   | 拿真结构假数据开发界面二  | 直接把 `docs/sample_trip.json` 当 `Trip` 用（它就是真实响应）                                                                                                         |
| 6   | 接真数据          | 只改数据来源，**页面代码不该动** —— 如果要动，说明契约有问题，提出来                                                                                                                  |

### 后端当前状态（2026-09-16，M6 + M7 已上线）

- **已真实可用**：`/api/health`、`/api/sessions` CRUD（含 `GET /sessions/{id}` 返回 messages）、`/api/trips` 列表/详情、**`POST /sessions/{id}/chat`（SSE，9 种事件全覆盖）**、**`POST /trips/paste`（热启动 SSE，同款事件流但不发 `session` 帧、`done.session_id=null`）**、**`PATCH /trips/{id}`（确定性重算：删/挪/改时间一批提交，返回完整 Trip 整体替换）**、**`POST /trips/{id}/recheck`（深度软校验，返回校验卡不是 Trip）** —— 全部带统一 `ErrorBody` 错误形状与 D28 限流
- **CORS 已开**：允许 `http://localhost:5173` / `http://127.0.0.1:5173`（Vite 默认端口）。前端起在别的端口要提出来加
- **`/chat` 与 `/trips/paste` SSE 已上线（M6/M7）**：真实事件流可直接接（联调顺序第 4 步）。⚠️ chat 需要 MySQL（checkpoint 库）—— Docker 没起时 chat/paste 会 500，其他接口照常；事件里的 `trip` / `check` 是真实结构，与 `docs/sample_trip.json` 同源。**paste 事件流的节点名与 chat 完全同一套枚举**（parse_intent/tool_step/generate_plan/check_plan/soft_check/render），前端按同一套渲染逻辑处理即可
- 起服务命令：`cd backend && ../.venv/Scripts/python.exe -m uvicorn app.api.main:app --port 8000`（MySQL 在 `docker-compose up -d`）
- **🔴 鉴权已上线（2026-09-16，M9）**：**所有 `/api/*` 路由（除 `/api/health`、`/api/auth/register`、`/api/auth/login`）都要求请求头 `Authorization: Bearer <token>`，没带 = `401 unauthorized`**。流程：注册/登录拿 `AuthResponse{token, expires_in, user}` → token 存 `localStorage` → 每个请求带上 → `401` 时清 token 跳登录页。`GET/PATCH /api/auth/me` 拿/改昵称邮箱头像。**注册登录页从此是前置页**（六个页面之外要先做这两个，见 api.md 2.1）
- **会话级模型选择（2026-09-16 新增，A45）**：`POST /sessions` 的 body 可带可选 `model` 字段（如 `"deepseek-v4-pro"` / `"qwen3.7-plus"`），省略 = 后端默认。`Session` 响应里新增 `model: string | null`（types.ts 已更新）。**建会话传不支持的模型 → `400 invalid_param`**（要在建会话的 UI 上做下拉框兜住，选项：`deepseek-flash` / `deepseek-v4-pro` / `qwen3.7-plus` / `qwen3.7-flash`；模型名单以后可能加，前端别写死判断逻辑）。会话内模型固定，chat 请求**不需要**传模型

### 验收清单（占位阶段）

1. **6 个导航项都能点开**，互相跳转不报错
2. **风格统一**：同一套装饰色 + 直角 + 细边 + 粗下划线；**工具页（助手/总览）已收敛掉漂浮几何装饰**
3. **零假数据**：没有"2 万个景点／热度 10.0／门票价格"；缺数据的格子隐藏或显示引导文案
4. **两个核心页有明确入口**（首页大按钮 + 导航项 + 行程卡上的「生成路线总览」）
5. **只保证 1440px**，不做手机端
6. **三态色用的是 `--color-status-*` 独立变量**，没有复用装饰色板

### 验收清单（接真数据时）

1. **字段严格等于 `docs/types.ts`** —— `npx tsc --noEmit` 零错误
2. **空态 / 加载态 / 错误态**都处理了（不是只有"有数据"一种）
3. **严格模式下 SSE 不重复连接**（Network 面板里只应出现一条 `/chat` 请求）

---

## 九、你可以现在就开始（不用等后端）

后端 M1 会先跑通 mock 模式，**接口路径、请求体、响应结构完全不变**。  
所以你现在就能：

1. 按 `docs/types.ts` 建 `src/types/contract.ts`
2. 按 `docs/sample_trip.json` 造开发期假数据（界面二直接用它，不需要后端）
3. 按 `docs/api.md` 写 `lib/api.ts` / `lib/sse.ts`
4. 六个页面全部搭出来（占位阶段的标准见第八节）

需要新字段 / 发现契约说不通的地方 → **提出来**，由后端改 `schemas.py` +  
重新生成 `types.ts`，`api.md` 同步。**不要自己加字段。**

---

## 十、怎么把这份交接包交给豆包（叶鸽儿操作步骤）

> 这一节是给**你**看的，豆包不用读。交接包 = `前端交接.md` + `docs/api.md` + `docs/types.ts` + `docs/sample_trip.json`，四个文件一起给。

### 步骤

1. **开一个新对话**，把 `前端交接.md` 全文粘贴给豆包，附带一句：
   > 「读这份交接文档，按第一、二节的技术栈与目录结构，先把六个页面的占位阶段搭出来。约束与验收清单在第六、八节。有契约说不通的地方停下来问我，不要自己加字段。」
2. **第二枪再给契约**：豆包开始写 `lib/api.ts` 时，把 `docs/api.md` 全文给它（37 个端点的请求/响应/错误全在里面）。
3. **类型文件直接复制**：`docs/types.ts` 是机器生成的（来自后端 `schemas.py`），**整个文件复制进项目 `src/types/contract.ts`，不要让豆包手抄** —— 手抄两天后必漂移，且漂移不报错。
4. **假数据用真结构**：界面二开发期把 `docs/sample_trip.json` 当接口返回用；后端随时可起（mock 模式），`fetch` 通了就切真数据，页面代码不用改。
5. **后端起服务**（要联调时）：
   ```
   cd backend && ../.venv/Scripts/python.exe -m uvicorn app.api.main:app --port 8000
   ```
   浏览器开 `http://127.0.0.1:8000/verify/` 点「开始自检」——12 项全绿（Docker 没起时 SSE 启动 1 项标黄跳过，属正常）说明后端行为正常，前端连不上就是你前端的问题（先查 Vite proxy）。
6. **验收**：第八节两份清单（占位阶段 / 接真数据）逐条过，别只看「页面长得对」。

### 防扯皮的铁律（豆包最容易犯的三件事）

| 豆包可能干的事              | 你怎么拦                                                               |
| -------------------- | ------------------------------------------------------------------ |
| 自己加字段 / 改字段名「顺手优化」   | 「字段以 `docs/types.ts` 为准，一个都不许动，有意见提出来走后端流程」                        |
| 用 `EventSource` 写聊天  | 「第七节第一行：EventSource 只能 GET，聊天必须 fetch + ReadableStream，照 7 的参考实现抄」 |
| 拿装饰色当三态色（通过/失败/无法判定） | 「三态必须用 `--color-status-*` 变量，那是语义色不是装饰色」                           |

## 十一、2026-09-16 深夜更新：M9 后半 + M10 + M11 后端已全部上线

契约**没有破坏性变更**（只给 `FavoriteCreateRequest` 加了可选 `name`/`cover` 快照字段，重生成 `contract.ts` 后 tsc 零错误），以下端点现在**真实可用**，豆包直接接：

| 端点 | 说明 |
|---|---|
| `GET /api/home` | 首页聚合：`hero`（轮播，mock 池 4 张真照片）+ `recommended`。**公开访问，不用 token** |
| `GET /api/spots/search` | 景点搜索。🔴 `keywords` 为空别发请求（400 `keyword_required`）；空结果页**必须**显示引导文案，不许硬凑卡片 |
| `POST /api/uploads/avatar` | 头像。multipart 字段 `file`，仅 JPG/PNG/WebP，≤10MB；后端强制重编码 512×512 WebP，返回 `url`（相对路径，直接塞 `PATCH /api/auth/me` 的 `avatar`） |
| `GET/POST /api/favorites`、`DELETE /api/favorites/{type}/{id}` | 收藏。🔴 收 **guide** 别传 name（后端取标题）；收 **poi** **必须传 `name`**（从当前 SpotCard 拿，快照落库） |
| `GET/POST /api/guides`、`GET/PATCH/DELETE /api/guides/{id}`、`publish/unpublish` | 攻略。创建默认 private；列表默认只回 public；`mine=1` 要 token |
| `GET/POST /api/guides/{id}/comments`、`DELETE /api/comments/{id}` | 评论。`is_mine` 后端算好；删评论 = 本人或攻略作者 |
| `POST /api/likes/toggle`、`GET /api/likes` | 点赞二合一，返回最终状态，前端不用本地猜 |

其余不变：`GET /api/trips/{id}/amap-import` 也已实现（返回高德唤端链接）。
`.env` 默认模型切回 `deepseek-flash`（实测 qwen3.7-flash 软检查慢 14 倍，D60）——
**对话窗口模型下拉框不用动**：选项列表仍是 deepseek-flash / deepseek-v4-pro / qwen3.7-plus / qwen3.7-flash，后端按会话路由。

## 十二、前端审计结果（2026-09-16 深夜）：已接好的 vs 豆包待办

后端审计通过的部分（不用动）：`lib/api.ts` 错误处理规范（只按 code 分支 + 401 全局兜底）、
`Spots.tsx` 完全符合契约（空关键词不发请求 + 引导文案 + 空结果不硬凑）、
`Home.tsx` hero 空态兜底正确、`Assistant/Overview` 都有 AbortController 清理（严格模式防双连）、
全项目零 `EventSource`。**现在接口全部可用，下面是剩余接线清单**：

| 页面 | 待办 | 用的封装（`lib/api.ts` 现成） |
|---|---|---|
| `Profile.tsx` | 🔴 编辑昵称/邮箱后**没有保存**（`patchMe` 零调用）；🔴 头像上传没接（`uploadAvatar` 零调用）——先 `uploadAvatar(file)` 拿 `url`，再 `patchMe({ avatar: url })` | `uploadAvatar` / `patchMe` |
| `Guides.tsx` | 🔴 **占位页**，没调任何接口。列表用 `GET /api/guides`（默认 public；「我的」tab 加 `mine=1`），卡片显示 `author_name / like_count / comment_count / summary` | 需在 api.ts 新增封装 |
| `GuideDetail.tsx` | 🔴 占位页。详情 + 评论列表 + 发评论 + 点赞按钮（`POST /api/likes/toggle` 返回最终状态，直接用它渲染，不要本地猜） | 同上 |
| 个人中心 | 收藏 tab 用 `GET /api/favorites`；删除用 `DELETE /api/favorites/{type}/{id}` | 需新增封装 |
| 会话列表 | `deleteSession` 封装已有但页面没接删除入口 | `deleteSession` |

写封装时照 `api.md` 2.6 的行为注释来（private 404 语义、点赞二合一、poi 收藏必传 name）。
⚠️ 收藏 poi 时 `name` 从当前 SpotCard 取（快照字段），后端会 400 兜底。

---

## 十三、2026-09-17 复验：豆包第二轮（已做完 / 还剩什么）

**先说结论**：第十二章的 5 条待办**全部完成**（Profile 保存+头像上传、Guides 列表、GuideDetail 详情+评论+点赞、收藏 tab、会话删除入口）。
契约用法**全对**（`toggleLike` 用返回的 `liked/count` 不本地猜、`FavoriteItem` 不二次请求、poi 收藏传 `name` 快照、guide 传 `name: null` 等价于不传）。
**但交付物编译不过，且有一条你亲口提过的需求没做。**

### 13.1 🔴 P0 构建阻断（必须改，否则 build / 部署都过不去）

`frontend/src/pages/Profile.tsx` **第 4 行**导入了 `addFavorite`，但整个文件没用到它 → `tsc` 报 `TS6133`：

```
src/pages/Profile.tsx(4,10): error TS6133: 'addFavorite' is declared but its value is never read.
```

**修法**：把 `addFavorite` 从第 4 行的 import 列表里删掉（同一行的 `deleteFavorite` 要留）。

`npm run build`（= `tsc -b && vite build`）现在**挂在第一步**，`vite build` 根本没执行。
已跑过全量类型检查（`tsc --noEmit -p tsconfig.app.json`）：**全项目只报这一个错**，删掉即通。

### 13.2 🔴 P1 提过但没做：景点卡不带图

需求原话：「站点名字不显示，格式实现不对，**我想要站点会带有图片介绍**」。

现状：`SpotCard.photos: string[]` **是真的有数据**（高德直连照片，mock 池也实测补采了真图），
但前端只把它用在了「收藏快照」上，**没有一处拿它渲染图片**：

| 位置 | 现在长什么样 | 该改成 |
|---|---|---|
| `Spots.tsx` → `ResultCard` 左侧 | 色块 + `s.name.slice(0, 2)` 两个字 | `s.photos[0]` 真图，`object-cover`，取不到才回退色块 |
| `Home.tsx` 推荐景点卡片 | 同上（纯色块 + 站名） | 同上 |

参考实现**照抄 `components/HeroCarousel.tsx` 第 17~27 行**：有图渲染 `<img>` → `onError` 隐藏 → 无图回退。
🔴 **绝对不要用灰底占位图 / AI 生图**（A39：拿不到照片就少给一张，不硬凑）。

### 13.3 🟡 P2 清空昵称/邮箱保存 = 静默无效（**先别动，等后端拍板**）

`Profile.tsx` 的 `saveProfile()` 传的是 `nickname: nickname.trim() || null`。
而后端 `update_profile` 用的是 `nickname = COALESCE(%s, nickname)` ——
**`None` 的语义是「这个字段别动」，不是「清空」**（`api.md` 2.4 写明了）。

后果：用户把昵称删光点保存 → 接口 200、界面显示"已保存" → 但 `reloadMe(u)` 拿回的是**旧昵称**，
**输入框里刚清掉的值又弹回来，且没有任何报错**。

两条路（二选一，别两边都改）：
- **前端拦**：昵称必填，空则提示「昵称不能为空」不发请求 ← 改动最小
- **后端支持清空**：契约加语义（约定空字符串 `""` = 清空）→ 属于**契约变更**，须先改 `schemas.py` + 重跑 `gen_ts_types.py`，再通知豆包接

### 13.4 🟢 P3 小项（有空再说，不阻塞）

- `Assistant.tsx` 会话项从 `<button>` 改成了 `<div onClick>`（为了塞内部删除按钮）→ **键盘不可达**。
  要语义正确：外层加 `role="button"` + `tabIndex={0}` + `onKeyDown` 回车；或把删除按钮绝对定位浮在 button 外层。
- `Spots.tsx` 的 `hasToken = !!getToken()` 是**渲染时求值**的：用户在别处登录后回到已挂载的景点页，
  收藏按钮不会出现。改用全局登录态或 `useLocation()` 触发重算。

### 13.5 审计通过的部分（不用动，登记备查）

- `lib/api.ts` 新增的 20 个封装：类型全部 `import type … from '../types/contract'`，**零手写契约类型** ✅
- `types/contract.ts` 本轮**零改动** ✅（契约没漂）
- `index.css` 新增的 `.field / .tab-btn / .badge` 都走 CSS 变量，符合几何拼贴风 ✅
- `GuideDetail.tsx` / `Guides.tsx` 的 `cover` 图渲染正确（`onError` 隐藏 + 不回退占位图）✅

---

## 十四、2026-09-17 M12 新功能：行程 → 攻略（豆包接线）

后端**已上线并真库验收通过**（`POST /trips/{id}/publish-as-guide` + `GET /guides?source_trip_id=`）。
契约细节见 `docs/api.md` **3.8**，视觉规范见 `界面设计.md` **2.7**。要做四件事。

### 14.1 路线总览页加第三个按钮「发布为攻略」（`Overview.tsx`）

现有操作行（`Overview.tsx` 第 318~345 行）已经排了两个按钮：`重新校验（软）` / `导出到高德`。
在**同一个 `ml-auto` 容器里**追加第三个，样式沿用 `btn-outline !text-[12px]`。

**点击后的流程（照这个顺序，别自己简化）：**

```
用户点击
  → 立即 disabled（挡双击）+ 文案变「发布中…」
  → 【判据】GET /api/guides?source_trip_id=<trip_id>
       ├─ total === 0  → 直接进第 3 步
       └─ total > 0    → 打开确认框（列出最近一篇的 title + published_at）
                           ├─ 用户取消 → 解锁按钮，结束
                           └─ 用户确认 → 进第 3 步
  → 【发布】const key = crypto.randomUUID()        ← 生成时机见 14.3，别改
      POST /api/trips/<trip_id>/publish-as-guide
      headers: { 'Idempotency-Key': key }
      body: {}                                      ← 不传 title 就用行程标题
  → 成功：解锁 + 提示「已发布」+ 可给「去看看」链接（/guides/<guide_id>）
  → 失败：解锁 + 显示错误（沿用页面现有的 errorBar）
```

⚠️ **判据必须每次点击现查**，不能用页面加载时缓存的本地状态 —— 本地状态一定会过期
（别的标签页发过、自己发过又忘了），过期时确认框会**谎报"没发过"**，这个兜底就白做了。

### 14.2 新建 `components/ConfirmDialog.tsx`

现在**全站没有任何弹窗组件**（我查过：`components/` 下没有 Modal/Dialog，也没用过 `window.confirm`），
所以这是第一个，需要新建。规格照 `界面设计.md` **2.7** 那张表逐条做，重点三条：

1. **直角 `rounded-[4px]` + 白底 + `1px #E5E5E5` 细边**，遮罩 `rgba(0,0,0,.35)`、**不加 blur**
2. 确认按钮 = `btn-black`（**黑实心 + 微旋转 -1°**），取消 = `btn-outline`；**取消在左、确认在右**
3. `Esc` 取消、打开时焦点落在确认按钮、关闭后焦点还给触发它的按钮；
   **请求进行中两个按钮都禁用**（半途关闭会留下"不知道成没成"的孤儿状态）

**props 建议**：

```ts
type Props = {
  open: boolean
  title: string
  children: React.ReactNode     // 正文，数据用黑色 500 标出
  confirmText?: string          // 默认 '确认'
  cancelText?: string           // 默认 '取消'
  busy?: boolean                // true → 两个按钮都 disabled + 确认文案变进行态
  onConfirm: () => void
  onCancel: () => void
}
```

**文案要有信息量**（规范里写死了：不能只问"是否继续"，要说清**已经有什么**）：

> 这条行程你已经发布过 **2** 篇攻略，最近一篇是《成都两日 · 古迹与熊猫》（3 天前）。要再发一篇吗？

### 14.3 🔴 幂等键的生成时机（**契约，写错会静默失效**）

「同一次请求被送达两次」必须只生效一次。后端只认一条规则：**同 key 返回同一篇结果**。
它**没有能力**判断"这是网络重试还是用户又点了一次" —— 所以这个判断只能由**你**来做，
靠 key 的生成时机：

```ts
// ✅ 正确：key 在 click handler 内生成一次，本次意图的所有重试共用它
async function handlePublish() {
  if (publishing) return
  setPublishing(true)
  const key = crypto.randomUUID()
  try {
    await publishAsGuide(tripId, key)
  } finally {
    setPublishing(false)   // 成功后解锁；用户再点 = 新 key = 允许发第二篇
  }
}

// ❌ 错误一：写在组件渲染体里 → 每次重渲染 key 都变，"重试"被当成"新意图"
const key = crypto.randomUUID()

// ❌ 错误二：每次重试重新生成 → 幂等完全失效，等于没做
await publishAsGuide(tripId, crypto.randomUUID())
```

**顺带纠正一个常见误解：双击不归幂等键管。** 双击会触发两次 click → 两个不同的 key →
后端会老老实实发两篇。双击只能靠 **UI 层 `disabled`** 挡。两者职责不同：

| 重复来源 | 谁负责挡 |
|---|---|
| 网络重发 / 网关重发 / 客户端超时自动重试 | **幂等键**（同 key） |
| 用户看到失败后点"重试" | **幂等键**（同 key） |
| **双击** | **UI 的 `disabled` / loading 态** |
| 用户有意再发一篇 | 谁都不挡 —— 本来就该放行 |

### 14.4 攻略 → 路线（「互转」的另一半，后端零改动）

`GuideDetail.tsx` 加一个按钮「**用它做路线**」：把 `content_md` 作为 `text` 喂给
`POST /api/trips/paste`（SSE，**后端已经支持，一行都不用改**），成功后跳到 `/overview`。

⚠️ `paste` 的文本上限是 **20000 字**：超长的攻略要在前端先提示，别让请求白跑一趟。

### 14.5 「我的攻略」的编辑与撤回（后端也已有）

`PATCH /guides/{id}`、`POST /guides/{id}/publish`、`POST /guides/{id}/unpublish`、`DELETE /guides/{id}`
**全部已上线**。用户想改已发布的那篇 → 走「我的攻略」里的编辑入口即可
（这也正是"允许同一行程发多篇"仍然可管理的原因）。

### 14.6 验收清单（改完自己先过一遍）

- [ ] 发布成功后攻略出现在 `/guides` 公开列表里（**匿名可见**）
- [ ] 同一次点击被重试（Network 里手动重放一次）→ 库里**只多一篇**
- [ ] 连点两次成功发布 → 确实出了**两篇**（这是允许的，不是 bug）
- [ ] 已发过一次后再点发布 → 弹出确认框，文案含**已发布条数 + 最近一篇标题**
- [ ] 确认框 `Esc` 可关，且**不会**触发发布
- [ ] 发布请求进行中，按钮和确认框都点不动
- [ ] `npm run build` 通过（**含第 13.1 节那个 `addFavorite` 未使用导入的修复**）


---

## 十五、2026-09-17 导航栏补齐 + 注册引导（豆包接线）

### 15.1 现状：导航栏右侧是空的

`layouts/Layout.tsx`（72 行）现在只有：logo + 6 个导航项（`nav` 上有 `ml-auto` 推到右侧），
**右边一片空白**。

> ⚠️ 参考图（左上角 `IRoute`）**不是本项目的截图** —— 本项目 logo 是黑底 `TR`，
> 导航项也已经有 6 个（顺序以下面为准）。参考图只用来定"要放什么"，
> **不要照抄它的配色/字体/圆角**。

### 15.2 目标结构（位置已定，按这个来）

🔴 **搜索框插在导航项中间** —— 在「旅游攻略」和「个人中心」之间，
**不是**放在右侧工具区（这是 2026-09-17 用户的明确要求）。

`NAV_ITEMS` 现有顺序（`Layout.tsx:3-10`）：
`首页 / 旅游景点 / AI路线规划助手 / 路线总览 / 旅游攻略 / 个人中心`

| 位置 | 元素 | 未登录 | 已登录 |
|---|---|---|---|
| ① | 导航项 1~5（首页 … 旅游攻略） | ✅ | ✅ |
| ② | **搜索框**（窄条输入 + 图标按钮） | ✅ 显示 | ✅ 显示 |
| ③ | 导航项 6（个人中心） | ✅ | ✅ |
| ④ | **头像 + 昵称**（右侧） | ❌ 不显示 | ✅ 来自 `GET /auth/me` |
| ⑤ | **注册 / 登录** 按钮（右侧） | ✅ 显示 | ❌ 不显示 |

🔴 **④ 和 ⑤ 是二选一，不是并列** —— 已登录还挂着"注册/登录"等于在说用户没登录。

```
未登录：  TR  首页 旅游景点 AI路线规划助手 路线总览 旅游攻略 [搜索景点…🔍] 个人中心 ···  [注册/登录]
已登录：  TR  首页 旅游景点 AI路线规划助手 路线总览 旅游攻略 [搜索景点…🔍] 个人中心 ···  [头像+昵称 ▾]
```

**实现提示**：`NAV_ITEMS` 现在是一次性 `.map()` 出来的，想让搜索框插在第 5 项之后，
**别把搜索框塞进 `NAV_ITEMS`**（数组里装的是 `<NavLink>`，搜索框是表单）。
用 `NAV_ITEMS.slice(0,5)` + 搜索框 + `NAV_ITEMS.slice(5)` 两段 map，比加 `kind` 标记位更好读。

⚠️ ④ 的下拉菜单项**不要用 `<a href>`** —— 会整页刷新、丢掉 SPA 状态。用 `NavLink` / `navigate`。

### 15.3 搜索框：按下回车之后发生什么

**搜索框一直可点，不要置灰** —— 置灰等于把功能藏起来，游客不知道这里有什么。

| 状态 | 行为 |
|---|---|
| 关键词为空 | **什么都不做**（不跳页、不弹窗 —— 空搜索是误触） |
| 未登录 | **不跳页**，弹登录弹窗（标题 `请先登录后搜索`）→ 登录成功后自动带着关键词继续搜（复用 15.9 的重放机制，用户感觉是"刚才那一下自己生效了"） |
| 已登录 | `navigate('/spots?kw=' + encodeURIComponent(kw))` |

🔴 **`/spots` 页现在不读 URL 参数** —— `Spots.tsx` 的关键词只用 `useState` 管，
**没有 `useSearchParams`**。照现在的代码，导航栏跳过去**关键词会丢**，用户看到的是一个空输入框。

`Spots.tsx` 要改两处：

1. 加 `useSearchParams()`，**初始化时**把 `params.get('kw')` 灌进 `keyword` state，
   并自动触发一次搜索（放 `useEffect` 里，注意别和用户手动搜索打架）
2. **页面内那个输入框要保持同步** —— 导航栏搜的词必须出现在页面输入框里，
   否则用户看到空框会以为没搜成功（**这里是"两个搜索框"最容易做错的地方**）

两个搜索框的关系说清楚：

| | 导航栏那个 | `/spots` 页内那个 |
|---|---|---|
| 定位 | 全局快捷入口（任何页面都能用） | 结果页自己的筛选器（还能填城市） |
| 行为 | 把词带过去 → 跳 `/spots` | 就地重搜 |

它们**不是两套独立状态** —— 页面内输入框要能显示导航栏带过来的词。

⚠️ 这不是"前端拦一下"，后端 `GET /spots/search` **真的需要登录**（`api.md` 1.1.2）。

### 15.4 注册 / 登录页 —— 🔴 **用户已定：不动**

**`pages/Login.tsx` 已经存在**（171 行，登录 + 注册二合一），功能完整。

> 🔴 **2026-09-17 用户原话：「登录注册那些既然已经实现那就不改了」**
> → **本节剩下的内容只当参考，不要拿它去改 `Login.tsx` 的视觉。**
> 除非发现**真 bug**（登录失败、token 存不进去、注册字段对不上），否则不动这个文件。

参考图对照（仅供了解现状，**不构成改动项**）：

| 参考图要素 | 现状 |
|---|---|
| `TR` 方块 logo | ✅ 项目黑底 `TR`（不是参考图的绿方块） |
| "欢迎回来 · Welcome Back" | ✅ |
| "账号登录" 大标题 | ✅ |
| 用户名 / 密码 两个输入框 | ✅ |
| 主按钮 | ✅ |
| "还没有账号？…" | ✅ 文案是"去注册"（**保持现状**） |

⚠️ **唯一要记住的一条**（如果哪天动了这个文件才用得上）：参考图是**大圆角**（卡片大圆角 + 胶囊按钮），
本项目是**几何拼贴风、直角 ≤ 4px**（`界面设计.md` 第二节）。
**照抄圆角会让登录页看起来像另一个产品** —— 结构可以借鉴，圆角一律 4px。

### 15.5 权限弹窗（本节是重点）

后端**早就有真鉴权**（`api.md` 1.1.2 那张表），所以前端**不需要自己判断该不该拦** ——
**照常发请求，401 会自己回来**。要做的只有两件事：

1. **`lib/auth.ts` 的 `handleUnauthorized()` 改成开弹窗**
   （⚠️ 它**不在** `api.ts` —— 在 `lib/auth.ts:29-35`，现在是 `window.location.assign('/login?redirect=...')`，**整页刷新**）
   细则见 `api.md` 1.1.3：不跳页 / 登录后重放原请求 / 只重放一次 / 只重放 `GET`
2. **弹窗复用 `Login.tsx` 的表单**：抽成 `components/AuthForm.tsx`，
   整页和弹窗渲染**同一个组件** —— 别抄两份表单，抄完"改一处忘一处"是必然的

**游客撞 401 的地方**（都是预期行为，不是 bug）—— 🔴 **分两类，处理方式不同**：

| 动作 | 撞哪个端点 | 401 后怎么办 |
|---|---|---|
| 用搜索框（导航栏 / `/spots` 页内） | `GET /spots/search` | 🟡 **弹窗** → 登录后自动搜出结果 |
| 点某个景点看详情 | `GET /spots/{poi_id}` | 🟡 **弹窗** → 登录后自动打开详情 |
| 点赞 / 收藏 / 发评论 / 发攻略 | 对应写接口 | 🔴 **跳登录页**（见 15.10） |
| 发聊天消息 | `POST /sessions/{id}/chat` | 🔴 **跳登录页**（见 15.10） |
| 进「AI路线规划助手 / 路线总览 / 个人中心」 | 页面级 `RequireAuth` | 🔴 **跳登录页**（见 15.10） |

**分界线只有一句话：登录成功后能自动接上的 → 弹窗；接不上的 → 跳页。**

弹窗的价值全在"自动重放" —— 搜索是 `GET`，登录完能自动再发一次，用户看到结果自己出来。
而收藏是写操作（**重放有重复提交风险，所以不重放**），弹窗盖上去顶多告诉他"去登录"，
登录完还得自己再点一次 —— **这个弹窗就是纯打断，属于多余**（2026-09-17 用户明确要求去掉）。

🟢 **游客能正常逛**：首页、景点搜索页、攻略列表、攻略详情、评论列表。

### 15.6 验收清单

**结构**

- [ ] 搜索框在**「旅游攻略」和「个人中心」中间**（不是在最右边）
- [ ] 未登录：最右侧是 `[注册/登录]`，**没有**头像和昵称
- [ ] 登录后：最右侧变成 `[头像+昵称 ▾]`，"注册/登录"消失
- [ ] 退出登录 → 变回"注册/登录"，**页面不刷新**
- [ ] 全程没有整页刷新（`F5` 那种闪白一次都没有）

**搜索**

- [ ] 导航栏搜索框输入 → 回车 → 跳到 `/spots?kw=xxx`
- [ ] 到了 `/spots` 页，**页面内输入框里是刚才那个词**（不是空的）
- [ ] 页面内输入框**直接就是搜过的结果**，不用再点一次搜索
- [ ] 搜索框留空回车 → **什么都不发生**（不跳页、不弹窗）
- [ ] 未登录搜索 → 弹窗 → 登录成功 → **刚才那个词自己搜出结果了**

**权限引导**

- [ ] 未登录点某个景点卡 → 弹窗出来，**页面还在下面**（不是跳走）
- [ ] 弹窗里登录成功 → **刚才那个景点详情自己出来了**，不用再点一次
- [ ] 未登录直接点导航栏「旅游景点」→ **正常进页面**（看到搜索表单，不弹窗）
- [ ] `Esc` 能关弹窗，关完**页面滚动位置没变**
- [ ] 一秒内连点三个需要登录的按钮 → **只弹一个弹窗**（不是三个）

**构建**

- [ ] `npm run build` 通过

### 15.8 改动坐标（2026-09-17 实读代码，照着找就行）

⚠️ 先纠正 15.5 里一处笔误：`handleUnauthorized` **不在 `api.ts`**，它在 `lib/auth.ts`。

| 要改的 | 位置 | 现状 → 目标 |
|---|---|---|
| `handleUnauthorized` 定义 | `lib/auth.ts:29-35` | `window.location.assign('/login?redirect=...')`（**整页刷新**）→ **开弹窗，不碰 location** |
| 401 兜底调用点 | `api.ts:78`（普通请求）、`api.ts:172`（上传头像） | **不用改**，仍调 `handleUnauthorized()` |
| 登录表单 | `pages/Login.tsx`（171 行，表单**内嵌**） | 抽成 `components/AuthForm.tsx`（现在还没有这个组件） |
| 登录成功动作 | `pages/Login.tsx:47-48` | `setToken` + `nav(redirect, {replace:true})` **写死的** → 交给 `onSuccess` 回调 |
| 导航栏（搜索框位） | `layouts/Layout.tsx:30-46`（`<nav>` 一次性 `.map()`） | 拆成 `NAV_ITEMS.slice(0,5)` + **搜索框** + `slice(5)`（插在旅游攻略↔个人中心之间） |
| 导航栏（右侧位） | `layouts/Layout.tsx:46`（`</nav>` 之后） | 补 `[头像昵称 ▾ / 注册登录]`（两态二选一） |
| **`/spots` 读 URL 参数** | `pages/Spots.tsx`（`useState` 管关键词，**无 `useSearchParams`**） | 加 `useSearchParams`：初始化灌 `kw` → 自动搜一次 → 输入框显示该词 |
| ~~注册链接文案~~ | ~~`pages/Login.tsx:158`~~ | ❌ **不改**（15.4：登录注册页用户定不动） |

### 15.9 401 重放的参考实现（照抄，别自己设计）

🔴 **这块最容易做错**，做错的后果是"登录完原操作没继续"或"同时弹了好几个弹窗"。

**问题**：`request()` 收到 401 时，调用方的 `await` 已经进 `catch` 了 —— 登录成功后**没人接着干原来那件事**。

**做法**：让 `request()` **透明地挂起等待**，登录完重放一次再返回。**调用方完全不知道中间登录过。**

**① `lib/auth.ts` 追加：**

```ts
type UnauthHandler = () => void
let onUnauth: UnauthHandler | null = null
let loginPromise: Promise<void> | null = null
let resolveLogin: (() => void) | null = null
let rejectLogin: ((e: unknown) => void) | null = null

/** App 挂载时注册（卸载传 null）。弹窗由它开 */
export function setUnauthorizedHandler(fn: UnauthHandler | null): void {
  onUnauth = fn
}

export function handleUnauthorized(): void {
  clearToken()
  onUnauth?.()            // ← 只做这一件事：开弹窗。**不跳转、不碰 location、不清滚动**
}

/** 等这次登录有结果。
 *  🔴 并发 401 必须共享同一个 Promise —— 否则 N 个请求 = N 个弹窗，
 *     而且只有最后一个的重放能成功（前面的 resolve 被覆盖掉了）。*/
export function waitForLogin(): Promise<void> {
  if (!loginPromise) {
    loginPromise = new Promise<void>((res, rej) => { resolveLogin = res; rejectLogin = rej })
  }
  return loginPromise
}

export function notifyLoginSuccess(): void {
  resolveLogin?.()
  loginPromise = resolveLogin = rejectLogin = null
}

export function notifyLoginCancelled(): void {
  rejectLogin?.(new Error('login_cancelled'))
  loginPromise = resolveLogin = rejectLogin = null
}
```

**② `api.ts`：原 `request` 改名 `_raw`（一行逻辑都别动），外面包一层：**

```ts
async function request<T>(path: string, init: RequestInit = {}): Promise<T> {
  try {
    return await _raw<T>(path, init)
  } catch (e) {
    const is401 = e instanceof ApiError && e.code === 'unauthorized'
    const isGet = !init.method || init.method.toUpperCase() === 'GET'
    if (!is401 || !isGet) throw e          // 🔴 只重放 GET（api.md 1.1.3 第 4 条）
    await waitForLogin()                   // 挂起等弹窗登录；取消 → reject
    return await _raw<T>(path, init)       // 重放**一次**，再失败就抛（不循环）
  }
}
```

**③ 弹窗宿主（`App.tsx` 或 `Layout.tsx`）：**

```tsx
const [authOpen, setAuthOpen] = useState(false)

useEffect(() => {
  setUnauthorizedHandler(() => setAuthOpen(true))
  return () => setUnauthorizedHandler(null)   // 🔴 React 19 严格模式必须清理（同 6.3）
}, [])
```

**④ `AuthForm` 的接口（整页和弹窗都渲染它）：**

```tsx
<AuthForm onSuccess={() => { /* 整页：nav(redirect)；弹窗：notifyLoginSuccess() */ }} />
```

- `setToken(auth.token)` **放在 `AuthForm` 内部**（两边都要存，别让调用方记着）
- `onSuccess` 只管"接下来干什么" —— 整页是跳转，弹窗是通知重放
- 弹窗**关闭时**必须调 `notifyLoginCancelled()`，否则挂在 `waitForLogin()` 上的请求**永远不返回**

⚠️ **写操作 401 后不会自动重放**（故意的，防重复提交）。所以用户点"收藏"→ 弹登录 → 登录完**收藏不会自动生效**，
要在弹窗关闭时给一句提示：`已登录，请再点一次`。**别为了顺滑就去重放 POST。**

### 15.7 ✅ 已定案：`/spots` 那一对**不拆**（B 方案，2026-09-17）

**用户拍板：后端不动** —— `GET /spots/search` 和 `GET /spots/{id}` **保持要登录**。

⚠️ 但这条的后果**比我上一版写的轻**。上一版我写"游客点「旅游景点」拿到空页面" ——
实读 `Spots.tsx:87-95` 后**这句是错的**：关键词为空时**直接 return、不发请求**
（后端会返 400 `keyword_required`），所以游客看到的是**标题 + 搜索表单 + 引导文案**，
**不是空白页**。

| 游客动作 | 结果 |
|---|---|
| 点导航栏「旅游景点」 | ✅ **正常进入**，看到搜索表单（**不发请求 → 根本不撞 401**） |
| 在页面搜索框提交 / 用导航栏搜索框 | 401 → 弹登录（15.5 统一弹窗）→ 登录后自动重放该 GET |
| 点某个景点卡看详情 | 401 → 同上 |

→ 前端**不需要为此做任何特殊处理**，401 按 15.5 的两分法走（GET 弹窗 / 其余跳页）。
→ 后端**一行都不用改**（限流键继续用 `user_id`，不用碰 IP 限流）。

> ⚠️ **一句被作废的需求表述（2026-09-17）**：早先记过"**导航栏各入口都允许进入**"，
> **用户已明确否掉这句话**。正确口径是：
> 只有**首页 / 景点搜索页 / 攻略列表 / 攻略详情 / 评论列表**对游客开放；
> 「AI路线规划助手 / 路线总览 / 个人中心」是**"我的数据"页，没登录直接跳登录页**（见 15.10）。
> **别再拿那句作废的话当需求依据。**

---

### 15.10 ✅ 写操作 401：改跳登录页（去掉弹窗）—— 2026-09-17 用户定案：**做**

> ⏳ **2026-09-18 校验：仍未做**（`redirectToLogin` 全项目 0 处）→ **改法见第十七节 17.2**

**2026-09-17 用户要求：「修改多余弹窗（收藏）」** —— 写操作上的弹窗是多余的，改掉。

**为什么多余**：弹窗机制的价值**全在"登录后自动重放"**（15.9）。但**写操作不能重放**
（`POST`/`DELETE` 重放有重复提交风险，15.9 已定），所以弹窗盖上去顶多告诉用户"去登录"，
登录完**他还得自己再点一次** —— 纯打断，没有任何收益。

**改成**：写操作 401 → **跳登录页**（带 `redirect` 回当前页）→ 登录后回到原页，用户自己再点。

要改的地方：

| 位置 | 现状 | 目标 |
|---|---|---|
| `lib/api.ts:79`（`_raw()` 的 401 兜底） | 一律 `handleUnauthorized()` 开弹窗 | **只有 GET** 开弹窗；非 GET **跳登录页** |
| `lib/api.ts:191` | 同上（上传头像等） | 同上 |
| `lib/sse.ts:54` | `handleUnauthorized()` 开弹窗 | **跳登录页**（聊天的 401 按 POST 流算，不能重放） |
| `lib/api.ts:91-101`（`request()` 重放） | 只重放 GET | **不用改**（非 GET 本来就不会被挂起） |
| `App.tsx:15-21`（`RequireAuth`） | `Navigate to="/login"`（跳页） | ✅ **保持不动** —— 它本来就是跳页，符合本次定案 |

`lib/auth.ts` 加一个跳页函数：

```ts
/** 写操作 / 页面级 401：跳登录页 + 带回跳地址（**不弹窗**） */
export function redirectToLogin(): void {
  const from = window.location.pathname + window.location.search
  window.location.assign(`/login?redirect=${encodeURIComponent(from)}`)
}
```

⚠️ 这里用 `window.location.assign` **可以接受**（和 15.9 弹窗的要求相反）—— 用户本来就要
离开当前页去登录，整页刷新不影响体验。**但能接进路由就更好**（`navigate()` 保住 SPA）。
两种都算对，别为这个纠结。

**顺带清掉一处重复反馈**：`Spots.tsx` 的 `ResultCard.onFavorite` 现在**既弹窗**（走
`handleUnauthorized`）**又在卡片上 `setFavMsg(err.message)` 显示红字**。跳页之后卡片会卸载，
红字没机会显示 —— 逻辑上留一份即可（`redirect` 回来后红字可作为兜底提示）。

**改完的完整口径（记这一张表就够）**：

| 触发 | 401 后 |
|---|---|
| `GET`（搜索、景点详情） | 🟡 **弹窗** → 登录成功 → **自动重放**，结果自己出来 |
| 写操作（收藏 / 点赞 / 评论 / 发攻略） | 🔴 **跳登录页** → 回来自己再点一次 |
| `POST` 聊天流（SSE） | 🔴 **跳登录页** |
| 「AI路线规划助手 / 路线总览 / 个人中心」 | 🔴 **跳登录页**（`RequireAuth`，现状不动） |

### 15.11 ✅ 13.2 站点卡配图：**已核实完成，不用做**

**需求原话**：「站点名字不显示，格式实现不对，**我想要站点会带有图片介绍**」

**2026-09-17 实读 + 浏览器实测核对：已完成** ✅

| 位置 | 现状 |
|---|---|
| `Home.tsx:28-42` | `photos[0]` 真图 + `onError` 隐藏 + 色块兜底 ✓（1440px 截图确认首页「猜你喜欢」显示真照片） |
| `Spots.tsx:28-42` | 同上 ✓ |

> ⚠️ **这节留着是为了防止再误判一次**：`Spots.tsx:8` 的
> `['pop-yellow','pop-cyan','pop-blue','pop-green'][s.poi_id.length % 4]`
> **是"取不到图时的兜底底色"，不是"拿色块当图"**。
> **只看这一行会得出"还没做"的错误结论**（我就这么错过一次）。

### 15.12 ✅ 1024px 导航栏溢出：**已修复（2026-09-17 实测验收）**

**项目定位是最小支持 1024px**（`界面设计.md` D30）。2026-09-17 用 headless Chrome 在
**1024×768** 实测（**修复前**）：

| 宽度 | 结果 |
|---|---|
| 1440px | ✅ 正常 —— 6 个导航项 + 搜索框 + 头像/登录注册全在 |
| **1024px** | 🔴 **溢出** —— 「个人中心」**完全不可见**、右侧「登录 / 注册」**完全不可见**、搜索框只露半个 |

根因：header 里"不让缩"的东西太多。

| 元素 | 占宽 |
|---|---|
| `header` 左右 padding（`px-14`） | 112px |
| logo 区（`shrink-0`，含"旅游规划平台"6 个字） | ~180px |
| 右侧工具区（`w-[132px] shrink-0`） | 132px |
| **剩给 nav** | **~600px** |
| **nav 实际需要**（6 项 + 搜索框 + 图标按钮） | **~730px** |

**实际采用的修法**（豆包实现，3 文件 5 行）：

| 位置 | 改法 |
|---|---|
| `index.css` `.page` | 加 `max-width: 100%` —— 根治整页横向溢出（hero 也被它一起救了） |
| `layouts/Layout.tsx` logo 文字 | `hidden min-[1280px]:block` —— < 1280px 只留 `TR` 方块 |
| `layouts/Layout.tsx` 搜索框 | `w-[100px] xl:w-[132px]` |
| `pages/Home.tsx` hero grid | `620px 1fr` → `minmax(480px,620px) minmax(0,1fr)` |

✅ **没做**：导航项减字（`AI路线规划助手` → `AI 规划`）—— 那是改产品文案，本来就要先问用户；
实测证明**不做也够了**。

**复测结果**（2026-09-17，同条件重跑 headless Chrome）：

| 宽度 | 结果 |
|---|---|
| **1024px** | ✅ **六个导航项 + 搜索框 + 个人中心 + 登录/注册 全部可见**（修复前这后两个完全不可见） |
| 1440px | ✅ logo 文字"旅游规划平台"正常显示，布局未回退 |

`tsc --noEmit` exit 0；`npm run build` 通过（4.06s）。

---

### 15.13 🔴 SSE 断线重连（后端已完整，前端待接）

> ✅ **2026-09-18 豆包已接主体**（`SseInterruptedError` + 退避 + 429 处理 + `Last-Event-ID`）。
> ⚠️ **还剩一处缺口：`sse.ts` 没捕获网络异常 → 「杀连接」根本不重连** → **改法见第十七节 17.3**。
> 📌 另有一条**已知问题不改**（重连后文字重复）→ 见第十七节 17.4。

**2026-09-17 用户定案：做。后端 100% 就绪，只差前端。** 本节给参考实现，照抄即可。

**后端已经具备的（已实读代码核实，前端不用管、也不用改后端）**：

| 能力 | 位置 |
|---|---|
| **每一帧都带 `id: <seq>`** | `api/sse.py:31` `return f"id: {seq}\ndata: {body}\n\n"` |
| 带 `Last-Event-ID` 请求头 = 走恢复模式 | `api/routes/chat.py:207` `resume = "last-event-id" in request.headers` |
| 恢复时**不重发用户消息、不重复落库** | `if not resume: session_repo.append_message(...)` |
| 恢复从 checkpoint 续推 | `build_chat_input(..., resume=True, has_checkpoint=True)`（D27） |

**后端已实测验证过的（2026-09-17，`Last-Event-ID` 头直探）**：

```
POST /sessions/{id}/chat   headers: Last-Event-ID: 3
→ HTTP 200
→ 事件序列：session → token → trip → done      （0.4s，tool_call 0 次）
→ 用户消息只落了一条（库里该会话只有 1 条 user 记录）  ✅
```

恢复模式确实生效（第一条就是 `session`）、**不重跑节点、不重复落用户消息**。

⚠️ **探测中发现一个边界（低危，先记录不修）**：对**已经正常 `done` 过的轮**再发一次恢复请求，
会**再落一条 assistant 消息**（库里出现两条内容相同的记录）。正常断线场景不会触发
（服务端断线时那步没执行到，重连后补落，总共只有一条）；但存在一个小窗口 ——
**服务端已落库、客户端没收到 `done`** → 重连会重复落一条。
只影响会话历史多一条，**不影响行程与校验**。

**前端要做的 4 件事**（都在 `lib/sse.ts` + 聊天页）：

1. **解析时把最后一条 `id:` 存进 ref** —— 现在没存（交接文档 449 行早就提醒过"现在不存 = M6 返工"）
2. **判定"断流"** = **收到 EOF 但整轮没收到 `done` 事件**。`done` 是唯一的成功信号
   （`api.md` 明文：EOF 而没 `done` = 断流，两者必须能区分）
3. **断流 → 退避重连**（1s → 2s → 4s，最多 **3** 次），复用同一个
   `POST /sessions/{id}/chat`：

```ts
await fetch(`/api/sessions/${sessionId}/chat`, {
  method: 'POST',
  headers: {
    ...authHeaders(),
    'Content-Type': 'application/json',
    Accept: 'text/event-stream',
    'Last-Event-ID': String(lastEventIdRef.current),   // ← 关键，就是它触发恢复
  },
  body: JSON.stringify({ message: lastUserMessage, steer: false }),  // ← 见警告 1
  signal: controller.signal,
})
```

🔴 **警告 1：重连也必须带非空 `message`** —— `ChatRequest.message` 是 `min_length=1`
（`backend/app/schemas.py:635`），传空串会直接 **422**。用**最后一次的用户消息**填即可：
后端是 `if not resume` 才落库，所以**不会重复落一条用户消息**。

🔴 **警告 2：重连可能撞 429** —— 服务端要等它自己发现连接断了，才释放"同会话并发流"的位子
（`registry.finish` 在 `_sse_body` 的 `finally`）。这个窗口里重连会收到
`429 rate_limited` + `detail.retry_after`（约 5 秒）→ **按 `retry_after` 等，不要立刻再打。**

🔴 **警告 3：恢复粒度是「节点」，不是 token**（D27 明文）—— 那一整段会**重新打字**。
前端要能接受"回退到上一个节点边界再继续"，**别在 UI 上把已经吐出的字当成真进度**。

🔴 **警告 4：重连本身也吃限流** —— `chat` 是 10/小时 · 30/天。无限重连会把额度烧光 →
**上限 3 次**，超了就给明确提示（"连接中断，请重新发送"），**不要静默转圈**。

**验收清单（4 条）**：

- [ ] 正常一轮：仍然**只在收到 `done` 时才判成功**（不能因为加了重连就把 EOF 当成功）
- [ ] 手动断网 / 杀连接 → 自动重连 → 最终收到 `done`，且**用户消息只落了一条**
- [ ] 重连撞 429 时按 `retry_after` 退避，不是立刻重打
- [ ] 连续断 3 次 → 给明确提示且停止重试

---

## 十六、2026-09-17 变更：景点页数据源换成「本站收录库」

> 决策：`方案.md` **A47** / `DECISIONS.md` **D70**。契约已改，`docs/types.ts` 已重生成。

### 16.1 一句话说清改了什么

**以前**：`GET /spots/search` 是**代理高德实时搜索**（后端每次真去调高德）。
**现在**：它搜的是**本站收录库**（`spots` 表），**零高德调用**。

所以这一页从"搜索工具"变成了"**有内容的内容站**"：用户搜的是我们收录的景点。

### 16.2 前端要改的三处（就三处）

| # | 改什么 | 具体 |
|---|---|---|
| 1 | **`source` 判等** | `SpotSearchResponse.source` 枚举新增 `"local"`。**搜索恒为 `local`**；`amap` / `mock` 只可能出现在**景点详情**（收录库没有时回落高德）。⚠️ 别写 `if (source === 'amap')` 这种假设，按值分支 |
| 2 | **空态文案** | 🔴 "搜不到"的含义变了：以前 = 高德没搜到，现在 = **本站没收录**。建议文案：**「本站暂未收录相关景点，换个关键词试试」**。<br>❌ 不许写"共收录 N 个景点" / ❌ 不许暗示"全网都没有"（我们只是没收录）<br>❌ 空态也**不许硬凑几张卡** |
| 3 | **`cached` 字段** | 现在**恒 `false`**（本地查询没有缓存层）。谁要显示"已缓存"标签，这条得去掉 |

**不用改的**：请求参数、URL、鉴权、限流（都照旧）。`limit` / `offset` / `city` / `keywords` 一个没变。

### 16.3 两个细节别踩

- **`city` 用包含匹配**：库里存的是高德返回的「成都市」，你传「成都」也能命中 ——
  所以**别自己给城市名加/去"市"**，直接传用户选的那个词就行
- **排序由后端定**：名称前缀命中优先 → 评分降序（缺评分排最后）。前端**不要二次排序**，
  否则"前缀命中优先"这条会被你自己的排序毁掉
- **`/spots/{poi_id}` 仍然可能返回 `amap`/`mock` 来源的数据**（收录库没有时回落）——
  详情页如果显示"数据来源"，要认这个差异

### 16.4 怎么自己验（不用等后端）

1. 搜「宽窄巷子」→ 有结果，`source === "local"`
2. 搜「太古里」（如果未被收录）→ **空列表** ← 这是**正确行为**，不是 bug
3. 点进详情 → `GET /spots/{poi_id}` 200
4. 导航栏搜索框 → 跳 `/spots?kw=xxx` → 页面输入框里**要显示这个词**并自动搜（15.3 那条）

⚠️ **本地没跑过收录脚本时，这一页永远是空的** —— 那是环境没准备，不是前端 bug。
后端收录命令：`python scripts/seed_spots.py --city 成都 --keywords "..."`（在 `backend/` 下跑）。

---

## 十七、2026-09-18 第三轮校验：做完的 / 没做的 / 两个缺口

> 校验人：后端侧。校验对象：豆包第三轮的 4 个文件改动
> （`lib/sse.ts` / `pages/Assistant.tsx` / `pages/Spots.tsx` / `types/contract.ts`）。
> 校验手段：逐行读代码 + `tsc -b && vite build` + `oxlint` + 后端实测。

### 17.1 做完且正确的（不用再动）

| 项 | 结论 |
|---|---|
| **15.3 搜索框 URL 带词** | ✅ `Layout.tsx` 三个分支（空词不动作 / 未登录弹窗 / 已登录跳 `?kw=`）都对；`Spots.tsx` 灌词 + 自动搜 + **`setKeyword` 同步页面输入框**（"两个搜索框"最容易漏的一处，做对了）；额外加了 `seqRef` 序列号防"自动搜/手动搜"撞车 —— 这个加分 |
| **十六节三处** | ✅ `source==='local'` 分支、空态文案、去掉 `cached` 标签 |
| **15.13 SSE 重连（主体）** | ✅ `SseInterruptedError` 带 lastEventId、退避 1/2/4s 最多 3 次、撞 429 按 `retry_after` 等、重连带 `Last-Event-ID` + 非空 message |
| **`types/contract.ts`** | ✅ 与 `docs/types.ts` **逐字节一致**（同 10825 字节）—— 没手抄漂移 |

**构建级**：`tsc -b && vite build` **exit 0**；`oxlint` **0 errors / 10 warnings**
（10 条全是既有的 `exhaustive-deps`，不是本轮引入）。

### 17.2 ✅ 已完成（2026-09-18，豆包实现 · AI 浏览器实测通过）：15.10 写操作 401 跳登录

`redirectToLogin` **全项目 0 处**，三个改点原样未动（照 15.10 原文做即可）：

| 位置 | 现状 | 目标 |
|---|---|---|
| `lib/api.ts` `_raw()` 的 401 分支 | 一律 `handleUnauthorized()` 开弹窗 | **只有 GET** 开弹窗；非 GET（`init.method` 存在且不是 GET）→ `redirectToLogin()` |
| `lib/api.ts` `uploadAvatar()` 的 401 分支 | 同上 | 同上（它是 POST，直接跳） |
| `lib/sse.ts` 的 `!res.ok` → 401 分支 | 同上 | 直接 `redirectToLogin()`（聊天流是 POST，不能重放） |

`lib/auth.ts` 加这个函数（15.10 原文照抄）：

```ts
/** 写操作 / 页面级 401：跳登录页 + 带回跳地址（**不弹窗**） */
export function redirectToLogin(): void {
  const from = window.location.pathname + window.location.search
  window.location.assign(`/login?redirect=${encodeURIComponent(from)}`)
}
```

⚠️ `_raw()` 里判 GET 要用 `init.method`，别用 `path` 猜 —— `request()` 传下来的 `init` 就是原始的那个。

### 17.3 ✅ 缺口 1 已修（2026-09-18，豆包实现 · 实测断网后 checkpoint 续推成功）：「杀连接」根本不走重连

**现象**：只有"服务端正常关流但没 `done`"（EOF）才会重连；**手动断网 / 杀连接**时**完全不重连**。

**根因**：`lib/sse.ts` 的读取循环**没有 try/catch** ——

```ts
while (true) {
  const { done, value } = await reader.read()   // ← 网络中断时这里抛 TypeError
  ...
}
```

`reader.read()` 抛出的 `TypeError` 直接冒泡出 `streamSSE()`，而 `Assistant.tsx` 的 catch 只认
`SseInterruptedError`（和 `AbortError` / `ApiError`），于是走到最后那个分支 → `setErrorBar(...)` → **不重连**。

**改法**（把读循环包起来，其余原样不动）：

```ts
const reader = res.body.getReader()
const decoder = new TextDecoder()
let buf = ''
let gotDone = false
let lastEventId: string | null = null

try {
  while (true) {
    const { done, value } = await reader.read()
    if (done) break
    buf += decoder.decode(value, { stream: true })

    // ……原有切帧逻辑一字不改……
  }
} catch (e) {
  // 🔴 主动取消（用户离开页面 / React 严格模式卸载）必须原样抛出 —— 它不是断流，不能触发重连
  if (e instanceof DOMException && e.name === 'AbortError') throw e
  // 其余（网络中断、连接被掐、解码失败）一律算断流 → 带上最后的 id 交给上层重连
  throw new SseInterruptedError(lastEventId)
}
if (!gotDone) throw new SseInterruptedError(lastEventId)
return { lastEventId }
```

两个别写错的地方：

- **`AbortError` 必须原样 `throw e`** —— `Assistant.tsx` 判的是 `e instanceof DOMException && e.name === 'AbortError'` 后 `return`。
  如果你把它也包成 `SseInterruptedError`，用户切页面时会**白白重连 3 次**（还烧 chat 配额）。
- `lastEventId` 要留在 `try` **外面**声明 —— 它是在循环里被赋值的，写在 `try` 内 `catch` 就看不见了。

改完这一处，15.13 验收清单第 2 条（「**手动断网 / 杀连接** → 自动重连」）才可能通过。

### 17.4 📌 缺口 2（**不改，记为已知问题**）—— 重连会让最后一段文字重复

**现象**：断在**某个节点执行中途**时，重连后界面会出现「半句话 + 整句话」的**重复**，
不是干净地退回节点开头重打一遍。

**根因**（后端侧已核实，不是前端 bug）：
`backend/app/api/chat_stream.py` 的 `if resume: return None` → LangGraph 语义"从 checkpoint 停下处继续"，
而 **checkpoint 是节点级、不是 token 级** → 那个还没执行完的节点会被**整个重跑**，它的 token 全部重发。
前端 `token` 分支是纯追加（`last.content + evt.text`）→ 追加 = 重复。

**判定：接受，不修**（2026-09-18 用户拍板）。理由：断线本身是极小概率事件，
为它给前端加"节点快照 + 回滚"的状态机会引入回归风险，改后端做按 seq 真续传则要重审 D27 那条决策。
**演示时不会遇到**。

→ 但**别去"修"它**：这不是你没写对，是 D27 那个决策（恢复粒度 = 节点）的已知代价。
真正的取舍记录在 `DECISIONS.md` 的 **D27** 和 **D74**。

⚠️ 同源现象：**轨迹也会重复** —— `upsertTrajectory` 里 `node` 事件按"同 node 第 N 次"生成新 key
（`node:x#2` 是**新增条目**不是覆盖），`tool_call` 用 `tool:${call_id}` 做 key 但**不查重**（重发 = 两条同 key）。
同上，**不修**。

### 17.5 本轮改完的验收清单（豆包自己跑一遍）

- [x] `npm run build` 通过（`tsc -b` + `vite build` 都要过）—— ✅ 2026-09-18 AI 实测
- [x] `npm run lint` **0 errors**（warnings 数量保持 10 条，别新增）—— ✅ 实测 0 errors / 10 warnings
- [x] 未登录点景点卡「收藏」→ **整页跳到 `/login?redirect=...`**（**不是**弹窗）—— ✅（注：未登录时搜索即 401 弹窗、无卡片入口，用「token 失效后点收藏」等效实测：`/login?redirect=%2Fspots`、无弹窗）
- [x] 登录后自动回到 `/spots`（`redirect` 生效），**不是回首页** —— ✅ 实测精确回跳
- [x] 未登录点攻略「点赞」/ 发评论 → **同样跳登录页**（走同一条 401 兜底）—— ✅ 实测 `/login?redirect=%2Fguides%2F{id}`
- [x] 未登录在景点页**搜索**（GET）→ **仍然是弹窗**（这条**不能**跟着改成跳页）—— ✅ 实测弹窗、URL 不变
- [x] 未登录在导航栏搜索 → **仍然是弹窗**，登录成功后那个词**自己搜出结果** —— ✅ 实测弹窗登录后自动搜出、URL 带 `?kw=...`
- [x] 手动断网（或把后端停掉）→ 聊天流**自动重连**并给出提示，不再是一句红字了事 —— ✅ 实测长流断网 15s 恢复后从 checkpoint 续推（中断节点重跑完成）。⚠️ 提示的呈现 = 重试期间静默（1/2/4s×3），耗尽才红字「连接中断，请重新发送」；若要求「重连中」实时提示，属后续可选优化

> 🔴 **GET→弹窗、写操作→跳页** 是这次分工的核心，两个方向**别都改成同一个**。

---

## 十八、2026-09-18 第四轮：新增 `skeleton` 事件（快速骨架）—— 契约扩展，需要前端配合

### 18.1 背景一句话

实测一次完整规划 9 分 48 秒（85% 耗在 LLM 调用上）。后端已做两件事：
**① 子任务并行**（前端无感知）；**② 骨架先行** —— 搜索循环开始前，先用一次
不挂工具的快速 LLM 调用（约 20~30 秒）给出"先看版"行程框架，让你**立刻有东西可渲染**。

### 18.2 契约变更（已走完流程：schemas.py → types.ts 重生成 → api.md 更新）

`docs/types.ts` 已重新生成（枚举 10 / 结构体 56 / 共 66），直接重新取文件。新增：

```ts
interface SkeletonDay {
  day: number            // 第几天，从 1 起
  date: string | null    // YYYY-MM-DD；推不出就是 null
  theme: string
  stops: string[]        // ⚠️ 地名文本，不是 POI id
}
interface SkeletonEvent {
  type: "skeleton"
  title: string          // "北京 5 天行程（草排）"
  days: SkeletonDay[]
  note: string           // 固定提示文案，后端给好
}
```

SSE 事件联合体新增 `"skeleton"` 成员；**旧代码不认识会忽略，不会崩**（联合体注释里
写死的规则），但要看到收益必须处理它。

### 18.3 前端要做的（就两处）

1. **`handleEvent` 加 `skeleton` 分支**：
   - 把 `days` 渲染成**只读骨架卡**：每天一行「Day N · 主题 · 地点名 → 地点名 → 地点名」
   - **`note` 必须显示**（"快速草排，地点未经验证；正在核实真实地点与可行性，完成后自动替换"）
     —— 这是骨架和正式行程的区别声明，瞒掉它等于骗用户
   - 🔴 **`stops` 是未验证地名文本**：**不可**渲染成站点卡、不可点进详情、不要显示 id
   - 骨架卡出现时，把"生成中"的 loading 换成"精排中"提示（骨架已在，别再转空圈）
2. **`trip` 事件到达时，用行程卡整体替换骨架卡**（不是合并、不是追加）；
   `trip` 没到但 `error` 到了 → 骨架卡换成错误提示，**别留着骨架当结果**

### 18.4 怎么自己验（不用等后端）

mock 脚本里加一条 `{"type":"skeleton","title":"成都 3 天（草排）","days":[{"day":1,"date":"2026-10-02","theme":"经典地标","stops":["宽窄巷子","人民公园"]}],"note":"快速草排，地点未经验证；正在核实真实地点与可行性，完成后自动替换"}`，
插在 `node: parse_intent end` 之后、第一个 `tool_call` 之前，确认骨架卡先出、行程卡后到时替换干净。

### 18.5 验收清单

- [ ] 骨架卡在**任何工具轨迹卡之前**出现
- [ ] `note` 原文显示，没有被吞掉或缩略
- [ ] 骨架的地点名**不可点击**、没有站点卡样式
- [ ] `trip` 到达后骨架卡被**整体替换**，DOM 里不留骨架残留
- [ ] 只有 `error` 没有 `trip` 的会话：最终显示错误提示，骨架卡不残留
