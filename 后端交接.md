# 后端交接文档

> 记录「待后端处理」的需求与事项，由前端负责人（豆包）转交、后端负责人（叶鸽儿）处理。
> 最新需求在最上方。

---

## R1. 首页数据源改为「本站收录库」（2026-09-19 提出）

**状态**：✅ 已完成（2026-09-19，后端 AI 执行；`home.py` 重写、`api.md` 3.6 已同步、测试 +3）
**执行说明**：`hero` = 收录库带照片排序前 3（`_HERO_MAX=3`）；`recommended` = 评分降序前 4 排除 hero；
进程内缓存**移除**（本地读毫秒级，D70 同款理由）；验收 1~4 全部由测试覆盖
（`test_spots_home_uploads.py`：零出站断言 + 不重复断言 + 无图不进 hero 断言）。

> ~~以下为原始需求（存档）~~

**需求**：首页「猜你喜欢」与「Hero 轮播图」的数据源从「实时高德搜索」改为「本站收录库 `spots` 表」。

**现状**（`backend/app/api/routes/home.py`）：
- `hero`：`_HERO_KEYWORDS`（宽窄巷子/武侯祠/熊猫基地/人民公园）逐个调 `provider.search_poi`，取带照片的第一个
- `recommended`：`_RECOMMEND_KEYWORDS`（锦里古街/太古里/春熙路/都江堰）逐个调 `provider.search_poi`，取第一个
- 依赖高德网络/配额，且与收录列表（`/spots`）**不同源**

**期望**：
- `recommended`：`spot_repo.list_all()` 按评分降序取前 4（与收录列表同源同排序口径，可解释）
- `hero`：收录库中**带照片**的景点取前 3（供轮播大图）
- hero 与 recommended **不重复**（沿用现有契约：`hero` 与 `recommended` 的景点不重复）

**约束（不要破坏）**：
- 接口形状不变：`GET /home` 仍返回 `HomeResponse{hero: HeroSlide[], recommended: SpotCard[]}`，字段零改动
- 收录库里的 `poi_id` 都是高德 ID，详情页 `/spots/{poi_id}` 仍走「收录库优先 → 高德回落」，无需变动
- 前端已就绪：猜你喜欢 4 张卡片已接 `<Link to="/spots/{poi_id}">` 详情入口，数据源切换后前端零改动

**验收**：
1. 高德不可用（断网/配额）时 `/api/home` 仍能返回（纯本地读库）
2. `recommended` 与 `/api/spots` 列表同源（同一批景点、同一排序）
3. 首页点任意推荐卡 → 详情页正常打开（收录库直接命中，不走高德回落）
4. hero 3 张图全部有照片，与 recommended 不重复

---

## R2. `GET /spots` limit 上限 50 → 需支持全量拉取（2026-09-20 提出）

**状态**：✅ 已完成（2026-09-20，后端 AI 执行；提交见 `git log`「R2 提上限」）
**执行说明**：
- **提上限**：`backend/app/api/routes/spots.py` 两处 `le=50` → `le=100`（`/spots` 列表 + `/spots/search` 同步提，保持两端口径一致）；`docs/api.md` 3.5 / 3.5.2 参数表已同步
- **扩库**：**已在 2026-09-20 凌晨完成**（本交接写作时信息滞后）——收录库实为 **10 城 × 10 = 100 条整**（成都/北京/上海/杭州/苏州/南京/西安/重庆/广州/厦门），非「当前 10 条」
- **测试**：`test_spots_home_uploads.py` 新增 2 条（`limit=100` → 200；`limit=101` → 400 `invalid_param`），全量 **614 passed**
- **真库实测**（8010 临时实例，测完已杀）：`limit=100&offset=0` → 200，`total=100`，`items=100`，城市分布 10 城各 10 条整；`limit=101` → 400，错误体 `ctx.le=100`
- 响应形状 / 默认值 / offset 语义零改动，前端 `listSpots(100)` 直接可用

> ~~以下为原始需求（存档）~~
**需求**：收录库扩至 100 条（10 城 × 10）后，前端按「前端交接.md 20.8」方案 B（一次拉全量、本地过滤城市），但 `GET /spots` 的 `limit` 上限仍是 **50**（`query.limit le=50`，传 100 → `400 invalid_param`），前端 `listSpots(100)` 拿不到全量。

**现状**（已实测 2026-09-20）：
- `GET /spots?limit=100&offset=0` → `400 {"error":{"code":"invalid_param","detail":{"errors":[{...,"ctx":{"le":50},...}]}}}`
- 当前收录库实际 **10 条**（成都），尚未扩到 100 条

**期望**：
- `limit` 上限从 50 提到 **≥100**（Pydantic `Field(le=50)` 所在处调整），或确认「全量拉取」的其他可行方式
- 收录库扩至 10 城 × 10 条（`seed_spots.py` 收录，走既有收录流程）

**约束（不要破坏）**：
- 响应形状不变：`SpotListResponse{items, total}`；`total` = 全量收录数（已实测分页下 total 恒为真值，需保持）
- 前端已按 20.8 实现：`listSpots(100)` 一次拉全量 + 本地城市过滤 + 本地截取「加载更多」（20.6）——后端改完前端零改动
- `limit` 默认值、`offset` 分页语义不变

**验收**：
1. `GET /spots?limit=100&offset=0` → 200，`items.length` = 当前收录库全量
2. 收录库含 10 城数据，`/spots` 默认态「全部」显示全量、「北京」等 tab 显示该城 10 条
3. 页头 total 仍为全量真值，不随分页变

---

## 模板（新需求照此追加）

**状态**：待后端处理
**需求**：（一句话）
**现状**：（对应文件与行为）
**期望**：（要改成什么样）
**约束**：（接口形状 / 前端依赖 / 不能破坏的契约）
**验收**：（可执行的验收条目）
