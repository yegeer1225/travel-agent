# 智能旅游规划系统 · 技术方案

> 本文件只回答「**用什么技术、怎么落地、还缺什么才开工**」。项目本身做什么见 `方案.md`。
>
> 状态标记：**✅ 已拍板**｜**⚠️ 待拍板**｜**💡 建议**（默认走，你有异议就说）
>
> 最后更新：2026-09-14

---

## 一、技术栈总表

| 层 | 选型 | 状态 | 代价 / 理由 |
|---|---|---|---|
| Agent 框架 | **LangGraph 1.2 手写 StateGraph** | ✅ | 样板代码自己写；换来三层循环每一步都在手里，面试能直接讲条件边。**架构路线 X/Y/Z 待拍（见 `方案.md` 7.4）** |
| 多 agent | **Subagents 形态**（主 agent 把子 agent 当工具调） | ⚠️ 待拍 | 官方数据：单次请求**多 1 次模型调用**、子 agent 无状态；换来上下文隔离与集中控制 |
| 语言 | Python 3.13 | 💡 | 与你 kb_api 一致 |
| 包管理 | **venv + pip**（清华源） | 💡 | 比 uv 慢，但不引入新工具 |
| 模型 | **百炼 qwen-plus**（继承 kb_api 的 key） | 💡 | 换供应商只改 `.env` 三行 |
| LLM 接入 | `langchain-openai`（OpenAI 兼容层） | 💡 | 多一层抽象，出问题要先判断是封装还是接口 |
| 结构化输出 | `function_calling`（不用 `json_schema`） | 💡 | 约束更弱，靠 Pydantic 抛错兜底；国产模型对 json_schema 支持不一致 |
| POI / 距离 / 天气 | **高德 Web 服务 API**（httpx 直连） | ✅ 数据源 | 需申请 key；坐标 GCJ-02，与前端地图天然一致 |
| 接入方式 | **直连 HTTP，不走 MCP** | ✅ | MCP 的独有价值是"LLM 自动发现工具"，而我们的工具是自己定义的，直连更快更可控可缓存 |
| MCP | **只用在「导入高德 APP」一个按钮** | ✅ | 多一个依赖 + 一个进程/端点；换来"接入了 MCP"这个标签和一个真本事（唤端导航） |
| 持久化 | **SQLite**（`langgraph-checkpoint-sqlite`） | 💡 | 单文件、零服务、**不需要 Docker**——绕开你本机 Docker 0xC0000005 + WSL2 不稳的坑 |
| 后端 | FastAPI | 💡 | 与你已有技术栈一致 |
| 前端 | **React + Vite** | ⚠️ | 地图 + 拖拽排序 + 卡片列表三处状态同步，原生 JS 手写是自找麻烦。**代价：你要学一点 React**（本来也在你阶段 4 计划里） |
| 地图渲染 | **高德 JS API 2.0**（前端） | ✅ | 唯一能出截图效果的路，约 40~60 行 |
| 测试 | pytest | 💡 | 只测纯逻辑（判据函数、数据转换），不测 LLM 输出 |
| 部署 | 只本机 | 💡 | 不碰 Docker |

### 为什么地图渲染只有 JS API 这一条路（实测过）

| 方案 | 能不能出截图效果 | 卡在哪 |
|---|---|---|
| **前端 JS API** | ✅ | 需要 Web 端 JS API Key + 安全密钥 |
| 静态地图 API | ❌ | markers **最多 10 个**、标记上的字**只能是单个数字/字母/单个汉字**、paths **最多 4 条**、图最大 1024×1024。你的行程 **12 个站、编号到 13**，数量两位数编号两条全超 |
| 高德 MCP | ❌ | 官方 MCP 全部工具里**没有任何一个能在网页里画出地图**；"生成专属地图"输出的是一串**高德 APP 唤端链接**（`surl.amap.com/...`） |

反证：官方那个旅游规划案例里用户原话是「#制作网页地图自定义绘制旅游路线和位置」，而文档写的实现方式是 **Cursor 生成了一个 HTML 文件**。**网页地图是 JS API 代码画出来的，MCP 只负责给数据。**

---

## 二、架构分层

```
┌──────────────────── 前端 React + Vite ────────────────────┐
│  界面一：历史栏 + 对话区（4 种消息：文字 / 轨迹卡 / 行程卡 / 校验卡）│
│  界面二：左表格 + 右高德地图 + 按天卡片（拖拽 / 删站 / 改时间）      │
└──────────────────────────┬────────────────────────────────┘
                           │ HTTP + SSE（界面一"实时看 agent 在干什么"）
┌──────────────────────────▼────────────────────────────────┐
│                      FastAPI                               │
│   /sessions           会话列表 / 新建 / 恢复                 │
│   /sessions/{id}/chat 对话（SSE 流式返回节点事件）           │
│   /trips/{id}         读行程                                │
│   /trips/{id}         PATCH 调顺序 / 删站 / 改时间（确定性重算）│
│   /trips/{id}/recheck 手动触发深度软校验                     │
│   /trips/{id}/amap-import  MCP 生成唤端链接                  │
└──────────────────────────┬────────────────────────────────┘
                           │
┌──────────────────────────▼────────────────────────────────┐
│                  LangGraph（手写 StateGraph）               │
│  parse_intent ─┬→ ask_more → END                           │
│                └→ agent_step ⇄ tool_step   ← 内循环         │
│                        ↓ 事实已够                          │
│                   generate_plan                            │
│                        ↓                                   │
│                   check_plan ─┬→ repair → 回 agent_step     │
│                               └→ render → END              │
│  SQLite checkpointer：会话状态可恢复                        │
└──────────────────────────┬────────────────────────────────┘
                           │
┌──────────────────────────▼────────────────────────────────┐
│  providers/  高德客户端（mock 与 real 同签名，切换只改 .env）  │
│  tools/      模型可调的 3 个工具：搜地点 / 查天气 / 算距离     │
│  store/      SQLite：会话、行程、校验历史                     │
└───────────────────────────────────────────────────────────┘
```

---

### 三层循环在这张图上的落点

| 层 | 落在哪 | 靠什么实现 |
|---|---|---|
| **L1 会话层** | **图外面** —— FastAPI 每收到一条新消息就再 invoke 一次 | checkpointer 从 SQLite 恢复状态 |
| **L2 工具层** | `agent_step ⇄ tool_step` 这条回边 | 条件边 `should_continue`（有 `tool_calls` → 回工具，没有 → 出） |
| **L2 插队（steering）** | agent 干活途中注入用户消息 | `interrupt()` + `update_state()` |
| **L3 校验层** | `check_plan → repair → 回 L2` | 条件边 + 计数器（上限见 `方案.md` 7.1.1） |
| **子 agent**（⚠️ 待拍） | `agent_step` 挂一个 `task` 工具，内部是一个独立 **Subgraph** | 见 `方案.md` 7.3 |

> 注意：**L1 不画在图上**。图只管"一次请求内部"的事；"用户又来了一句话"是图外面的事，由 FastAPI 层再 invoke 一次触发。这跟你给的外层 `while True` 是同一个语义，只是换了个地方放。

---

## 三、数据契约（**M0，写第一行代码前必须定死**）

三方共用同一份结构：agent 产出它、后端存它、前端渲染它。**这份 JSON 不定，界面就是空中楼阁。**

```jsonc
{
  "trip_id": "uuid",
  "session_id": "uuid",            // 归属会话；界面二粘贴时自动建
  "title": "成都3日游·带爸妈",
  "destination": "成都",
  "source": "generated",           // generated | pasted
  "created_at": "2026-09-14T22:30:00+08:00",
  "updated_at": "2026-09-14T22:31:12+08:00",

  "days": [
    {
      "day": 1,
      "date": "2026-10-01",
      "theme": "市区古迹·走得少",
      "weather": {                     // 高德天气 extensions=all，只覆盖 3 天
        "status": "ok",                // ok | unavailable
        "day_weather": "晴",           // dayweather
        "night_weather": "多云",       // nightweather
        "day_temp": 26,                // daytemp
        "night_temp": 18,              // nighttemp
        "day_wind": "东北风",          // daywind
        "day_power": "3",              // daypower
        "report_time": "2026-09-14 18:00:00",
        "note": null                   // unavailable 时写原因："出发日距今超过 3 天，无预报数据"
      },
      "stops": [
        {
          "seq": 1,
          "name": "武侯祠",
          "poi_id": "B00141xxxxxx",       // 高德 POI ID：证明它不是编的
          "lng": 104.0494,
          "lat": 30.6427,
          "coord_sys": "GCJ-02",
          "arrive": "09:00",
          "stay_min": 90,
          "leave": "10:30",
          "from_prev_km": 0,              // 距上一站距离（代码算）
          "from_prev_drive_min": 0,
          "cost_per_person": 50,          // 高德 biz_ext.cost，是"人均"不是"门票"
          "rating": "4.6",                // 可能为空
          "match_reason": "历史古迹，与偏好匹配",  // LLM 生成
          "checks": [
            // status 三态：pass 通过 | fail 不通过（硬错打回）| unknown 数据拿不到（如实标注）
            {"level": "hard", "code": "poi_exists",   "status": "pass"},
            {"level": "hard", "code": "reachable",    "status": "pass"},
            {"level": "hard", "code": "open_today",   "status": "unknown",
             "msg": "该 POI 未返回开放时间字段"},
            {"level": "soft", "code": "need_reserve", "status": "warn",
             "msg": "旺季建议提前 3 天线上预约"}
          ]
        }
      ],
      "day_stats": {"distance_km": 12.4, "drive_min": 46, "walk_km": 4.2}
    }
  ],

  "summary": {
    "total_distance_km": 38.7,
    "total_cost_per_person": 320,
    "stop_count": 12,
    "hard_errors": 0,
    "soft_warnings": 3
  },

  "validation": {
    "rounds": 2,                     // 打回重排了几轮
    "fixed":     [{"code": "reachable",  "msg": "Day2 原第 3 站车程 1h50m，已换近点"}],
    "remaining": [{"code": "need_reserve", "level": "soft", "msg": "武侯祠需提前预约"}]
  }
}
```

### 字段来源必须分清（否则你会以为高德什么都给）

| 字段 | 来源 | 说明 |
|---|---|---|
| 名称 / 经纬度 | 高德 POI 搜索 | 直接给，GCJ-02 |
| `rating` | 高德 `biz_ext.rating` | **仅餐饮/酒店/景点/影院类 POI 有**，可能为空 |
| `cost_per_person` | 高德 `biz_ext.cost` | 是**人均消费**，不是门票 —— 卡片上别写"门票" |
| `poi_id` | 高德 POI 搜索 | 封闭世界校验的锚点 |
| `match_reason` | **LLM 生成** | 接口给不了 |
| `weather` | 高德天气接口 `extensions=all` | **只有 3 天（含当天）、且从"今天"往后算**；拿不到时 `status: "unavailable"`，**不能标成 pass** |
| `from_prev_km` / `drive_min` | **代码算**（高德测距） | 确定性 |
| `checks[].status` | 代码（硬）/ LLM（软） | **三态**：pass / fail / **unknown**，见 `方案.md` 6.2 |
| 参考图里的「趣味度」 | ❌ **高德没有这个字段** | 那是原项目自己造的，我们删掉 |

**天气接口的两个隐藏条件**（官方文档实查）：

1. 参数是 **`city=adcode`（城市编码，如成都 510100）**，不是城市名 → 需要先用**地理编码**把"成都"转成 adcode。这个转换放在**天气工具内部**，不额外增加工具数量（A7 定的 3 个不变）
2. `casts` 数组**固定 3 个元素**：当天、第二天、第三天。**没有"按指定日期查"的能力**

---

## 四、目录结构 💡

```
travel-agent/
├── 方案.md                  项目方案（做什么）
├── 技术方案.md              本文件（用什么）
├── README.md                跑起来的说明
├── requirements.txt
├── .env / .env.example      密钥（.env 不入库）
├── DECISIONS.md             决策记录：决定 / 替代方案 / 代价
│
├── backend/
│   ├── app/
│   │   ├── config.py        配置读取
│   │   ├── schemas.py       ★ 数据契约（Pydantic，第三节那份 JSON）
│   │   ├── llm.py           LLM 接入（OpenAI 兼容层）
│   │   ├── providers/       高德客户端（mock 与 real 同签名）
│   │   ├── tools/           模型可调工具：搜地点 / 查天气 / 算距离
│   │   ├── graph/           state.py / nodes.py / graph.py
│   │   ├── store/           SQLite：会话、行程
│   │   └── api/             FastAPI 路由
│   └── tests/               只测纯逻辑
│
├── frontend/                React + Vite
│   └── src/
│       ├── pages/           界面一（Chat）/ 界面二（Overview）
│       └── components/      轨迹卡 / 行程卡 / 校验卡 / 地图
│
└── eval/                    评测
    ├── fixtures/            攻略原文（手动存 10~20 篇）
    ├── cases/               结构化用例 + 已知错误标注
    └── report.py            跑分与报告
```

---

## 五、开工前必备清单

### A. 必须你去申请（我做不了）

| # | 事项 | 怎么弄 | 用在哪 |
|---|---|---|---|
| **1** | **高德「Web 服务」Key** | [lbs.amap.com](https://lbs.amap.com/) → 控制台 → 应用管理 → 创建应用 → 添加 Key → **服务平台选「Web服务」** | **后端**：搜 POI、算距离、查天气、静态地图 |
| **2** | **高德「Web端(JS API)」Key + 安全密钥** | **同一个应用**下再「添加 Key」→ 服务平台选「**Web端(JS API)**」→ 会同时给一个 `securityJsCode` | **前端**：在页面里画地图、打点、连线 |

> ⚠️ 这两个 Key **不能互换**，申请时选错服务平台就用不了。先申请 1 就能开工（M1~M3 全用不到前端）。
>
> 百炼 API Key **✅ 已有**（已从 kb_api 继承到 `.env`）。

### B. 必须你准备（半小时的事）

| # | 事项 | 说明 |
|---|---|---|
| **3** | **评测集：10~20 篇旅游攻略** | 浏览器打开复制存成 `.md`，放 `eval/fixtures/`。**要含具体行程（Day1/Day2…）、交通方式、时间**。先攒成都的。来源：马蜂窝游记 / 知乎 / 公众号 |
| **4** | **项目目录名** | 现在叫 `agent`（太泛），建议改 `travel-agent` |
| **5** | （可选）每篇攻略里你知道的问题 | 比如"这天排了 3 个相距 40 公里的点"——**这是评测集的"标准答案"**，有它才能算"抓错率" |

### C. 已就绪

| 项 | 状态 |
|---|---|
| 项目目录 | `D:\vscode Projiects\agent`（D 盘，没写 C 盘） |
| git 仓库 | 已 init，3 个 commit（`19b5b83`、`ef82c47`、`2059319`），能回滚 |
| Python 环境 | `.venv` 已建，依赖已装：`langgraph 1.2.11` / `langchain-core 1.6.3` / `langchain-openai 1.6.2` / `pydantic 2.13.5` / `pytest 9.1.1` |
| LLM Key | 已继承 kb_api 的百炼 key，写入 `.env` |

### D. 已完成 / 待做

**已做：**

- ✅ `.env.example` 已拆出高德两个 Key 的占位
- ✅ 旧 `PLAN.md` + 旧原型已归档到 `.workbuddy/archive/`（可恢复）
- ✅ 两份文档已 commit（`2059319`）

**待做（拍板后立刻开工）：**

- `schemas.py` —— 把第三节的 JSON 落成 Pydantic 模型（**M0 的产物**）
- `DECISIONS.md` —— 决策记录模板
- 目录骨架 `backend/ + frontend/ + eval/`

### E. 开工顺序（每步一个 commit）

```
1. 建目录骨架 + DECISIONS.md                         ← 无风险，先做
2. M0 数据契约定案（schemas.py + 一份手写样例 JSON） ← 你过一遍字段，这是第一道门
3. M1 双层循环 + 3 个 mock 工具                      ← 命令行能跑出第一份行程
4. 每个里程碑结束：git commit
```

---

## 六、风险与不确定项（不粉饰）

| # | 风险 | 影响 | 缓解 |
|---|---|---|---|
| 1 | **高德 MCP 服务端集成没验证过** | 官方 MCP 主要面向 Cursor/Claude 类桌面客户端，服务端能不能调通不确定 | 接之前先写个 20 行脚本打通；不通就砍掉这个按钮，不影响主线 |
| 2 | **封闭世界校验会"误杀"** | 逐字命中时，模型写「武侯祠」而池子里是「武侯祠博物馆」就判失败 | 加别名容错（高德 `alias` 字段）+ 模糊匹配阈值；**方法要你认** |
| 3 | **自主选工具 → 输出方差变大** | 跑两次工具调用序列可能不同 → 行程不同 → **评测数字会抖** | 压低 temperature + 完整记录调用轨迹 + 用 **pass^k（多次一致性）** 而不是单次准确率 |
| 4 | **高德字段缺失** | `rating` / `cost` / **开放时间** 不是每个 POI 都有 | 缺就隐藏，不显示"暂无"也不让 LLM 补（会编）；判据标 `unknown` |
| 5 | **天气只覆盖 3 天** | 出发日在 3 天之后的行程，**天气校验天生不可用**；演示时容易被问"怎么没天气" | 显示 `unknown` 而不是 `pass`；评测报告单列 unknown 占比 |
| 6 | **多 agent 增加不确定性** | 官方数据：Subagents 单次请求**多 1 次模型调用**，且子 agent **无状态**、每次重走全流程 | 先只切 1 个子 agent；子 agent 只产出**结构化清单**不产出自由文本；各自记录独立 trace |
| 7 | **前端 React 学习成本** | 你 TS/React 是缺口 | 前期只写界面一（列表+气泡，简单）；地图相关集中在界面二 |
| 8 | **评测集"标准答案"怎么定** | 攻略本身也可能是错的、过时的 | 只标注**能用硬判据验证的错**（距离/时间/不存在的地点），主观问题不进评测 |
| 9 | **模型对 function calling 的稳定性** | qwen-plus 上一版原型实测可用，但工具变多后选错率会上升 | 工具控制在 3 个；工具描述写清"什么时候该调" |

---

## 七、待你拍板

| # | 问题 | 我的建议 |
|---|---|---|
| **1** | **架构路线 X / Y / Z**（详细对比见 `方案.md` 7.4）—— **最优先** | **Z 混合**：外层会话 + 持久化交给 LangGraph，工具循环用你给的双层骨架语义，插队走 `interrupt()` |
| **2** | **多 agent 切几个** | 先只切 1 个**搜索子 agent**；校验子 agent 等第一个跑通再说 |
| 3 | **前端框架** React + Vite（要学一点）还是原生 JS（写起来痛苦） | **React** |
| 4 | 目录结构用 `backend/ + frontend/` 还是扁平 `src/` | `backend/ + frontend/` |
| 5 | 项目目录名 | `travel-agent` |
| 6 | 高德 MCP 那个按钮 | 先留接口，验证通过再接 |
| 7 | 我把第三节的数据契约落成 Pydantic 模型，你过一遍字段 | 先做 M0 |
