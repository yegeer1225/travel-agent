# 智能旅游规划系统 · 技术方案

> 本文件只回答「**用什么技术、怎么落地、还缺什么才开工**」。项目本身做什么见 `方案.md`。
>
> 状态标记：**✅ 已拍板**｜**⚠️ 待拍板**｜**💡 建议**（默认走，你有异议就说）
>
> 最后更新：2026-09-15

---

## 一、技术栈总表

| 层 | 选型 | 状态 | 代价 / 理由 |
|---|---|---|---|
| Agent 框架 | **LangGraph 1.2 手写 StateGraph** | ✅ | 架构路线 **X（全 LangGraph）** 已拍板：三层循环每一步都在手里，面试能直接讲条件边 |
| 多 agent | **Subagents 形态**，只切 **1 个搜索子 agent** | ✅ | 官方数据：单次请求**多 1 次模型调用**、子 agent 无状态；换来上下文隔离 |
| 语言 | Python 3.13 | ✅ | 与 kb_api 一致 |
| 包管理 | **venv + pip**（清华源） | ✅ | 比 uv 慢，但不引入新工具 |
| 模型 | **DeepSeek `deepseek-flash`**（可切 `deepseek-v4-pro`） | ✅ | 实测并行 tool_calls / 多轮 / 中文全过。**⚠️ 思考模式必须按节点分开设**（见本节末实测） |
| 模型切换 | **配置驱动**（`MODEL_REGISTRY` + 前端可选） | ✅ | 一次调用只用一个模型；粒度见本节末 |
| LLM 接入 | `langchain-openai` + `extra_body={"thinking":{"type":"disabled"}}` | ✅ | **不显式关思考，工具循环会稳定 400**（实测 2/2 复现） |
| 结构化输出 | `function_calling`（不用 `json_schema`） | ✅ | 约束更弱，靠 Pydantic 抛错兜底 |
| POI / 距离 / 天气 | **高德 Web 服务 API**（httpx 直连） | ✅ 数据源 | 坐标 GCJ-02，与前端地图天然一致 |
| 数据层接入方式 | **自己写 3 个工具函数**（httpx 直连，约 150 行） | ✅ | 不接 MCP 做数据源 —— MCP 层会丢 `location`/`biz_ext` |
| MCP | **工具门面 + 代码级降级**（默认关） | ✅ | 对模型只暴露 `search_poi` 一个名字；内部 `provider=mcp 试 → 失败/字段缺 → httpx 直连`。**降级是代码逻辑，不是模型判断** |
| 持久化 | **MySQL 8**（Docker 起）+ `langgraph-checkpoint-mysql` | ✅ | **⚠️ 该包是第三方**（作者个人，非 langchain-ai 官方），要求 MySQL ≥ 8.0.19，LangGraph 1.2 兼容性待实测。**Docker 已修好可跑**（2026-09-15 确认） |
| **鉴权** | **JWT**（`PyJWT`）+ `bcrypt` 密码哈希 | ✅ | 只回答"你是谁"；**隔离靠 `WHERE user_id`，跟 JWT 是两件事**。不做 refresh token / Redis 黑名单 / RBAC（见第三节末「鉴权」） |
| 流式输出 | **SSE**（`astream_events` → FastAPI `StreamingResponse`） | ✅ | 白送。**坑：浏览器 `EventSource` 只支持 GET** → 聊天要 POST，得用 `fetch` + `ReadableStream` 手写解析 |
| 后端 | FastAPI | ✅ | 与已有技术栈一致 |
| 前端实现 | **交给「豆包」**（对话式生成页面） | ✅ | 本 AI 只做后端 + 契约 + 交接文档。理由：**切换成本 = 契约变更次数，跟换不换工具无关**（见 `界面设计.md` 第一节） |
| 前端技术栈 | **React 19.3 + Vite 8.3 + TS 7 + Tailwind 4.3**（＋ 轮播 **Swiper 14.2**） | ✅ | 精确版本与四个坑见 `界面设计.md` 第一节；⚠️ **Tailwind 4 是 CSS-first，没有 `tailwind.config.js`** |
| 前端依赖 | react-router-dom 7.18 / `@dnd-kit/core` 6.3 / `@amap/amap-jsapi-loader` 1.0.1 | ✅ | 拖拽**不用 SortableJS**（要手动跟 React DOM 对齐，更绕）；SSE 用 `fetch` + `ReadableStream` |
| 前端视觉 | **几何拼贴风**（白底 + 高饱和色块 + 直角 + 粗下划线） | ✅ | 见 `界面设计.md` 第二节；三态色**独立定义**，不复用装饰色板 |
| 地图渲染 | **高德 JS API 2.0**（前端） | ✅ | 唯一能出截图效果的路 |
| 前后端对接 | **契约冻结 + mock 模式**（`providers/` mock 与 real 同签名） | ✅ | 契约冻结后后端内部改动**前端零改动**；mock 让豆包能并行开工 |
| 测试 | pytest | ✅ | 只测纯逻辑（判据函数、数据转换），不测 LLM 输出 |
| 部署 | 本机；Docker 只用于 MySQL | ✅ | 应用本身不容器化 |

### 为什么地图渲染只有 JS API 这一条路（实测过）

| 方案 | 能不能出截图效果 | 卡在哪 |
|---|---|---|
| **前端 JS API** | ✅ | 需要 Web 端 JS API Key + 安全密钥 |
| 静态地图 API | ❌ | markers **最多 10 个**、标记上的字**只能是单个数字/字母/单个汉字**、paths **最多 4 条**、图最大 1024×1024。你的行程 **12 个站、编号到 13**，数量两位数编号两条全超 |
| 高德 MCP | ❌ | 官方 MCP 全部工具里**没有任何一个能在网页里画出地图**；"生成专属地图"输出的是一串**高德 APP 唤端链接**（`surl.amap.com/...`） |

反证：官方那个旅游规划案例里用户原话是「#制作网页地图自定义绘制旅游路线和位置」，而文档写的实现方式是 **Cursor 生成了一个 HTML 文件**。**网页地图是 JS API 代码画出来的，MCP 只负责给数据。**

### DeepSeek 接入实测（2026-09-15，全部一手实测）

**结论先说：思考模式不是"全程关"，而是"按节点分开设"。**

| 测试项 | 结果 |
|---|---|
| Key 有效性 | ✅ |
| **可用模型名** | `deepseek-flash` / `deepseek-v4-pro` —— **没有** `deepseek-v4.1-flash`（填错直接 404） |
| 并行 tool_calls | ✅ 一句话返回 3 个调用，正确拆成两次 `search_poi` |
| 多轮工具循环 | ✅ |
| 中文输出 | ⚠️ 无 system prompt 时**回英文**，必须显式约束 |
| 思考模式 | ⚠️ **默认开启**，响应带 `reasoning_content`（实测 68~197 字符） |

**🔴 400 的触发条件（实测锁定，不是猜的）**

> 本次请求**带 `tools`**，且历史里 assistant 消息**缺 `reasoning_content`** → 400。

| 场景 | 实测结果 |
|---|---|
| 带 `tools` + 历史缺 reasoning | ❌ **2/2 稳定复现 400** |
| **不带** `tools` + 历史缺 reasoning | ✅ **2/2 通过，且思考真的生效**（`reasoning_content` 非空） |

**为什么踩坑的人多**：`reasoning_content` 不是 OpenAI 标准字段，**`langchain-openai` 会静默剥掉它**（拦截请求体实测：assistant 消息只剩 `content`/`role`/`tool_calls`）。同类事故已见于 opencode #24104、claude-code-router #1378、n8n。

**✅ 解法：按节点分开设**

| 节点 | 挂 tools？ | 思考模式 | 结果 |
|---|---|---|---|
| **工具循环** `agent_step` | 挂 | **关**：`extra_body={"thinking":{"type":"disabled"}}` | 避开 400；**`temperature` 恢复生效** |
| **最终生成** `generate_plan` / `render` | **不挂** | **开**（保持默认） | **CoT 规划能力保住**，且无坑 |

> 这个切法**天然契合已定的 A6**（循环只收事实、循环结束后另起固定节点出 JSON）——架构上本来就分开了，不需要额外设计。

**🔴 顺带确认：思考模式下 `temperature` 完全失效**

官方原话：*"Thinking mode does not support the temperature, presence_penalty, or frequency_penalty parameters... setting these parameters will not trigger an error but will also have no effect."*；`top_p` 下限被抬到 0.95。
→ `方案.md` 风险 3 的"压低 temperature 降方差"**只在关思考的节点上有效**。

**模型切换的三个粒度**（一次调用只用一个模型）

| 粒度 | 怎么做 | 价值 |
|---|---|---|
| ① 全局默认 | `.env` 的 `LLM_MODEL` | 基础 |
| ② **节点级分配** | `MODEL_REGISTRY`：工具循环用 `flash`（快/省），规划节点可挂 `v4-pro` | **能讲"按成本与质量分配模型"** |
| ③ **离线对比评测** | 同批 case 跑两个模型，出对比报告 | **才是"切换模型"的真正用途**（评测模块本来就要做） |

**一个必须写进 prompt 的真实行为**：模型会如实报告"接口没返回纬度"，但结尾自己补一句"据我了解大致位于武侯区武侯祠大街 231 号"——**这是模型知识，不是接口数据**。prompt 必须明确禁止补全接口没给的字段，否则校验层直接被污染。

---

### 高德 Web 服务接口实测（2026-09-15，Key = `backend-ws`）

四个接口全部打通。**其中两条结论推翻了本文档原先的写法**（天气天数、字段可用性）。

| 接口 | 端点 | 实测结果 |
|---|---|---|
| POI 关键词搜索 | `/v3/place/text` | ✅ 返回 99~600 条。`location` / `photos` / `biz_ext` / `alias` **全都拿得到**（反过来证实「MCP 层丢字段」的坑，自己写 httpx 是对的） |
| 地理编码 | `/v3/geocode/geo` | ✅ 「成都市」→ `adcode=510100`、`citycode=028`、`level=市` |
| 天气 | `/v3/weather/weatherInfo` | ✅ `extensions=all` → **4 天**预报；`extensions=base` → 实况 `lives`（温度/湿度/风向/风力） |
| 距离测量 | `/v3/distance` | ✅ `type=1` 驾车，返回 `distance`(米) / `duration`(秒)。**支持 `origins=a\|b\|c` 配单个 `destination`，一次算多起点到同一终点** |

**🔴 新发现的硬约束：QPS 限流（会真实打断流程）**

连续快速请求 10 个关键词，**第 4 个开始返回 `CUQPS_HAS_EXCEEDED_THE_LIMIT`**（`status=0`）。
个人开发者 Key 的每秒并发很低，这不是偶发而是必然 —— 而**一次行程要搜十几个 POI**。

→ 工具层必须做（M2 落地）：

1. **串行 + 固定间隔**（实测 **1.2s 间隔**稳定不触发）
2. **`search_poi` 一次多返回候选**（`offset` 调大），从源头减少调用次数
3. **遇到 `CUQPS_*` 必须重试 + 退避** —— 绝不能当成"搜不到"，否则模型会认为这个景点不存在，**触发一个假的硬错并白白打回重排**
4. 同一目的地的结果**进程内缓存**（景点页也复用同一份缓存，见风险 10 的"挂回主线"）
5. 日配额以控制台「配额管理」显示为准，**不要按网上的数字估算**

**🔴 字段类型陷阱：`cost` / `open_time` / `rating` 是 `str | list` 双类型**

| POI 类型 | `biz_ext.cost` 实测 | `biz_ext.open_time` 实测 | `biz_ext.rating` 实测 |
|---|---|---|---|
| 餐饮 / 咖啡 | `'73.00'` / `'90.00'` ✅ | `'11:00-02:00'` ✅ | `'4.7'` ✅ |
| 景点 | `[]` ❌ | `'08:30-18:30'` ✅（可能双时段） | `'4.8'` ✅ |
| 酒店 | `[]` ❌ | `[]` ❌ | `'4.6'` ✅ |
| 地铁站 / 交通 | `[]` ❌ | `[]` ❌ | `[]` ❌ |

→ **解析层统一收敛**：空数组 → `None`；**绝不能当 0**。
这条已在 `backend/app/schemas.py` 的 `AmapPoi` 里定死（含字段级注释）。

**其他实测细节**

- `open_time` 双时段格式：`'07:30-17:00 08:00-16:30'`（旺季 / 淡季），`biz_ext.opentime2` 更详细
- `photos` URL 有两个域名：`https://aos-comment.amap.com/...`（实测 200 / `image/jpeg` / 653KB）和 `http://store.is.autonavi.com/...`（**HTTP 非 HTTPS**，前端混用要留意）
- 跨区数据：青城山 / 都江堰的 `adname` 是「都江堰市」，不在成都市区 → 距离校验要能区分"市区"和"远郊"
- `poi_id` 实测形如 `B001C07VJ2` / `B0FFFD3P2C` → 校验层可用 `^B0` 前缀做基本形状检查（**不是充分条件，只做兜底**）

---

## 二、架构分层

```
┌───── 前端 React 19 + TS + Tailwind 4（由「豆包」实现）──────┐
│  界面一：历史栏 + 对话区（4 种消息：文字 / 轨迹卡 / 行程卡 / 校验卡）│
│  界面二：左表格 + 右高德地图 + 按天卡片（拖拽 / 删站 / 改时间）      │
│  另外 4 页（首页/景点/攻略/个人中心）占位 —— 见 界面设计.md   │
└──────────────────────────┬────────────────────────────────┘
                           │ HTTP + SSE（界面一"实时看 agent 在干什么"）
┌──────────────────────────▼────────────────────────────────┐
│                      FastAPI                               │
│   /sessions           会话列表 / 新建 / 恢复                 │
│   /sessions/{id}/chat 对话（SSE 流式返回节点事件）           │
│   /trips/{id}         读行程                                │
│   /trips/{id}         PATCH 调顺序 / 删站 / 改时间（确定性重算）│
│   /trips/{id}/recheck 手动触发深度软校验                     │
│   /trips/{id}/amap-import  MCP 生成唤端链接                  │
└──────────────────────────┬────────────────────────────────┘
                           │
┌──────────────────────────▼────────────────────────────────┐
│                  LangGraph（手写 StateGraph）               │
│  parse_intent ─┬→ ask_more → END                           │
│                └→ agent_step ⇄ tool_step   ← 内循环         │
│                        ↓ 事实已够                          │
│                   generate_plan                            │
│                        ↓                                   │
│                   check_plan ─┬→ repair → 回 agent_step     │
│                               └→ render → END              │
│  MySQL checkpointer：会话状态可恢复                         │
│  ⚠️ agent_step 关思考（避 400）／generate_plan 开思考（保 CoT）│
└──────────────────────────┬────────────────────────────────┘
                           │
┌──────────────────────────▼────────────────────────────────┐
│  providers/  高德客户端（mock 与 real 同签名，切换只改 .env）  │
│  tools/      模型可调的 3 个工具：搜地点 / 查天气 / 算距离     │
│  store/      MySQL：会话/消息/行程 🔒私有 · 攻略 🌐发布后公开   │
│              ⚠️ 隔离靠后端 owned_session()/owned_trip()，不是前端隐藏  │
└───────────────────────────────────────────────────────────┘
```

---

### 三层循环在这张图上的落点

| 层 | 落在哪 | 靠什么实现 |
|---|---|---|
| **L1 会话层** | **图外面** —— FastAPI 每收到一条新消息就再 invoke 一次 | checkpointer 从 MySQL 恢复状态 |
| **L2 工具层** | `agent_step ⇄ tool_step` 这条回边 | 条件边 `should_continue`（有 `tool_calls` → 回工具，没有 → 出） |
| **L2 插队（steering）** | agent 干活途中注入用户消息 | `interrupt()` + `update_state()` |
| **L3 校验层** | `check_plan → repair → 回 L2` | 条件边 + 计数器（上限见 `方案.md` 7.1.1） |
| **子 agent** ✅ | `agent_step` 挂一个 `task` 工具，内部是一个独立 **Subgraph**；**先只切 1 个搜索子 agent** | 见 `方案.md` 7.3 |

> 注意：**L1 不画在图上**。图只管"一次请求内部"的事；"用户又来了一句话"是图外面的事，由 FastAPI 层再 invoke 一次触发。这跟你给的外层 `while True` 是同一个语义，只是换了个地方放。

---

## 三、数据契约（**M0，写第一行代码前必须定死**）

三方共用同一份结构：agent 产出它、后端存它、前端渲染它。**这份 JSON 不定，界面就是空中楼阁。**

```jsonc
{
  "trip_id": "uuid",
  "session_id": null,              // 归属会话；⚠️ source=pasted 时**为 null**（粘贴不建会话，A37）
  "user_id": 1,                    // ⚠️ 先写死；M9 起由 token 决定。行程默认私有，接口必须校验归属（A27/A28）
  "title": "成都3日游·带爸妈",
  "destination": "成都",
  "source": "generated",           // generated | pasted
  "created_at": "2026-09-14T22:30:00+08:00",
  "updated_at": "2026-09-14T22:31:12+08:00",

  "days": [
    {
      "day": 1,
      "date": "2026-10-01",
      "theme": "市区古迹·走得少",
      "weather": {                     // 高德天气 extensions=all，实测只覆盖 4 天（从今天算起）
        "status": "ok",                // ok | unavailable
        "day_weather": "晴",           // dayweather
        "night_weather": "多云",       // nightweather
        "day_temp": 26,                // daytemp
        "night_temp": 18,              // nighttemp
        "day_wind": "东北风",          // daywind
        "day_power": "3",              // daypower
        "report_time": "2026-09-14 18:00:00",
        "note": null                   // unavailable 时写原因："出发日距今超过 3 天，无预报数据"
      },
      "stops": [
        {
          "seq": 1,
          "name": "武侯祠",
          "poi_id": "B00141xxxxxx",       // 高德 POI ID：证明它不是编的
          "lng": 104.0494,
          "lat": 30.6427,
          "coord_sys": "GCJ-02",
          "arrive": "09:00",
          "stay_min": 90,
          "leave": "10:30",
          "from_prev_km": 0,              // 距上一站距离（代码算）
          "from_prev_drive_min": 0,
          "cost_per_person": 50,          // 高德 biz_ext.cost，是"人均"不是"门票"
                                          // ⚠️ 实测：只有餐饮/咖啡类可靠（'73.00'），
                                          //    景点/酒店/交通返回空数组 [] → 一律 None
                                          // ⚠️ 且原始值是 `str | list` 双类型，解析层必须兼容
          "rating": "4.6",                // 可能为空
          "match_reason": "历史古迹，与偏好匹配",  // LLM 生成
          "checks": [
            // status 三态（**只有这三个值，没有第四个**）：pass / fail / unknown
            // ⚠️ level 与 status 分工：level 决定「要不要打回重排」，status 决定「显示什么颜色」
            //    hard + fail → 打回 L2 重排；soft + fail → 只提示，不阻塞
            {"level": "hard", "code": "poi_exists",   "status": "pass"},
            {"level": "hard", "code": "reachable",    "status": "pass"},
            // 实测：青城山返回的 biz_ext 里没有 open_time → 只能 unknown，不能标 pass
            {"level": "hard", "code": "open_today",   "status": "unknown",
             "msg": "该 POI 未返回开放时间字段"},
            {"level": "soft", "code": "need_reserve", "status": "fail",
             "msg": "旺季建议提前 3 天线上预约"}
          ]
        }
      ],
      "day_stats": {"distance_km": 12.4, "drive_min": 46, "walk_km": 4.2}
    }
  ],

  "summary": {
    "total_distance_km": 38.7,
    "total_cost_per_person": 320,
    "stop_count": 12,
    "hard_errors": 0,
    "soft_warnings": 3
  },

  "validation": {
    "rounds": 2,                     // 打回重排了几轮
    "fixed":     [{"code": "reachable",  "msg": "Day2 原第 3 站车程 1h50m，已换近点"}],
    "remaining": [{"code": "need_reserve", "level": "soft", "msg": "武侯祠需提前预约"}]
  }
}
```

### 字段来源必须分清（否则你会以为高德什么都给）

| 字段 | 来源 | 实测说明（2026-09-15 全部一手验证） |
|---|---|---|
| 名称 / 经纬度 | 高德 POI 搜索 | 直接给，GCJ-02。`location` 是 `'104.047992,30.646168'` 字符串，**要拆** |
| `poi_id` | 高德 POI 搜索 | 封闭世界校验的锚点。实测形如 `B001C07VJ2` |
| `alias` | 高德 POI 搜索 | ✅ **实测有值**（`'武侯祠\|武侯祠景区'`）—— 风险 3「封闭世界误杀」的解药就在这里 |
| `rating` | 高德 `biz_ext.rating` | 实测是**字符串** `'4.8'`；餐饮/酒店/景点/影院有，**地铁站等返回 `[]`** |
| `cost_per_person` | 高德 `biz_ext.cost` | 是**人均消费**，不是门票 —— 卡片上别写"门票"。⚠️ 实测**只有餐饮/咖啡类可靠**（`'73.00'`），**景点/酒店/交通全返回 `[]`** |
| `open_time` | 高德 `biz_ext.open_time` | ✅ **实测有值**（`'08:30-18:30'`）→ `open_today` 硬判据**真能做**。但可能**双时段**（`'07:30-17:00 08:00-16:30'` 旺季/淡季），青城山为空 → `unknown` |
| `photos` | 高德 POI 搜索 | ✅ **实测每个 POI 都有 3 张实景图**，可直接作首页轮播素材（A39 / 风险 15 解除） |
| `match_reason` | **LLM 生成** | 接口给不了 |
| `weather` | 高德天气接口 `extensions=all` | **实测覆盖 4 天（含当天）、从"今天"往后算**；拿不到时 `status: "unavailable"`，**不能标成 pass** |
| `from_prev_km` / `drive_min` | **代码算**（高德测距 `/v3/distance`） | 确定性。`type=1` 驾车，返回 `distance`(米) / `duration`(秒) |
| `checks[].status` | 代码（硬）/ LLM（软） | **三态**：pass / fail / **unknown**，见 `方案.md` 6.2 |
| 参考图里的「趣味度」 | ❌ **高德没有这个字段** | 那是原项目自己造的，我们删掉 |

**天气接口的两个隐藏条件**（官方文档 + 实测双确认）：

1. 参数是 **`city=adcode`（城市编码，如成都 510100）**，不是城市名 → 需要先用**地理编码**把"成都"转成 adcode。这个转换放在**天气工具内部**，不额外增加工具数量（A7 定的 3 个不变）
   ✅ 实测：`/v3/geocode/geo?address=成都市` → `adcode=510100`、`citycode=028`，一次调用搞定
2. `casts` 数组**实测固定 4 个元素**：当天 + 之后 3 天（2026-09-15 复测 2 次完全一致）。**没有"按指定日期查"的能力**

### 数据库表设计 ✅ 字段已过板（2026-09-15）

**业务表和 checkpoint 表分开，都在同一个 MySQL 库里。9 张业务表 + checkpoint 自动建表。**

| 表 | 归属模块 | 什么时候做 | 可见性 | 关键字段 |
|---|---|---|---|---|
| `users` | 用户 | **M9** | — | id / username / password_hash / nickname / avatar / created_at |
| `sessions` | AI 规划 | **M5** | 🔒 私有 | id / user_id / title / created_at / updated_at |
| `messages` | AI 规划 | **M5** | 🔒 私有 | id / session_id / role / content / meta_json / created_at |
| `trips` | AI 规划 | **M5** | 🔒 私有 | id / **session_id（可空）** / user_id / title / destination / source / trip_json / created_at |
| `favorites` | 用户 | **M9** | 🔒 私有 | user_id / target_type / target_id（收藏景点、收藏攻略共用一张） |
| `guide_posts` | 攻略社区 | **M10** | 🌐 **发布后公开** | id / **user_id（可空）** / **author_type** / title / **content_md** / destination / cover / **visibility** / **published_at** / created_at |
| `guide_pois` | 攻略社区 | **M10** | 跟随攻略 | guide_id / poi_id / name（攻略 ↔ 景点关联） |
| `comments` | 攻略社区 | **M11** | 跟随对象 | id / **target_type** / **target_id** / user_id / **author_type** / content / created_at |
| `likes` 🆕 | 攻略社区 | **M11** | 公开可数 | user_id / target_type / target_id / created_at + **唯一索引** |
| `checkpoints*` / `checkpoint_*` | LangGraph | **M5** | 🔒 私有 | **由 `langgraph-checkpoint-mysql` 自动建表，不要手写** |

**四个字段设计上的决策（会直接影响能不能跑）**

1. **`guide_posts.user_id` 必须可空** —— 系统发布的官方攻略**没有作者**，值就是 NULL。设成 `NOT NULL` 会在插入第一条系统攻略时直接报错。靠 `author_type`（`user` / `system`）区分，前端显示"官方"而不是空白用户名
2. **`trips.session_id` 必须可空**（A37）—— 界面二的**粘贴模式不建会话**，行程直接落库，`session_id = NULL`。设成 `NOT NULL` 会在第一次粘贴时直接报错。同时**访问控制载体要换**：`/trips/{id}` 不能靠 `trip.session_id` 反查会话（为 NULL 时反查不到），必须直接用 `owned_trip()` 按 `trips.user_id` 校验（见下节代码）
3. **`comments` 必须多态**（`target_type` + `target_id`），**不能用 `poi_id` 单挂** —— 它要同时能评论「景点」和「攻略」。`author_type` 同理：系统也能发评论（官方提示）
4. **点赞数先不存冗余字段** —— 列表页要显示"赞 12"，两条路：每次 `COUNT(*)`（简单、永远对）vs 表里存 `like_count`（快、但要保证同步）。**先选 COUNT** —— 数据量小的时候，冗余字段带来的"不一致 bug"远比那点性能值钱。等真出现"一页 100 条攻略"再谈缓存

**四条设计原则**

1. **`user_id` 字段现在就全表留好**，值先写死 `1`。登录功能可以后置，但**表结构不能后补** —— 否则所有 API 和前端请求都要返工
2. **行程存 `trip_json` 一个 JSON 列**，不拆表。理由：行程结构（天 → 站 → 校验）在 M1~M8 期间一定会反复改，拆表等于每次改结构都要动 schema
3. **攻略表一鱼两吃** ✅：`guide_posts.content_md` 既是产品数据，**又是评测集的原文**（`eval/fixtures/` 那 10~20 篇）。同一份数据，两个用途，不用维护两套
4. **只有 `guide_posts` 需要 `visibility`** ✅（A27）：其余表**一律默认全私有**，靠 `user_id` 就够。**不要给每张表都加 `visibility`** —— 会让人误以为存在公开读路径，从而在查询里漏写 `WHERE user_id`。`visibility` 默认 `private`，用户点「发布」才置 `public` 并写 `published_at`

### 鉴权：用什么证明"你是谁" ✅ 已定 JWT

**先纠一个最容易混的点：JWT 不带来任何隔离。** JWT 只回答"你是谁"；"你只能看自己的数据"是**每个查询都写 `WHERE user_id`** 换来的。加了 JWT 但某个接口漏了 `WHERE`，照样能看别人的数据。**这两件事必须分开理解**，也是面试最容易追问的点。

**四条路线对比（这就是"电商平台用什么"的答案）：**

| 方案 | 服务端存不存身份 | 客户端怎么带 | 能不能主动踢人 | 谁在用 |
|---|---|---|---|---|
| **Session + Cookie** | 存（内存 / Redis） | 浏览器自动带 cookie | ✅ 删 session 即可 | 传统网站、后台管理系统 |
| **JWT（无状态）** | ❌ 不存 | `Authorization: Bearer xxx` | ❌ **不能**，只能等过期 | 前后端分离、开放 API、教程最常讲 |
| **不透明 token + Redis** | 存 | 同上 | ✅ | **大厂主流** |
| OAuth2 / OIDC | 第三方 | 第三方签发 | 看实现 | 微信 / Google 登录 |

**电商（淘宝/京东这类）的真实做法不是"纯 JWT"**，是四层叠起来：

1. **短效 access token（约 15 分钟）+ 长效 refresh token** —— 把泄露窗口压小
2. **token 状态放 Redis**（或黑名单）—— 因为纯 JWT **签发后无法撤回**：用户改密码 / 被封号，旧 token 到期前照样能登，这在电商是硬伤
3. **网关统一校验**（Spring Cloud Gateway / Nginx+Lua），业务服务只从 token 里读 `userId`，不每个服务重写一遍鉴权
4. **授权是另一套**：RBAC（角色-权限）+ 数据权限（部门可见范围）+ 租户隔离（SaaS 的 `tenant_id`）

**JWT 结构（原理层，得知道）：**

```
header.payload.signature
  ↓
eyJhbGciOiJIUzI1NiJ9 . eyJ1aWQiOjEsImV4cCI6MTc2MH0 . 9f2c1a...
```

三段用 `.` 连接，**base64 编码 —— 是编码不是加密**。两个后果：

- **前端能直接解开看内容** → payload 里**绝不能放密码、手机号**
- **但改不了**（改一个字符签名就对不上）→ 所以能当身份凭证

一句话记法：**JWT 是一张"可读但不可改"的纸条。**

**我们这个项目怎么用（取舍）：**

| 企业级会做 | 我们要不要 | 为什么 |
|---|---|---|
| access + refresh 双 token | ❌ | 本地演示，token 泄露不在威胁模型内 |
| Redis 黑名单 / 不透明 token | ❌ | 只为"能踢人"引入 Redis，不划算 |
| **密码 bcrypt 加盐哈希** | ✅ **必做** | 成本极低，明文存密码是硬伤，面试必问 |
| `exp` 过期时间 | ✅ | 一行的事 |
| **payload 里只放 `user_id` + `exp`** | ✅ **必做** | 塞角色等可变信息 → 签发后就改不动 |
| **统一 `Depends(get_current_user_id)`** | ✅ **必做** | 隔离的落点，见下一节 |
| RBAC 角色权限 | ❌ | 全站只有"登录用户"和"系统"两种身份，用不上权限矩阵 |

> **结论：JWT 只用来"证明你是谁"，隔离永远是 `WHERE user_id` 的事。**

### 数据隔离实现（✅ 已拍板 A27/A28，详见 `方案.md` 8.3）

**统一走依赖，不逐个路由手写** —— 路由一多，漏写一个是必然而非偶然：

```python
# ---- M5 阶段：占位，先返回常量 1 ----
def get_current_user_id(...) -> int:
    return 1

# ---- M9 阶段：只改这一个函数，所有路由零改动 ----
def get_current_user_id(authorization: str = Header(None)) -> int:
    token = (authorization or "").removeprefix("Bearer ").strip()
    try:
        payload = jwt.decode(token, SECRET_KEY, algorithms=["HS256"])  # 验签 + 验 exp
    except jwt.PyJWTError:
        raise HTTPException(401, "invalid token")
    return int(payload["user_id"])

# 私有资源统一走它：拿不到 = 404（不区分"不存在"与"无权"，不泄露存在性）
async def owned_session(session_id: str, uid: int = Depends(get_current_user_id)):
    s = await db.get_session(session_id)
    if not s or s.user_id != uid:
        raise HTTPException(404, "not found")
    return s

# 行程单独一个：粘贴来的行程没有会话（A37），不能靠 session 反查
async def owned_trip(trip_id: str, uid: int = Depends(get_current_user_id)):
    t = await db.get_trip(trip_id)
    if not t or t.user_id != uid:
        raise HTTPException(404, "not found")
    return t

# 攻略的公开读是唯一例外：public 直通，private 才校验归属
async def visible_guide(guide_id: str, uid: int = Depends(get_current_user_id)):
    g = await db.get_guide(guide_id)
    if not g or (g.visibility != "public" and g.user_id != uid):
        raise HTTPException(404, "not found")
    return g
```

| 接口 | 依赖 | 说明 |
|---|---|---|
| `/sessions` `/sessions/{id}` `/sessions/{id}/chat` | `owned_session` | 会话列表只查自己的；**别人的 id 直接 404** |
| `/trips/{id}` GET / PATCH / recheck | **`owned_trip`** | **不通过会话反查**（粘贴行程 `session_id` 为 NULL，反查不到，A37）；**路线总览不能靠前端隐藏** |
| `/guides/{id}` | `visible_guide` | 唯一有公开读的接口 |
| `/favorites` | `get_current_user_id` + `WHERE user_id` | — |

⚠️ **`user_id` 从常量变真实值的那一刻是风险最高点（M9）** —— M5~M8 全部测试都跑在 `user_id=1` 上，漏掉的 `WHERE` **不会报错，只会静默泄露**。所以 M9 的验收判据是**双账号互测**：A 登录后拿 B 的 `session_id` / `trip_id` 直接请求，必须 404。

**MySQL 怎么起** ✅ 已拍：Docker。硬要求 **≥ 8.0.19**。建议 M0 之后就 `docker run` 一个 MySQL 8 先验证能连上，别拖到 M5 才发现起不来。

---

## 四、目录结构 💡

```
travel-agent/
├── 方案.md                  项目方案（做什么）
├── 技术方案.md              本文件（用什么）
├── 界面设计.md              界面设计稿（每页放什么 / 占位做到哪一步）
├── README.md                跑起来的说明
├── requirements.txt
├── docker-compose.yml       MySQL 8（只起数据库，应用本身不容器化）
├── .env / .env.example      密钥（.env 不入库）
├── DECISIONS.md             决策记录：决定 / 替代方案 / 代价
│
├── backend/
│   ├── app/
│   │   ├── config.py        配置读取（含 MODEL_REGISTRY 模型表）
│   │   ├── schemas.py       ★ 数据契约（Pydantic，第三节那份 JSON）
│   │   ├── llm.py           LLM 接入：工具循环用 flash（关思考）／规划节点开思考
│   │   ├── providers/       高德客户端（mock 与 real 同签名）
│   │   │   └── amap_facade.py  ★ MCP→HTTP 降级门面（对模型只暴露 1 个工具名）
│   │   ├── tools/           模型可调工具：搜地点 / 查天气 / 算距离
│   │   ├── graph/           state.py / nodes.py / graph.py
│   │   ├── store/           MySQL：用户 / 会话 / 消息 / 行程 / 攻略
│   │   └── api/             FastAPI 路由（chat 走 SSE）
│   │       └── deps.py      ★ get_current_user_id / owned_session / owned_trip / visible_guide
│   └── tests/               只测纯逻辑
│
├── frontend/                React 19 + Vite + TS + Tailwind 4（由「豆包」实现）
│   ├── package.json         锁定的依赖版本见 界面设计.md 第一节
│   └── src/
│       ├── index.css        ★ Tailwind 4 的 @theme（**没有 tailwind.config.js**）
│       ├── layouts/         导航栏 + 页脚（六个路由共用）
│       ├── pages/           home / spots / assistant / overview / guides / profile
│       ├── components/      轨迹卡 / 行程卡 / 校验卡 / 地图 / Day 页签 / HeroCarousel
│       ├── assets/          首页轮播照片（优先取高德 photos，见 界面设计.md 2.6）
│       ├── lib/api.ts       ★ 按 API 契约封装的请求层（交接文档里给）
│       └── lib/sse.ts       ★ fetch + ReadableStream 手写 SSE（EventSource 不支持 POST）
│
└── eval/                    评测
    ├── fixtures/            攻略原文（手动存 10~20 篇，与 guide_posts 同源）
    ├── cases/               结构化用例 + 已知错误标注
    └── report.py            跑分与报告（含双模型对比）
```

---

## 五、开工前必备清单

### A. 必须你去申请（我做不了）

**前置：账号 + 实名认证**

1. 打开 [lbs.amap.com](https://lbs.amap.com/) → 右上角注册 / 登录（手机号即可）
2. **首次要实名认证才能建 Key**：控制台 → 账号信息 → 选「个人开发者」→ 传身份证
   （个人认证是免费额度版，学习用途够用。**每日上限以控制台「配额管理」显示为准**，别按网上的数字估算）

**申请步骤：在同一个应用下「添加 Key」两次**

| 步 | 操作 | 关键项 |
|---|---|---|
| 1 | 控制台 → **应用管理 → 我的应用** → 右上角【创建新应用】 | 应用名称随意（如 `travel-agent`）；应用类型随意 |
| 2 | 在刚建的应用下点【添加 Key】（**第 1 次**） | 名称 `backend-ws`；**服务平台选「Web服务」**；IP 白名单**留空**（本地开发 IP 不固定，留空 = 不限制） |
| 3 | 提交后复制 Key | → `.env` 的 `AMAP_WEBSERVICE_KEY` |
| 4 | **同一个应用下**再点【添加 Key】（**第 2 次**） | 名称 `frontend-js`；**服务平台选「Web端(JS API)」** |
| 5 | 提交后该行会多出一列【安全密钥】→ 点开复制 `securityJsCode` | → `.env` 的 `AMAP_JS_KEY` + `AMAP_JS_SECURITY_CODE` |

**三个必踩的坑**

1. 🔴 **服务平台选错这个 Key 就废了** —— 选成 Android / iOS / 微信小程序，拿不到 Web 接口，调用时报「Key 无效」
2. 🔴 **JS Key 必须配安全密钥** —— 2021-12-02 之后申请的 JS key，不配 `securityJsCode` 直接报 `INVALID_USER_SCODE`
3. 🔴 **`_AMapSecurityConfig` 的位置** —— `window._AMapSecurityConfig = { securityJsCode: "..." }` 必须写在加载 JS API `<script>` **之前**；写在后面**不报错、静默失效**（更难查）。这条要进给豆包的交接文档

> ⚠️ 两个 Key **不能互换、不能互相替代**。**先申请第 1 个就能开工** —— M1~M3 全走 mock，前端一行代码还没写。
>
> 百炼 API Key **✅ 已有**（已从 kb_api 继承到 `.env`）。

### B. 必须你准备（半小时的事）

| # | 事项 | 说明 |
|---|---|---|
| **3** | **评测集：10~20 篇旅游攻略** | 浏览器打开复制存成 `.md`，放 `eval/fixtures/`。**要含具体行程（Day1/Day2…）、交通方式、时间**。先攒成都的。来源：马蜂窝游记 / 知乎 / 公众号 |
| **4** | **项目目录名** | 现在叫 `agent`（太泛），建议改 `travel-agent` |
| **5** | （可选）每篇攻略里你知道的问题 | 比如"这天排了 3 个相距 40 公里的点"——**这是评测集的"标准答案"**，有它才能算"抓错率" |

### C. 已就绪

| 项 | 状态 |
|---|---|
| 项目目录 | `D:\vscode Projiects\agent`（D 盘，没写 C 盘） |
| git 仓库 | ✅ 已 init，**每完成一步就 commit**，能回滚（`git log --oneline` 看全部） |
| Python 环境 | `.venv` 已建，依赖已装：`langgraph 1.2.11` / `langchain-core 1.6.3` / `langchain-openai 1.6.2` / `pydantic 2.13.5` / `pytest 9.1.1` |
| LLM Key | 已继承 kb_api 的百炼 key，写入 `.env` |

### D. 已完成 / 待做

**已做：**

- ✅ `.env.example` 已拆出高德两个 Key 的占位
- ✅ 旧 `PLAN.md` + 旧原型已归档到 `.workbuddy/archive/`（可恢复）
- ✅ `.env` 已按新结构重写（DeepSeek 模型名 + 思考开关 + 高德两 Key 位置）
- ✅ **`AMAP_WEBSERVICE_KEY` 实测可用** —— POI 搜索 / 地理编码 / 天气 / 测距**四个接口全通**（2026-09-15）
- ✅ **目录骨架**：`backend/{app/{providers,tools,graph,store,api},tests}` + `eval/{fixtures,cases}` + `docs/`
- ✅ **`backend/app/schemas.py`** 数据契约（含 SSE 事件契约）+ `docs/sample_trip.json` 真实数据样例 + `backend/tests/test_schemas.py`（**15 项自检全绿**）

**待做：**

- ⚠️ **`AMAP_JS_KEY` / `AMAP_JS_SECURITY_CODE` 待你核对** —— 你给的第 2 个值对 Web 服务接口返回 `INVALID_USER_KEY`(10001)，而 **JS Key 与安全密钥在这个接口上的表现完全一样，无法自动区分**。需要你去控制台看那一行的【服务平台】列，再把 Key 和安全密钥**分别**拷出来
- M0 剩余两件：`api.md`（接口契约）、`前端交接.md`（给豆包的交接包）
- `DECISIONS.md` —— 决策记录模板
- ⚠️ **起 MySQL 8 + 装 checkpoint 包跑最小样例**（Docker 崩过，越早暴露越好，别拖到 M5）

### E. 开工顺序（每步一个 commit）

```
0. 起 MySQL + 装 langgraph-checkpoint-mysql 跑最小样例 ← 不通立刻转原生装，别耗超过半天
1. 建目录骨架 + DECISIONS.md                          ← 无风险，先做
2. M0 三件套（第一道门，也是给豆包的交接包）：
   a. schemas.py      数据契约（Pydantic）
   b. api.md          接口契约 + SSE 事件表
   c. 前端交接.md      给豆包的完整交接文档
   ← 你过一遍字段，签了才算冻结
3. M1 双层循环 + 3 个 mock 工具 + **开 mock 模式**      ← 命令行能跑出第一份行程；豆包可并行开工
4. 每个里程碑结束：git commit
```

> ⚠️ **M0 之后契约变更 = 前后端双倍成本。** 所以 M0 不是"先随便定一版"，是"一次定扎实"。

---

## 六、风险与不确定项（不粉饰）

| # | 风险 | 影响 | 缓解 |
|---|---|---|---|
| 1 | **DeepSeek 思考模式 + tools → 400** | 工具循环直接崩（实测稳定复现） | **按节点关思考**（见第一节实测）；在 `llm.py` 里把参数写死，不给业务代码漏掉的机会 |
| 2 | **MySQL checkpoint 用的是第三方包** | 若包停更 / 不兼容 LangGraph 1.2 → M5 卡住 | M0 之后立刻起容器 + 装包跑最小样例；真不通就**降级**：业务表仍用 MySQL，checkpoint 退回 SQLite（两者可并存） |
| 3 | **封闭世界校验会"误杀"** | 模型写「武侯祠」而池子里是「武侯祠博物馆」就判失败 | 用高德 `alias` 字段 + 模糊匹配阈值；**阈值怎么定要你认** |
| 4 | **自主选工具 → 输出方差变大** | 跑两次工具调用序列不同 → 行程不同 → 评测数字会抖 | ⚠️ **`temperature` 只在"关思考"的节点上有效**；再叠加 **pass^k（多次一致性）**，不只看单次 |
| 5 | **高德字段缺失** | `rating` / `cost` / 开放时间不是每个 POI 都有 | 缺就隐藏，不显示"暂无"也不让 LLM 补（会编）；判据标 `unknown` |
| 6 | **模型会补全接口没给的字段** | 实测：接口没返回纬度，模型自己在正文里补了地址 | prompt 明令禁止；**校验层只认工具的返回值，不认模型正文** |
| 7 | **天气只覆盖 4 天**（本轮实测修正，原写 3 天） | 出发日在 4 天之后的行程，**天气校验天生不可用** | 显示 `unknown` 而不是 `pass`；评测报告单列 unknown 占比 |
| 8 | **多 agent 增加不确定性** | Subagents 单次请求**多 1 次模型调用**，子 agent **无状态**、每次重走全流程 | 先只切 1 个子 agent；只产出**结构化清单**不产出自由文本；各自记录独立 trace |
| 9 | **前端由外部工具（豆包）实现，本 AI 看不到它的代码质量** | 页面能跑但结构混乱；更危险的是**绕过契约自己加字段** | **契约即唯一接口**：交接文档给全 TS interface + API 契约 + SSE 事件表。验收只看三点 —— ① 字段严格等于契约 ② 空态/加载态/错误态都处理 ③ 严格模式下 SSE 不重复连接 |
| 10 | **范围已确定翻倍（用户/景点/攻略三个模块，M9~M11）** ✅ 已拍 | 时间被 CRUD 吃掉，agent 主线做不完 | **严格排序**：agent 引擎 → 界面 → 评测 → 三模块。且三个模块**必须改造成"主线延伸"**（个人中心挂 `trips`、攻略挂评测集与热启动、景点挂 `search_poi` 缓存），**挂不回主线的部分砍掉** |
| 11 | **评测集"标准答案"怎么定** | 攻略本身也可能是错的、过时的 | 只标注**能用硬判据验证的错**（距离/时间/不存在的地点）；主观问题不进评测 |
| 12 | **越权泄露（最容易被忽略）** | `user_id` 从常量变真实值时，漏一个 `WHERE` 就静默泄露他人会话/行程；**不报错、最难测** | 统一依赖 `owned_session()` / `owned_trip()`，不逐个路由手写；M9 验收 = **双账号互测改 id 必须 404**（见第三节末） |
| 13 | **公开攻略污染评测集** | `guide_posts` 与 `eval/fixtures/` 同源，用户发的脏数据直接进评测 | **不做审核**（A38，避免功能膨胀）；改为**单向同步**：评测集只读 `eval/fixtures/` 里人工筛过的原文，**永不回读线上库**。发布功能只影响产品侧 |
| 14 | **占位页写进编造的数据** | 抄参考设计会带入「21006 个景点」「热度 10.0」「门票 ¥405」这类**高德根本没有的字段**；面试官一问来源就露馅，整个项目可信度崩 | **占位零假数据**：没有来源的字段一律留白或显示引导文案。已明确禁抄三项（门票/热度/总数），见 `界面设计.md` 4.2 |
| 15 | **首页轮播图的素材来源与版权** —— ✅ **本轮已解除** | 网上随手找的图有版权风险，被问"这图哪来的"答不上来 | **实测高德 `photos` 字段每个 POI 都返回 3 张实景图**（`https://aos-comment.amap.com/...`，实测 200 / `image/jpeg`）→ 真数据、零额外版权成本、可追溯到具体 POI。不足时用无版权图库（Unsplash / Pexels）并在页脚标注来源。**禁止用 AI 生图充当景点照片**（会画出不存在的景点） |
| 16 | **高德 QPS 限流会打断工具循环**（本轮实测新发现） | 连续请求**第 4 个就触发 `CUQPS_HAS_EXCEEDED_THE_LIMIT`**；若被当成"搜不到"，会**触发假硬错 → 白白打回重排 → 甚至输出错误行程**。⚠️ 这类错误**不抛异常、只返回 `status=0`**，最容易被写漏 | 工具层**串行 + 1.2s 间隔**；`CUQPS_*` 必须**重试退避，而不是返回空列表**；同目的地结果**进程内缓存**；把 `status != 1` 统一转成明确的工具错误（见第一节末尾实测） |

---

## 七、剩下要做的（不是讨论题，是产物题）

**已拍板，不再问：** 架构路线 X ✅｜范围三模块做+改造（A26）✅｜必问项 7 项（A25）✅｜模型 DeepSeek（思考按节点分设）✅｜MySQL + Docker ✅｜MCP 门面降级 ✅｜数据隔离规则与公开攻略（A27/A28）✅｜SSE ✅｜**视觉风格 = 几何拼贴风（A35）** ✅｜**前端交豆包 + 契约冻结（A36）** ✅｜**粘贴不建会话（A37）** ✅｜**不做审核（A38）** ✅｜**首页轮播图（A39）** ✅｜9 张业务表字段过板 ✅｜Docker 已修好可跑 ✅

### 7.1 M0 三件套（= 给豆包的交接包）

| # | 产物 | 状态 | 说明 |
|---|---|---|---|
| **1** | **`schemas.py`** —— 数据契约（Trip JSON → Pydantic）+ **SSE 事件契约** | ✅ **已出，待你过字段** | `backend/app/schemas.py`；配套 `docs/sample_trip.json`（真实高德数据）+ `backend/tests/test_schemas.py`（**15 项自检全绿**） |
| **2** | **`api.md`** —— 接口契约（路径 / 方法 / 请求 / 响应 / 错误码）+ **SSE 事件表** | ⬜ 待做 | 豆包照这个写请求层。SSE 事件类型已在 `schemas.py` 里定死，直接引用 |
| **3** | **`前端交接.md`** —— 技术栈版本 / 设计 token / 页面结构 / 代码约束 / 参考片段 | ⬜ 待做 | 直接丢给豆包，你不用当传话筒 |

### 7.2 要定的数字（🔴 阻塞，但建议可直接用）

| # | 问题 | 建议 |
|---|---|---|
| 1 | L2 工具层上限 / L3 校验层上限 | **8 次 / 2 次**（L3 超了降级输出 + 列剩余风险） |
| 2 | 项目目录名 | **`travel-agent`**（`agent` 太泛） |

### 7.3 本轮已拍（✅ 三条修正，2026-09-15）

| 项 | 结论 | 连带影响（**已同步进本文档其他章节**） |
|---|---|---|
| 界面二粘贴**不建会话**（A37） | 粘完直接生成总览，行程直接落库 | ⚠️ `trips.session_id` 必须**可空**；`/trips/{id}` 的依赖由"反查会话"改为 **`owned_trip()`**（第三节末） |
| 公开攻略**不做审核**（A38） | 砍掉审核开关，避免功能膨胀 | 评测集改**单向同步**：只读 `eval/fixtures/` 人工筛过的原文，**永不回读线上库**（风险 13） |
| 首页 Hero 用**轮播图 + 真实照片**（A39） | 不用自绘几何插画 | 素材首选高德 `photos`（风险 15）；轮播库 **Swiper 14.2**（豆包实现，版本写进交接文档） |

**首页仍按"真做"处理**（占位形态已定稿，见 `方案.md` 十二节）。

---

## 八、已拍板决策（本轮新增）

| # | 决策 | 依据 |
|---|---|---|
| A26 | 三个管理模块**都做，但改造成"主线延伸"**，排 M9~M11 | 用户："写是因为要完善项目，做个半成品怎么进简历" |
| A27 | **数据归属**：会话 / 消息 / 行程（路线总览）/ 收藏 **私有**；攻略**发布后公开** | 用户："用户层记得做用户隔离，不能看到别人的路线总览和 agent 规划，但攻略发布后是大家都能看到的" |
| A28 | **隔离写在后端**，统一 `owned_session()` / `owned_trip()` / `visible_guide()` 依赖；验收 = 双账号互测改 id 返回 404 | 由 A27 推导 |
| A37 | 界面二**粘贴不建会话**：粘完直接校验 → 生成路线总览（`source=pasted`，`session_id=NULL`） | 用户："粘贴时自动建会话，**不用建会话**，这里会放已经讨论完的攻略直接生成总览就行了" |
| A38 | 公开攻略**不做审核**（砍掉审核开关） | 用户："攻略不用审核（复杂了）" |
| A39 | 首页 Hero = **轮播图 + 真实景点照片**（不用自绘几何插画） | 用户："不用插画，那是轮播图（网上找图片放进去）" |
