# 智能旅游规划系统 · 项目方案

> 本文件只回答「**做什么、给谁用、怎么用**」。技术选型见 `技术方案.md`。
>
> 状态标记：**✅ 已拍板**（讨论中你当场确认过）｜**⚠️ 待拍板**｜**💡 建议**（我提的，你没确认）
>
> 最后更新：2026-09-15

---

## 一、一句话定位

> **给一份行程——不管是 agent 现生成的、你自己排的、还是从网上抄的——用真实世界的数据验证它到底能不能走通；走不通就改，并说清改了哪、为什么改。**

市场上一万个「AI 帮你生成行程」，几乎没有「AI 帮你验证行程能不能走通」。**生成的结果看着都像，校验的对错有客观答案。**

---

## 二、这个项目要证明什么

填空：**「这个项目要证明我会 _____」**

答：**让 agent 在外部数据可查、物理约束真实存在的环境里自主决策，并且能用可量化的评测说清它强在哪、弱在哪。**

Agent 的 loop 是否真必要，三条判据：

| 判据 | 本项目 | 证据 |
|---|---|---|
| 信息不在一次上下文里 | ✅ | 景点是否存在、开不开放、两地多远、那天下不下雨，都得现查 |
| 需要外部工具验证 | ✅ | 模型编一个"武侯祠东门"很容易，只有 POI 接口能证伪 |
| 有环境反馈 | ✅ | 校验节点打回 → 重排 → 再验 |

**三条全占 → 这个 loop 是刚需，不是装饰。**

---

## 三、两个入口，一套内核 ✅

| 入口 | 在哪个界面 | 用户给什么 | 系统干什么 |
|---|---|---|---|
| **冷启动** | 界面一（对话） | 一句需求（"10月1号去成都3天，带爸妈"） | 先追问补全 → 生成 → 拿真实数据回头验 → 改到走得通 |
| **热启动 A** | 界面一（对话） | 直接粘一段现成行程 | 识别为行程 → 跳过生成 → 直接验 → 改 |
| **热启动 B** | 界面二（路线总览） | 粘一段攻略/行程 | 同上，生成后留在界面二看结果 |

**内核只有一个：`验证 → 定位问题 → 修正 → 再验证`。**

「生成」= 先造一份再验；「校验」= 跳过制造直接验。A 和 C 不是两套系统，只是多开了一个入口。

---

## 四、用户流程

### 4.1 冷启动（主路径）

```
进入「AI 路线规划助手」
   ↓
agent 首条消息：引导用户提供必问项（目的地 / 时间 / 天数 / 同行人 / 特别要求）
   ↓
用户回答（可多轮，agent 追问补齐缺口）
   ↓
agent 调工具收集事实（搜景点 / 查天气 / 算距离）
   ↓ 「实时显示 agent 在干什么」← 轨迹卡
生成行程 → 自动校验 → 硬错打回重排（≤N 次）→ 软风险记下来
   ↓
输出：行程卡（每天一行 + 改按钮）+ 校验卡（绿=已修 / 黄=待确认）
   ↓
用户点「查看路线总览」→ 跳界面二
```

### 4.2 热启动（粘贴）

```
界面一：直接往聊天框粘一段行程/攻略 → agent 自己认出这是行程不是需求 → 校验 → 出报告
界面二：空手进来 = 粘贴模式 → 粘进去 → 生成 → URL 换成新 trip_id
```

💡 建议：界面二粘贴时**自动建一个会话**（标题"粘贴的成都行程"），这样两条路径统一成同一个数据模型——**路线总览永远挂在某个会话上**，用户想改时永远有地方说。

### 4.3 必问项清单 ⚠️ 待你定稿

**先分清两个概念**（否则会写成一个会卡死的 agent）：

- **必问** = 这条一定要主动问用户
- **阻塞** = 用户答不上来时，流程**必须停下**等着
- **必问 ≠ 阻塞**：大部分项问完可以缺省，**不能让 agent 卡在那**

| # | 字段 | 必问 | 阻塞 | 缺省值 | 影响什么 |
|---|---|---|---|---|---|
| 1 | `destination` 目的地 | ✅ | **✅ 硬** | — | 高德的 `city` 参数、地理编码取 adcode、标题 |
| 2 | `date` 出行日期 | ✅ | **✅ 硬** | — | 天气校验（只覆盖 3 天）、星期、是否节假日 |
| 3 | `days` 天数 | ✅ | ❌ | 3 天 | 行程分天、天气覆盖天数 |
| 4 | `travelers` 同行人 | ✅ | ❌ | 成人 ×2 | **走路量阈值**（带老人小孩要放宽）、节奏 |
| 5 | `budget` 预算 | ✅ | ❌ | 不设限 | 人均参考对比（⚠️ 高德给的是**人均消费**不是门票，只能粗算） |
| 6 | `preferences` 旅行偏好 | ✅ | ❌ | 综合 | 搜什么关键词（历史古迹 / 自然风光 / 美食 / 亲子 / 摄影） |
| 7 | `notes` 特别要求 / 禁忌 | ✅ | ❌ | 无 | 忌口、必去、要避开的类型 |

**这 7 项就是代码里的常量集合**（对应 A8）：

```python
REQUIRED_FIELDS = {"destination", "date", "days", "travelers", "budget", "preferences", "notes"}
BLOCKING_FIELDS = {"destination", "date"}   # 只有这两个卡流程
```

LLM 生成首条消息时输出结构化 `{greeting, questions[{field, text}]}`，**代码校验 `field` 集合 == `REQUIRED_FIELDS`**：措辞自由在 `text`，一项不漏由代码保证。

**⚠️ 预算这条要提前说清**：高德的 `biz_ext.cost` 是**人均消费**，且**门票数据大面积缺失**。所以预算只能做到"人均参考的粗略对比"，**做不到"精确控制人均不超 500"**。界面上如实写"人均参考"，不承诺精度。

---

## 五、界面结构 ✅

### 5.1 导航栏（6 项，只实现 2 项）

| 位置 | 项 | 状态 |
|---|---|---|
| 1 | 首页 | ⚠️ 占位（💡 建议做成真的：入口卡片 + 最近行程，半天工作量） |
| 2 | 旅游景点 | 占位 |
| 3 | **AI 路线规划助手** | **核心 ①** |
| 4 | **路线总览** | **核心 ②** |
| 5 | 旅游攻略 | 占位 |
| 6 | 个人中心 | 占位 |

### 5.2 界面一：AI 路线规划助手

```
┌──────────────┬──────────────────────────────────┐
│  历史栏       │  对话区                           │
│              │                                  │
│  成都3日游    │  [agent] 你好，先告诉我…（必问项）  │
│  10-01        │  [user]  10月1号去成都3天带爸妈    │
│              │  [轨迹卡] 已查：景点搜索×3 天气×1  │
│  粘贴的行程   │  [行程卡] 每天带 ☀26/18℃    │
│  09-30        │  [校验卡] ✅2 项已修  ⚠️1 项待确认  │
│              │  ─────────────────────────────   │
│              │  [输入框]                         │
└──────────────┴──────────────────────────────────┘
```

- 左历史栏 ✅ / 右聊天框 ✅
- 历史会话**点开能继续改**（活会话，不是只读快照）✅
- agent 首条引导消息：**LLM 生成，但必问项一个不漏** ✅
  - 实现约束：LLM 输出**结构化数据**（`greeting` + `questions[{field,text}]`），代码校验 `field` 集合 == 必问项集合，缺了就重试/补齐。**措辞自由在 text，一项不漏由代码保证。**
- **每一天要带当天天气** ✅：行程卡里每天显示**白天/夜间天气 + 白天/夜间温度 + 风力**，不是只给一个"当天天气"

> ⚠️ **天气这条有个硬约束，先说清**（官方文档实查）：
> 高德天气接口 `extensions=all` 返回的 `casts` 数组**只有 3 个元素——当天、第二天、第三天**，而且**从"今天"往后算，不是从出发日算**。
>
> | 情况 | 能不能给天气 |
> |---|---|
> | 出发日在 3 天内，且行程 ≤3 天 | ✅ 能给 |
> | 出发日在 3 天以后（比如今天 9/14 规划 10/1） | ❌ **查不到** |
> | 行程超过 3 天 | ⚠️ 只有前 3 天有 |
>
> 所以界面必须能显示"**未知**"，不能因为没数据就显示"通过"。**规划远期行程时，天气这项校验是天生不可用的。**

### 5.3 界面二：路线总览

```
┌───────────────────────┬──────────────────────────┐
│  行程表格              │   高德地图                │
│  序号 时间 名称 距离   │   ①..②..③ 只画当天       │
│  1    09:00 武侯祠  —  │   编号 marker + 连线      │
│  2    11:30 锦里  1.2km│                          │
│  …                    │                          │
├───────────────────────┴──────────────────────────┤
│  顶部统计条：总里程 / 人均参考 / 站点数 / 硬错数    │
├──────────────────────────────────────────────────┤
│  按天分组卡片（截图1 那种）                        │
│  Day1 市区古迹 ── 路线逻辑文案 ── 每站 ↓           │
│    ① 武侯祠  [距上一站 1.2km · 车程 6 分钟] [🟢]   │
│       人均参考 ¥50 · 匹配理由「历史古迹」          │
│    ② 锦里    […距离…] [🟡 需预约]                 │
└──────────────────────────────────────────────────┘
```

- **地图左上角放 Day 页签** ✅：`[Day1] [Day2] [Day3]`，**页签数量按实际天数生成**（5 天行程就 5 个）
  - **选中哪天，地图上只画那天的路线**，其他天的 marker 和连线不显示
  - 为什么必须这么做：12 个站全画一张图上必然糊成一团；按天分页后每页 3~4 个站，看得清
  - 💡 建议：切页签时**左侧表格同步切到那天**，两边始终指同一天
- **左侧表格 + 右侧地图** ✅，下方接按天分组卡片
  - 表格行也带**当天天气**，与卡片一致
- **不带历史栏**（一次性方便使用）✅
- 与界面一之间加**跳转按钮**（有行程才出现）✅
- 能改：**调顺序 / 删站 / 改时间** ✅（先做这些，"换站/加站"后议）

### 5.4 界面二"能改"的真实链路

```
拖拽/删除/改时间
   → 前端乐观更新（先动，手感即时）
   → 调后端 PATCH /trips/{id}
   → 后端确定性重算（相邻距离、车程、总里程、时间轴冲突）
   → 返回校正后的数据
```

**关键分工 ✅：手动改只触发「确定性重算」，不触发 LLM 软校验。**
原因：LLM 一次 3~8 秒，拖一下等 5 秒体验直接崩。想跑软校验时点【重新深度检查】按钮。

---

## 六、校验体系（项目的核心）

**总原则 ✅：有唯一答案的交给代码，没有唯一答案的交给 LLM。**
不这么分，同一个行程跑两次会一次说行一次说不行——评测数字就没意义了。

| 层 | 校验项 | 谁判 | 判据来源 | 判定后 |
|---|---|---|---|---|
| **硬** | 景点是否真实存在 | 代码 | POI 接口候选池 | **打回重排** |
| **硬** | 两地当天能否到达 | 代码 | 距离/耗时接口 | **打回重排** |
| **硬** | 那天是否开放 | 代码 | POI 开放时间 | **打回重排** |
| **硬** | 天气是否冲突 | 代码 | 天气接口 | **打回重排** |
| **硬** | 累计走路量 vs 同行人 | 代码 | 坐标累计 | **打回重排** |
| **软** | 是否需要预约 | LLM | 模型知识 | 提醒用户 |
| **软** | 旺季排队时长 | LLM | 经验判断 | 提醒用户 |
| **软** | 是否适合带老人 | LLM | 经验判断 | 提醒用户 |
| **软** | 整体可行不可行 | LLM | 综合判断 | 汇总呈现 |

**硬错打回、软风险提醒。**

### 6.1 范围：v1 就做全 ✅

硬判据 **5 类** + 软判据 **4 类**，一次做全，不分批。代价是 M3 这一档会变重（见第九节）。

### 6.2 但"做全"必然撞上一个问题：判据得有三态

有些判据依赖的数据**拿不到**。这时不能只给"通过 / 不通过"两个选项——会逼出一个错误答案。

| 状态 | 含义 | 界面 | 处置 |
|---|---|---|---|
| ✅ **通过** | 有数据，且成立 | 🟢 绿 | 继续 |
| ❌ **不通过** | 有数据，但不成立 | 🔴 红 | **打回重排** |
| ⚪ **无法判定** | **依赖的数据拿不到** | ⚪ 灰 | **不打回、也不算通过**，如实标注 |

**哪些判据会落到"无法判定"：**

| 判据 | 什么时候无法判定 | 确定性 |
|---|---|---|
| 天气冲突 | **出发日在 3 天之后，或行程超过 3 天**（高德只给 3 天预报，见 5.2） | 已查实 |
| 那天是否开放 | 该 POI 返回里**没有开放时间字段**（💡 高德 `opentime` 字段是否覆盖全部景点类 POI，**我没实测**，需 key 到手后打一次真实返回确认） | 待验证 |
| 景点是否真实存在 | 有候选池就一定能判 → 不会无法判定 | — |
| 两地能否到达 | 有坐标就一定能算 → 不会无法判定 | — |
| 累计走路量 | 有坐标就一定能算 → 不会无法判定 | — |

**为什么这条必须有**：只有 pass/fail 时，缺数据会被迫二选一——**选"通过"是撒谎，选"不通过"是误打回，让 agent 白白重排几轮**。三态是唯一诚实的做法。

而且它直接对上评测：**评测报告要单列"无法判定"的比例**，否则"通过率"会虚高。

---

## 七、Agent 形态

| # | 决策 | 选择 | 代价 |
|---|---|---|---|
| A4 ✅ | **自主权层次** | **有约束的自主**：给工具 + prompt 写明工作流，但调不调、调几次、够了没由模型定 | prompt 要写细 |
| A5 ✅ | **实现路径** | **手写 StateGraph**（不用 `create_agent` 高层封装） | 样板代码自己写 |
| A6 ✅ | **循环终点** | 工具循环只**收事实**，循环结束后另起**固定节点**出 JSON | 多一次 LLM 调用 |
| A7 ✅ | **工具集（v1）** | **就这 3 个够用**：搜地点 / 查天气 / 算距离 | 工具越少模型越不易选错；细节（adcode、开放时间）在**工具内部**处理，不额外加工具 |
| A15 ✅ | **循环骨架** | **双层循环**（见 7.1） | 见 7.4 的取舍 |
| A19 ⚠️ | **多 agent 形态** | 待拍：Subagents / Handoffs / Skills / Router（见 7.3） | — |

### 7.1 循环骨架（你给的）

```python
while True:                                          # 外层：会话级
    while has_more_tool_calls or pending_messages:   # 内层：单轮
        run_tool_calls()
        drain_pending_messages()                     # 用户插队（steering）
    if not get_follow_up_messages():
        break
```

**两层各管什么：**

| 层 | 续跑条件 | 在我们的项目里是什么 |
|---|---|---|
| 外层 | `getFollowUpMessages()` 有货 | 用户又说了句话 → 接着改这份行程 |
| 内层 | `has_more_tool_calls` | 模型又发起工具调用 → 继续跑 |
| 内层 | `or pending_messages` | **用户趁 agent 干活时插话**（steering）→ 消息插进上下文，下一圈生效 |

**这份骨架正好把我们前面的三条设计落到了实处**：外层 = "历史会话能继续改"；内层 = "模型自主决定查什么、查几次"；`pending_messages` = "用户随时能把 agent 掰回来"。

### 7.1.1 把校验回环叠上去，其实是三层

```
L1  会话层    while True:  靠 follow-up 续跑          ← 你骨架的外层
    └─ L2  工具层   while has_more_tool_calls:        ← 你骨架的内层
           agent_step ⇄ tool_step
                 ↓ 模型判定事实已够
           generate_plan
                 ↓
           L3  校验层   check_plan ─ 不通过 → repair → 回 L2 重查
                              └ 通过 → render → 出结果
```

| 层 | 谁说了算 | 管什么 | 上限 |
|---|---|---|---|
| **L1 会话层** | 用户 | 还要不要接着改 | 无（用户说了算） |
| **L2 工具层** | 模型 | 查什么、查几次、够了没 | ⚠️ 💡 8 次，且禁止同参数重复调 |
| **L3 校验层** | 代码 + LLM | 行程合格吗、哪里要改 | ⚠️ 💡 2 次，超了**降级输出**（列风险清单）而不是死磕 |

**注意 L3 打回后回的是 L2**（A9）：重排前要重新走一遍工具循环，让模型能**补查缺的数据**。否则它只能硬凑一个改法。

### 7.2 骨架和 LangGraph 的对应关系

| 骨架里的东西 | LangGraph 里的等价物 |
|---|---|
| 内层 `has_more_tool_calls` | 条件边 `should_continue`：有 `tool_calls` → `tool_step`，没有 → 出 |
| 内层 `pending_messages`（插队） | `interrupt()` 中断运行 + `update_state()` 注入消息，靠 checkpointer 恢复 |
| 外层 `getFollowUpMessages()` | 会话级循环：新消息 = 新一次 `invoke`，状态从 SQLite checkpoint 捞回来 |
| 子 agent 调用 | 把子 agent 做成 **Subgraph**，或包成一个 `task` 工具 |

**概念能一一对上，但写法是两种范式**（见 7.4）。

### 7.3 多 agent：官方现版就这四种（实查 docs.langchain.com）

| 形态 | 怎么运作 | 适合什么 |
|---|---|---|
| **Subagents** | 主 agent 把**子 agent 当工具**调，所有路由都过主 agent | 上下文隔离、多领域并行、要集中控制 |
| **Handoffs** | tool call 改状态变量 → 切换 agent，或换它的工具/prompt | 多轮对话里角色会变 |
| **Skills** | 单个 agent 掌控，按需加载专业 prompt/知识 | 单 agent 够用、只想省上下文 |
| **Router** | 先分类输入 → 派给专门 agent → 结果综合 | 输入类型能清晰分类 |

官方文档同时给了一句实话（原话）：

> *"not every complex task requires this approach—a single agent with the right (sometimes dynamic) tools and prompt can often achieve similar results."*

**你说的"多 agent 调用"= Subagents 形态**（主 agent 调子 agent）。官方给的代价数据：

| 场景 | Subagents 表现 | 含义 |
|---|---|---|
| 单次请求 | **比单 agent 多 1 次模型调用** | 结果要回流主 agent，这层开销换集中控制 |
| 重复请求 | 每次 4 次调用，**无状态** | 上下文隔离强，但每次都重走全流程 |
| 多领域任务 | **5 次调用 / ~9K token（最省）** | 子 agent 各带上下文，互不污染 |

**子 agent 怎么切（✅ 已拍板 A19：只切 1 个）：**

| 子 agent | 干什么 | 判断 |
|---|---|---|
| **搜索 agent** | 找景点/餐饮，自己决定搜几轮、换几个关键词 | ✅ **就它了**：搜索是"多轮试错"的活，独立上下文能放开手 |
| 校验 agent | 跑判据，返回问题清单 | ❌ 先不做：**硬判据是纯代码，不需要 agent**；软判据那点活不值得多一次调用 |
| 天气/距离 agent | — | ❌ 太薄，一次调用的事，封成子 agent 是浪费 |

一次只切一个 —— 切两个出问题，你分不清是哪个坏的。

### 7.4 ✅ 已拍板：架构路线 **X（全 LangGraph）**

> **结论**：选 **X**，你原话"一那就X"。
> 下面保留当初的三方对比，作为**决策依据**存档，不再探讨。

你给的骨架是**命令式 while**，已拍板的 A5 是**声明式 StateGraph**——这不是二选一的口舌之争，它决定后面所有代码怎么写。

| | **X. 全 LangGraph 声明式** | **Y. 全手写命令式**（骨架原样） | **Z. 混合** |
|---|---|---|---|
| 内层工具循环 | 条件边 | `while has_more_tool_calls` | 条件边 |
| 用户插队 | `interrupt()` + `update_state()` | `pending_messages` 队列，天然支持 | `interrupt()` |
| 外层续跑 | 图的循环 / 服务层再 invoke | `while True` + follow-up | 服务层再 invoke |
| 子 agent | Subgraph | 直接递归调一个函数 | Subgraph 或函数 |
| **白拿的** | 持久化、中断恢复、流式事件、可视化 | 无，全自己写 | 持久化、中断、流式 + 命令式的直观 |
| **代价** | 插队要绕 interrupt 一层 | **"刷新状态还在""实时看 agent 在干什么""历史会话继续改"这三条全得自己造** | 两种范式混着，调试要分清"现在是图在跑还是循环在跑" |

💡 **我倾向 Z**，理由是三条需求已经把你锁住了：

1. **"历史会话能继续改"** → 必须持久化会话状态 → LangGraph checkpointer 白送 SQLite
2. **"实时显示 agent 在干什么"** → 必须流式事件 → LangGraph 的 stream 白送
3. **"刷新页面状态还在"** → 同 1

纯 Y 就得自己实现这三样，那是**又一个项目的量**。但纯 X 的"插队"要用 interrupt 绕，不如你骨架里 `pending_messages` 直观。

**如果你更看重"和 Claude Code / pi-agent 同构的循环"**，那 Y 是合理选择，但请认清：**代价是把 LangGraph 白送的东西全接过来自己写**，而且简历上"用 LangGraph"这个标签会变弱。

---

## 八、范围边界

### 8.1 明确不做

| 不做 | 理由 |
|---|---|
| **爬虫抓攻略** | 实测：携程跳人机验证、马蜂窝返 202 挑战、小红书要登录、去哪儿空响应。攻略手动存 10~20 篇就够（而且这些原文**同时是攻略模块的数据源**，见 8.2） |
| **Neo4j / 知识图谱** | 标题党（原视频简介里 Neo4j 只出现在标题）+ 要多起一个服务；旅游 POI 用经纬度 + 聚类就够 |
| **3D 街景、天气图层** | 炫技、不加分、易出 bug |
| **手动加站 / 换站** | 必须从真实 POI 候选池里挑，要一整套搜索交互，成本高且与界面一的 agent 职责重叠 |
| **RBAC 角色权限（角色表 / 权限矩阵 / 管理后台）** | 全项目只有两种身份：**登录用户**和**系统**。用户之间是"平级互不可见"，靠 `user_id` 过滤就够了 —— 不需要"角色 → 权限"那套矩阵 |
| ~~用户认证~~ | ~~不做~~ → ✅ **已改为要做**（M9）。见 8.3 |

### 8.2 ✅ 范围扩展：三个管理模块（已拍板 A26）

除了「AI 路线规划」，还有三个模块 —— **原来判定"跟 agent 无关"，现在决定都做，但每个都要改造成"主线延伸"。**

| 模块 | 功能 | 跟 agent 的关系 | 难度 |
|---|---|---|---|
| 用户管理 | 注册 / 登录 / 个人中心 / 我的收藏 / 我的攻略 | **有一个交集**：个人中心 = `trips` 表的展示 | 教程级 CRUD |
| 景点管理 | 景点列表 / 详情 / 搜索 / 收藏 / 评论 | **有一个交集**：数据源复用高德 `search_poi`，不手写 POI | 教程级 CRUD |
| 攻略管理 | 攻略列表 / 详情 / 发布 / **评论 / 点赞** / 关联景点 | **有一个交集** 👇 | 教程级 CRUD |

**攻略模块 = 一个旅游攻略社区** ✅（2026-09-15 用户澄清）

不是"后台里的攻略 CRUD"，是一个**社区**：

| 特性 | 说明 |
|---|---|
| 谁发的能看到 | 列表/详情**显示作者**（昵称 + 头像） |
| 公开发布 | 发布后**所有人可见**（唯一有公开读的数据） |
| **两种来源** | `author_type` = `user`（用户发布）｜`system`（系统发布，如官方示例攻略） |
| **评论** | 有系统评论，也有用户评论（同 `author_type` 区分） |
| **点赞** | 用户对攻略/景点点赞，**一人一次**（唯一索引兜底） |

⚠️ **系统发布的内容 `user_id` 为 NULL**，前端显示成"官方"而不是空白用户名 —— 所以 `user_id` 必须**可空**，不能设 `NOT NULL`。

**一个白捡的复用** ✅：攻略模块的 `content_md` **就是评测集原文**。你本来就要手存 10~20 篇攻略当评测集，同一份数据直接进 `guide_posts` 表，**不用维护两套**。

**但代价必须说清楚：**

1. **工作量**：三个模块加起来 ≈ 一个完整毕设系统的体量，**不比 agent 主线小**
2. **前端页面从 2 个涨到 7~8 个**，React 学习成本翻倍
3. **面试官不看这些**。简历上写"用户注册登录 CRUD"是减分项（等于告诉对方你没别的可写）

**✅ 已拍板（2026-09-15）：三个模块都做，但每个都要能接回主线。**

| 档 | 做什么 | 定位 |
|---|---|---|
| M1~M5 | agent 引擎（工具循环 + 校验 + 服务化） | **难点的全部** |
| M6~M7 | 两个核心界面（对话 + 路线总览） | **效果的全部** |
| M8 | 评测（pass@k / pass^k 报告） | **简历差异化的全部** |
| —— | —— 做到这里，项目已经完整可讲 —— | |
| M9 | 用户管理：注册 / 登录 / **个人中心（我的行程 / 我的收藏 / 我的攻略）** | **主线延伸**：个人中心 = `trips` 表的展示，不是纯 CRUD |
| M10 | 攻略管理：列表 / 详情 / 发布 + **「拿这篇去校验」** | **主线延伸**：攻略库 = 评测集录入入口，校验按钮 = **热启动入口** |
| M11 | 景点管理：详情 / 搜索 / 收藏 / 评论 | **主线延伸**：数据来自 `search_poi` **缓存**，不手写 POI 数据、不建 CRUD |

理由：**agent 主线是唯一的护城河，CRUD 是体力活。** 先挖护城河——万一时间不够，项目在 M8 就是一个完整作品；反过来先做 CRUD，时间被吃掉后 agent 只做了一半，那才是真废。

**改造的关键**：每个模块都要挂回主线（个人中心挂 `trips`、攻略挂评测集与热启动、景点挂 `search_poi` 缓存）。**挂不回去的就是纯体力活，那部分砍掉。**

### 8.3 数据归属与可见性 ✅ 已拍板（2026-09-15）

| 数据 | 归属 | 可见性 | 实现 |
|---|---|---|---|
| `sessions` / `messages`（会话与规划过程） | 某用户 | **私有** | 所有查询强制 `WHERE user_id = :current_user` |
| `trips`（含**路线总览**） | 某用户 | **私有** | 同上 |
| `favorites` | 某用户 | **私有** | 同上 |
| `guide_posts` | 发布者（**系统内容 `user_id` 为 NULL**） | **发布后公开** | 加 `visibility`（`private`/`public`）+ `published_at` + `author_type`；`public` 的进公海，谁都能看 |
| `comments` | 挂在景点/攻略下 | **跟随被评论对象** | 多态挂载（`target_type` + `target_id`）；同样有 `author_type`（系统/用户） |
| `likes` 🆕 | 某用户 | **公开可数** | 一人一次由**唯一索引** `(user_id, target_type, target_id)` 兜底，不靠代码判重 |

**两条硬规则：**

1. **隔离必须在后端做，不能靠前端隐藏。** 前端不发请求 ≠ 后端拿不到数据；`GET /sessions/{id}` 少一个 `where user_id` 就是一个**越权漏洞**（改个 URL 数字就能看别人的）。所有私有数据查询统一走**一个** `owned_by_current_user()` 依赖，不逐个路由手写——**路由一多，漏写一个是必然而非偶然**。
2. **`user_id` 占位要在 M5 就埋好，不是 M9。** 从 M5 起路由一律用 `Depends(get_current_user_id)`（先返回常量 `1`），M9 只改这一个函数去解析 JWT，**所有路由一行不用动**。若 M5 直接写死字面量 `1`，M9 就得逐个路由翻找修改——这是最容易漏、最难测的一类 bug。

**"攻略发布后公开"到底要改什么？** 一句话：**别的表都是"只给本人看"，只有攻略是"发出去给大家看"，所以只有它需要多几个字段。**

| 字段 | 取值 | 大白话 |
|---|---|---|
| `visibility` | `private` / `public` | 这条攻略是**草稿**还是**发出去了** |
| `published_at` | 时间 | 发布那一刻；列表按时间排序要用 |
| `author_type` | `user` / `system` | 是**用户发的**还是**系统（官方）发的** |

其余表（会话 / 消息 / 行程 / 收藏）**不加 `visibility`** —— 它们压根没有"公开"这个选项，加了反而让人误以为存在公开读路径，从而在查询里漏写 `WHERE user_id`。

**唯一一个额外顾虑**：公开攻略和评测集是**同一份数据**（`guide_posts.content_md` = `eval/fixtures/`）。用户随手发一篇烂攻略，评测集就被掺了脏数据 → 所以留一个**审核开关**，**默认自动放行**，需要时才拦。

---

## 九、里程碑与验收判据

每档都能独立停，停下时项目是完整可讲的。

| 档 | 内容 | 完成判据（做到哪算过） |
|---|---|---|
| **M0** | **数据契约定案**（Trip JSON 结构） | 前后端 + agent 三方对同一份 JSON 无歧义 |
| **M1** | 骨架：**双层循环** + 3 个 mock 工具 + 固定节点出 JSON | 命令行打印出 3 天行程；**trace 里能看到模型自己决定调了哪几个工具、调了几次**；手动塞一条 `pending_message`，能看到它下一圈被吃进去 |
| **M2** | 真实数据源（高德 Web 服务） | 每个景点都能在工具返回的候选池里找到，一个都不是模型编的；天气能拿到逐日预报（3 天内） |
| **M3** | **校验体系**：硬 5 类 + 软 4 类 + **三态** + 打回上限 | 故意喂一个冲突约束，**能看到被硬错打回重排**；**无数据的判据显示"无法判定"而不是"通过"**；循环有上限、不死循环 |
| **M4** | **多 agent**：只切 1 个搜索子 agent | 主 agent 的 trace 里能看到它调了子 agent，且子 agent 上下文与主 agent 隔离 |
| **M5** | 服务化：FastAPI + **MySQL checkpoint** + **SSE 流式** | **重启服务，会话还在，能接着改**；**界面能实时看到 agent 在干什么**。⚠️ 本档同时实测 `langgraph-checkpoint-mysql` 与 LangGraph 1.2 的兼容性，不通则 checkpoint 退回 SQLite（业务表仍 MySQL） |
| **M6** | 界面一：对话 + 历史栏 + 结构化卡片 | 首条消息**必问项一个不漏**；每天带天气；历史会话点开能继续改 |
| **M7** | 界面二：地图（**含 Day 页签**）+ 按天卡片 + 拖拽/删站/改时间 | **切 Day 页签，地图只显示当天路线**；拖动站点，距离数字同步更新 |
| **M8** | 评测：评测集 + pass@k / pass^k 报告 + **双模型对比** | **改一行 prompt，评测能告诉你变好还是变差**；报告单列"无法判定"占比 |
| — | **—— 做到这里 = 一个完整、可讲、可演示的项目 ——** | |
| **M9** | 用户管理：注册 / 登录 / 个人中心（我的行程 / 我的收藏 / 我的攻略） | **开两个账号实测：A 登录后拿 B 的 `session_id` 直接请求，返回 403/404 而不是数据**。会话真正归属到用户；`get_current_user_id()` 从常量变真实解析，**所有路由零改动** |
| **M10** | 攻略社区：列表 / 详情（**显示作者**）/ 发布（**公开**）/ **评论 / 点赞** / 「拿这篇去校验」 | 发布一篇 → **换另一个账号看得到、且看得到是谁发的**；同一篇**重复点赞被唯一索引挡住**（不是靠代码判重）；「去校验」能进 agent **热启动**入口；数据与 `eval/fixtures/` 同源 |
| **M11** | 景点管理：详情 / 搜索 / 收藏 / 评论 | 搜索复用 `search_poi`（**不新建 POI 数据表**，高德即数据源）；评论与收藏落 `comments`（多态）/ `favorites`；**系统发布的内容显示"官方"而不是空白用户名** |

**三道硬门：**

1. **M0 之前不写代码** —— 数据契约定不了，界面就是空中楼阁
2. **M1–M5 是引擎，M6–M7 是壳** —— 引擎跑通前画的界面是空壳。你说过"先实现具体页面再调整"，我的建议是**先把 M1–M3 跑通**（那时命令行就能出一份带坐标、带校验结果的行程），再动手画界面 —— 否则你会在假数据上反复调整布局，数据一换又得重来
3. **M9–M11 必须排在 M8 之后** —— CRUD 是体力活，agent 是护城河。顺序反了，时间会被 CRUD 吃掉，最后留下一个"能注册登录但 agent 半成品"的项目（8.2 已说明理由）
4. **私有数据必须做到"改 URL 也拿不到"** —— 验收方式是**开两个账号互测**：A 登录后把 URL 里的 `session_id` 换成 B 的，必须返回 403/404 而不是数据。**前端隐藏不算隔离**，`WHERE user_id` 写在后端才算（8.3 已说明）

---

## 十、已拍板决策索引

| # | 决策 | 你确认于 |
|---|---|---|
| A1 | 项目形态 = 生成 + 校验修正，双入口 | "C 方案不错，但希望与 a 结合" |
| A2 | 攻略只当评测集，不当运行时输入 → 砍爬虫 | "搜攻略不是作为评测集吗" |
| A3 | 校验判据分工原则 | "要调用大模型去判断可行性，根据天气、预约、开放与否" |
| A4 | Agent 有约束的自主 | "d16 选2" |
| A5 | 手写 StateGraph | "d17选2" |
| A6 | 循环结束另起固定节点出 JSON | "d18选b" |
| A7 | 工具集 v1 = 3 个（搜地点/查天气/算距离），**够用** | "A7目前这些工具够用了" |
| A8 | 界面一：左历史栏 + 右聊天框；首条消息 LLM 生成且必问项不漏 | "可以 llm 生成，但是要加入提示词必问项一个不漏，措辞又不死板" |
| A9 | 历史会话能继续改（活会话） | "1.能继续改" |
| A10 | 两个独立页面 + 中间跳转按钮 | "到时做两个界面 … 然后中间做个跳转按键" |
| A11 | 导航 6 项，只实现 2 项，其余占位 | "这五个 还有路线总览在助手旁边，可以先不实现其它的占位" |
| A12 | 界面二不带历史栏 | "2.不加，这作为一次性方便使用" |
| A13 | 界面二能改：调顺序 / 删站 / 改时间 | "目前就调顺序，删站，改时间这些" |
| A14 | 地图用高德（前端 JS API 画图） | "地图调高德地图" |
| A15 | **循环骨架 = 双层**（会话层 + 工具层 + 插队 steering） | "双层循环骨架就三句…" |
| A16 | **每天带天气预报**（白天/夜间天气 + 温度 + 风力） | "天气要每天的天气预报" |
| A17 | **地图左上角 Day 页签**，按天数生成，只显示当天路线 | "左上角加个day1,day2,day3的路线分页卡" |
| A18 | **校验硬软一次做全**（硬 5 + 软 4），并引入**三态** | "六的体系硬软判断先做全" |
| A19 | **要做多 agent 调用** → 定为 Subagents 形态，**只切 1 个搜索子 agent** | "我想做成多agent调用" + "可以就1个子agent" |
| A20 | **架构 = X（全 LangGraph）** | "一那就X" |
| A21 | **浏览器用 SSE 流式输出** | "浏览器采用流式输出sse" |
| A22 | **数据库 = MySQL（Docker 起）**，存用户 / 消息 / 攻略 | "数据库换成mysql" + "数据库用docker起" |
| A23 | **MCP = 工具门面 + 代码级降级**：对模型只暴露 1 个工具名，降级逻辑写在工具内部 | "mcp你推荐门面降级可行" |
| A24 | **模型 = DeepSeek**（`deepseek-flash`，可切 `deepseek-v4-pro`）；**工具循环关思考、规划节点开思考** | "模型换成deepseek" + 9/15 实测定 |
| A25 | **必问项 7 项**：目的地 / 日期 / 天数 / 同行人 / 预算 / 偏好 / 特别要求 | "必要的目的地，预算，时间，旅行偏好" |
| A26 | 三个管理模块**都做，但改造成"主线延伸"**（M9~M11 排在 M8 之后） | "写是因为要完善项目，做个半成品怎么进简历" |
| A27 | **数据归属**：会话 / 消息 / 行程 / 收藏 **私有**；攻略**发布后公开** | "用户层记得做用户隔离，不能看到别人的路线总览和 agent 规划，但攻略发布后是大家都能看到的" |
| A28 | **隔离写在后端**（统一 `owned_by_current_user()` 依赖），验收 = 双账号互测改 id | 由 A27 推导，见 8.3 |
| A29 | **攻略模块 = 旅游攻略社区**：列表/详情**显示作者**、**评论**、**点赞**；内容分**用户发布**与**系统发布**两种来源 | "就是个旅游攻略社区，然后也可以有评论点赞功能（会有系统发布的，也会有用户发布的）" |
| A30 | **鉴权 = JWT**；且明确**隔离与鉴权是两件事**（JWT 只证明"你是谁"，隔离靠 `WHERE user_id`） | "用户隔离有什么技术能用，一般电商这些平台用什么技术，要用鉴权 jwt" |
| C1 | git init + 首次 commit 先于任何业务代码 | 删库事件后你自己提的 |

---

## 十一、待拍板清单

| # | 问题 | 选项 | 我的建议 |
|---|---|---|---|
| **1** | **数据契约落成 Pydantic 模型（M0 的产物）** —— **最优先，其余都等它** | 字段增删 | 你过一遍再开工 |
| 2 | L2 工具层上限（最大工具调用次数） | 具体数字 | 8 次，且禁止同参数重复调 |
| 3 | L3 校验层上限（最大重排次数） | 具体数字 | 2 次，超了**降级输出**并列出剩余风险 |
| 4 | 首页做真的还是占位 | 真做 / 占位 | 真做（半天） |
| 5 | 界面二粘贴时是否自动建会话 | 建 / 不建 | 建（数据模型统一） |
| 6 | 站点卡字段的最终清单 | 增删 | 见 `技术方案.md` 数据契约节 |
| 7 | 项目目录名 | 保持 `agent` / 改 `travel-agent` | 改（`agent` 太泛） |
| 8 | 公开攻略的"审核"开关 | 加 / 不加 | **加，默认自动过**——只为脏数据预留拦截面 |

**已拍完，不再问：** 三模块做+改造（A26）｜必问项 7 项（A25）｜架构路线 X（A20）｜SSE（A21）｜MySQL + Docker（A22）｜MCP 门面降级（A23）｜DeepSeek 按节点分设思考（A24）｜数据隔离与公开攻略（A27/A28）｜攻略社区含评论点赞（A29）｜鉴权 JWT（A30）｜**9 张业务表字段过板** ✅｜**Docker 已修好可跑** ✅
