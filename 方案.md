# 智能旅游规划系统 · 项目方案

> 本文件只回答「**做什么、给谁用、怎么用**」。技术选型见 `技术方案.md`。
>
> 状态标记：**✅ 已拍板**（讨论中你当场确认过）｜**⚠️ 待拍板**｜**💡 建议**（我提的，你没确认）
>
> 最后更新：2026-09-14

---

## 一、一句话定位

> **给一份行程——不管是 agent 现生成的、你自己排的、还是从网上抄的——用真实世界的数据验证它到底能不能走通；走不通就改，并说清改了哪、为什么改。**

市场上一万个「AI 帮你生成行程」，几乎没有「AI 帮你验证行程能不能走通」。**生成的结果看着都像，校验的对错有客观答案。**

---

## 二、这个项目要证明什么

填空：**「这个项目要证明我会 _____」**

答：**让 agent 在外部数据可查、物理约束真实存在的环境里自主决策，并且能用可量化的评测说清它强在哪、弱在哪。**

Agent 的 loop 是否真必要，三条判据：

| 判据 | 本项目 | 证据 |
|---|---|---|
| 信息不在一次上下文里 | ✅ | 景点是否存在、开不开放、两地多远、那天下不下雨，都得现查 |
| 需要外部工具验证 | ✅ | 模型编一个"武侯祠东门"很容易，只有 POI 接口能证伪 |
| 有环境反馈 | ✅ | 校验节点打回 → 重排 → 再验 |

**三条全占 → 这个 loop 是刚需，不是装饰。**

---

## 三、两个入口，一套内核 ✅

| 入口 | 在哪个界面 | 用户给什么 | 系统干什么 |
|---|---|---|---|
| **冷启动** | 界面一（对话） | 一句需求（"10月1号去成都3天，带爸妈"） | 先追问补全 → 生成 → 拿真实数据回头验 → 改到走得通 |
| **热启动 A** | 界面一（对话） | 直接粘一段现成行程 | 识别为行程 → 跳过生成 → 直接验 → 改 |
| **热启动 B** | 界面二（路线总览） | 粘一段攻略/行程 | 同上，生成后留在界面二看结果 |

**内核只有一个：`验证 → 定位问题 → 修正 → 再验证`。**

「生成」= 先造一份再验；「校验」= 跳过制造直接验。A 和 C 不是两套系统，只是多开了一个入口。

---

## 四、用户流程

### 4.1 冷启动（主路径）

```
进入「AI 路线规划助手」
   ↓
agent 首条消息：引导用户提供必问项（目的地 / 时间 / 天数 / 同行人 / 特别要求）
   ↓
用户回答（可多轮，agent 追问补齐缺口）
   ↓
agent 调工具收集事实（搜景点 / 查天气 / 算距离）
   ↓ 「实时显示 agent 在干什么」← 轨迹卡
生成行程 → 自动校验 → 硬错打回重排（≤N 次）→ 软风险记下来
   ↓
输出：行程卡（每天一行 + 改按钮）+ 校验卡（绿=已修 / 黄=待确认）
   ↓
用户点「查看路线总览」→ 跳界面二
```

### 4.2 热启动（粘贴）

```
界面一：直接往聊天框粘一段行程/攻略 → agent 自己认出这是行程不是需求 → 校验 → 出报告
界面二：空手进来 = 粘贴模式 → 粘进去 → 生成 → URL 换成新 trip_id
```

💡 建议：界面二粘贴时**自动建一个会话**（标题"粘贴的成都行程"），这样两条路径统一成同一个数据模型——**路线总览永远挂在某个会话上**，用户想改时永远有地方说。

---

## 五、界面结构 ✅

### 5.1 导航栏（6 项，只实现 2 项）

| 位置 | 项 | 状态 |
|---|---|---|
| 1 | 首页 | ⚠️ 占位（💡 建议做成真的：入口卡片 + 最近行程，半天工作量） |
| 2 | 旅游景点 | 占位 |
| 3 | **AI 路线规划助手** | **核心 ①** |
| 4 | **路线总览** | **核心 ②** |
| 5 | 旅游攻略 | 占位 |
| 6 | 个人中心 | 占位 |

### 5.2 界面一：AI 路线规划助手

```
┌──────────────┬──────────────────────────────────┐
│  历史栏       │  对话区                           │
│              │                                  │
│  成都3日游    │  [agent] 你好，先告诉我…（必问项）  │
│  10-01        │  [user]  10月1号去成都3天带爸妈    │
│              │  [轨迹卡] 已查：景点搜索×3 天气×1  │
│  粘贴的行程   │  [行程卡] Day1 … Day2 … Day3 …    │
│  09-30        │  [校验卡] ✅2 项已修  ⚠️1 项待确认  │
│              │  ─────────────────────────────   │
│              │  [输入框]                         │
└──────────────┴──────────────────────────────────┘
```

- 左历史栏 ✅ / 右聊天框 ✅
- 历史会话**点开能继续改**（活会话，不是只读快照）✅
- agent 首条引导消息：**LLM 生成，但必问项一个不漏** ✅
  - 实现约束：LLM 输出**结构化数据**（`greeting` + `questions[{field,text}]`），代码校验 `field` 集合 == 必问项集合，缺了就重试/补齐。**措辞自由在 text，一项不漏由代码保证。**

### 5.3 界面二：路线总览

```
┌───────────────────────┬──────────────────────────┐
│  行程表格              │   高德地图                │
│  序号 时间 名称 距离   │   ①..②..③ 按天分色       │
│  1    09:00 武侯祠  —  │   编号 marker + 连线      │
│  2    11:30 锦里  1.2km│                          │
│  …                    │                          │
├───────────────────────┴──────────────────────────┤
│  顶部统计条：总里程 / 人均参考 / 站点数 / 硬错数    │
├──────────────────────────────────────────────────┤
│  按天分组卡片（截图1 那种）                        │
│  Day1 市区古迹 ── 路线逻辑文案 ── 每站 ↓           │
│    ① 武侯祠  [距上一站 1.2km · 车程 6 分钟] [🟢]   │
│       人均参考 ¥50 · 匹配理由「历史古迹」          │
│    ② 锦里    […距离…] [🟡 需预约]                 │
└──────────────────────────────────────────────────┘
```

- **左侧表格 + 右侧地图** ✅，下方接按天分组卡片
- **不带历史栏**（一次性方便使用）✅
- 与界面一之间加**跳转按钮**（有行程才出现）✅
- 能改：**调顺序 / 删站 / 改时间** ✅（先做这些，"换站/加站"后议）

### 5.4 界面二"能改"的真实链路

```
拖拽/删除/改时间
   → 前端乐观更新（先动，手感即时）
   → 调后端 PATCH /trips/{id}
   → 后端确定性重算（相邻距离、车程、总里程、时间轴冲突）
   → 返回校正后的数据
```

**关键分工 ✅：手动改只触发「确定性重算」，不触发 LLM 软校验。**
原因：LLM 一次 3~8 秒，拖一下等 5 秒体验直接崩。想跑软校验时点【重新深度检查】按钮。

---

## 六、校验体系（项目的核心）

**总原则 ✅：有唯一答案的交给代码，没有唯一答案的交给 LLM。**
不这么分，同一个行程跑两次会一次说行一次说不行——评测数字就没意义了。

| 层 | 校验项 | 谁判 | 判据来源 | 判定后 |
|---|---|---|---|---|
| **硬** | 景点是否真实存在 | 代码 | POI 接口候选池 | **打回重排** |
| **硬** | 两地当天能否到达 | 代码 | 距离/耗时接口 | **打回重排** |
| **硬** | 那天是否开放 | 代码 | POI 开放时间 | **打回重排** |
| **硬** | 天气是否冲突 | 代码 | 天气接口 | **打回重排** |
| **硬** | 累计走路量 vs 同行人 | 代码 | 坐标累计 | **打回重排** |
| **软** | 是否需要预约 | LLM | 模型知识 | 提醒用户 |
| **软** | 旺季排队时长 | LLM | 经验判断 | 提醒用户 |
| **软** | 是否适合带老人 | LLM | 经验判断 | 提醒用户 |
| **软** | 整体可行不可行 | LLM | 综合判断 | 汇总呈现 |

**硬错打回、软风险提醒。**

⚠️ 待拍板：v1 只做前 2~3 类硬判据，还是硬判据一次做全？（上一版 PLAN 里我写了"v1 只做前两类"，那是我自己加的，你没拍过。）

---

## 七、Agent 形态（四条已拍板）

| # | 决策 | 选择 | 代价 |
|---|---|---|---|
| A4 ✅ | **自主权层次** | **有约束的自主**：给工具 + prompt 写明工作流，但**调不调、调几次、够了没由模型定** | prompt 要写细；换来的是"自主性"在面试里说得清边界 |
| A5 ✅ | **实现路径** | **手写 StateGraph**（不用 `create_agent` 高层封装） | 约 40 行样板代码，`ToolMessage` / `tool_call_id` 要自己拼 |
| A6 ✅ | **循环终点** | 工具循环只**收事实**，循环结束后另起**固定节点**出结构化 JSON | 多一次 LLM 调用 |
| A7 ⚠️ | 工具集 | 💡 3 个起步：**搜地点 / 查天气 / 算距离** | 工具越少模型越不易选错；"查详情"暂不单独成工具 |

### 双循环结构

```
  START → parse_intent ─┬─→ ask_more → END（等用户补信息）
                        └─→ agent_step ⇄ tool_step      ← 内循环（模型自主选工具）
                                  ↓ 模型判定事实已够
                            generate_plan
                                  ↓
                            check_plan ─┬─→ repair ──→ 回 agent_step   ← 外循环
                                        └─→ render → END
```

| 循环 | 谁说了算 | 管什么 | 上限 |
|---|---|---|---|
| **内循环** | 模型 | 查什么、查几次、查够了没 | ⚠️ 待定：最大工具调用次数 + 禁止同参数重复调 |
| **外循环** | 代码 + LLM | 行程合格吗、哪里要改 | ⚠️ 待定：最大重排次数，超了**降级输出**（列风险清单）而不是死磕 |

---

## 八、范围边界：明确不做什么

| 不做 | 理由 |
|---|---|
| **爬虫抓攻略** | 实测：携程跳人机验证、马蜂窝返 202 挑战、小红书要登录、去哪儿空响应。且攻略只当评测集，手动存 10~20 篇就够 |
| **Neo4j / 知识图谱** | 标题党（原视频简介里 Neo4j 只出现在标题）+ 要 Docker（你本机 Docker 崩）+ 旅游 POI 用经纬度+聚类就够 |
| **用户认证 / 个人中心 / 景点浏览 CRUD / 攻略发布** | 跟 agent 无关，是毕设模板的主体 |
| **3D 街景、天气图层** | 炫技、不加分、易出 bug |
| **手动加站 / 换站** | 必须从真实 POI 候选池里挑，要一整套搜索交互，成本高且与界面一的 agent 职责重叠 |

---

## 九、里程碑与验收判据

每档都能独立停，停下时项目是完整可讲的。

| 档 | 内容 | 完成判据（做到哪算过） |
|---|---|---|
| **M0** | **数据契约定案**（Trip JSON 结构） | 前后端 + agent 三方对同一份 JSON 无歧义 |
| **M1** | 骨架：手写 agent loop + 3 个 mock 工具 + 固定节点出 JSON | 命令行打印出 3 天行程，**trace 里能看到模型自己决定调了哪几个工具、调了几次** |
| **M2** | 校验回环：硬判据 + 次数上限 | 故意喂一个冲突约束，**能看到被打回重排**；循环有上限、不死循环 |
| **M3** | 接真实数据源（高德 Web 服务） | 行程里**每个景点都能在工具返回的候选池里找到**，一个都不是模型编的 |
| **M4** | 服务化：FastAPI + SQLite checkpoint | **重启服务，会话还在，能接着改** |
| **M5** | 界面一：对话 + 历史栏 + 结构化卡片 | 首条消息**必问项一个不漏**；历史会话点开能继续改 |
| **M6** | 界面二：地图 + 卡片 + 拖拽/删站/改时间 | **拖动站点，地图和距离数字同步更新** |
| **M7** | 评测：评测集 + pass@k / pass^k 报告 | **改一行 prompt，评测能告诉你变好还是变差** |

**M0 和 M1 之间有道硬门：数据契约定不了，界面就是空中楼阁。**

---

## 十、已拍板决策索引

| # | 决策 | 你确认于 |
|---|---|---|
| A1 | 项目形态 = 生成 + 校验修正，双入口 | "C 方案不错，但希望与 a 结合" |
| A2 | 攻略只当评测集，不当运行时输入 → 砍爬虫 | "搜攻略不是作为评测集吗" |
| A3 | 校验判据分工原则 | "要调用大模型去判断可行性，根据天气、预约、开放与否" |
| A4 | Agent 有约束的自主 | "d16 选2" |
| A5 | 手写 StateGraph | "d17选2" |
| A6 | 循环结束另起固定节点出 JSON | "d18选b" |
| A7 | 界面一：左历史栏 + 右聊天框，首条消息 LLM 生成且必问项不漏 | "可以 llm 生成，但是要加入提示词必问项一个不漏，措辞又不死板" |
| A8 | 历史会话能继续改（活会话） | "1.能继续改" |
| A9 | 两界面 + 跳转按钮 | "到时做两个界面 … 然后中间做个跳转按键" |
| A10 | 导航 6 项，只实现 2 项，其余占位 | "这五个 还有路线总览在助手旁边，可以先不实现其它的占位" |
| A11 | 界面二不带历史栏 | "2.不加，这作为一次性方便使用" |
| A12 | 界面二能改：调顺序 / 删站 / 改时间 | "目前就调顺序，删站，改时间这些" |
| A13 | 地图用高德（前端 JS API 画图） | "地图调高德地图" |
| C1 | git init + 首次 commit 先于任何业务代码 | 删库事件后你自己提的 |

---

## 十一、待拍板清单

| # | 问题 | 选项 | 我的建议 |
|---|---|---|---|
| 1 | 首页做真的还是占位 | 真做 / 占位 | 真做（半天） |
| 2 | 界面二粘贴时是否自动建会话 | 建 / 不建 | 建（数据模型统一） |
| 3 | v1 校验做几类硬判据 | 2 类 / 全做 | 先 2 类（真实性 + 可达性） |
| 4 | 内循环上限（最大工具调用次数） | 具体数字 | 8 次 + 禁止同参重复调 |
| 5 | 外循环上限（最大重排次数） | 具体数字 | 2 次，超了降级输出风险清单 |
| 6 | 站点卡字段的最终清单 | 增删 | 见 `技术方案.md` 数据契约节 |
| 7 | 项目目录名 | 保持 `agent` / 改 `travel-agent` | 改（`agent` 太泛） |
